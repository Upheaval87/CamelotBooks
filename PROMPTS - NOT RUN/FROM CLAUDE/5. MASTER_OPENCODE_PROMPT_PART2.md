# MASTER OPENCODE PROMPT — KUWALA SACCO ADMIN SUITE (PART 2 REDESIGN)
## Loan Types · Users & Companies · Approval Thresholds · Authorizer Tiers · Email Settings

---

## 0. HOW TO USE THIS PROMPT

This document is self-contained. It includes:
1. **Strict scope guardrails** (section 1) — read this first, every time.
2. **The functional/data-model specification** (section 2) — what to build, wired to the real database and existing business logic.
3. **The exact reference HTML/CSS/JS for every screen** (sections 3–5) — this is the *exact* visual design to reproduce. It is pasted in full below so you do not need to open, search for, or guess at any external HTML file. Treat every embedded `<style>` block, class name, layout, icon, copy string, and interaction pattern in these reference blocks as the literal target design — reproduce it pixel-for-pixel in the framework this project already uses (Blade/Tailwind/Alpine per the project's existing conventions), replacing only the static/mock JS arrays with real server-rendered data, real routes, and real form submissions.
4. **Wiring checklist** (section 6) — every link, button, nav entry, and route that must be connected for this to be reachable and usable end to end.
5. **QA / Definition of Done** (section 7).

Do not restyle, redesign, simplify, or "improve" the reference markup below — match it exactly (colors, spacing, radii, pill/chip/toggle/icon styles, table layout, modal structure, sticky form bars, stat-chip rows, view-switcher tab bars). The only changes allowed to the reference HTML/CSS/JS are the ones required to wire it to real data and real backend behavior (see section 6).

---

## 1. SCOPE GUARDRAILS — READ FIRST

**This prompt authorizes changes ONLY to the following, and nothing else:**

- The 5 admin modules named in this document: **Loan Types**, **Users**, **Companies**, **Approval Thresholds**, **Authorizer Tiers**, **Email Settings** (and their list/add/edit/access-modal screens exactly as specified).
- The specific files, routes, controllers, services, models, and migrations listed in section 2.
- Navigation entries, links, and buttons that must point to the above (section 6).

**Explicitly OUT OF SCOPE — do not touch, refactor, rename, or "clean up":**

- Any existing business logic for loan calculations, interest computation, approval/authorization workflow execution, repayment schedules, disbursement, or payroll assessment logic itself. You are changing **admin configuration screens and their persistence**, not the engines that consume that configuration.
- Any module, page, controller, route, or file not named in this prompt (e.g. dashboards, reports, the public loan calculator, the member-facing application flow, the forms/document-generation module, the payslip generator). If a change in this prompt requires touching a shared/global file (e.g. a shared layout, main nav partial, shared Blade component), make the **minimum edit necessary** to wire the new pages in — do not restructure that shared file.
- Do not delete or rewrite existing working features "while you're in there." If something outside this prompt's scope looks broken or inconsistent, leave it and note it separately — do not fix it as a side effect.
- Do not change database columns/tables beyond exactly what section 2's migrations specify. Do not drop or rename existing columns.
- Do not introduce new npm/composer dependencies unless strictly required to match the reference design (this design uses only Google Fonts Inter + Space Grotesk, inline SVG icons, and vanilla CSS — no icon library or component library is required).

**Before writing any code:** locate the existing equivalent screens/routes/controllers for loan types, users, companies, thresholds, tiers, and mail settings in the current codebase (Part 1 of this project already exists). Extend/replace those in place rather than creating parallel duplicate modules. If Part-1 equivalents don't exist yet for a given screen, create them fresh per this spec.

**After implementation:** run the project's full test suite (`php artisan test`) and confirm no unrelated test fails. Do not modify or delete existing passing tests to force a pass.

---

## 2. FUNCTIONAL SPECIFICATION (DATA MODEL, SERVICES, ROUTES)

Everything in this section is the authoritative functional spec — implement it exactly, then skin it with the reference designs in sections 3–5.

*(The functional spec below is the original build prompt provided by the SACCO admin team, included verbatim and unedited — it is authoritative for data model, services, routes, and QA criteria. Section headings inside it (0–12) are internal to this spec and independent of this document's own section numbering.)*

# Prerequisite: Part-1 consolidation (public calculator, product-driven forms, form
# builder, loan access control) is implemented. This prompt extends it.

============================================================
## 0. GLOBAL CONSTRAINTS & DESIGN SYSTEM
============================================================
- Preserve ALL existing business logic (calculations, workflows, approvals, access
  filtering). This prompt adds/extends ADMIN configuration + presentation only.
- Reuse design tokens: navy #16202C/#1E2A3A, amber #F6A21E/#FFC466, amber-ink
  #9A5B00, bg #F4F6F9, panel #fff, line rgba(22,32,44,.10); fonts Inter + Space
  Grotesk; radii 12–16px; soft shadows; min font 12px; money tabular-nums.
- Reusable admin components (build once, use everywhere):
  • Page header card (title + subtitle + primary CTA)
  • Stat chips row; navy sticky table headers; row hover; status pills (Active/Inactive)
  • Icon-only row actions with tooltip+aria-label: Edit(pencil), Deactivate(ban/red),
    Activate(check/green), Assign-PO(person+), Save(disk)
  • Toggle switches (amber) for booleans; segmented "class cards" with amber check-boxes
  • Sectioned form cards (uppercase micro-label + hairline), sticky Save/Cancel bar
  • Modals with navy header; searchable multi-select lists; toasts.
- Security: all admin routes behind auth + permission gates; email password stored
  encrypted; test-email throttled; server-side validation everywhere.

============================================================
## 1. DATA MODEL CHANGES (migrations)
============================================================
users: + member_classes JSON nullable  // ["ordinary","staff"]; keep is_member synced
       (is_member = count(member_classes)>0). Migrate existing member_class → array.
company_payroll_officer: id, company_id FK cascade, user_id FK cascade,
       assigned_by FK users nullable, timestamps, unique(company_id,user_id).
system_settings (existing key/value) — add keys:
  mail_transport, mail_host, mail_port, mail_username, mail_password (ENCRYPTED),
  mail_encryption, mail_from_address, mail_from_name,
  mail_notification_prefs JSON  // {account_created,loan_submitted,payroll_assessment,
                                //  approval_stage,final_decision,delegation,account_status}
(loan_types.application_form_id, loan_type_access, authorizer_tiers,
 approval_thresholds already exist from Part 1.)

============================================================
## 2. SERVICES
============================================================
- LoanAccessService (Part 1) — UPDATE: a loan type is visible iff
  intersection(user.member_classes, type.access_classes) non-empty
  OR user.id ∈ type.access_users OR type.access_classes empty;
  AND (type.access_companies empty OR contains user.company_id).
- PayrollAssignmentService: assign/remove(company,user,by); companiesFor(user);
  officersFor(company). Payroll assessment scoping (Part 1) now reads the PIVOT
  (officer may assess any company they're assigned to); seed pivot from existing
  payroll officers' company_id.
- MailSettingsService: get()/save(array) (encrypt mail_password with
  Crypt::encryptString; decrypt on read); apply() → set config() at boot:
  config(['mail.default'=>transport, 'mail.mailers.smtp.host'=>…, port, username,
  password(decrypted), encryption, 'mail.from.address'=>…, 'mail.from.name'=>…]);
  register apply() in AppServiceProvider::boot.
- NotificationRouter (Part 1) — honour mail_notification_prefs booleans per event.

============================================================
## 3. ROUTES & CONTROLLERS (admin, gated)
============================================================
admin/loan-types            index/create/store/edit/update  (LoanTypeController)
admin/loan-types/{id}/access  update                        (updateAccess)
admin/users                 index/create/store/edit/update  (UserController; member_classes)
admin/companies             index/create/store/edit/update  (CompanyController)
admin/companies/{id}/payroll-officers  update               (assignPayrollOfficers)
admin/thresholds            index/create/store/edit/update  (ThresholdController)
admin/tiers                 index/update                    (AuthorizerTierController)
admin/email-settings        index/update/test               (EmailSettingsController;
                            test → Mail::to(x)->send(new TestMail), throttle:5,1)

============================================================
## 4. FEATURE A — LOAN TYPES (list / add-edit / access modal)
============================================================
LIST: header + "Add Loan Type"; stat chips (Products, Active, Staff-only, Forms
attached); search + status filter; navy table: Loan Type, Code, Application Form
(inline select → update application_form_id), Access (class chips + Manage), Rate
(% p.m. tabular), Method, Status pill, icon actions Edit/Deactivate-Activate.
ADD/EDIT: sectioned card — Product Identity (name, code, description); Pricing &
Interest (rate %, interest period, method select w/ "System default", application
form select + hint, insurance % + period, processing fee %); Limits (max months,
min/max MK); Requirements & Status (toggles guarantor/collateral/active); Access
(button → modal). Sticky Save/Cancel.
ACCESS MODAL ("Loan Product Access"): navy header w/ product name; member-class
segmented cards (Ordinary / Staff) with hint "leave both unchecked = allow all";
companies cards (hint "leave all unchecked = all"); searchable individual-users
multi-select (hint "leave empty = class/company only"); Save Access/Cancel.

============================================================
## 5. FEATURE B — USERS (membership: ordinary / staff / both)
============================================================
LIST: stat chips (Users, Members, Staff-class, Active); search + role + status
filters; navy table: User (avatar+name), Username(mono), Email, Role pill, Company,
Membership chips (Ordinary=amber / Staff=blue / "Not member"), Status pill, icon
actions Edit/Deactivate-Activate.
REGISTER/EDIT: sections — Personal (first/other/last, DOB, marital, employment
type); Contact & Identity (email, national ID, telephone); Role & Assignment
(role select, company select *required, branch select); MEMBERSHIP / LOAN-ACCESS
CLASS: two class cards (Ordinary member / Staff member) — check one = single class,
BOTH = ordinary+staff, none = cannot apply; live hint reflects state; Identity
Documents upload cards (passport, ID front/back, contract). Sticky bar
(Create User / Save Changes). On save: set member_classes + sync is_member.
Header sub: emailed secure link + auto unique usernames.

============================================================
## 6. FEATURE C — COMPANIES (assign payroll officers)
============================================================
LIST: stat chips (Companies, Active, Payroll officers); navy table: Company, Code,
Contact, Users, Payroll Officers (name chips + Manage/Assign), Status pill, icon
actions Assign-PO / Edit / Deactivate-Activate.
ADD/EDIT: sections — Identity (name*, code); Contact (email, phone, address);
PAYROLL OFFICERS: searchable checklist of users with role Payroll Officer (checked
= assigned to this company) + hint; sticky Save/Cancel.
ASSIGN MODAL (from list): same checklist; Save writes pivot (assigned_by=auth user).

============================================================
## 7. FEATURE D — APPROVAL THRESHOLDS
============================================================
LIST: header sub "director threshold NOT hardcoded — reads from flagged threshold";
stat chips (Thresholds, Director level = MK value of flagged row, Active); navy
table: Name, Min, Max (∞ if null), Approvers, Director Auth (DIRECTOR LEVEL pill +
amber left-edge on flagged row), Directors (mgmt out), Branch Filter, Status pill,
icon actions.
ADD/EDIT: note "exactly one threshold flagged as requiring director authorization";
sections — Definition (name, min MK, max MK blank=∞); Approval Chain (approver
count, director count if manager absent, director branch filter); Flags (toggles
Requires Director Authorization, Active). Enforce single director-flag on save
(unflag others). Sticky bar.

============================================================
## 8. FEATURE E — AUTHORIZER TIERS
============================================================
Header sub explains Primary/Secondary/Auxiliary; stat chips (Primary/Secondary/
Auxiliary counts); navy table: User (avatar+name), Role pill, three amber
check-tiles (Primary/Secondary/Auxiliary) toggle in place, per-row Save icon.
Persist to authorizer_tiers (user may hold multiple tiers).

============================================================
## 9. FEATURE F — EMAIL SETTINGS (NEW)
============================================================
Header + live status chip (Not tested / Test sent / OK).
Sections:
1 Mail Transport: transport select (SMTP/Mailgun/SES/Sendmail/Log), encryption
  (None/TLS/SSL), host, port, username, password (show/hide eye).
2 Sender Identity: from address, from name.
3 Notification Preferences: toggle switches per event (account_created+password
  link, loan_submitted→officers, payroll_assessment→officers, approval_stage→
  authorizers, final_decision→applicant, delegation alerts, account_status).
4 Send Test Email: recipient input + "Send test email" button (throttled) →
  updates status chip + toast.
Sticky Save/Cancel. Save → MailSettingsService::save (encrypt password) + toast.

============================================================
## 10. SEED / COMPAT
============================================================
- users.member_classes: ordinary members → ["ordinary"]; staff → ["staff"];
  both where applicable; non-members → [].
- company_payroll_officer: seed from each payroll officer's current company_id.
- system_settings mail_*: sensible defaults (transport SMTP, from no-reply@kuwala.mw,
  "Kuwala SACCO Ltd", prefs all true).

============================================================
## 11. SECURITY
============================================================
- All routes admin-gated; email settings view/update restricted to administrator.
- mail_password encrypted at rest, masked in UI, decrypted only in apply()/send.
- Test email throttled (5/min); no secret echoed in responses/toasts.
- Server-side validation for every form; access changes audited (audit_trail).

============================================================
## 12. QA & DEFINITION OF DONE
============================================================
- Loan Types: inline form select persists; access modal saves classes/companies/
  users; member calculator/application reflect access (ordinary never sees staff-only).
- Users: membership cards save ["ordinary"]/["staff"]/both/none; loan access follows.
- Companies: payroll officers assignable via form + modal; payroll assessment
  scoped by pivot; users count + PO chips render.
- Thresholds: exactly one director-flag enforced; director threshold read from it.
- Tiers: multi-tier per user persists; counts update.
- Email: settings save (password encrypted); apply() affects outgoing mail at boot;
  test email delivers; notification prefs suppress disabled events.
- All pages use the shared admin components; responsive + a11y passes;
  `php artisan test` green; no unrelated logic changed.
---

## 3. REFERENCE DESIGN — LOAN TYPES (list, add/edit form, access modal)

**Routes to wire (per section 2):** `admin/loan-types` (index/create/store/edit/update), `admin/loan-types/{id}/access` (updateAccess).

**Reproduce this exact screen set.** It has two full-page views (`view-list`, `view-form`) toggled by JS, plus a modal (`accessModal`) for managing member-class/company/user access. Below is the complete, exact HTML/CSS/JS reference — replace only the mock arrays (`PRODUCTS`, `FORMS`, `USERS`) and inline handler functions (`renderList`, `openForm`, `saveForm`, `openAccess`, `saveAccess`, `setForm`, `toggleActive`) with real server-rendered/Blade+Alpine equivalents backed by the `loan_types`, `loan_type_access`, and `application_form` tables/relationships. Keep every class name, layout, copy string, icon, and interaction exactly as shown.

**Wiring specifics for this module:**
- "Add Loan Type" button → opens the add form view (real create route), not just a JS view-toggle over static data.
- The inline "Application Form" `<select>` per row must persist immediately on change (`updateAccess`/dedicated inline-update endpoint), matching `setForm()`'s optimistic-update + toast pattern.
- "Manage" access link (both from the list row and from inside the edit form's Access section) opens the same Access modal, scoped to that loan type, and must save to `loan_type_access` (classes/companies/users) via the real `updateAccess` route.
- Row actions: Edit → real edit route with existing data prefilled; Activate/Deactivate icon → real toggle-active persistence, not just local state.
- Stat chips (Products / Active / Staff-only / Forms attached) must be computed from real query counts, not client-side array filtering.

```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1">
<title>Kuwala SACCO — Loan Types Admin (Icon Actions)</title>
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;600;700&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
:root{--navy:#16202C;--navy2:#1E2A3A;--amber:#F6A21E;--amber2:#FFC466;--amber-ink:#9A5B00;--amber-soft:rgba(246,162,30,.16);
--bg:#F4F6F9;--panel:#fff;--line:rgba(22,32,44,.10);--line-soft:rgba(22,32,44,.06);--ink:#16202C;--body-c:#3D4756;--mut:#5D6B7E;--good:#0E9F6E;--good-soft:rgba(14,159,110,.12);--bad:#D92D20;--bad-soft:rgba(217,45,32,.10);
--r-lg:16px;--r-md:12px;--sh-2:0 1px 2px rgba(16,24,40,.05),0 16px 40px -24px rgba(16,24,40,.16);--font-sans:'Inter',system-ui,sans-serif;--font-disp:'Space Grotesk','Inter',sans-serif}
*{margin:0;padding:0;box-sizing:border-box}body{font-family:var(--font-sans);font-size:15px;line-height:1.6;background:var(--bg);color:var(--body-c)}
button{font-family:inherit;cursor:pointer}:focus-visible{outline:2px solid rgba(246,162,30,.7);outline-offset:2px;border-radius:8px}
.wrap{max-width:1180px;margin:0 auto;padding:26px 24px 80px}
.crumb{font-size:12px;color:var(--mut);text-transform:uppercase;letter-spacing:.06em;font-weight:600}.crumb b{color:var(--ink)}
.head{display:flex;justify-content:space-between;align-items:flex-end;gap:16px;flex-wrap:wrap;margin:6px 0 18px}
h1{font:700 26px var(--font-disp);color:var(--ink)}
.sub{font-size:13.5px;color:var(--mut)}
.btn{display:inline-flex;gap:8px;align-items:center;font-size:13.5px;font-weight:600;border-radius:12px;padding:11px 16px;border:1px solid var(--line);background:#fff;color:var(--ink);transition:.2s}
.btn:hover{border-color:var(--amber);background:var(--amber-soft)}
.btn svg{width:15px;height:15px;stroke:currentColor;fill:none;stroke-width:1.8;stroke-linecap:round;stroke-linejoin:round}
.btn-primary{background:linear-gradient(180deg,var(--amber2),var(--amber));border-color:var(--amber);color:var(--navy);font-weight:700}
.btn-primary:hover{transform:translateY(-1px);box-shadow:0 10px 24px -8px rgba(246,162,30,.55)}
.btn-ghost{background:transparent}
.stats{display:flex;gap:10px;flex-wrap:wrap;margin-bottom:16px}
.stat{background:var(--panel);border:1px solid var(--line);border-radius:12px;padding:10px 16px;box-shadow:var(--sh-2)}
.stat .l{font-size:11px;letter-spacing:.1em;text-transform:uppercase;color:var(--mut);font-weight:600}
.stat .v{font:700 18px var(--font-disp);color:var(--ink)}
.card{background:var(--panel);border:1px solid var(--line);border-radius:var(--r-lg);box-shadow:var(--sh-2);overflow:hidden}
.thead-bar{display:flex;justify-content:space-between;align-items:center;gap:12px;padding:18px 22px;border-bottom:1px solid var(--line);flex-wrap:wrap}
.filter input,.filter select{font:inherit;font-size:13.5px;border:1px solid var(--line);border-radius:11px;padding:9px 13px}
.filter input{width:240px}.filter input:focus{border-color:var(--amber);box-shadow:0 0 0 3px var(--amber-soft);outline:none}
.twrap{overflow:auto}
table{width:100%;border-collapse:collapse;min-width:920px}
th{position:sticky;top:0;background:linear-gradient(150deg,var(--navy),var(--navy2));color:#EAF0F6;font-size:11.5px;letter-spacing:.1em;text-transform:uppercase;font-weight:600;text-align:left;padding:12px 16px}
td{font-size:14px;padding:13px 16px;border-bottom:1px solid var(--line-soft);vertical-align:middle}
tbody tr:hover{background:#F7F9FC}
td .name{font-weight:600;color:var(--ink)}
td select{font:inherit;font-size:13px;border:1px solid var(--line);border-radius:10px;padding:8px 10px;background:#fff}
.rate{font-variant-numeric:tabular-nums;font-weight:600;color:var(--ink)}.rate small{color:var(--mut);font-weight:500}
.mono{font-size:12.5px;color:var(--mut);font-variant-numeric:tabular-nums}
.pill{font-size:11px;font-weight:700;padding:4px 11px;border-radius:999px}
.pill.on{background:var(--good-soft);color:var(--good)}.pill.off{background:var(--bad-soft);color:var(--bad)}
.achips{display:flex;gap:5px;flex-wrap:wrap;align-items:center}
.achip{font-size:11px;font-weight:600;background:#F1F4F8;border:1px solid var(--line-soft);border-radius:999px;padding:3px 10px;color:var(--body-c)}
.linkbtn{border:0;background:none;color:var(--amber-ink);font-size:12.5px;font-weight:700;padding:2px 4px}
.linkbtn:hover{text-decoration:underline}
.rowact{display:flex;gap:6px;justify-content:flex-end}
/* ICON action buttons */
.act{width:34px;height:34px;border-radius:10px;border:1px solid var(--line);background:#fff;display:grid;place-items:center;color:var(--mut);transition:.15s}
.act svg{width:16px;height:16px;stroke:currentColor;fill:none;stroke-width:1.8;stroke-linecap:round;stroke-linejoin:round}
.act:hover{border-color:var(--amber);color:var(--amber-ink);background:var(--amber-soft)}
.act.danger:hover{border-color:var(--bad);color:var(--bad);background:var(--bad-soft)}
.act.good:hover{border-color:var(--good);color:var(--good);background:var(--good-soft)}
/* form */
.formcard{padding:26px 28px}
.fsec{display:flex;align-items:center;gap:8px;font-size:11.5px;font-weight:700;letter-spacing:.14em;text-transform:uppercase;color:var(--mut);margin:24px 0 12px}
.fsec::after{content:"";flex:1;height:1px;background:var(--line-soft)}.fsec:first-child{margin-top:0}
.fgrid{display:grid;grid-template-columns:1fr 1fr;gap:14px}.fgrid .full{grid-column:1/-1}
.field label{display:flex;gap:7px;align-items:center;font-size:13px;font-weight:600;color:var(--ink);margin-bottom:6px}
.field label .lic{width:15px;height:15px;stroke:var(--mut);fill:none;stroke-width:1.7;stroke-linecap:round;stroke-linejoin:round}
.field input,.field select,.field textarea{width:100%;font:inherit;font-size:14.5px;color:var(--ink);background:#fff;border:1px solid var(--line);border-radius:11px;padding:10px 13px;transition:.18s}
.field input:focus,.field select:focus,.field textarea:focus{border-color:var(--amber);box-shadow:0 0 0 3px var(--amber-soft);outline:none}
.ctl{position:relative}.ctl .suf{position:absolute;right:13px;top:50%;transform:translateY(-50%);font-size:12px;font-weight:600;color:var(--mut)}
.ctl.has-suf input{padding-right:52px}
.hint{font-size:11.5px;color:var(--mut);margin-top:5px}
.swrow{display:flex;gap:18px;flex-wrap:wrap}
.sw{display:flex;align-items:center;gap:9px;font-size:13.5px;font-weight:500;color:var(--body-c);cursor:pointer}
.sw input{display:none}
.sw i{width:40px;height:22px;border-radius:999px;background:#D7DDE5;position:relative;transition:.2s;flex:0 0 40px}
.sw i::after{content:"";position:absolute;top:3px;left:3px;width:16px;height:16px;border-radius:50%;background:#fff;transition:.2s;box-shadow:0 1px 3px rgba(0,0,0,.25)}
.sw input:checked+i{background:var(--amber)}
.sw input:checked+i::after{left:21px}
.formbar{position:sticky;bottom:0;background:rgba(255,255,255,.92);backdrop-filter:blur(8px);border-top:1px solid var(--line);padding:14px 28px;display:flex;gap:10px;justify-content:flex-end}
/* modal */
.mov{position:fixed;inset:0;background:rgba(22,32,44,.5);backdrop-filter:blur(3px);display:flex;align-items:center;justify-content:center;padding:20px;opacity:0;pointer-events:none;transition:.25s;z-index:60}
.mov.show{opacity:1;pointer-events:auto}
.mmodal{background:#fff;border-radius:18px;max-width:600px;width:100%;max-height:90vh;display:flex;flex-direction:column;overflow:hidden;transform:translateY(14px);transition:.25s}
.mov.show .mmodal{transform:none}
.mhead{display:flex;justify-content:space-between;align-items:center;padding:18px 22px;background:linear-gradient(150deg,var(--navy),var(--navy2))}
.mhead h3{font:600 17px var(--font-disp);color:#fff}.mhead .ms{font-size:12px;color:#AEB9C6}
.mclose{width:32px;height:32px;border-radius:9px;border:1px solid rgba(255,255,255,.25);background:rgba(255,255,255,.08);color:#AEB9C6}
.mbody{padding:20px 22px;overflow:auto}
.msec{font-size:11px;font-weight:700;letter-spacing:.14em;text-transform:uppercase;color:var(--mut);margin:16px 0 8px}.msec:first-child{margin-top:0}
.classcards,.cogrid{display:grid;grid-template-columns:1fr 1fr;gap:10px}
.ccard2{border:1.5px solid var(--line);border-radius:12px;padding:12px 14px;display:flex;gap:10px;align-items:center;cursor:pointer;transition:.18s}
.ccard2 input{display:none}
.ccard2 .bx{width:20px;height:20px;border-radius:6px;border:1.5px solid var(--line);display:grid;place-items:center;color:#fff;font-size:12px}
.ccard2 input:checked~.bx{background:var(--amber);border-color:var(--amber)}
.ccard2 input:checked~.bx::after{content:"✓"}
.ccard2.on{border-color:var(--amber);background:var(--amber-soft)}
.ccard2 b{font-size:13.5px;color:var(--ink)}.ccard2 small{display:block;font-size:11.5px;color:var(--mut)}
.usersearch input{width:100%;font:inherit;font-size:13.5px;border:1px solid var(--line);border-radius:11px;padding:9px 13px;margin-bottom:8px}
.userlist{border:1px solid var(--line);border-radius:12px;max-height:180px;overflow:auto}
.urow{display:flex;gap:10px;align-items:center;padding:9px 13px;border-bottom:1px solid var(--line-soft);font-size:13.5px;cursor:pointer}
.urow:last-child{border:0}.urow:hover{background:#F7F9FC}
.urow input{accent-color:var(--amber)}
.urow .role{margin-left:auto;font-size:11px;color:var(--mut);background:#F1F4F8;border-radius:999px;padding:2px 9px}
.mfoot{padding:14px 22px;border-top:1px solid var(--line);display:flex;gap:10px;justify-content:flex-end}
.ctoast{position:fixed;right:24px;bottom:24px;background:var(--navy);color:#fff;border-radius:14px;padding:12px 18px;font-size:13.5px;opacity:0;transform:translateY(10px);transition:.3s;z-index:90}
.ctoast.show{opacity:1;transform:none}
@media(max-width:760px){.fgrid,.classcards,.cogrid{grid-template-columns:1fr}}
</style>
</head>
<body>
<div class="wrap">
  <div class="crumb">Administration · <b>Loan Products</b></div>

  <!-- ═══ LIST ═══ -->
  <div id="view-list">
    <div class="head">
      <div><h1>Loan Types</h1><div class="sub">Configure loan products, interest, fees, application forms and access.</div></div>
      <button class="btn btn-primary" onclick="openForm(null)"><svg viewBox="0 0 24 24"><path d="M12 5v14M5 12h14"/></svg>Add Loan Type</button>
    </div>
    <div class="stats">
      <div class="stat"><div class="l">Products</div><div class="v" id="stTotal">3</div></div>
      <div class="stat"><div class="l">Active</div><div class="v" id="stActive">3</div></div>
      <div class="stat"><div class="l">Staff-only</div><div class="v" id="stStaff">1</div></div>
      <div class="stat"><div class="l">Forms attached</div><div class="v" id="stForms">3</div></div>
    </div>
    <div class="card">
      <div class="thead-bar">
        <div class="filter"><input id="q" placeholder="Search loan types…"></div>
        <div class="filter"><select id="fStatus"><option value="">All statuses</option><option>Active</option><option>Inactive</option></select></div>
      </div>
      <div class="twrap"><table>
        <thead><tr><th>Loan Type</th><th>Code</th><th>Application Form</th><th>Access</th><th>Rate</th><th>Method</th><th>Status</th><th style="text-align:right">Actions</th></tr></thead>
        <tbody id="tbody"></tbody>
      </table></div>
    </div>
  </div>

  <!-- ═══ ADD / EDIT ═══ -->
  <div id="view-form" hidden>
    <div class="head">
      <div><h1 id="formTitle">Add Loan Type</h1><div class="sub">Define pricing, limits, application form and requirements.</div></div>
      <button class="btn btn-ghost" onclick="showList()">← Back to Loan Types</button>
    </div>
    <div class="card">
      <div class="formcard">
        <div class="fsec">Product Identity</div>
        <div class="fgrid">
          <div class="field"><label><svg class="lic" viewBox="0 0 24 24"><rect x="3" y="6" width="18" height="12" rx="2.5"/><circle cx="12" cy="12" r="2.6"/></svg>Loan Name</label><input id="f_name"></div>
          <div class="field"><label><svg class="lic" viewBox="0 0 24 24"><path d="M4 9h16M4 15h16M10 3 8 21M16 3l-2 18"/></svg>Loan Code</label><input id="f_code" placeholder="e.g. STF"></div>
          <div class="field full"><label>Description</label><textarea id="f_desc" rows="2"></textarea></div>
        </div>
        <div class="fsec">Pricing & Interest</div>
        <div class="fgrid">
          <div class="field"><label>Interest Rate</label><div class="ctl has-suf"><input id="f_rate"><span class="suf">%</span></div></div>
          <div class="field"><label>Interest Period</label><select id="f_iperiod"><option>Per Month</option><option>Per Quarter</option><option>Per Year</option></select></div>
          <div class="field"><label>Interest Method</label><select id="f_method"><option value="">— System default (REDUCING_BALANCE)</option><option>Reducing Balance</option><option>Flat Rate</option></select></div>
          <div class="field"><label>Application Form</label><select id="f_form"><option value="">— No form (product not available to members)</option><option>Standard Loan Application</option><option>Staff Loan Application</option><option>Pompo-Pompo Advance</option></select>
            <div class="hint">Determines which application format members see for this product.</div></div>
          <div class="field"><label>Insurance</label><div class="ctl has-suf"><input id="f_ins"><span class="suf">%</span></div></div>
          <div class="field"><label>Insurance Period</label><select><option>Per Month</option><option>One-off</option></select></div>
          <div class="field"><label>Processing Fee</label><div class="ctl has-suf"><input id="f_proc"><span class="suf">%</span></div></div>
        </div>
        <div class="fsec">Limits</div>
        <div class="fgrid">
          <div class="field"><label>Max Repayment Months</label><input id="f_maxm"></div>
          <div class="field"><label>Minimum Amount</label><div class="ctl has-suf"><input id="f_min"><span class="suf">MK</span></div></div>
          <div class="field"><label>Maximum Amount</label><div class="ctl has-suf"><input id="f_max"><span class="suf">MK</span></div></div>
        </div>
        <div class="fsec">Requirements & Status</div>
        <div class="swrow">
          <label class="sw"><input type="checkbox" id="f_guar" checked><i></i>Requires guarantor</label>
          <label class="sw"><input type="checkbox" id="f_coll"><i></i>Requires collateral</label>
          <label class="sw"><input type="checkbox" id="f_active" checked><i></i>Active</label>
        </div>
        <div class="fsec">Access</div>
        <div class="field"><button class="btn" onclick="openAccess(editingId||1)"><svg viewBox="0 0 24 24"><path d="M12 3l8 4v5c0 5-3.5 8-8 9-4.5-1-8-4-8-9V7z"/></svg>Manage member-class / company / user access</button>
        <div class="hint">Access can also be managed from the list via “Manage”.</div></div>
      </div>
      <div class="formbar">
        <button class="btn btn-ghost" onclick="showList()">Cancel</button>
        <button class="btn btn-primary" onclick="saveForm()">Save Loan Type</button>
      </div>
    </div>
  </div>
</div>

<!-- ═══ ACCESS MODAL ═══ -->
<div class="mov" id="accessModal"><div class="mmodal">
  <div class="mhead"><div><h3>Loan Product Access</h3><div class="ms" id="acProduct">—</div></div><button class="mclose" onclick="closeAccess()">✕</button></div>
  <div class="mbody">
    <div class="msec">Member classes allowed to apply</div>
    <div class="classcards">
      <label class="ccard2" id="ccOrd"><input type="checkbox" id="ac_ord"><span class="bx"></span><span><b>Ordinary members</b><small>Standard membership</small></span></label>
      <label class="ccard2" id="ccStaff"><input type="checkbox" id="ac_staff"><span class="bx"></span><span><b>Staff members</b><small>Employee / staff class</small></span></label>
    </div>
    <div class="hint">Leave both unchecked to allow every member class.</div>
    <div class="msec">Companies allowed to apply</div>
    <div class="cogrid">
      <label class="ccard2"><input type="checkbox" class="ac_co" value="ABC Corp Ltd"><span class="bx"></span><span><b>ABC Corp Ltd</b></span></label>
      <label class="ccard2"><input type="checkbox" class="ac_co" value="XYZ Ltd"><span class="bx"></span><span><b>XYZ Ltd</b></span></label>
    </div>
    <div class="hint">Leave all unchecked to allow every company.</div>
    <div class="msec">Individual users allowed to apply</div>
    <div class="usersearch"><input id="uq" placeholder="Search users…"></div>
    <div class="userlist" id="userList"></div>
    <div class="hint">Leave empty to allow by class / company only.</div>
  </div>
  <div class="mfoot"><button class="btn btn-ghost" onclick="closeAccess()">Cancel</button><button class="btn btn-primary" onclick="saveAccess()">Save Access</button></div>
</div></div>

<div class="ctoast" id="toast"><span id="toastMsg"></span></div>

<script>
const FORMS=['Standard Loan Application','Staff Loan Application','Pompo-Pompo Advance'];
let PRODUCTS=[
 {id:1,name:'Standard Loan',code:'STD',form:'Standard Loan Application',access:{classes:['ordinary','staff'],companies:[],users:[]},rate:15,method:'REDUCING_BALANCE',active:true},
 {id:2,name:'Staff Loan',code:'STF',form:'Staff Loan Application',access:{classes:['staff'],companies:[],users:[]},rate:12,method:'REDUCING_BALANCE',active:true,desc:'Preferential loan product for Sacco staff.',maxm:24,ins:0.5,proc:1.5,min:50000,max:500000,guar:true,coll:false},
 {id:3,name:'Pompo-Pompo Advance',code:'PMP',form:'Pompo-Pompo Advance',access:{classes:['ordinary','staff'],companies:[],users:[]},rate:14,method:'REDUCING_BALANCE',active:true}];
const USERS=[['Chikondi Phiri','director1'],['Dalitso Gondwe','officer1'],['Kondwani Mtambo','payroll3'],['Lusungu Kachale','director3'],['Member User0','member1'],['Member User1','member2'],['Member User2','member3']];
let editingId=null, accessId=null;
const $=id=>document.getElementById(id);
const IC={
 edit:'<svg viewBox="0 0 24 24"><path d="M12 20h9"/><path d="M16.5 3.5l4 4L7 21l-4 1 1-4z"/></svg>',
 ban:'<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M5.7 5.7l12.6 12.6"/></svg>',
 check:'<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M8 12.5l2.7 2.7L16 9.5"/></svg>'};
function toast(m){$('toastMsg').textContent=m;$('toast').classList.add('show');setTimeout(()=>$('toast').classList.remove('show'),2400)}
function showList(){$('view-list').hidden=false;$('view-form').hidden=true;renderList()}
function renderList(){const q=($('q').value||'').toLowerCase();const st=$('fStatus').value;
 const rows=PRODUCTS.filter(p=>(!q||p.name.toLowerCase().includes(q)||p.code.toLowerCase().includes(q))&&(!st||(st==='Active')===p.active));
 $('tbody').innerHTML=rows.map(p=>`<tr>
  <td><span class="name">${p.name}</span></td><td class="mono">${p.code}</td>
  <td><select onchange="setForm(${p.id},this.value)">${FORMS.map(f=>`<option ${p.form===f?'selected':''}>${f}</option>`).join('')}</select></td>
  <td><div class="achips">${(p.access.classes.length?p.access.classes.map(c=>`<span class="achip">${c==='ordinary'?'Ordinary':'Staff'}</span>`).join(''):'<span class="achip">All</span>')}<button class="linkbtn" onclick="openAccess(${p.id})">Manage</button></div></td>
  <td class="rate">${p.rate}% <small>p.m.</small></td><td class="mono">${p.method}</td>
  <td><span class="pill ${p.active?'on':'off'}">${p.active?'Active':'Inactive'}</span></td>
  <td><div class="rowact">
    <button class="act" title="Edit" aria-label="Edit ${p.name}" onclick="openForm(${p.id})">${IC.edit}</button>
    <button class="act ${p.active?'danger':'good'}" title="${p.active?'Deactivate':'Activate'}" aria-label="${p.active?'Deactivate':'Activate'} ${p.name}" onclick="toggleActive(${p.id})">${p.active?IC.ban:IC.check}</button>
  </div></td></tr>`).join('');
 $('stTotal').textContent=PRODUCTS.length;$('stActive').textContent=PRODUCTS.filter(p=>p.active).length;
 $('stStaff').textContent=PRODUCTS.filter(p=>p.access.classes.join()==='staff').length;
 $('stForms').textContent=PRODUCTS.filter(p=>p.form).length;}
$('q').oninput=renderList;$('fStatus').onchange=renderList;
function setForm(id,v){PRODUCTS.find(p=>p.id===id).form=v;toast('Application form updated');renderList()}
function toggleActive(id){const p=PRODUCTS.find(x=>x.id===id);p.active=!p.active;toast(p.name+(p.active?' activated':' deactivated'));renderList()}
function openForm(id){editingId=id;$('view-list').hidden=true;$('view-form').hidden=false;
 const p=id?PRODUCTS.find(x=>x.id===id):null;$('formTitle').textContent=p?'Edit Loan Type':'Add Loan Type';
 $('f_name').value=p?p.name:'';$('f_code').value=p?p.code:'';$('f_desc').value=p&&p.desc?p.desc:'';
 $('f_rate').value=p?p.rate:'';$('f_method').value=p&&p.method==='FLAT'?'Flat Rate':(p?'Reducing Balance':'');
 $('f_form').value=p&&p.form?p.form:'';$('f_ins').value=p&&p.ins!=null?p.ins:0;$('f_proc').value=p&&p.proc!=null?p.proc:0;
 $('f_maxm').value=p&&p.maxm?p.maxm:'';$('f_min').value=p&&p.min?p.min:0;$('f_max').value=p&&p.max?p.max:'';
 $('f_guar').checked=!p||p.guar!==false;$('f_coll').checked=!!(p&&p.coll);$('f_active').checked=!p||p.active;}
function saveForm(){if(!$('f_name').value)return alert('Loan name required');toast('Loan type saved');showList()}
function openAccess(id){accessId=id;const p=PRODUCTS.find(x=>x.id===id);$('acProduct').textContent=p.name;
 $('ac_ord').checked=p.access.classes.includes('ordinary');$('ac_staff').checked=p.access.classes.includes('staff');
 syncCards();document.querySelectorAll('.ac_co').forEach(c=>c.checked=p.access.companies.includes(c.value));
 renderUsers(p);$('accessModal').classList.add('show')}
function closeAccess(){$('accessModal').classList.remove('show')}
function syncCards(){$('ccOrd').classList.toggle('on',$('ac_ord').checked);$('ccStaff').classList.toggle('on',$('ac_staff').checked)}
$('ac_ord').onchange=syncCards;$('ac_staff').onchange=syncCards;
document.querySelectorAll('.ccard2').forEach(c=>{const i=c.querySelector('input');if(i)i.addEventListener('change',()=>c.classList.toggle('on',i.checked))});
function renderUsers(p){$('userList').innerHTML=USERS.map(u=>`<label class="urow"><input type="checkbox" class="ac_u" value="${u[1]}" ${(p.access.users||[]).includes(u[1])?'checked':''}> ${u[0]} <span class="role">${u[1].replace(/\d/,'')}</span></label>`).join('')}
$('uq').oninput=e=>{const q=e.target.value.toLowerCase();document.querySelectorAll('.urow').forEach(r=>r.style.display=r.textContent.toLowerCase().includes(q)?'':'none')};
function saveAccess(){const p=PRODUCTS.find(x=>x.id===accessId);p.access.classes=[];
 if($('ac_ord').checked)p.access.classes.push('ordinary');if($('ac_staff').checked)p.access.classes.push('staff');
 p.access.companies=[...document.querySelectorAll('.ac_co:checked')].map(c=>c.value);
 p.access.users=[...document.querySelectorAll('.ac_u:checked')].map(c=>c.value);
 closeAccess();renderList();toast('Access rules saved')}
renderList();
</script>
</body>
</html>
```

---

## 4. REFERENCE DESIGN — USERS & COMPANIES (membership classes, payroll officer assignment)

**Routes to wire (per section 2):** `admin/users` (index/create/store/edit/update), `admin/companies` (index/create/store/edit/update), `admin/companies/{id}/payroll-officers` (assignPayrollOfficers).

**Reproduce this exact screen set.** It has four full-page views (`view-users`, `view-userform`, `view-companies`, `view-companyform`) toggled by a shared view-bar, plus a modal (`poModal`) for assigning payroll officers to a company from the list. Below is the complete, exact HTML/CSS/JS reference — replace only the mock arrays (`COMPANIES`, `USERS`) and inline handler functions (`renderUsers`, `openUser`, `saveUser`, `renderCompanies`, `openCompany`, `saveCompany`, `openPO`, `savePO`, `toggleUser`, `toggleCompany`) with real server-rendered/Blade+Alpine equivalents backed by the `users` (incl. new `member_classes` JSON column), `companies`, and `company_payroll_officer` pivot tables. Keep every class name, layout, copy string, icon, and interaction exactly as shown.

**Wiring specifics for this module:**
- "Register User" → real create route. On save: write `member_classes` from the two class-cards (`Ordinary member` / `Staff member` — checking one = single-class array, both = `["ordinary","staff"]`, none = `[]`), and sync `is_member = count(member_classes) > 0` server-side exactly as the `mcHint` live-text in the reference describes.
- Users list Membership column chips (amber "Ordinary", blue "Staff", or "Not member") must reflect the real `member_classes` value per user.
- Identity Documents upload cards (Passport Photo, ID Front, ID Back, Employment Contract) must become real file upload inputs (styled to match the `.upcard` look, including the `.done` state once a file is attached) — wire to actual storage, not just a CSS toggle.
- Company select in the user form is required server-side (`*` marked field) — every user must belong to a company.
- Companies list "Payroll Officers" column: chips + "Manage"/"Assign" link must open the real `poModal`, prefilled from the `company_payroll_officer` pivot, listing only users with role Payroll Officer; Save must write the pivot (`assigned_by` = the authenticated admin) via `assignPayrollOfficers`.
- Company add/edit form's own "Payroll Officers" checklist section must save the same pivot on company save (not just via the separate modal) — both paths must stay in sync.
- Stat chips on both list views (Users/Members/Staff-class/Active and Companies/Active/Payroll officers) must be real query counts.

```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1">
<title>Kuwala SACCO — Users & Companies Admin (Redesign)</title>
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;600;700&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
:root{--navy:#16202C;--navy2:#1E2A3A;--amber:#F6A21E;--amber2:#FFC466;--amber-ink:#9A5B00;--amber-soft:rgba(246,162,30,.16);
--bg:#F4F6F9;--panel:#fff;--line:rgba(22,32,44,.10);--line-soft:rgba(22,32,44,.06);--ink:#16202C;--body-c:#3D4756;--mut:#5D6B7E;--good:#0E9F6E;--good-soft:rgba(14,159,110,.12);--bad:#D92D20;--bad-soft:rgba(217,45,32,.10);
--r-lg:16px;--sh-2:0 1px 2px rgba(16,24,40,.05),0 16px 40px -24px rgba(16,24,40,.16);--font-sans:'Inter',system-ui,sans-serif;--font-disp:'Space Grotesk','Inter',sans-serif}
*{margin:0;padding:0;box-sizing:border-box}body{font-family:var(--font-sans);font-size:15px;line-height:1.6;background:var(--bg);color:var(--body-c)}
button{font-family:inherit;cursor:pointer}:focus-visible{outline:2px solid rgba(246,162,30,.7);offset:2px;border-radius:8px}
.wrap{max-width:1240px;margin:0 auto;padding:26px 24px 80px}
.crumb{font-size:12px;color:var(--mut);text-transform:uppercase;letter-spacing:.06em;font-weight:600}.crumb b{color:var(--ink)}
.viewbar{display:flex;gap:8px;flex-wrap:wrap;margin:14px 0 20px}
.vbtn{font-size:13px;font-weight:600;border:1px solid var(--line);background:#fff;border-radius:999px;padding:9px 16px;color:var(--mut)}
.vbtn.on{background:var(--navy);border-color:var(--navy);color:#fff}
.head{display:flex;justify-content:space-between;align-items:flex-end;gap:16px;flex-wrap:wrap;margin:6px 0 16px}
h1{font:700 26px var(--font-disp);color:var(--ink)}.sub{font-size:13.5px;color:var(--mut)}
.btn{display:inline-flex;gap:8px;align-items:center;font-size:13.5px;font-weight:600;border-radius:12px;padding:11px 16px;border:1px solid var(--line);background:#fff;color:var(--ink);transition:.2s}
.btn:hover{border-color:var(--amber);background:var(--amber-soft)}
.btn svg{width:15px;height:15px;stroke:currentColor;fill:none;stroke-width:1.8;stroke-linecap:round;stroke-linejoin:round}
.btn-primary{background:linear-gradient(180deg,var(--amber2),var(--amber));border-color:var(--amber);color:var(--navy);font-weight:700}
.btn-primary:hover{transform:translateY(-1px);box-shadow:0 10px 24px -8px rgba(246,162,30,.55)}
.btn-ghost{background:transparent}
.stats{display:flex;gap:10px;flex-wrap:wrap;margin-bottom:16px}
.stat{background:var(--panel);border:1px solid var(--line);border-radius:12px;padding:10px 16px;box-shadow:var(--sh-2)}
.stat .l{font-size:11px;letter-spacing:.1em;text-transform:uppercase;color:var(--mut);font-weight:600}
.stat .v{font:700 18px var(--font-disp);color:var(--ink)}
.card{background:var(--panel);border:1px solid var(--line);border-radius:var(--r-lg);box-shadow:var(--sh-2);overflow:hidden}
.thead-bar{display:flex;justify-content:space-between;align-items:center;gap:12px;padding:16px 22px;border-bottom:1px solid var(--line);flex-wrap:wrap}
.filter input,.filter select{font:inherit;font-size:13.5px;border:1px solid var(--line);border-radius:11px;padding:9px 13px}
.filter input:focus{border-color:var(--amber);box-shadow:0 0 0 3px var(--amber-soft);outline:none}
.twrap{overflow:auto}table{width:100%;border-collapse:collapse;min-width:960px}
th{position:sticky;top:0;background:linear-gradient(150deg,var(--navy),var(--navy2));color:#EAF0F6;font-size:11.5px;letter-spacing:.1em;text-transform:uppercase;font-weight:600;text-align:left;padding:12px 16px}
td{font-size:14px;padding:12px 16px;border-bottom:1px solid var(--line-soft);vertical-align:middle}
tbody tr:hover{background:#F7F9FC}
.ucell{display:flex;gap:10px;align-items:center}.ucell .name{font-weight:600;color:var(--ink)}
.avatar{width:34px;height:34px;border-radius:50%;background:var(--navy);color:var(--amber2);display:grid;place-items:center;font:700 12px var(--font-disp);flex:0 0 34px}
.mono{font-size:12.5px;color:var(--mut);font-variant-numeric:tabular-nums}
.rolepill{font-size:11px;font-weight:700;background:#EEF2F6;border:1px solid var(--line-soft);border-radius:999px;padding:3px 10px;color:var(--body-c);white-space:nowrap}
.achips{display:flex;gap:5px;flex-wrap:wrap;align-items:center}
.achip{font-size:11px;font-weight:600;background:#F1F4F8;border:1px solid var(--line-soft);border-radius:999px;padding:3px 10px;color:var(--body-c)}
.achip.ord{background:var(--amber-soft);border-color:rgba(180,105,14,.3);color:var(--amber-ink)}
.achip.stf{background:#E8F1FB;border-color:rgba(35,96,143,.3);color:#23608F}
.pill{font-size:11px;font-weight:700;padding:4px 11px;border-radius:999px}.pill.on{background:var(--good-soft);color:var(--good)}.pill.off{background:var(--bad-soft);color:var(--bad)}
.linkbtn{border:0;background:none;color:var(--amber-ink);font-size:12.5px;font-weight:700}
.linkbtn:hover{text-decoration:underline}
.rowact{display:flex;gap:6px;justify-content:flex-end}
.act{width:34px;height:34px;border-radius:10px;border:1px solid var(--line);background:#fff;display:grid;place-items:center;color:var(--mut);transition:.15s}
.act svg{width:16px;height:16px;stroke:currentColor;fill:none;stroke-width:1.8;stroke-linecap:round;stroke-linejoin:round}
.act:hover{border-color:var(--amber);color:var(--amber-ink);background:var(--amber-soft)}
.act.danger:hover{border-color:var(--bad);color:var(--bad);background:var(--bad-soft)}
.act.good:hover{border-color:var(--good);color:var(--good);background:var(--good-soft)}
/* forms */
.formcard{padding:26px 28px}
.fsec{display:flex;align-items:center;gap:8px;font-size:11.5px;font-weight:700;letter-spacing:.14em;text-transform:uppercase;color:var(--mut);margin:24px 0 12px}
.fsec::after{content:"";flex:1;height:1px;background:var(--line-soft)}.fsec:first-child{margin-top:0}
.fgrid{display:grid;grid-template-columns:repeat(3,1fr);gap:14px}.fgrid .full{grid-column:1/-1}.fgrid .h2{grid-column:span 1}
.field label{display:flex;gap:7px;align-items:center;font-size:13px;font-weight:600;color:var(--ink);margin-bottom:6px}
.field input,.field select,.field textarea{width:100%;font:inherit;font-size:14.5px;color:var(--ink);background:#fff;border:1px solid var(--line);border-radius:11px;padding:10px 13px;transition:.18s}
.field input:focus,.field select:focus{border-color:var(--amber);box-shadow:0 0 0 3px var(--amber-soft);outline:none}
.hint{font-size:11.5px;color:var(--mut);margin-top:5px}
.classcards{display:grid;grid-template-columns:1fr 1fr;gap:10px}
.ccard2{border:1.5px solid var(--line);border-radius:12px;padding:12px 14px;display:flex;gap:10px;align-items:center;cursor:pointer;transition:.18s}
.ccard2 input{display:none}.ccard2 .bx{width:20px;height:20px;border-radius:6px;border:1.5px solid var(--line);display:grid;place-items:center;color:#fff;font-size:12px}
.ccard2 input:checked~.bx{background:var(--amber);border-color:var(--amber)}.ccard2 input:checked~.bx::after{content:"✓"}
.ccard2.on{border-color:var(--amber);background:var(--amber-soft)}
.ccard2 b{font-size:13.5px;color:var(--ink)}.ccard2 small{display:block;font-size:11.5px;color:var(--mut)}
.upgrid{display:grid;grid-template-columns:repeat(4,1fr);gap:12px}
.upcard{border:1.5px dashed var(--line);border-radius:12px;padding:16px 12px;text-align:center;background:#FBFCFE;transition:.18s;cursor:pointer}
.upcard:hover{border-color:var(--amber);background:var(--amber-soft)}
.upcard .ic{width:34px;height:34px;border-radius:10px;background:var(--amber-soft);color:var(--amber-ink);display:grid;place-items:center;margin:0 auto 8px;font-weight:700}
.upcard b{font-size:12.5px;color:var(--ink);display:block}.upcard small{font-size:11px;color:var(--mut)}
.upcard.done{border-style:solid;border-color:var(--good);background:var(--good-soft)}
.formbar{position:sticky;bottom:0;background:rgba(255,255,255,.92);backdrop-filter:blur(8px);border-top:1px solid var(--line);padding:14px 28px;display:flex;gap:10px;justify-content:flex-end}
/* modal */
.mov{position:fixed;inset:0;background:rgba(22,32,44,.5);backdrop-filter:blur(3px);display:flex;align-items:center;justify-content:center;padding:20px;opacity:0;pointer-events:none;transition:.25s;z-index:60}
.mov.show{opacity:1;pointer-events:auto}
.mmodal{background:#fff;border-radius:18px;max-width:560px;width:100%;max-height:90vh;display:flex;flex-direction:column;overflow:hidden;transform:translateY(14px);transition:.25s}
.mov.show .mmodal{transform:none}
.mhead{display:flex;justify-content:space-between;align-items:center;padding:18px 22px;background:linear-gradient(150deg,var(--navy),var(--navy2))}
.mhead h3{font:600 17px var(--font-disp);color:#fff}.mhead .ms{font-size:12px;color:#AEB9C6}
.mclose{width:32px;height:32px;border-radius:9px;border:1px solid rgba(255,255,255,.25);background:rgba(255,255,255,.08);color:#AEB9C6}
.mbody{padding:20px 22px;overflow:auto}
.urow{display:flex;gap:10px;align-items:center;padding:10px 13px;border:1px solid var(--line-soft);border-radius:12px;margin-bottom:8px;font-size:13.5px;cursor:pointer}
.urow:hover{background:#F7F9FC}.urow input{accent-color:var(--amber)}
.urow .co{margin-left:auto;font-size:11px;color:var(--mut);background:#F1F4F8;border-radius:999px;padding:2px 9px}
.mfoot{padding:14px 22px;border-top:1px solid var(--line);display:flex;gap:10px;justify-content:flex-end}
.ctoast{position:fixed;right:24px;bottom:24px;background:var(--navy);color:#fff;border-radius:14px;padding:12px 18px;font-size:13.5px;opacity:0;transform:translateY(10px);transition:.3s;z-index:90}
.ctoast.show{opacity:1;transform:none}
@media(max-width:900px){.fgrid{grid-template-columns:1fr 1fr}.upgrid{grid-template-columns:1fr 1fr}}
@media(max-width:640px){.fgrid,.classcards,.upgrid{grid-template-columns:1fr}}
</style>
</head>
<body>
<div class="wrap">
  <div class="crumb">Administration · <b>People & Companies</b></div>
  <div class="viewbar">
    <button class="vbtn on" data-view="users">Users</button>
    <button class="vbtn" data-view="userform">Register / Edit User</button>
    <button class="vbtn" data-view="companies">Companies</button>
    <button class="vbtn" data-view="companyform">Add / Edit Company</button>
  </div>

  <!-- ═══ USERS LIST ═══ -->
  <div id="view-users">
    <div class="head"><div><h1>Users</h1><div class="sub">Register and manage users, membership classes and roles.</div></div>
      <button class="btn btn-primary" onclick="openUser(null)"><svg viewBox="0 0 24 24"><path d="M12 5v14M5 12h14"/></svg>Register User</button></div>
    <div class="stats">
      <div class="stat"><div class="l">Users</div><div class="v" id="uTotal">0</div></div>
      <div class="stat"><div class="l">Members</div><div class="v" id="uMembers">0</div></div>
      <div class="stat"><div class="l">Staff-class</div><div class="v" id="uStaff">0</div></div>
      <div class="stat"><div class="l">Active</div><div class="v" id="uActive">0</div></div>
    </div>
    <div class="card">
      <div class="thead-bar">
        <div class="filter"><input id="uq" placeholder="Search name, email or username…"></div>
        <div class="filter"><select id="uRole"><option value="">All roles</option><option>Member</option><option>Loan Officer</option><option>Payroll Officer</option><option>Manager Accountant</option><option>Director</option><option>Administrator</option></select></div>
        <div class="filter"><select id="uStatus"><option value="">All statuses</option><option>Active</option><option>Inactive</option></select></div>
      </div>
      <div class="twrap"><table>
        <thead><tr><th>User</th><th>Username</th><th>Email</th><th>Role</th><th>Company</th><th>Membership</th><th>Status</th><th style="text-align:right">Actions</th></tr></thead>
        <tbody id="uBody"></tbody></table></div>
    </div>
  </div>

  <!-- ═══ USER FORM ═══ -->
  <div id="view-userform" hidden>
    <div class="head"><div><h1 id="uTitle">Register User</h1><div class="sub">New users receive an emailed secure link to set their first password. Usernames are auto-generated and globally unique.</div></div>
      <button class="btn btn-ghost" onclick="show('users')">← Back to Users</button></div>
    <div class="card"><div class="formcard">
      <div class="fsec">Personal Details</div>
      <div class="fgrid">
        <div class="field"><label>First Name</label><input id="uf_first"></div>
        <div class="field"><label>Other Name</label><input id="uf_other"></div>
        <div class="field"><label>Last Name</label><input id="uf_last"></div>
        <div class="field"><label>Date of Birth</label><input type="date" id="uf_dob"></div>
        <div class="field"><label>Marital Status</label><select id="uf_marital"><option>Single</option><option>Married</option><option>Divorced</option><option>Widowed</option></select></div>
        <div class="field"><label>Employment Type</label><select id="uf_emp"><option>Permanent</option><option>Contract</option></select></div>
      </div>
      <div class="fsec">Contact & Identity</div>
      <div class="fgrid">
        <div class="field"><label>Email</label><input type="email" id="uf_email"></div>
        <div class="field"><label>National ID</label><input id="uf_nid"></div>
        <div class="field"><label>Telephone</label><input id="uf_tel"></div>
      </div>
      <div class="fsec">Role & Assignment</div>
      <div class="fgrid">
        <div class="field"><label>Role</label><select id="uf_role"><option>Member</option><option>Loan Officer</option><option>Payroll Officer</option><option>Manager Accountant</option><option>Operations Officer</option><option>Director</option><option>Administrator</option></select></div>
        <div class="field"><label>Company <span style="color:var(--bad)">*</span></label><select id="uf_company"></select><div class="hint">Every user must belong to a company.</div></div>
        <div class="field"><label>Branch</label><select id="uf_branch"><option>— None —</option><option>Lilongwe</option><option>Blantyre</option></select></div>
      </div>
      <div class="fsec">Membership / Loan-Access Class</div>
      <div class="classcards">
        <label class="ccard2" id="mcOrd"><input type="checkbox" id="uf_ord"><span class="bx"></span><span><b>Ordinary member</b><small>Access to ordinary loan products</small></span></label>
        <label class="ccard2" id="mcStaff"><input type="checkbox" id="uf_staff"><span class="bx"></span><span><b>Staff member</b><small>Access to staff loan products</small></span></label>
      </div>
      <div class="hint" id="mcHint">Leave both unchecked — user cannot apply for loans. Check one or both to grant the matching loan access.</div>
      <div class="fsec">Identity Documents</div>
      <div class="upgrid">
        <div class="upcard" onclick="this.classList.toggle('done')"><span class="ic">📷</span><b>Passport Photo</b><small>JPG/PNG · click to attach</small></div>
        <div class="upcard" onclick="this.classList.toggle('done')"><span class="ic">🪪</span><b>ID Front</b><small>JPG/PNG/PDF</small></div>
        <div class="upcard" onclick="this.classList.toggle('done')"><span class="ic">🪪</span><b>ID Back</b><small>JPG/PNG/PDF</small></div>
        <div class="upcard" onclick="this.classList.toggle('done')"><span class="ic">📄</span><b>Employment Contract</b><small>Required if Contract</small></div>
      </div>
    </div>
    <div class="formbar"><button class="btn btn-ghost" onclick="show('users')">Cancel</button><button class="btn btn-primary" id="uf_save" onclick="saveUser()">Create User</button></div>
    </div>
  </div>

  <!-- ═══ COMPANIES LIST ═══ -->
  <div id="view-companies">
    <div class="head"><div><h1>Companies</h1><div class="sub">Employer companies that pay in member payroll, and their payroll officers.</div></div>
      <button class="btn btn-primary" onclick="openCompany(null)"><svg viewBox="0 0 24 24"><path d="M12 5v14M5 12h14"/></svg>Add Company</button></div>
    <div class="stats">
      <div class="stat"><div class="l">Companies</div><div class="v" id="cTotal">0</div></div>
      <div class="stat"><div class="l">Active</div><div class="v" id="cActive">0</div></div>
      <div class="stat"><div class="l">Payroll officers</div><div class="v" id="cPO">0</div></div>
    </div>
    <div class="card"><div class="twrap"><table>
      <thead><tr><th>Company</th><th>Code</th><th>Contact</th><th>Users</th><th>Payroll Officers</th><th>Status</th><th style="text-align:right">Actions</th></tr></thead>
      <tbody id="cBody"></tbody></table></div></div>
  </div>

  <!-- ═══ COMPANY FORM ═══ -->
  <div id="view-companyform" hidden>
    <div class="head"><div><h1 id="cTitle">Add Company</h1><div class="sub">Company identity, contact details and payroll-officer assignments.</div></div>
      <button class="btn btn-ghost" onclick="show('companies')">← Back to Companies</button></div>
    <div class="card"><div class="formcard">
      <div class="fsec">Company Identity</div>
      <div class="fgrid">
        <div class="field h2"><label>Company Name <span style="color:var(--bad)">*</span></label><input id="cf_name"></div>
        <div class="field"><label>Company Code</label><input id="cf_code" placeholder="e.g. KUW"></div>
      </div>
      <div class="fsec">Contact</div>
      <div class="fgrid">
        <div class="field"><label>Contact Email</label><input type="email" id="cf_email"></div>
        <div class="field"><label>Contact Phone</label><input id="cf_phone"></div>
        <div class="field full"><label>Address</label><textarea id="cf_addr" rows="2"></textarea></div>
      </div>
      <div class="fsec">Payroll Officers</div>
      <div class="hint" style="margin:-6px 0 10px">Assign one or more payroll officers to this company. They will assess payroll for its members only.</div>
      <div id="cf_poList"></div>
    </div>
    <div class="formbar"><button class="btn btn-ghost" onclick="show('companies')">Cancel</button><button class="btn btn-primary" onclick="saveCompany()">Save Company</button></div>
    </div>
  </div>
</div>

<!-- ASSIGN PO MODAL -->
<div class="mov" id="poModal"><div class="mmodal">
  <div class="mhead"><div><h3>Assign Payroll Officers</h3><div class="ms" id="poCompany">—</div></div><button class="mclose" onclick="closePO()">✕</button></div>
  <div class="mbody"><div id="poList"></div></div>
  <div class="mfoot"><button class="btn btn-ghost" onclick="closePO()">Cancel</button><button class="btn btn-primary" onclick="savePO()">Save Assignments</button></div>
</div></div>

<div class="ctoast" id="toast"><span id="toastMsg"></span></div>

<script>
let COMPANIES=[
 {id:1,name:'Kuwala Head Office',code:'KUW',email:'info@kuwala.mw',active:true,po:[5],users:12},
 {id:2,name:'AgroFresh Malawi Ltd',code:'AGR',email:'hr@agrofresh.mw',active:true,po:[7],users:1},
 {id:3,name:'Sunrise Textiles Ltd',code:'SUN',email:'hr@sunrise.mw',active:true,po:[6],users:1}];
let USERS=[
 {id:1,name:'Member User3',username:'member4',email:'member4@kuwala.mw',role:'Member',company:1,classes:['ordinary'],active:true},
 {id:2,name:'Member User2',username:'member3',email:'member3@kuwala.mw',role:'Member',company:1,classes:['ordinary'],active:true},
 {id:3,name:'Member User0',username:'member1',email:'member1@kuwala.mw',role:'Member',company:1,classes:['ordinary'],active:true},
 {id:4,name:'Member User1',username:'member2',email:'member2@kuwala.mw',role:'Member',company:1,classes:['ordinary','staff'],active:true},
 {id:5,name:'Kondwani Mtambo',username:'payroll3',email:'payroll3@kuwala.mw',role:'Payroll Officer',company:1,classes:[],active:true},
 {id:6,name:'Tamandani Zulu',username:'payroll2',email:'payroll2@kuwala.mw',role:'Payroll Officer',company:3,classes:[],active:true},
 {id:7,name:'Wongani Chirwa',username:'payroll1',email:'payroll1@kuwala.mw',role:'Payroll Officer',company:2,classes:[],active:true},
 {id:8,name:'Yamikani Nyirenda',username:'officer2',email:'officer2@kuwala.mw',role:'Loan Officer',company:1,classes:[],active:true},
 {id:9,name:'Dalitso Gondwe',username:'officer1',email:'officer1@kuwala.mw',role:'Loan Officer',company:1,classes:[],active:true},
 {id:10,name:'Lusungu Kachale',username:'director3',email:'director3@kuwala.mw',role:'Director',company:1,classes:['ordinary'],active:true},
 {id:11,name:'Mphatso Banda',username:'manager',email:'manager@kuwala.mw',role:'Manager Accountant',company:1,classes:['ordinary'],active:true},
 {id:12,name:'System Administrator',username:'admin',email:'admin@kuwala.mw',role:'Administrator',company:1,classes:[],active:true}];
let editUserId=null, editCompanyId=null, poCompanyId=null;
const $=id=>document.getElementById(id);
const IC={edit:'<svg viewBox="0 0 24 24"><path d="M12 20h9"/><path d="M16.5 3.5l4 4L7 21l-4 1 1-4z"/></svg>',ban:'<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M5.7 5.7l12.6 12.6"/></svg>',check:'<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M8 12.5l2.7 2.7L16 9.5"/></svg>',po:'<svg viewBox="0 0 24 24"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M19 8v6M22 11h-6"/></svg>'};
function toast(m){$('toastMsg').textContent=m;$('toast').classList.add('show');setTimeout(()=>$('toast').classList.remove('show'),2400)}
function show(v){['users','userform','companies','companyform'].forEach(x=>$('view-'+x).hidden=(x!==v));
 document.querySelectorAll('.vbtn').forEach(b=>b.classList.toggle('on',b.dataset.view===v));}
document.querySelectorAll('.vbtn').forEach(b=>b.onclick=()=>{show(b.dataset.view);
 if(b.dataset.view==='users')renderUsers(); if(b.dataset.view==='companies')renderCompanies();});
/* users */
function mchips(u){if(!u.classes.length)return '<span class="achip">Not member</span>';
 return u.classes.map(c=>`<span class="achip ${c==='ordinary'?'ord':'stf'}">${c==='ordinary'?'Ordinary':'Staff'}</span>`).join('')}
function renderUsers(){const q=($('uq').value||'').toLowerCase();const r=$('uRole').value;const s=$('uStatus').value;
 const rows=USERS.filter(u=>(!q||u.name.toLowerCase().includes(q)||u.email.includes(q)||u.username.includes(q))&&(!r||u.role===r)&&(!s||(s==='Active')===u.active));
 $('uBody').innerHTML=rows.map(u=>`<tr>
  <td><div class="ucell"><span class="avatar">${u.name.split(' ').map(w=>w[0]).join('').slice(0,2).toUpperCase()}</span><span class="name">${u.name}</span></div></td>
  <td class="mono">${u.username}</td><td>${u.email}</td><td><span class="rolepill">${u.role}</span></td>
  <td>${(COMPANIES.find(c=>c.id===u.company)||{}).name||'—'}</td><td><div class="achips">${mchips(u)}</div></td>
  <td><span class="pill ${u.active?'on':'off'}">${u.active?'Active':'Inactive'}</span></td>
  <td><div class="rowact"><button class="act" title="Edit" aria-label="Edit ${u.name}" onclick="openUser(${u.id})">${IC.edit}</button>
  <button class="act ${u.active?'danger':'good'}" title="${u.active?'Deactivate':'Activate'}" onclick="toggleUser(${u.id})">${u.active?IC.ban:IC.check}</button></div></td></tr>`).join('');
 $('uTotal').textContent=USERS.length;$('uMembers').textContent=USERS.filter(u=>u.classes.length).length;
 $('uStaff').textContent=USERS.filter(u=>u.classes.includes('staff')).length;$('uActive').textContent=USERS.filter(u=>u.active).length;}
$('uq').oninput=renderUsers;$('uRole').onchange=renderUsers;$('uStatus').onchange=renderUsers;
function toggleUser(id){const u=USERS.find(x=>x.id===id);u.active=!u.active;toast(u.name+(u.active?' activated':' deactivated'));renderUsers()}
function openUser(id){editUserId=id;show('userform');$('uTitle').textContent=id?'Edit User':'Register User';$('uf_save').textContent=id?'Save Changes':'Create User';
 $('uf_company').innerHTML=COMPANIES.map(c=>`<option value="${c.id}">${c.name}</option>`).join('');
 const u=id?USERS.find(x=>x.id===id):null;
 $('uf_first').value=u?u.name.split(' ')[0]:'';$('uf_last').value=u?u.name.split(' ').slice(1).join(' '):'';
 $('uf_email').value=u?u.email:'';$('uf_role').value=u?u.role:'Member';$('uf_company').value=u?u.company:1;
 $('uf_ord').checked=!!(u&&u.classes.includes('ordinary'));$('uf_staff').checked=!!(u&&u.classes.includes('staff'));syncMC();}
function syncMC(){$('mcOrd').classList.toggle('on',$('uf_ord').checked);$('mcStaff').classList.toggle('on',$('uf_staff').checked);
 const both=$('uf_ord').checked&&$('uf_staff').checked;
 $('mcHint').textContent=both?'Both — full access to ordinary AND staff loan products.':(!$('uf_ord').checked&&!$('uf_staff').checked)?'Leave both unchecked — user cannot apply for loans.':'One class granted — matching loan products only.'}
$('uf_ord').onchange=syncMC;$('uf_staff').onchange=syncMC;
document.querySelectorAll('.ccard2').forEach(c=>{const i=c.querySelector('input');if(i)i.addEventListener('change',()=>c.classList.toggle('on',i.checked))});
function saveUser(){if(!$('uf_first').value)return alert('First name required');toast('User saved');show('users');renderUsers()}
/* companies */
function poChips(c){return c.po.length?c.po.map(id=>{const u=USERS.find(x=>x.id===id);return `<span class="achip stf">${u?u.name:'—'}</span>`}).join('')+'<button class="linkbtn" onclick="openPO('+c.id+')">Manage</button>':'<span class="achip">None</span><button class="linkbtn" onclick="openPO('+c.id+')">Assign</button>'}
function renderCompanies(){$('cBody').innerHTML=COMPANIES.map(c=>`<tr>
 <td><span class="name" style="font-weight:600;color:var(--ink)">${c.name}</span></td><td class="mono">${c.code}</td>
 <td>${c.email||'—'}</td><td>${c.users||0}</td><td><div class="achips">${poChips(c)}</div></td>
 <td><span class="pill ${c.active?'on':'off'}">${c.active?'Active':'Inactive'}</span></td>
 <td><div class="rowact"><button class="act" title="Assign payroll officers" onclick="openPO(${c.id})">${IC.po}</button>
 <button class="act" title="Edit" onclick="openCompany(${c.id})">${IC.edit}</button>
 <button class="act ${c.active?'danger':'good'}" title="${c.active?'Deactivate':'Activate'}" onclick="toggleCompany(${c.id})">${c.active?IC.ban:IC.check}</button></div></td></tr>`).join('');
 $('cTotal').textContent=COMPANIES.length;$('cActive').textContent=COMPANIES.filter(c=>c.active).length;
 $('cPO').textContent=COMPANIES.reduce((a,c)=>a+c.po.length,0);}
function toggleCompany(id){const c=COMPANIES.find(x=>x.id===id);c.active=!c.active;toast(c.name+(c.active?' activated':' deactivated'));renderCompanies()}
function openCompany(id){editCompanyId=id;show('companyform');$('cTitle').textContent=id?'Edit Company':'Add Company';
 const c=id?COMPANIES.find(x=>x.id===id):null;
 $('cf_name').value=c?c.name:'';$('cf_code').value=c?c.code:'';$('cf_email').value=c?c.email:'';$('cf_addr').value='';
 renderPOList(c?c.po:[]);}
function renderPOList(assigned){const pos=USERS.filter(u=>u.role==='Payroll Officer');
 $('cf_poList').innerHTML=pos.map(u=>`<label class="urow"><input type="checkbox" class="cf_po" value="${u.id}" ${assigned.includes(u.id)?'checked':''}> <b>${u.name}</b> <span class="co">${u.username}</span></label>`).join('')||'<div class="hint">No payroll officers yet — register users with the Payroll Officer role.</div>';}
function saveCompany(){if(!$('cf_name').value)return alert('Company name required');toast('Company saved');show('companies');renderCompanies()}
/* assign PO modal */
function openPO(id){poCompanyId=id;const c=COMPANIES.find(x=>x.id===id);$('poCompany').textContent=c.name;
 const pos=USERS.filter(u=>u.role==='Payroll Officer');
 $('poList').innerHTML=pos.map(u=>`<label class="urow"><input type="checkbox" class="po_chk" value="${u.id}" ${c.po.includes(u.id)?'checked':''}> <b>${u.name}</b> <span class="co">${u.username} · ${(COMPANIES.find(k=>k.id===u.company)||{}).name||''}</span></label>`).join('');
 $('poModal').classList.add('show')}
function closePO(){$('poModal').classList.remove('show')}
function savePO(){const c=COMPANIES.find(x=>x.id===poCompanyId);c.po=[...document.querySelectorAll('.po_chk:checked')].map(i=>+i.value);
 closePO();renderCompanies();toast('Payroll officers assigned to '+c.name)}
renderUsers();renderCompanies();
</script>
</body>
</html>
```

---

## 5. REFERENCE DESIGN — APPROVAL THRESHOLDS, AUTHORIZER TIERS, EMAIL SETTINGS

**Routes to wire (per section 2):** `admin/thresholds` (index/create/store/edit/update), `admin/tiers` (index/update), `admin/email-settings` (index/update/test).

**Reproduce this exact screen set.** It has four views (`view-thresholds`, `view-thresholdform`, `view-tiers`, `view-email`) toggled by a shared view-bar. Below is the complete, exact HTML/CSS/JS reference — replace only the mock arrays (`THRESHOLDS`, `TIERS`) and inline handler functions (`renderT`, `openThreshold`, `saveThreshold`, `toggleT`, `renderTiers`, `toggleTier`, `saveTier`, `togglePw`, `sendTest`, `saveEmail`) with real server-rendered/Blade+Alpine equivalents backed by `approval_thresholds`, `authorizer_tiers`, and the `system_settings` mail_* keys. Keep every class name, layout, copy string, icon, and interaction exactly as shown.

**Wiring specifics for this module:**
- Thresholds list: the "Director level" stat chip and the row with the amber left-edge (`tr.dirlevel`) + `DIRECTOR LEVEL` pill must be computed from whichever real threshold row has `requires_director_authorization = true` — never hardcoded.
- Threshold add/edit form: saving with "Requires Director Authorization" checked must, server-side, unflag every other threshold (enforce exactly one director-flagged row at a time), matching the single-flag note shown in the reference form.
- Authorizer Tiers table: the three amber `tierbox` checkboxes (Primary/Secondary/Auxiliary) per user must toggle and persist to the `authorizer_tiers` table on that row's Save icon click — a user may hold multiple tiers simultaneously. Stat chips (Primary/Secondary/Auxiliary counts) are real counts.
- Email Settings: all fields (transport, encryption, host, port, username, password with show/hide eye toggle, from address/name, the 7 notification-preference toggles, test-email recipient) must persist via `MailSettingsService::save()`, with `mail_password` encrypted at rest (`Crypt::encryptString`) and masked/never echoed back in plaintext in the UI or any toast.
- "Send test email" must actually send via `Mail::to($recipient)->send(new TestMail)`, be rate-limited (throttle: 5 per minute), and update the `mailStatus` chip to reflect real send success/failure (`Not tested` / `Test sent` / failure state) rather than the reference's optimistic-only JS.
- `MailSettingsService::apply()` must be registered in `AppServiceProvider::boot()` so saved settings actually take effect on outgoing mail at every request boot, per section 2.
- `NotificationRouter` must honor each `mail_notification_prefs` boolean — a disabled event must not send, matching the toggles' real effect, not just their visual state.

```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1">
<title>Kuwala SACCO — Thresholds, Tiers & Email Settings (Redesign)</title>
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;600;700&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
:root{--navy:#16202C;--navy2:#1E2A3A;--amber:#F6A21E;--amber2:#FFC466;--amber-ink:#9A5B00;--amber-soft:rgba(246,162,30,.16);
--bg:#F4F6F9;--panel:#fff;--line:rgba(22,32,44,.10);--line-soft:rgba(22,32,44,.06);--ink:#16202C;--body-c:#3D4756;--mut:#5D6B7E;--good:#0E9F6E;--good-soft:rgba(14,159,110,.12);--bad:#D92D20;--bad-soft:rgba(217,45,32,.10);
--r-lg:16px;--sh-2:0 1px 2px rgba(16,24,40,.05),0 16px 40px -24px rgba(16,24,40,.16);--font-sans:'Inter',system-ui,sans-serif;--font-disp:'Space Grotesk','Inter',sans-serif}
*{margin:0;padding:0;box-sizing:border-box}body{font-family:var(--font-sans);font-size:15px;line-height:1.6;background:var(--bg);color:var(--body-c)}
button{font-family:inherit;cursor:pointer}:focus-visible{outline:2px solid rgba(246,162,30,.7);outline-offset:2px;border-radius:8px}
.wrap{max-width:1240px;margin:0 auto;padding:26px 24px 80px}
.crumb{font-size:12px;color:var(--mut);text-transform:uppercase;letter-spacing:.06em;font-weight:600}.crumb b{color:var(--ink)}
.viewbar{display:flex;gap:8px;flex-wrap:wrap;margin:14px 0 20px}
.vbtn{font-size:13px;font-weight:600;border:1px solid var(--line);background:#fff;border-radius:999px;padding:9px 16px;color:var(--mut)}
.vbtn.on{background:var(--navy);border-color:var(--navy);color:#fff}
.head{display:flex;justify-content:space-between;align-items:flex-end;gap:16px;flex-wrap:wrap;margin:6px 0 16px}
h1{font:700 26px var(--font-disp);color:var(--ink)}.sub{font-size:13.5px;color:var(--mut)}
.btn{display:inline-flex;gap:8px;align-items:center;font-size:13.5px;font-weight:600;border-radius:12px;padding:11px 16px;border:1px solid var(--line);background:#fff;color:var(--ink);transition:.2s}
.btn:hover{border-color:var(--amber);background:var(--amber-soft)}
.btn svg{width:15px;height:15px;stroke:currentColor;fill:none;stroke-width:1.8;stroke-linecap:round;stroke-linejoin:round}
.btn-primary{background:linear-gradient(180deg,var(--amber2),var(--amber));border-color:var(--amber);color:var(--navy);font-weight:700}
.btn-primary:hover{transform:translateY(-1px);box-shadow:0 10px 24px -8px rgba(246,162,30,.55)}
.btn-ghost{background:transparent}
.stats{display:flex;gap:10px;flex-wrap:wrap;margin-bottom:16px}
.stat{background:var(--panel);border:1px solid var(--line);border-radius:12px;padding:10px 16px;box-shadow:var(--sh-2)}
.stat .l{font-size:11px;letter-spacing:.1em;text-transform:uppercase;color:var(--mut);font-weight:600}
.stat .v{font:700 18px var(--font-disp);color:var(--ink)}
.card{background:var(--panel);border:1px solid var(--line);border-radius:var(--r-lg);box-shadow:var(--sh-2);overflow:hidden}
.twrap{overflow:auto}table{width:100%;border-collapse:collapse;min-width:960px}
th{position:sticky;top:0;background:linear-gradient(150deg,var(--navy),var(--navy2));color:#EAF0F6;font-size:11.5px;letter-spacing:.1em;text-transform:uppercase;font-weight:600;text-align:left;padding:12px 16px}
td{font-size:14px;padding:12px 16px;border-bottom:1px solid var(--line-soft);vertical-align:middle}
tbody tr:hover{background:#F7F9FC}
tr.dirlevel{box-shadow:inset 3px 0 0 var(--amber)}
.name{font-weight:600;color:var(--ink)}.mono{font-size:12.5px;color:var(--mut);font-variant-numeric:tabular-nums}
.rolepill{font-size:11px;font-weight:700;background:#EEF2F6;border:1px solid var(--line-soft);border-radius:999px;padding:3px 10px;color:var(--body-c);white-space:nowrap}
.pill{font-size:11px;font-weight:700;padding:4px 11px;border-radius:999px}.pill.on{background:var(--good-soft);color:var(--good)}.pill.off{background:var(--bad-soft);color:var(--bad)}
.dlpill{font-size:10.5px;font-weight:800;letter-spacing:.06em;background:var(--amber-soft);border:1px solid rgba(180,105,14,.3);color:var(--amber-ink);border-radius:999px;padding:4px 12px}
.rowact{display:flex;gap:6px;justify-content:flex-end}
.act{width:34px;height:34px;border-radius:10px;border:1px solid var(--line);background:#fff;display:grid;place-items:center;color:var(--mut);transition:.15s}
.act svg{width:16px;height:16px;stroke:currentColor;fill:none;stroke-width:1.8;stroke-linecap:round;stroke-linejoin:round}
.act:hover{border-color:var(--amber);color:var(--amber-ink);background:var(--amber-soft)}
.act.danger:hover{border-color:var(--bad);color:var(--bad);background:var(--bad-soft)}
.act.good:hover{border-color:var(--good);color:var(--good);background:var(--good-soft)}
/* forms */
.formcard{padding:26px 28px}
.fsec{display:flex;align-items:center;gap:8px;font-size:11.5px;font-weight:700;letter-spacing:.14em;text-transform:uppercase;color:var(--mut);margin:24px 0 12px}
.fsec::after{content:"";flex:1;height:1px;background:var(--line-soft)}.fsec:first-child{margin-top:0}
.fgrid{display:grid;grid-template-columns:1fr 1fr;gap:14px}.fgrid .full{grid-column:1/-1}
.field label{display:flex;gap:7px;align-items:center;font-size:13px;font-weight:600;color:var(--ink);margin-bottom:6px}
.field input,.field select,.field textarea{width:100%;font:inherit;font-size:14.5px;color:var(--ink);background:#fff;border:1px solid var(--line);border-radius:11px;padding:10px 13px;transition:.18s}
.field input:focus,.field select:focus{border-color:var(--amber);box-shadow:0 0 0 3px var(--amber-soft);outline:none}
.ctl{position:relative}.ctl .suf{position:absolute;right:13px;top:50%;transform:translateY(-50%);font-size:12px;font-weight:600;color:var(--mut)}
.ctl.has-suf input{padding-right:52px}
.hint{font-size:11.5px;color:var(--mut);margin-top:5px}
.note{background:var(--amber-soft);border:1px solid rgba(180,105,14,.3);color:var(--amber-ink);border-radius:12px;padding:11px 15px;font-size:12.5px;font-weight:600}
/* toggle switch */
.swrow{display:flex;gap:20px;flex-wrap:wrap}
.sw{display:flex;align-items:center;gap:9px;font-size:13.5px;font-weight:500;color:var(--body-c);cursor:pointer}
.sw input{display:none}
.sw i{width:40px;height:22px;border-radius:999px;background:#D7DDE5;position:relative;transition:.2s;flex:0 0 40px}
.sw i::after{content:"";position:absolute;top:3px;left:3px;width:16px;height:16px;border-radius:50%;background:#fff;transition:.2s;box-shadow:0 1px 3px rgba(0,0,0,.25)}
.sw input:checked+i{background:var(--amber)}
.sw input:checked+i::after{left:21px}
.formbar{position:sticky;bottom:0;background:rgba(255,255,255,.92);backdrop-filter:blur(8px);border-top:1px solid var(--line);padding:14px 28px;display:flex;gap:10px;justify-content:flex-end}
/* tier checkboxes */
.tierbox{width:22px;height:22px;border-radius:7px;border:1.5px solid var(--line);display:grid;place-items:center;color:#fff;font-size:12px;cursor:pointer;transition:.15s}
.tierbox.on{background:var(--amber);border-color:var(--amber)}.tierbox.on::after{content:"✓"}
.avatar{width:34px;height:34px;border-radius:50%;background:var(--navy);color:var(--amber2);display:grid;place-items:center;font:700 12px var(--font-disp);flex:0 0 34px}
.ucell{display:flex;gap:10px;align-items:center}
/* email */
.pw{position:relative}.pw button{position:absolute;right:8px;top:50%;transform:translateY(-50%);border:0;background:none;color:var(--mut);font-size:14px}
.statuschip{font-size:11px;font-weight:700;border-radius:999px;padding:4px 12px}
.statuschip.ok{background:var(--good-soft);color:var(--good)}.statuschip.warn{background:var(--amber-soft);color:var(--amber-ink)}
.ctoast{position:fixed;right:24px;bottom:24px;background:var(--navy);color:#fff;border-radius:14px;padding:12px 18px;font-size:13.5px;opacity:0;transform:translateY(10px);transition:.3s;z-index:90}
.ctoast.show{opacity:1;transform:none}
@media(max-width:760px){.fgrid{grid-template-columns:1fr}}
</style>
</head>
<body>
<div class="wrap">
  <div class="crumb">Administration · <b>Approvals & Notifications</b></div>
  <div class="viewbar">
    <button class="vbtn on" data-view="thresholds">Approval Thresholds</button>
    <button class="vbtn" data-view="thresholdform">Add / Edit Threshold</button>
    <button class="vbtn" data-view="tiers">Authorizer Tiers</button>
    <button class="vbtn" data-view="email">Email Settings</button>
  </div>

  <!-- ═══ THRESHOLDS LIST ═══ -->
  <div id="view-thresholds">
    <div class="head"><div><h1>Approval Thresholds</h1><div class="sub">The director threshold is NOT hardcoded — it reads from the threshold flagged “requires director authorization”.</div></div>
      <button class="btn btn-primary" onclick="openThreshold(null)"><svg viewBox="0 0 24 24"><path d="M12 5v14M5 12h14"/></svg>Add Threshold</button></div>
    <div class="stats">
      <div class="stat"><div class="l">Thresholds</div><div class="v" id="tTotal">2</div></div>
      <div class="stat"><div class="l">Director level</div><div class="v" id="tDir">MK 3,000,000</div></div>
      <div class="stat"><div class="l">Active</div><div class="v" id="tActive">2</div></div>
    </div>
    <div class="card"><div class="twrap"><table>
      <thead><tr><th>Threshold</th><th>Min Amount</th><th>Max Amount</th><th>Approvers</th><th>Director Auth</th><th>Directors (mgmt out)</th><th>Branch Filter</th><th>Status</th><th style="text-align:right">Actions</th></tr></thead>
      <tbody id="tBody"></tbody></table></div></div>
  </div>

  <!-- ═══ THRESHOLD FORM ═══ -->
  <div id="view-thresholdform" hidden>
    <div class="head"><div><h1 id="tTitle">Add Threshold</h1><div class="sub">Define the amount band and its approval chain.</div></div>
      <button class="btn btn-ghost" onclick="show('thresholds')">← Back to Thresholds</button></div>
    <div class="card"><div class="formcard">
      <div class="note" style="margin-bottom:18px">Exactly one threshold must be flagged as requiring director authorization — it becomes the director threshold.</div>
      <div class="fsec">Threshold Definition</div>
      <div class="fgrid">
        <div class="field full"><label>Threshold Name</label><input id="tf_name"></div>
        <div class="field"><label>Min Amount</label><div class="ctl has-suf"><input id="tf_min"><span class="suf">MK</span></div></div>
        <div class="field"><label>Max Amount</label><div class="ctl has-suf"><input id="tf_max" placeholder="blank = ∞"><span class="suf">MK</span></div></div>
      </div>
      <div class="fsec">Approval Chain</div>
      <div class="fgrid">
        <div class="field"><label>Approver Count</label><input id="tf_app" value="3"></div>
        <div class="field"><label>Director Count (if Manager absent)</label><input id="tf_dirout" value="3"></div>
        <div class="field"><label>Director Branch Filter</label><input id="tf_branch" placeholder="e.g. Lilongwe (blank = all)"></div>
      </div>
      <div class="fsec">Flags</div>
      <div class="swrow">
        <label class="sw"><input type="checkbox" id="tf_director"><i></i>Requires Director Authorization (sets the director threshold)</label>
        <label class="sw"><input type="checkbox" id="tf_active" checked><i></i>Active</label>
      </div>
    </div>
    <div class="formbar"><button class="btn btn-ghost" onclick="show('thresholds')">Cancel</button><button class="btn btn-primary" onclick="saveThreshold()">Save Threshold</button></div>
    </div>
  </div>

  <!-- ═══ AUTHORIZER TIERS ═══ -->
  <div id="view-tiers">
    <div class="head"><div><h1>Authorizer Tiers</h1><div class="sub">Assign Primary (manager/ops), Secondary (delegation pool) and Auxiliary (backfill pool). The system selects three distinct authorizers per chain.</div></div></div>
    <div class="stats">
      <div class="stat"><div class="l">Primary</div><div class="v" id="tierP">0</div></div>
      <div class="stat"><div class="l">Secondary</div><div class="v" id="tierS">0</div></div>
      <div class="stat"><div class="l">Auxiliary</div><div class="v" id="tierA">0</div></div>
    </div>
    <div class="card"><div class="twrap"><table>
      <thead><tr><th>User</th><th>Role</th><th>Primary</th><th>Secondary</th><th>Auxiliary</th><th style="text-align:right">Save</th></tr></thead>
      <tbody id="tierBody"></tbody></table></div></div>
  </div>

  <!-- ═══ EMAIL SETTINGS (NEW) ═══ -->
  <div id="view-email" hidden>
    <div class="head"><div><h1>Email Settings</h1><div class="sub">Configure how the system delivers account and loan notifications.</div></div>
      <span class="statuschip warn" id="mailStatus">Not tested</span></div>
    <div class="card"><div class="formcard">
      <div class="fsec">Mail Transport</div>
      <div class="fgrid">
        <div class="field"><label>Transport</label><select id="em_transport"><option>SMTP</option><option>Mailgun</option><option>Amazon SES</option><option>Sendmail</option><option>Log (testing)</option></select></div>
        <div class="field"><label>Encryption</label><select id="em_enc"><option>None</option><option selected>TLS</option><option>SSL</option></select></div>
        <div class="field"><label>SMTP Host</label><input id="em_host" placeholder="smtp.mailgun.org"></div>
        <div class="field"><label>SMTP Port</label><input id="em_port" value="587"></div>
        <div class="field"><label>Username</label><input id="em_user"></div>
        <div class="field"><label>Password</label><div class="pw"><input id="em_pass" type="password"><button onclick="togglePw()" title="Show / hide">👁</button></div></div>
      </div>
      <div class="fsec">Sender Identity</div>
      <div class="fgrid">
        <div class="field"><label>From Address</label><input id="em_from" value="no-reply@kuwala.mw"></div>
        <div class="field"><label>From Name</label><input id="em_fromname" value="Kuwala SACCO Ltd"></div>
      </div>
      <div class="fsec">Notification Preferences</div>
      <div class="swrow">
        <label class="sw"><input type="checkbox" checked><i></i>Account created + password link</label>
        <label class="sw"><input type="checkbox" checked><i></i>Loan submitted → loan officers</label>
        <label class="sw"><input type="checkbox" checked><i></i>Payroll assessment → payroll officers</label>
        <label class="sw"><input type="checkbox" checked><i></i>Approval stage → authorizers</label>
        <label class="sw"><input type="checkbox" checked><i></i>Final decision → applicant</label>
        <label class="sw"><input type="checkbox" checked><i></i>Payroll delegation alerts</label>
        <label class="sw"><input type="checkbox" checked><i></i>Account status changes</label>
      </div>
      <div class="fsec">Send Test Email</div>
      <div class="fgrid">
        <div class="field"><label>Recipient</label><input id="em_testto" placeholder="you@kuwala.mw"></div>
        <div class="field" style="align-self:end"><button class="btn" onclick="sendTest()"><svg viewBox="0 0 24 24"><path d="M22 2 11 13M22 2l-7 20-4-9-9-4z"/></svg>Send test email</button></div>
      </div>
    </div>
    <div class="formbar"><button class="btn btn-ghost" onclick="toast('Email settings discarded')">Cancel</button><button class="btn btn-primary" onclick="saveEmail()">Save Email Settings</button></div>
    </div>
  </div>
</div>
<div class="ctoast" id="toast"><span id="toastMsg"></span></div>

<script>
let THRESHOLDS=[
 {id:1,name:'Below 3,000,000',min:0,max:2999999.99,app:3,director:false,dirout:3,branch:'All',active:true},
 {id:2,name:'3,000,000 and above (Director Level)',min:3000000,max:null,app:3,director:true,dirout:3,branch:'Lilongwe',active:true}];
let TIERS=[
 {name:'Chikondi Phiri',role:'Director',p:1,s:0,a:0},{name:'Dalitso Gondwe',role:'Loan Officer',p:0,s:0,a:0},
 {name:'Lusungu Kachale',role:'Director',p:1,s:0,a:0},{name:'Mphatso Banda',role:'Manager Accountant',p:1,s:0,a:0},
 {name:'System Administrator',role:'Administrator',p:0,s:0,a:0},{name:'Tiwonge Mbewe',role:'Director',p:1,s:0,a:0},
 {name:'Yamikani Nyirenda',role:'Loan Officer',p:0,s:0,a:1}];
let editTid=null;
const $=id=>document.getElementById(id);
const IC={edit:'<svg viewBox="0 0 24 24"><path d="M12 20h9"/><path d="M16.5 3.5l4 4L7 21l-4 1 1-4z"/></svg>',ban:'<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M5.7 5.7l12.6 12.6"/></svg>',check:'<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M8 12.5l2.7 2.7L16 9.5"/></svg>',save:'<svg viewBox="0 0 24 24"><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><path d="M17 21v-8H7v8M7 3v5h8"/></svg>'};
function toast(m){$('toastMsg').textContent=m;$('toast').classList.add('show');setTimeout(()=>$('toast').classList.remove('show'),2400)}
function show(v){['thresholds','thresholdform','tiers','email'].forEach(x=>$('view-'+x).hidden=(x!==v));
 document.querySelectorAll('.vbtn').forEach(b=>b.classList.toggle('on',b.dataset.view===v));
 if(v==='thresholds')renderT(); if(v==='tiers')renderTiers();}
document.querySelectorAll('.vbtn').forEach(b=>b.onclick=()=>show(b.dataset.view));
/* thresholds */
function renderT(){$('tBody').innerHTML=THRESHOLDS.map(t=>`<tr class="${t.director?'dirlevel':''}">
 <td><span class="name">${t.name}</span></td><td class="mono">${fmt(t.min)}</td><td class="mono">${t.max==null?'∞':fmt(t.max)}</td>
 <td>${t.app}</td><td>${t.director?'<span class="dlpill">DIRECTOR LEVEL</span>':'<span class="mono">—</span>'}</td>
 <td>${t.dirout}</td><td>${t.branch||'All'}</td><td><span class="pill ${t.active?'on':'off'}">${t.active?'Active':'Inactive'}</span></td>
 <td><div class="rowact"><button class="act" title="Edit" onclick="openThreshold(${t.id})">${IC.edit}</button>
 <button class="act ${t.active?'danger':'good'}" title="${t.active?'Deactivate':'Activate'}" onclick="toggleT(${t.id})">${t.active?IC.ban:IC.check}</button></div></td></tr>`).join('');
 $('tTotal').textContent=THRESHOLDS.length;const d=THRESHOLDS.find(x=>x.director);$('tDir').textContent=d?'MK '+fmt(d.min):'—';
 $('tActive').textContent=THRESHOLDS.filter(x=>x.active).length;}
const fmt=n=>n.toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});
function toggleT(id){const t=THRESHOLDS.find(x=>x.id===id);t.active=!t.active;toast('Threshold '+(t.active?'activated':'deactivated'));renderT()}
function openThreshold(id){editTid=id;show('thresholdform');$('tTitle').textContent=id?'Edit Threshold':'Add Threshold';
 const t=id?THRESHOLDS.find(x=>x.id===id):null;
 $('tf_name').value=t?t.name:'';$('tf_min').value=t?t.min:'';$('tf_max').value=t&&t.max!=null?t.max:'';
 $('tf_app').value=t?t.app:3;$('tf_dirout').value=t?t.dirout:3;$('tf_branch').value=t?t.branch:'';
 $('tf_director').checked=!!(t&&t.director);$('tf_active').checked=!t||t.active;}
function saveThreshold(){if(!$('tf_name').value)return alert('Name required');
 if(editTid){const t=THRESHOLDS.find(x=>x.id===editTid);Object.assign(t,{name:$('tf_name').value,min:+$('tf_min').value||0,max:$('tf_max').value?+$('tf_max').value:null,app:+$('tf_app').value,dirout:+$('tf_dirout').value,branch:$('tf_branch').value,director:$('tf_director').checked,active:$('tf_active').checked});}
 else THRESHOLDS.push({id:Date.now(),name:$('tf_name').value,min:+$('tf_min').value||0,max:$('tf_max').value?+$('tf_max').value:null,app:+$('tf_app').value,dirout:+$('tf_dirout').value,branch:$('tf_branch').value,director:$('tf_director').checked,active:$('tf_active').checked});
 if($('tf_director').checked)THRESHOLDS.forEach(x=>{if(x.id!==editTid)x.director=false}); // enforce single
 toast('Threshold saved');show('thresholds');}
/* tiers */
function renderTiers(){$('tierBody').innerHTML=TIERS.map((u,i)=>`<tr>
 <td><div class="ucell"><span class="avatar">${u.name.split(' ').map(w=>w[0]).join('').slice(0,2)}</span><span class="name">${u.name}</span></div></td>
 <td><span class="rolepill">${u.role}</span></td>
 ${['p','s','a'].map(k=>`<td><span class="tierbox ${u[k]?'on':''}" onclick="toggleTier(${i},'${k}',this)"></span></td>`).join('')}
 <td><div class="rowact"><button class="act" title="Save tiers" onclick="saveTier(${i})">${IC.save}</button></div></td></tr>`).join('');
 $('tierP').textContent=TIERS.filter(u=>u.p).length;$('tierS').textContent=TIERS.filter(u=>u.s).length;$('tierA').textContent=TIERS.filter(u=>u.a).length;}
function toggleTier(i,k,el){TIERS[i][k]=TIERS[i][k]?0:1;el.classList.toggle('on',!!TIERS[i][k]);}
function saveTier(i){toast('Tiers saved for '+TIERS[i].name);renderTiers()}
/* email */
function togglePw(){const p=$('em_pass');p.type=p.type==='password'?'text':'password'}
function sendTest(){const to=$('em_testto').value||'admin@kuwala.mw';$('mailStatus').className='statuschip ok';$('mailStatus').textContent='Test sent';toast('Test email sent to '+to)}
function saveEmail(){toast('Email settings saved');}
renderT();renderTiers();
</script>
</body>
</html>
```

---

## 6. WIRING CHECKLIST — LINKS, NAV, BUTTONS

Every item below must be a real, working link/route — no dead ends, no placeholder `href="#"`, no orphaned pages reachable only by typing a URL.

- [ ] Add/confirm a main-admin-nav entry (in whatever shared sidebar/topbar partial the existing admin section already uses) pointing to each of: Loan Types, Users, Companies, Approval Thresholds, Authorizer Tiers, Email Settings. Group Thresholds/Tiers/Email under one "Approvals & Notifications" nav section and Users/Companies under one "People & Companies" nav section, matching the two reference documents' own breadcrumb groupings (`Administration · Approvals & Notifications` and `Administration · People & Companies`) — Loan Types keeps its own breadcrumb (`Administration · Loan Products`).
- [ ] Within each module, the in-page view-switcher tab bar (`.viewbar` buttons in the Users/Companies and Thresholds/Tiers/Email reference files) must route between the real pages/Livewire views for that module, not just toggle hidden `<div>`s over static data — unless the team's existing pattern for Part-1 screens already uses client-side view toggling over a single Blade view, in which case match that existing pattern for consistency.
- [ ] "Add Loan Type" / "Register User" / "Add Company" / "Add Threshold" primary buttons → real create routes/forms.
- [ ] Every row's Edit icon → real edit route, prefilled with that row's actual data.
- [ ] Every row's Activate/Deactivate icon → real status-toggle persistence + toast confirmation.
- [ ] Loan Types row "Manage" (Access) link and the Access section button inside the edit form both open the same real Access modal for that loan type.
- [ ] Companies row "Manage"/"Assign" (Payroll Officers) link and the company edit form's own Payroll Officers checklist both write to the same `company_payroll_officer` pivot and stay in sync.
- [ ] "← Back to X" ghost buttons on every form/sub-view return to that module's real list view.
- [ ] Cancel buttons on every form/modal discard unsaved changes and return without persisting.
- [ ] Sticky Save bars on every form persist via the real route and show the toast on success; validation errors must surface inline (matching the reference's `.field` styling) rather than only via `alert()`.
- [ ] Loan Types member-class/company/user Access modal, Users' membership class cards, and the Loan Access Service (Part 1) must stay logically consistent: a user's `member_classes` and a loan type's `access_classes`/`access_companies`/`access_users` together determine visibility exactly per the `LoanAccessService` rule in section 2 — verify end-to-end by confirming an ordinary-only member never sees a staff-only-flagged product in the member-facing calculator/application flow.

---

## 7. DEFINITION OF DONE

Carried over from the functional spec (section 2 §12) plus this prompt's own additions:

- Loan Types: inline application-form select persists; Access modal saves classes/companies/users; the member-facing calculator/application correctly filters products so an ordinary member never sees staff-only products.
- Users: membership class cards save `["ordinary"]` / `["staff"]` / both / none correctly; `is_member` stays in sync; loan access follows from it.
- Companies: payroll officers assignable via both the form checklist and the Assign modal; payroll assessment scoping reads the pivot; user counts and PO chips render from real data.
- Thresholds: exactly one director-flagged threshold enforced server-side; the director-level stat and pill read from that flagged row, never hardcoded.
- Tiers: multi-tier-per-user persists correctly; Primary/Secondary/Auxiliary counts update from real data.
- Email: settings persist with the password encrypted at rest and masked in the UI; `MailSettingsService::apply()` affects outgoing mail at boot; test email actually sends and is throttled; disabled notification-preference toggles actually suppress those events.
- Every screen visually matches its reference HTML in sections 3–5 exactly — same layout, spacing, colors, pills, chips, toggles, icons, sticky bars, modals, and copy — with only the necessary data-wiring changes.
- No file, route, controller, or existing feature outside this prompt's named scope (section 1) was touched.
- `php artisan test` passes in full; no pre-existing test was altered or deleted to force a pass; responsive layout and basic accessibility (aria-labels on icon-only actions, focus-visible states) match the reference markup.
