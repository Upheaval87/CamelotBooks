# AGENTS.md

## Commands
- Build: `npm run build` (compile Vite assets)

## State
### Completed redesign phases:
1. **CSS tokens** â€” `ink`/`line`/`panel`/`gold`/`brick`/`forest` color scales, serif/mono fonts, `max-w-8xl`
2. **App CSS** â€” component-layer: cards, buttons (4 variants), inputs, datasheets, modals, status pills, KPI cards, page titles
3. **Button component** â€” unified `x-button`, all old button components delegate to it
4. **Header overhaul** â€” all pages (~180) stripped old `<h2 class="font-semibold...">` wrapper from `<x-slot name="header">`; titles now plain text (rendered below topbar)
5. **Datasheets** â€” all tables (~178) converted to `datasheet` class
6. **Form layouts** â€” all forms (~160) use `card p-6`, `form-section-label`, `input mt-1` for selects
7. **Content widths** â€” all pages now use consistent `max-w-8xl mx-auto sm:px-6 lg:px-8` (was a mix of max-w-7xl/5xl/4xl/3xl/2xl/full)
8. **Show pages** â€” all 24 show pages converted: `card p-6`, `detail-grid`/`x-detail-field`, `kpi-card`/`kpi-label`/`kpi-value`, `x-record-toolbar`, `status-pill`, `datasheet`, `x-button`, entity names appended where appropriate (Customer Detail - Name, Employee Detail: Name)
9. **Invoice-form input styling** â€” shared `.input` CSS changed to bottom-border-only (no box/bg/radius), `.input-label` â†’ 11px, `.btn-primary` â†’ gold accent, `.btn-ghost` â†’ permanent border, `.btn-remove` added
10. **Record page redesign** â€” all 35 show pages converted to new `x-record-toolbar` (#F3ECDC bg, grouped actions), `detail-grid` (3-column), `balance-grid`/`balance-total-row`, `x-detail-field` component
11. **Two-row topbar navigation** â€” replaced sidebar + single-row topnav with horizontal two-row bar. Row 1 (60px `bg-ink`): gold "L" brand mark + "CamelotBooks" + divider + company/branch (session-derived) + user avatar/name/role + logout. Row 2 (46px `#212B37`): Dashboard, Payments, Invoices, Customers, Journal, Reports + "Â·Â·Â·" overflow dropdown with all other section links (Inventory, Banking, Chart of Accounts, General Ledger, Trial Balance, Budgets, Fixed Assets, Payroll, Analytics, BI, POS, Settings, Users). Old sidebar CSS removed. Dark mode sidebar CSS removed.
12. **System Settings redesign** â€” left sidebar nav (3 groups, gold accent), bottom-border-only inputs, pill toggles, callouts, numbered eyebrow headers, status pills, inline audit-log diffs. Components: `resources/views/components/settings/` (sidebar, field, toggle, callout, table). CSS: `.settings-*` styles in `app.css`. 9 tab partials (`_tab-*.blade.php`) plus backups and audit-log pages rewritten.
13. **List page redesign** â€” applied customer-list design to all 7 entity list pages (Customers, Vendors, Products, Employees, Invoices, Bills, Sales Receipts). Shared components: `resources/views/components/list/` (quick-links, icon-button, avatar-initials, filter-bar, header). CSS: `.list-*` styles in `app.css` (sidebar layout, warm-header table, avatar initials, icon buttons with tooltips, mobile card rows below 720px). Pattern: italic serif title + Create button header, sticky 240px quick-links sidebar, underline-style filter bar, warm-header table with avatar initials for person entities, right-aligned bold numbers, status pills, icon-only action buttons with hover tooltips, mobile card rows.
14. **Currency symbol moved to column headers** â€” all 8 core financial report views (~28 files): replaced `format_money()` â†’ `format_number()` in every amount cell; added `({{ $cs }})` suffix to column headers where missing (Inventory Valuation, AR/AP Aging); stripped redundant `{{ $cs }}` prefix from subtotals/totals in print views (Income Statement, Balance Sheet, Cash Flow). Symbol now appears ONLY in column headers, never on individual amounts.
15. **Two-mode search system** â€” Mode 1 scoped inline search (`<x-scoped-search-field>` + `scopedSearchFieldHtml()` in `resources/js/scoped-search-field.js`, server mode queries `accounting.search.{entity}`, client mode for static lists, barcode/SKU exact-match auto-select, emits `item-selected`) retrofitted into every picker: 11 product line-item pickers (invoices, purchase-orders, purchase-requisitions, quotations create/edit, GRN, credit-notes, sales-receipts), customer pickers on 7 forms, POS checkout product search. Mode 2 global search modal (`<x-global-search-modal>` in `layouts/app.blade.php`, Alpine data in `resources/js/global-search-modal.js`, groups results from `accounting.search.global`, keyboard nav + Ctrl+K, `open-global-search` window event). Topbar Row 1 search button added. All 7 list/index pages now show a boxed scoped-search-style search (`.list-filter-search` â†’ `.scoped-search-field`) with a "search across all records" button that opens the global search modal (shared `components/list-filter-bar.blade.php`; dead duplicate `components/list/filter-bar.blade.php` deleted). Modal opened from a scoped field is scoped to that entity (gold chip to expand to all) and SELECTS into the field via `global-search-selected` (field-ref targeted, full row passed through so line-item autofill works); opened from topbar/Ctrl+K it navigates to the record. POS listens and calls `selectProduct`. Legacy `searchable-select`/`advanced-search-modal` components deleted. Later expansion batch: entity pickers â†’ `x-scoped-search-field` with `old()`/request precedence for the label via `->firstWhere('id', (int) ...)`: vendor pickers (bills/expenses/fixed-assets create+edit, GRN create, landed-costs create, vendor-payments create w/ `onVendorSelected`â†’`loadBills()` reading `input[name="vendor_id"]`, vendor-credits create, PO create+edit), bank-account pickers (settlements/deposits/cheques create, expenses create+edit, customer-payments create, vendor-payments create, petty-cash show Ã—2, payroll-runs show Ã—3), asset pickers (asset-usage index, asset-transfers/revaluations/impairments/disposals create), report pickers (customer-statement customer, vendor-statement vendor, payslip-report employee + payroll-run, stock-movement product+branch), index-filter pickers (accounts/credit-notes/quotations/vendor-credits index search boxes, expenses index vendor, general-ledger index account, cheques index+register bank, audit-log index user, bank-reconciliation index bank w/ query-selector JS). Hidden input keeps `name` so GET filter forms submit normally; `required` preserved where the original select was required. Final sweep batch (Aug 3, 2026): account pickers on products create/edit (`income_account_id`/`expense_account_id`), item-categories create/edit (3 default accounts), accounts create/edit `parent_id` (removed `filterParentAccounts()`/`parentSelect` JS), cheques create `debit_account_id`, bank-accounts transfer (`from_account_id`/`to_account_id` â†’ bank-account) + manual-transaction (`debit_account_id`/`credit_account_id`), petty-cash show `debit_account_id`, asset-transfers create (`from_cost_center_id`/`to_cost_center_id`), asset-categories create/edit (all 7 account pickers), POS payment-methods (clearing + settlement â€” settlement is an `accounts` FK validated `exists:accounts`, so `entity="account"` NOT bank-account; label from `$accounts`), landed-costs create components section rewritten from Alpine to Blade server rows using `scopedSearchFieldHtml({name: 'components[idx][payee_account_id]', entity: 'account', ...})` with `componentIndex = {{ count($oldComponents) }}` and `ACCOUNT_SEARCH_URL`. Dynamic line rows on invoices/create: `income_account_id`/`cost_center_id` via `scopedSearchFieldHtml` + `ACCOUNT_SEARCH_URL`/`COST_CENTER_SEARCH_URL`, product-selected autofills account via `scopedSearchFieldSet()`. Remaining filters: budget-vs-actual-trend `fiscal_year_id` (fiscal-year), audit-log `user_id` (user), assemblies history `assembly_product_id` (product), POS checkout customer (server picker + `window.posCustomerSelected`/`clearPosCustomer` dispatching `pos-customer-selected` â†’ Alpine sets `customerId`), POS returns create `pos_sale_id` (client-mode with `window.posReturnSaleSelected` â†’ `pos-return-sale-selected` event â†’ `loadSale()`), system-settings `_tab-accounts.blade.php` default-account-mapping selects (looped `availableKeys()`, each `name="{{ $key }}"` â†’ `entity="account"`). Client-mode convention for small reference lists: `mode="client"`, `:items="$collection->map(...['id','label','subtitle'])`, `:value`, `:label`, `placeholder`. Component `entity` prop now defaults to `''` so client-mode calls without entity don't error (caught by smoke test â€” GRN create crashed on `Undefined variable $entity`). Left as native selects (not in SearchCatalog): assemblies create/unbuild `bom_id`, POS settlements `payment_method_id`, till-sessions/cashier-login `terminal_id`, admin permissions `role_id`, users edit `role`, sales-receipts Alpine payment method/provider selects, chart-of-accounts type, accounts index type, audit-log group, report `type`/`status`/`compare_mode`/`dimension`/`payment_terms`/`gender`/`frequency`/`depreciation_method_*`/`residual_value_type_*` enums. List-page search magnifier: `x-list-filter-bar` now accepts an `entity` prop; pages with a catalog entity (customers/vendors/products/employees) dispatch `open-global-search` with `entity` so the modal opens pre-scoped to that list (gold chip to expand to all), and `selectResult` in `global-search-modal.js` navigates when opened without a field ref (list/topbar) vs fills the picker field when a `_fieldRef` exists. SearchCatalog extended (Aug 3, 2026) with document entities `invoice`, `bill`, `sales-receipt`, `quotation`, `credit-note`, `vendor-credit` (each searches number + reference + related customer/vendor name, links to its show page). List pages now all entity-scoped: customers, vendors, products, employees, invoices, bills, sales-receipts (`x-list-filter-bar entity="..."`) plus accounts, credit-notes, quotations, vendor-credits (inline `.scoped-search-open` buttons dispatch with entity). Topbar/Ctrl+K remain unscoped global.

### Completed performance audit (Jul 30, 2026):
- **N+1 fixes (7 locations)**: CreditNoteController (added `'invoice'`), VendorCreditController (added `'bill'`), ItemCategoryController (added accounts), TrialBalanceController (replaced per-account SUM loop with single grouped query), BalanceSheetService (replaced per-account computeBalanceAsOf with single grouped query), InventoryValuationController (single cost-layer query for all products), VendorCentreService (getVendorSummary: 5 queries/vendor â†’ 4 grouped; getVendorStats: 8 queries â†’ 4 grouped)
- **Indexes migration**: `2026_07_30_200423_add_performance_indexes.php` â€” added indexes on purchase_orders, purchase_requisitions, goods_received_notes, landed_cost_vouchers, inventory_adjustments, inventory_transfers, inventory_cost_layers, stock_counts, budgets, cheques, journal_entry_lines
- **Unbounded queries**: Added `->limit(100)` to CustomerPaymentController::create() and VendorPaymentController::create() open invoice/bill queries
- **Async mail queueing**: Added `implements ShouldQueue` to all 4 mailables (SalesReceiptMail, QuotationMail, PayslipMail, ExecutiveDigestMail); changed `->send()` â†’ `->queue()` in controllers/commands
- **Other findings**: EncryptedPayslipService generates PDF synchronously with mPDF (blocking); QUEUE_CONNECTION=sync (needs redis/beanstalkd in production); SESSION_DRIVER=database, CACHE_STORE=database (both can switch to file/redis); jobs table migration already exists; `optimize-autoloader: true` in composer.json

### Render-verification bug fixes (Aug 3, 2026)
Found via `ScopedSearchRenderSmokeTest` (renders every converted route as company_admin with features enabled); all were pre-existing in HEAD:
- `AssetTransferController` â€” `Branch::...->active()` â†’ `where('is_active', true)` (Branch has no `active()` scope; Asset/Account do).
- `CustomerStatementController` + `VendorStatementController` â€” service called with wrong arg order (`generate($companyId, $dateFrom, $dateTo, $customerId)` vs signature `(companyId, customerId, dateFrom, dateTo)`), no empty state when no entity selected, and never passed `$customer`/`$vendor` to the view.
- `PayslipReportController` â€” missing `$runsList`/`$employeesList` view vars (added); view `$total_net_pay` undefined (now computed inline from `$items`); payroll-run select converted to `<x-scoped-search-field entity="payroll-run">`.
- `stock-movement.blade.php` â€” file was mangled (duplicated body + broken `@php` missing `'$`); service returns arrays (`product_name`, `sku`, `quantity`, `running_qty`) not objects, and `reconciliation` is a per-product list (`running_qty`/`on_hand`/`variance`), not `opening`/`net_movement`/`closing`.
- `bank-reconciliation/index.blade.php` â€” stray `</x-slot>` after an inline-closed header slot â†’ moved toolbar div into the `<x-slot name="header">` wrapper.
- `payroll-runs/show.blade.php` â€” `$color` undefined (inline status map added); dead routes `payroll-runs.print-payslips` + `payroll-runs.print` â†’ `payroll-runs.payslips`; pay-employee modal posted to `{employeeId}` route with the payroll-run-item id â†’ button now passes `$item->employee_id`, Alpine state holds `employeeId`, form `:action` builds `/accounting/payroll-runs/{run}/pay-employee/{employeeId}`.
- `petty-cash/show.blade.php` â€” removed dead `petty-cash.print` quick-action link (no such route/view exists).
- `invoices/show.blade.php` + `bills/show.blade.php` â€” `@if` directive embedded inside a component `:groups="[...]"` attribute expression (Blade leaves it verbatim â†’ invalid PHP at render, missed by `view:cache` since it doesn't lint). Fixed by precomputing `$invoiceInsightLinks`/`$billInsightLinks` in a `@php` block. Rule: NEVER put Blade directives inside component attribute expressions (`:prop="[ @if ... ]"`); build the array in `@php` first. Verify with: compile via `view()`, then `php -l` on the compiled output.

### Personal task list ("My Tasks", Aug 4, 2026)
- **`todo_tasks` table** â€” migration `2026_08_04_000001`. Personal + company-scoped: every query filters by **both** `company_id` AND `user_id` (`forCompany()`/`forUser()` scopes); `authorizeAccess()` in the controller 404s otherwise. No delegation/approval flow â€” a task is always owned by the creating user.
- **Deadlines**: store the end-of-period date (Sun 23:59:59 for week, last day for month, Dec 31 for year) + original `deadline_granularity` (`day|week|month|year`). `TodoTask::deadlineFor()` mirrors the JS chips in `resources/js/todo.js` (`todoDeadline` Alpine data, hidden inputs `<name>_granularity`/`<name>_date`).
- **Buckets** â€” `TodoTask::bucketKey()` shared by the controller's index grouping AND the dashboard counts so they always agree: `overdue`, `today`, `this_month`, `this_year`, `no_deadline`. View: `resources/views/todo/index.blade.php` (+ `_deadline-chips`, `_task-row`, `_task-row-completed`), CSS `.todo-*` in `app.css`, Alpine in `resources/js/todo.js` (registered in `app.js`).
- **Task detail modal** â€” a SINGLE shared `<x-modal name="task-detail">` rendered at page level in `index.blade.php`, OUTSIDE the `.todo-list` card. `.todo-list { overflow: hidden }` clips `fixed` descendants, so never nest modals inside it. Rows dispatch `open-task-detail` (window event with task data) â†’ `todoDetailModal()` Alpine data (`todo.js`) populates state and opens the modal. Modal form posts via `:action="updateUrl"`, delete via `:action="deleteUrl"`. Deadline chips re-initialize per task via `:key="taskId"`. `@item-selected` on the modal body updates the link via `onLinkSelected`. **IMPORTANT â€” the containing-block bug**: `layouts/app.blade.php` wraps every page in `<div class="animate-fade-in-up">`. Its animation used `both` fill-mode and the `fadeInUp`/`scaleIn` keyframes end with `transform: translateY(0)`/`scale(1)` â€” even a "no-op" transform is a non-none `transform`, so the wrapper stays the containing block for ALL `fixed` descendants, breaking backdrop coverage (`fixed inset-0`) and centering (`fixed left-1/2 top-1/2`) of every modal app-wide. Fixed in `app.css`: `.animate-fade-in`/`.animate-fade-in-up`/`.animate-scale-in` fill mode changed `both` â†’ `backwards` so no transform is retained after the animation ends. Do NOT re-add `both`/`forwards` to an animation whose 100% keyframe sets a transform, and NEVER rely on `x-teleport` to dodge this (teleported content with a nested `x-data`/component scope is fragile â€” `global-search-modal` uses it because its markup is self-contained, but a scoped component inside a teleport template breaks Alpine scope resolution). **No nested forms**: the delete `<form id="task-delete-form">` is a sibling AFTER the update form; the Delete button uses `type="submit" form="task-delete-form"` (a button outside a form can submit it by `id`). Browsers drop nested `<form>` tags, so a nested delete form's hidden `_method=DELETE` becomes part of the outer form and the submit deletes the task instead of updating it. **Link-resolution fix (Aug 4, 2026)**: `todoResolveLinkFromEvent()` reads the entity key as `item.groupKey || item.entity || item.type`. Global-search rows (selected via the magnifier) carry `type` (the catalog key) but NOT `entity`/`groupKey` â€” `selectResult(row)` passes the raw row from `group.results`, not the `flatRows` version. Before the fix, `entityKey` fell through to `''`, `linkableType` was empty, and `TodoTaskController::normalizeLink()` wiped the link on save (task kept its label visually until reload, then "No record linked"). Verified live in headless Chrome: inline dropdown + global-search magnifier both populate `linkable_type` and persist after save+reopen; modal Delete works.
- **Record links are snapshots**: `linkable_type`/`linkable_id`/`link_label`/`link_url` all stored; `LINKABLE_CLASS_MAP` maps the SearchCatalog entity key â†’ model class (validation restricts `linkable_type` via `Rule::in(array_values(...))`). Deleting the linked record leaves a muted "no longer available" chip, no error. Wildcard pickers use `<x-scoped-search-field entity="" allow-global-search search-url="route('accounting.search.any')">` â€” accepts selections of ANY entity (new `GET accounting/search/any` endpoint, `GlobalSearchController::any()` merges `entity` into each row); `global-search-selected` handler + `window.TODO_LINKABLE_CLASS_MAP` (set in `index.blade.php`).
- **Routes**: `routes/web.php` `todo.*` group under `role_or_permission:system_admin|company_admin|accountant|approver|viewer|bookkeeper|cashier|auditor` (index/store/update/complete/reopen/destroy). NOTE: `TodoTaskController` must be imported at the top of `routes/web.php` (it's a `use App\Http\Controllers\...` import, not fully-qualified inline) â€” forgetting it yields "Target class [TodoTaskController] does not exist".
- **Dashboard widget**: `DashboardController::index` computes `$todoOverdue`/`$todoToday` (personal, `->get(['id','title','deadline_date','deadline_granularity'])`), dashboard.blade.php shows a "My Tasks" card; topbar overflow menu has a "My Tasks" link.
- **CSS tokens**: `app.css` now defines a `:root` block with `--ink`, `--ink-soft`, `--ink-muted`, `--gold`, `--gold-soft`, `--gold-line`, `--brick`, `--forest`, `--forest-soft`, `--font-serif`. These were previously USED by `.report-*`/`.todo-*` but never defined; the block fixes both.
- **Tests**: `tests/Feature/Accounting/TodoTaskTest.php` â€” personal isolation (same-company + cross-company 404s), CRUD + complete/reopen, bucket grouping, `deadlineFor()`/`bucketKey()` unit checks, link-snapshot survival after linked-record deletion, dashboard counts, validation (bad `linkable_type`, bad granularity/priority), auth redirect.

16. **Quick-actions sidebar redesign** â€” form/detail quick-actions now render the SAME `list-sidebar` visual language as list pages (icon squares, warm hover, dividers, mobile trigger). `components/form/quick-actions.blade.php` + `components/detail-quick-actions.blade.php` both delegate to a new shared partial `components/quick-actions/sidebar.blade.php` (prop `asideClass` picks `form-page-sidebar`/`detail-page-sidebar` wrapper). The partial keeps the iconMap key system (union of form + detail icon sets, incl. `statement`/`adjust`/`transfer`/`payslip`/`settings`/`attach`/`document`/`banknotes`/etc.) and adds optional `subtitle` per link. Dead `.form-sidebar-*`/`.detail-sidebar-*` CSS (plus dark variants) removed from `app.css`; wrapper classes `.form-page-sidebar`/`.detail-page-sidebar` (sticky 240px) retained. ScopedSearchRenderSmokeTest extended with `customers.create`/`customers.edit`/`customers.show`.

17. **Page-container padding reduced** â€” all 51 blade pages using `<div class="py-12">` page wrappers (plus report views with `max-w-7xl mx-auto py-12 px-4`) switched to `py-6` (24px top/bottom). Verified zero `py-12` remaining in `resources/views`. Pure Tailwind-class change, no asset rebuild needed. Performed via inline PowerShell bulk replace (script files blocked by execution policy; use `Get-ChildItem` + `[System.IO.File]::WriteAllText`, not `rg` piped relative paths).

### Key patterns:
- Page titles: `<x-slot name="header">{{ __('Title') }}</x-slot>` â€” renders as `<h1>` below the topbar (italic serif, `text-ink`)
- Show pages with entity: `{{ __('Customer Detail') }} - {{ $customer->name }}`
- Show pages with ref#: `{{ __('Invoice') }} #{{ $invoice->invoice_number }}`
- Record toolbar: `<x-record-toolbar>` with `.tr-group`/`.tr-group-label`/`.tr-item`/`.tr-save`/`.tr-archive`/`.tr-more` classes
- Detail grid: `<div class="detail-grid">` with `<x-detail-field label="...">value</x-detail-field>`
- Balance panel: `<div class="balance-grid">` + `<div class="balance-total-row">`
- Form inputs: `.input` class (bottom-border only), `<x-input-label>` (11px uppercase), `<x-text-input>`
- List pages: `<x-list-header>`, `<x-list-filter-bar>` (underline-style inputs/selects in a form), `<x-list-quick-links>` (sidebar with groups/links/icons), `<x-list-avatar-initials>` (initials in circle), `list-table-wrap` + `list-table` (warm-header `#EDE8DA`), `icon-btn` (28px icon buttons with tooltip on hover), `list-mobile-cards` (card view below 720px)
- Settings pages: `<x-settings.sidebar>` (sticky 220px nav with divider groups), `<x-settings.field>` (bottom-border label+value), `<x-settings.toggle>` (pill switch), `<x-settings.callout>` (info/warning block), `<x-settings.table>` (compact with mapping statuses)

18. **Inter-only font system (Aug 5, 2026)** â€” body font (Inter) applied to ALL on-screen text. `fontFamily.serif`/`fontFamily.mono` in `tailwind.config.js` now resolve to Inter (`sans` stack); `--font-serif` CSS token repointed to Inter. All `font-serif`/`font-mono` utility classes in 22 screen blade files bulk-replaced â†’ `font-sans` (report `td` cells, textareas, `<code>`/kbd spans). Direct `font-family` declarations in `app.css` (`.settings-exit-code`, `.global-search-title`, `.global-search-kbd`, `.global-search-footer kbd`, `.avatar-initials`, `.list-numeric`, `.report-head .report-title`, `.report-line .code`, `.report-cell-code`) and `Chart.defaults.font.family` in `analytics.js` switched to `'Inter', system-ui, sans-serif`. Webfont link in `app.blade.php` trimmed to `inter:400,500,600,700` only. Print/PDF views untouched (POS thermal receipt, quotation/invoice/sales-receipt prints, `print-export.blade.php`, payroll paye/pension/payslip schedules, POS cashier login) â€” they keep Courier New/Arial/Figtree/Georgia per print styling. Unrouted `welcome.blade.php` (Laravel starter with embedded Tailwind v4 CSS) left as-is. Verified: `font-mono`/`font-sans`/`font-serif` utilities all emit `font-family:Inter,...`; `ScopedSearchRenderSmokeTest` passes.

19. **Multi-Tenancy Phase 1 â€” DB-per-tenant provisioning (Aug 5, 2026)** â€”
- `app/Services/Tenancy/CompanyProvisioningService.php`: `provision(Company, ?string $migrationPath)` â†’ generate unique DB name (`acct_{slug}_xxxxxxxx`, checks information_schema + `companies.db_name`), `CREATE DATABASE` via the `provisioning` connection (config has `database => null`), register a runtime connection `tenant_{id}` (array-merged from `connections.mysql` + optional per-company creds), run `migrate --database tenant_{id} --path ...` (passes `--realpath` when the path is absolute), seed tenant defaults, seed central `modules`/`company_modules`, then mark `provisioning_status = active`, persist `db_name`/`provisioned_at`, return `$company->fresh()`. Compensating rollback on ANY `Throwable`: restore the central default connection FIRST, drop the created DB, mark `failed` + `last_provisioning_error`, rethrow. Guard: provisioning an already-active company throws.
  - **WARNING**: `migrate --database X` switches the app's default connection (`Migrator::setConnection()` â†’ `DatabaseManager::setDefaultConnection()`). The service captures `DB::getDefaultConnection()` at entry and restores it in both the success and catch paths â€” otherwise the failed-status `$company->save()` would hit the (already-dropped) tenant DB and mask the original error.
- `app/Services/Tenancy/TenantDefaultsSeeder.php`: `seed(Company $company, string $connection, string $centralConnection)` â€” mirrors central `branches` (or seeds a Head Office row), writes tenant `companies`/`branches`, fiscal year, 12 accounting periods, full parent-linked chart of accounts, supplemental accounts, default account mappings, approval settings + thresholds, numbering sequences. Restores the default connection to `$centralConnection` in a `finally`.
- **Boot fix (pre-existing breakage)**: `bootstrap/app.php` called `->withMigrationPaths([...])`, a method that does NOT exist on Laravel 12's `ApplicationBuilder` â€” the app could not boot AT ALL (every `php artisan`/phpunit invocation crashed). Removed the call; the tenant migration dir is now registered via `AppServiceProvider::boot()` â†’ `loadMigrationsFrom(database_path('migrations/tenant'))`. This is the supported way to add migration paths (default `database/migrations` is auto-registered; don't re-add it or the same files glob twice).
- **Central/tenant merge conflict**: central (`0001_01_01_000000` users, `2026_01_01_000010` companies, `2026_01_01_000020` branches) and the tenant bootstrap used to BOTH `create` those tables, so migrating the merged set failed with "table users already exists". Renamed `database/migrations/tenant/0000_01_01_000000_create_tenant_bootstrap_tables.php` â†’ `2026_01_01_000025_create_tenant_bootstrap_tables.php`. In the merged central DB it now sorts after the central create migrations, so its `hasTable` guards make it a no-op (central full-schema tables already exist); in a fresh tenant-only DB it still runs first (before `accounts` @000050) to provide the minimal `companies`/`branches`/`users` the tenant schema and `TenantDefaultsSeeder` need. Existing tests depend on tenant tables (e.g. Spatie `permissions`) being in the central DB â€” do NOT remove `loadMigrationsFrom`.
- **Tests**: `tests/Feature/Tenancy/CompanyProvisioningTest.php` â€” happy path (creates a REAL MySQL tenant DB via fixtures `tests/Fixtures/TenancyMigrations/valid`, asserts probe/accounts/numbering_sequences tables, Head Office branch, chart of accounts, fiscal year/periods/approvals/modules, default connection restored to `sqlite`), rollback (fixture `tests/Fixtures/TenancyMigrations/broken` has a migration that `throw new RuntimeException('...must fail...')` â†’ DB dropped (no new `acct_%` DBs) + company `failed` + error recorded + connection restored), already-active guard, `generateDatabaseName()` slugging. Skips via `markTestSkipped` if the `provisioning` connection is unreachable. Tenant DBs dropped in `tearDown`. Full suite after these changes: 582 tests / 1953 assertions OK (5 skipped).

20. **Multi-Tenancy Phase 2 â€” live-data migration to tenant DBs (Aug 5, 2026)** â€” copy an existing company's rows OUT of the shared DB into a fresh tenant DB. Command: `php artisan company:provision-and-migrate --company=<id> [--verify-only] [--yes]`.
- `app/Services/Tenancy/CompanyDataMigrator.php` â€” manifest-driven raw-SQL `INSERTâ€¦SELECT` copy that **preserves original primary keys** (so intra-company FKs survive). Copy order: `ALL_ROWS_TABLES` (Spatie `permissions`/`roles`/`role_has_permissions` â€” company-scoped via `company_id IS NULL`/explicit join) â†’ `DIRECT_TABLES` (85 company-scoped tables, `WHERE company_id=?`) â†’ `CHAIN_TABLES` (child/line tables with EXISTS-vs-parent predicates, `{source}` placeholder â†’ backticked source DB) â†’ `MEMBERSHIP_TABLES` (`user_favourites`/`user_preferences`, via `company_user.user_id`) â†’ stub users. `assertSchemaParity()` throws `\RuntimeException('Schema drift')` listing every manifest table whose columns differ between source and tenant before copying. `sourceSelect()`/`sourceCount()` are the single source of truth for scoped row queries and are reused by the verifier so counts always agree. Entire copy wrapped in `SET FOREIGN_KEY_CHECKS=0`/`=1` (restored in a `finally`).
- **Stub users**: the tenant `users` table is `id`+timestamps only. The migrator seeds a stub row per user referenced by any copied row (via per-table `USER_FK_COLUMNS`, plus `model_has_roles.model_id`, `company_user.user_id`, `user_favourites`/`user_preferences`), `INSERT IGNORE` so no FKs ever dangle. No passwords/emails carried over (auth is a later phase).
- **`seedDefaults=false` on `CompanyProvisioningService::provision(Company, ?string $migrationPath = null, bool $seedDefaults = true)`**: `TenantDefaultsSeeder` seeds fiscal year/periods/chart/mappings/approvals **unconditionally** â€” running it before a real-data copy duplicates them. Migration mode passes `false`, which skips the seeder and restores the central default connection manually (the seeder's `finally` normally does it). New/user-created companies keep the seeder path. `registerConnection()` is now public (verify-only needs it without provisioning).
- `app/Services/Verification/CompanyMigrationVerifier.php` â€” `verify(Company, tenantConnection, sourceConnection)` returns `['passed' => bool, 'checks' => [...]]`. Check categories: per-manifest-table row counts (source via `sourceCount`, live shared DB â€” **shared DB is never modified at copy time** so counts remain comparable), `SUM_SPOT_CHECKS` aggregates (`accounts.opening_balance`, `invoices.amount`/`amount_paid`, `bills.amount`, `journal_entry_lines.debit`/`credit`; `journal_entries` deliberately excluded â€” it has no `amount` column), `stub_rows` (companies=1, usersâ‰¥1), `trial_balance` (posted+reversed `journal_entry_lines` SUM(debit)=SUM(credit)), and 11 FK-orphan `LEFT JOIN` checks.
- `app/Console/Commands/CompanyProvisionAndMigrate.php` â€” flow: resolve company â†’ **preflight** (ping `provisioning`; if `companies.provisioning_status` missing, run `migrate --force` for the 5 `2026_12_01_*` central migrations; `pruneStaleMigrationRecords()` deletes recorded migration rows whose file no longer exists on disk) â†’ plan table (per-table row counts) â†’ `--yes`/confirm â†’ `provision($company, null, false)` â†’ `migrate()` â†’ "Rows copied" table â†’ verification table â†’ SUCCESS only if all checks pass. Any failure â†’ `rollback()` (drop tenant DB via `provisioning`, company â†’ `failed`, `db_name` null, `last_provisioning_error` truncated). `--verify-only` registers the runtime connection WITHOUT provisioning and **does not drop on failure** (tenant left for inspection).
- **Live-DB gotchas hit during the ACME run**: (1) the live `migrations` table had stale rows from renamed files (`2026_07_27_*` â†’ `2026_11_09_*` quotations/sales_receipts/bank_transfer, and `2026_07_24_084804` cash_tendered), and the renamed `2026_11_09_000002`/`000003` were pending while their tables/columns already existed â€” fixed by deleting the 4 stale rows and `INSERT`-ing the 2 renamed names as applied (batch 31). (2) `--verify-only` previously crashed on the array-valued aggregate cells passed to the Symfony table renderer â€” coerced with `json_encode`. Both companies now `active`: ACME â†’ `acct_acme_149593cc`, BETA â†’ `acct_beta_0b50add8`.
- **Not in scope of this phase (later)**: runtime tenant connection routing (`db_name` switching), BI mart rebuild per tenant (`bi:refresh-data-mart` â€” `dim_*`/`fact_*`/`bi_refresh_log` are `REBUILT_TABLES`, never copied), auth/permission binding to tenant `users`, go-live/cutover flip, retention â‰¥90 days before dropping a source tenant DB.
- **Tests**: `tests/Feature/Tenancy/CompanyDataMigrationTest.php` â€” spins a real MySQL scratch DB (`migrator_source_xxxxxxxx`) migrated from the real tenant migrations, seeds representative rows (companies/users/branches/accounts/periods/customers/products/one posted JE pair/invoice+lines/Spatie grants/company_user/user_favourites+preferences), provisions with `seedDefaults=false`, runs `migrate()` + `verify()`, asserts ID preservation, row counts, stub users `[1,2]`, no duplicated defaults, central connection restored to `sqlite`, and that `assertSchemaParity()` catches a dropped column. Both tests pass (24 assertions); combined with Phase 1: 6 tenancy tests green.

21. **Multi-Tenancy Phase 3 â€” runtime tenant routing + two-step auth (Aug 5, 2026)** â€” the default connection NEVER flips to tenant; a session-scoped `tenant` connection is bound per request.
- **`app/Models/Concerns/TenantScoped.php`**: `getConnectionName()` â†’ `TenantConnectionResolver::connectionName()` â€” returns `null` when unbound (models use default/central or test sqlite) and `'tenant'` when bound (or `tenancy.routing.connection_override` in tests). Applied via PowerShell patch to **116 model files** plus `App\Models\Permission`/`Role` (Spatie classes extended + trait; `config/permission.php` now imports `App\Models\Permission`/`Role`). Central models NOT trait-scoped: `User`, `Company`, `Module`, `CompanyModule`, `UserCompanyAssignment`, `CompanyAccessLog`. Before this, no model in the repo had a per-model connection override.
- **`app/Services/Tenancy/TenantConnectionResolver.php`** (container **singleton** via `AppServiceProvider::register()` â€” per-request bound state must survive container resolutions): `resolve(Company)` idempotently registers a runtime `tenant` connection (`Config::set("database.connections.tenant", ...)` merged from the base connection + company `db_host/db_port/db_username/db_password` (decrypted) + `db_name`, then `DB::purge('tenant')`), keyed by `(company_id, configHash)` so middleware runs every request without reconnecting. Throws `\RuntimeException` for non-provisioned/inactive/blank-`db_name` companies. `clear()` unbinds. `connectionOverride()` reads `config('tenancy.routing.connection_override')`.
- **`config/tenancy.php`**: `routing.base_connection` (`TENANT_ROUTING_BASE_CONNECTION`, default `mysql`), `routing.connection_override` (`TENANT_ROUTING_OVERRIDE`, `null` in prod), `audit.enabled` (`TENANT_ACCESS_AUDIT`, default true).
- **Test override**: `phpunit.xml` sets `TENANT_ROUTING_OVERRIDE=sqlite` â€” in tests the resolver does NOT register a real connection; bound tenant models resolve to the shared sqlite `:memory:` DB (RefreshDatabase merged schema), `Config('database.connections.tenant')` stays null. `TenantRoutingMySqlTest` sets it to `null` to exercise REAL MySQL routing (E2E: provision `tests/Fixtures/TenancyMigrations/valid`, bind, `Account::create` code `1400` â€” `1000` already seeded by `TenantDefaultsSeeder`'s 42-account chart â€” and asserts the row lands in the real tenant DB while the central `Company` model still reads sqlite).
- **`app/Http/Middleware/BindTenantConnection.php`** (alias `tenant.bind` in `bootstrap/app.php`; route group order is `tenant.bind` â†’ `company.context` â†’ `company.active`): company id = `session('current_company_id')` with legacy fallback to `$user->current_company_id` (persisted into the session); rejects (forget session + redirect `companies.index` w/ error) when the user lacks `hasAccessToCompany()` or the company is missing/inactive. **Legacy mode**: only PROVISIONED companies get the tenant connection bound â€” unprovisioned companies (pre-Phase-2 data still in the shared DB) pass through unbound. This is what keeps the ~600 pre-existing feature tests green (their fixtures create companies with `provisioning_status=pending`). `setPermissionsTeamId($company->id)` runs on every bound/legacy request.
- **Two-step auth** (`AuthenticatedSessionController::store()`): super admin â†’ `panel.dashboard` (never bound, no access log); else `resolveCompanyForLogin()` â†’ exactly 1 active `user_company_assignments` row (or 1 legacy `company_user` row) â†’ `CompanyAccessService::enter(..., ACTION_LOGIN)` (bind-if-provisioned + session + `setPermissionsTeamId` + `company_access_logs` row if `audit.enabled`) â†’ dashboard; multiple/none â†’ `companies.index` picker. **Laravel 12 gotcha**: `redirect()->route(..., absolute: false)` throws `Unknown named parameter $absolute` (the `Redirector::route()` signature has no `$absolute` param â€” only the global `route()` helper does) â€” use `redirect(route('name', absolute: false))`.
- **Company entry** (`CompanyController::select()`, now `POST /companies/{id}/select`): 404 on forged/mismatched/nonexistent company ids; inactive â†’ redirect w/ error; `CompanyAccessService::enter()` logs `support` for super admins (explicit logged support access) vs `select` for normal users. `index()` lists `activeCompanyAssignments()->with('company')` (falls back to legacy `company_user` pivot when empty). `store()` now attempts `CompanyProvisioningService::provision()`; on success auto-enters, on failure leaves `pending` ("Provisioning will complete shortly").
- **Assignment authority** (`app/Models/User.php`): `companyAssignments()`/`activeCompanyAssignments()` (central `user_company_assignments`, `is_active`), `hasAccessToCompany(int)` (active assignment OR legacy `company_user` row â€” back-compat; assignments are authoritative going forward), `accessibleCompanies()` (assignments with company, else legacy relation). `CompanyContext::userCompanies` shares `accessibleCompanies()`; **the `current_company_id` column fallback in `CompanyContext::handle()` is REQUIRED** â€” removing it broke 138 tests (`RbacTest`-style tests `actingAs()` + set the user column in `setUp`, never the session). The column is server-set-only so it stays trusted, and `BindTenantConnection` re-verifies on every request.
- **Access audit**: central `app/Models/CompanyAccessLog.php` (no `updated_at`, `UPDATED_AT = null`), migration `2026_12_01_000050_create_company_access_logs_table.php`, actions `login`/`select`/`support`. Also `app/Models/UserCompanyAssignment.php` (fillable user_id/company_id/role/branch_ids/is_active, casts branch_ids array).
- **Panel placeholder (Phase 4)**: `GET /panel` (`panel.dashboard`, OUTSIDE the tenant group) â†’ `app/Http/Controllers/PanelController.php` + `resources/views/panel/index.blade.php` listing `$user->accessibleCompanies()` with POST switch forms. Super admins land here at login and are never bound until they explicitly select a company (`support` log).
- **Tests**: `TenantConnectionResolverTest` (10: null unbound, config building, bind/idempotent/clear, throws, `resolveForCompanyId`, override routing), `CompanyAccessFlowTest` (13: single-assignment auto-select, multi/no-assignment â†’ picker, super-admin â†’ panel + support log, forged/mismatched ids 404, switch-without-re-login A/B, **legacy mode for unprovisioned companies** on select + middleware, deactivated-assignment + middleware rejection, bound models resolve to override connection), `TenantRoutingMySqlTest` (2 E2E). 31 tenancy tests / 164 assertions green (incl. Phase 1/2). Pre-existing test adjustments: `AuthenticationTest` no-assignment login now asserts `companies.index`; the 2 POS isolation tests attach the acting user to the "other" company so the middleware passes and the controller's `abort_unless(resource->company_id === session company, 403)` still fires. Full suite: **604 passed / 5 skipped / 0 failed (2086 assertions)**.
- **Not in scope (later)**: super-admin Panel UI (Phase 4), auth/permission binding to tenant `users`, tenant routing for queue/jobs, BI mart rebuild per tenant.

22. **Multi-Tenancy Phase 4 â€” Super Admin panel + central feature management + central audit (Aug 5, 2026)** â€” `/superadmin` group OUTSIDE the tenant group, guarded by the `superadmin` middleware on the central `is_super_admin` flag (+ `is_active`); the default connection is never flipped and no tenant connection is bound while browsing it.
- **Feature management is now central (ask #1)**: `app/Services/FeatureManagement.php` rewritten to read/write central `company_modules` (`Module::where('code', ...)` lookup) instead of tenant `system_settings` `features` rows â€” `isEnabled()` = `CompanyModule?->is_active === true` (missing row = OFF), `enable()/disable($companyId, $feature, ?int $actorId = null)` upsert with `activated_at`/`activated_by`. Old tenant `system_settings` feature rows are inert. Tenant System Settings Features page (`resources/views/system-settings/features.blade.php`) is now READ-ONLY (status pills + "controlled by your system administrator" callout; Action column + form removed); `SettingsController::featuresToggle()` deleted; `system-settings.features.toggle` route removed. Core modules (accounting/sales/reports/system/users, `modules.is_core`) are always-on/locked; the 10 feature modules default off and toggle via the panel. Existing tests that enabled features via `SystemSetting::setValue('features', ...)` were switched to `FeatureManagement::enable/disable` (RbacTest Ã—2, GlobalSearchTest Ã—2).
- **Central audit log (ask #4)**: `super_admin_audit_logs` (migration `2026_12_02_000010`) + `app/Models/SuperAdminAuditLog.php` â€” **deliberately NOT TenantScoped** so it always writes to the central DB even while a tenant is bound; genuinely separate from each tenant's `audit_logs` (which IS TenantScoped). `UPDATED_AT = null`, `log($actorId, $action, $companyId, $targetType, $targetId, $before, $after, $description)`; action constants `company.created/suspended/reactivated/provision_failed`, `module.enabled/disabled`, `user.created/updated/deactivated/reactivated/password_reset`, `assignment.created/updated/deleted`.
- **Users**: migration `2026_12_02_000020` adds `users.is_active` (boolean default true); `User` `$fillable`+cast; `UserFactory` sets `'is_active' => true` (without it a factory-created model's `is_active` is NULL â†’ falsy â†’ the new middleware 403s every superadmin test). Login blocks deactivated accounts (pre-auth email lookup â†’ back with errors) and super admins now redirect to `superadmin.dashboard` (was `panel.dashboard`; `CompanyAccessFlowTest` assertion updated). `Admin\UserController` untouched.
- **Middleware**: `app/Http/Middleware/SuperAdmin.php` (abort 403 unless central `is_super_admin` AND `is_active`), alias `superadmin` in `bootstrap/app.php`.
- **Controllers** (`app/Http/Controllers/SuperAdmin/`): `DashboardController` (KPIs + recent central audit), `CompaniesController` (index with `withCount` active_modules/assignments; create+`store` calls the REAL `CompanyProvisioningService::provision()` inside try/catch â€” success â†’ show, failure â†’ `company.provision_failed` audit + error flash; suspend/reactivate; show with provisioning-status pill/error banner/assigned users/active modules/recent activity; `modules` + `toggleModule` with core locked + audit; `dbPreview` GET returning `previewDatabaseName()`; `branches` GET returning `TenantBranchReader` result), `UsersController` (index/create/store/show/edit/update/deactivate/reactivate/resetPassword â€” self-deactivate blocked, reset clears `failed_login_attempts`/`locked_until` + sets `password_changed_at`), `AssignmentsController` (index paginated; create with `RoleCatalog::companyRoles()`; store takes `assignments[]` rows each `{company_id, role, branch_ids[]}`, upserts `UserCompanyAssignment` + syncs Spatie roles; edit loads branches from the tenant DB; update; destroy clears roles; private `syncCompanyRole()` swaps team via `getPermissionsTeamId()`/`setPermissionsTeamId()` + `syncRoles`), `AuditLogController` (central log, filterable by company/action).
- **Services**: `app/Services/SuperAdmin/RoleCatalog.php` â€” `companyRoles()` = config/permissions.php roles filtered to company scope (excludes `system_admin`), `isValidRole()`; validation uses `Rule::in(array_keys(...))`. `app/Services/SuperAdmin/TenantBranchReader.php` â€” `branchesFor(Company)` binds the tenant connection via `TenantConnectionResolver`, reads that company's `branches`, and **`clear()`s in a `finally`** so no tenant binding leaks into the superadmin request; returns [] for unprovisioned/inactive companies. `CompanyProvisioningService::previewDatabaseName()` (public) returns `acct_{slug}_{xxxxxxxx}` (x-padded) without a DB round-trip.
- **Views** (`resources/views/superadmin/`): shared `_nav.blade.php` horizontal tab bar (Overview/Companies/Users/Assignments/Audit Log) using `x-nav-link`; pages use `x-app-layout` + `.list-header`/`list-table`/`card`/`x-status-badge`/`x-detail-field` conventions. `companies/create` has an Alpine `x-data` block debouncing a fetch to `superadmin.db-preview` and showing the live tenant DB name before submit. `assignments/create` uses Alpine `x-data` rows (`template x-for`) with per-row company/role selects + branch checkboxes fetched from `superadmin.companies.branches` (`route(...['company'=>'__ID__']).replace(...)`), "+ Add Another Company Assignment", `:name="'assignments[' + index + '][...]'"` â€” checkbox `:value="branch.id"` (shared `value=""` + x-model breaks the id link). Topbar overflow menu gains a "Super Admin" link when `$user?->is_super_admin`.
- **Login test nuance**: in feature tests, after `actingAs($super)` a subsequent `$this->post('/login', ...)` is a no-op redirect because the user is already authenticated â€” call `Auth::logout()` first. Middleware `is_active` check must read a loaded attribute (`UserFactory` sets it).
- **Tests**: `tests/Feature/SuperAdmin/SuperAdminPanelTest.php` (23 tests: non-super 403 / guest redirect / all sections render / companies create success-via-mock + failure-fallback audit / suspend-reactivate / db-preview name / user CRUD + audit / deactivated-login block / self-deactivate blocked / password-reset unlock / assignment create+update+remove role-sync / role validation / catalog excludes system_admin / FeatureManagement central reads / module toggle+audit / core locked / modules screen / audit filterable / edit pages render / deactivated superadmin 403). `CompanyProvisioningService` mocked in tests (`$this->mock(...)->shouldReceive('provision')`) so no real MySQL is touched. Full suite after Phase 4: **626 passed / 5 skipped / 0 failed (2172 assertions)**.
- **Not in scope (later)**: auth/permission binding to tenant `users`, tenant routing for queue/jobs, BI mart rebuild per tenant, tenant-side feature setting rows cleanup.

23. **Multi-Tenancy Phase 5 â€” per-tenant BI data mart (Aug 5, 2026)** â€” the BI mart moved from the central shared DB into each tenant DB. `bi:refresh-data-mart` now loops provisioned companies, binds each tenant connection, and rebuilds dims/facts on the bound mart connection; `bi:send-digest` does the same for executive digest schedules.
- **`app/Services/BI/Concerns/MartConnection.php`** (new trait): `mart(): Connection` / `martTable(string $table): Builder` resolve via `DB::connection(TenantConnectionResolver::connectionName())`. Passing `null` to `DB::connection()` falls back to the default, so a single expression works for bound MySQL tenants, unbound contexts, and the sqlite test suite. Applied to all 5 fact builders, `DimSyncService`, `DimDateFiscalMapper`, `BiRefreshService`, and the 5 BI consumers (`CustomerLifetimeValueService`, `TrueTotalCostService`, `EmployeeProductivityService`, `BranchProfitabilityService`, `ExecutiveDigestService`). Consumer behavior is unchanged in web requests (no tenant bound â†’ default connection).
- **Fact builders are now company-scoped**: every builder takes `build(?int $companyId = null)` and each source leg adds `->when($companyId, fn ($q) => $q->where('<table>.company_id', $companyId))`. In production each tenant DB holds one company (filter is a no-op); it makes the shared-DB sqlite test override actually isolate tenants. Back-compat: no-arg `->build()` (BiPosCoverageTest) still works.
- **`DimDateSeeder::run(?string $connection = null)`** now chunk-inserts the calendar in 500-row batches â€” the single ~2,190-row insert exceeded SQLite's 999-variable prepared-statement limit (surfaced by the new command test; MySQL would also have hit packet limits).
- **`app/Console/Commands/RefreshDataMart.php`** rewritten: `{--company=}` optional filter; selects `is_active` + `provisioning_status = active` companies; per company â€” `resolve()` binding, log `running` â†’ truncate+seed `dim_date` â†’ `DimSyncService->syncAll($company)` (note `dim_company` is central `Company` data, `syncCompanies($company)` filters to the current company, all other dims read/write the mart connection) â†’ `DimDateFiscalMapper::mapFiscalYears()` â†’ truncate+rebuild each fact via `build($company->id)` â†’ log `completed` with `json_encode`d `rows_refreshed`; any throw marks log `failed` and rethrows (outer `handle()` catches, counts, returns `FAILURE`). Binding cleared in `finally`. `bi_refresh_log` is written on the mart connection (has `company_id`), and `rows_refreshed` is a JSON column so raw inserts need `json_encode` (Laravel's cast is not applied to query-builder inserts).
- **`app/Console/Commands/SendExecutiveDigest.php`**: `bi:send-digest {--dry-run}` â€” same per-company bind loop; for each company queries due `BiDigestSchedule` rows on the mart connection, `ExecutiveDigestService->collectForSchedule($schedule)`, queues `ExecutiveDigestMail` to `$schedule->recipients`, stamps `last_sent_at`; counts sent/failed and returns `FAILURE` if any failed. The `TenantConnectionResolver` singleton (`AppServiceProvider::register()`) is required so per-company bound state survives command/service container resolutions, and `clear()` in `finally` prevents leakage.
- **Tests**: `tests/Feature/BI/RefreshDataMartTenantTest.php` (4 tests) â€” full single-company rebuild (facts keyed by `company_key`, dims populated, `dim_date` seeded, completed log with JSON `rows_refreshed`, resolver unbound after run), company scoping (refreshing A leaves B untouched), unprovisioned-company skip â†’ non-zero exit, refresh-all loop (both companies get completed logs). NOTE: with the sqlite routing override all tenants share one DB, so a later company's truncate wipes an earlier one's facts in the refresh-all test â€” asserted via logs, not rows. Full suite after Phase 5: **631 passed / 5 skipped / 0 failed (2204 assertions)**.
- **Not in scope (later)**: auth/permission binding to tenant `users`, tenant routing for queue/jobs, BI mart E2E on real MySQL (covered in sqlite-override + `TenantRoutingMySqlTest` separately), per-tenant digest scheduling beyond `last_sent_at`.

24. **Multi-Tenancy Phase 6 â€” Security & Hardening (Aug 6, 2026)** â€” forge-proof company context, `db_password` at-rest encryption hygiene, rate-limited + logged login/company-switch, per-tenant backups via a shared service, tenant audit-log verification, and explicit super-admin support sessions (actor/target/duration).
- **Shared backup service** (`app/Services/Admin/DatabaseBackupService.php`): one dump primitive parameterized by a connection config. `backupConfigFor(Company)` â†’ `TenantConnectionResolver::connectionConfig()` (decrypted per-company MySQL config) when provisioned+`db_name`, else base `mysql` (legacy shared-DB mode). `backup(Company, ?userId, triggeredBy)` self-manages the tenant binding (saves `boundCompanyId()`, resolves when needed, restores prior binding/`clear()` in `finally`) so it works from both the HTTP controller (already bound) and the scheduler command (unbound); writes the `BackupLog` on the bound (tenant) connection with `success`/`failed` + `error_message`. Filename `backup_{Y-m-d_His}_company{id}.sql`.
- **WINDOWS GOTCHA (fixed)**: dumps used to be raw `exec()` strings with `escapeshellarg()`'d values â€” on Windows PHP runs through cmd.exe which passes single quotes through verbatim, so mysqldump got `--host='127.0.0.1'` â†’ exit code 2, empty output. Both `dumpToFile()`/`restoreFromFile()` now use **Symfony `Process` with an array command** (proper per-arg quoting, no shell): dump captures SQL on stdout and `file_put_contents`'s it (no `>` redirection), restore streams the SQL file in as Process input (no `<` redirection). `errorLines()` extracts stderr. Binaries resolve via `config('database.backup.binary'/'restore_binary')` (env `DB_MYSQLDUMP`/`DB_MYSQL`) â€” tests resolve XAMPP/Program Files candidates at runtime.
- **`BackupController::trigger()`** refactored to `app(DatabaseBackupService::class)->backup($company, $user->id, 'manual')`, same flash behavior. **`app/Console/Commands/BackupDatabases.php`** (`backup:databases {--company=}`): loops `is_active` + `provisioning_status = active` companies, per-tenant BackupLog, per-company try/catch, FAILURE exit with summary (Windows Task Scheduler can invoke it per tenant, or pass `--company`).
- **`db_password` at rest**: cast was already `encrypted`; added `protected $hidden = ['db_password']` so the plaintext never leaves via `toArray()`/`json`/`Response` (decryption still works through the cast for `connectionConfig()`). Tests assert the stored raw value differs from plaintext and the show view never renders it.
- **Login logging** (`login_attempt_logs` migration `2026_12_03_000010` + central `app/Models/LoginAttemptLog.php`, immutable, `UPDATED_AT = null`, `record()` helper): `LoginRequest::authenticate()` logs `invalid_credentials` (user_id null), `success` (user_id set, `user_agent`), and `rate_limited` inside `ensureIsNotRateLimited()`. `AuthenticatedSessionController` also logs the deactivated-account rejection (`failure_reason = 'deactivated'`). Login was already rate-limited at 5/min (`LoginRequest` throttle), company-switch was logged but not throttled â†’ **`routes/web.php`**: `companies.store` â†’ `throttle:5,1`, `companies.select` â†’ `throttle:20,1` (test sends 21 selects â†’ 429).
- **Forge-proofing already existed** (`CompanyController::select()` 404s on forged ids + `BindTenantConnection` re-verifies `hasAccessToCompany()` every request server-side); new tests lock the behavior in (forged session company â†’ middleware redirect + session clear; select without assignment â†’ 404).
- **Support sessions** (`company_support_sessions` migration `2026_12_03_000020` + central `app/Models/CompanySupportSession.php`): `started_at`/`ended_at`/`ended_reason` (`logout`/`context_changed`/`super_admin_revoked`), `duration` = `started_at->diffForHumans($end, true)`, **both `UPDATED_AT = null` AND `CREATED_AT = null`** (table has no `created_at` column â€” `UPDATED_AT = null` alone still makes Eloquent insert `created_at` and fail). `CompanyAccessService::enter()` closes all open support sessions for the user first, then when `action === ACTION_SUPPORT` creates one; `closeOpenSupportSessions(User, string $reason): int`. `AuthenticatedSessionController::destroy()` closes them on logout. SuperAdmin `CompaniesController::show()` loads latest 10 + "Enter Company (Support)" POST form (visible when `is_active && db_name`) + Support Access Sessions table (admin/started/ended/duration/reason, "Ongoing" badge). Binding: a support session is active only while the super admin is actually bound to that tenant.
- **Tests**: `tests/Feature/Tenancy/SecurityHardeningTest.php` (10) â€” db_password at-rest encryption + not in `toArray()`/json; superadmin show never renders plaintext; forged-session middleware rejection; select-without-assignment 404; failed/success/deactivated login logging; switch rate-limit 429; support-session entryâ†’switch (`context_changed`)â†’logout (`logout`) with duration; **backupâ†’restore round trip on REAL MySQL** (skipped when `provisioning` unreachable): scratch source tenant migrated via tenant migrations (asserts `audit_logs` table exists â†’ tenant audit logs land in the tenant DB), `backup()` via service, BackupLog row count in tenant DB, restore into a fresh DB, `CompanyMigrationVerifier::verify()` reconciles (asserts `passed`), resolver unbound afterward. Real-MySQL tests set `Config::set('tenancy.routing.connection_override', null)` and use the `provisioning` connection. Full suite after Phase 6: **646 passed / 5 skipped / 0 failed (2245 assertions)**.
- **Not in scope (later)**: auth/permission binding to tenant `users`, tenant routing for queue/jobs, BI mart rebuild per tenant, backup retention/rotation/pruning beyond the manual runbook.

25. **Currency catalog (Aug 6, 2026)** â€” central `currencies` table (migration `2026_12_03_000030` seeds 20 defaults incl. MWK as first, UTF-8 verified; `CurrencySeeder` is idempotent and wired into `DatabaseSeeder`). `App\Models\Currency` is deliberately NOT TenantScoped (global reference data); `label()` â†’ `"CODE - Name"`, `active()`/`ordered()` scopes. Super Admin panel gains **Currencies** CRUD (`SuperAdmin\CurrenciesController`, nav tab, index/create/edit views, toggle, `currency.created/updated/toggled` central audit actions). All three hardcoded base-currency selects now iterate the catalog: `superadmin/companies/create` (defaults to MWK now), `companies/index` create modal, `system-settings/_tab-currency`. Empty-catalog fallback option added to each select. **Gotchas**: (1) a `@forelse($currencies as $currency)` loop in `_tab-currency.blade.php` SHADOWS the outer `$currency` SystemSetting array used later in the same view â€” name the loop var `$curOption`; (2) unique-index test fixtures must use codes NOT in the migration seed set (MWK/ZWL/XAF collide). Tests: `tests/Feature/SuperAdmin/CurrenciesPanelTest.php` (10). Suites green: SuperAdminPanel 23, AdminModule/CompanyAccessFlow/Auth/SecurityHardening 59.

26. **Company branch-limit enforcement (Aug 6, 2026)** â€” per-company `branch_limit`/`branch_count` on the CENTRAL `companies` row (migration `2026_08_06_000010`; `branch_limit` nullable unsigned = unlimited, `0` = blocked; `branch_count` unsigned cached count of ACTIVE branches). Creation is enforced in `app/Services/Accounting/BranchLimitService.php` for every actor incl. Company Managers (they keep the ability, the system enforces the count); `superadmin.companies.branch-limit` PATCH endpoint (Super Admin can set/clear the limit directly â€” exception path; normal limit raises go through a future billing flow). `NULL`/pre-existing companies are never blocked; migration does NOT backfill limits. Concurrency: `createBranch()` runs in a DB transaction with `Company::lockForUpdate()` on the CENTRAL row (works while branch rows live in tenant/shared DBs because every creator takes the same lock); live active-branch count is authoritative and the cached `branch_count` reconciles on every enforcement/usage read, so stale values self-heal. **Override is server-side only**: client may send `override=true`, but it only takes effect when `User::isSuperAdmin()` is verified; `assertCanCreate($company, $override, $actor)`. Blocked â†’ `BranchLimitExceededException` (`error_code 'branch_limit_reached'`, payload `{success, error_code, message, branch_limit, branch_count}`) â†’ JSON 422 or redirect w/ session errors. Per-creation **tenant audit** (`branch_audit_log`, singular table name â€” model MUST set `protected $table = 'branch_audit_log'`): branch_id, company_id, created_by_user_id (no FK â€” tenant users hold stub rows), created_by_role (`super_admin`/`company_manager`), was_override, branch_limit_at_creation, branch_count_at_creation, created_at (UPDATED_AT = null). **Gotchas**: (1) `branch_count_at_creation` must snapshot the count BEFORE `$locked->increment()` â€” `increment()` mutates the in-memory model, so reading it after yields post-create count; (2) a BLOCKED create rolls back the reconcile save, so the persisted cached count stays stale until the next successful create or `usage()` read (assert via `usage()`, not `fresh()->branch_count`); (3) tenant table name vs Eloquent plural â€” keep them consistent. Routes: branch writes (`branches.store/update/toggle`) now gated `role_or_permission:system_admin|company_admin` (were UNGUARDED before); `GET /branches/usage` â†’ `{branch_limit, branch_count}` (binds tenant connection only when needed, clears after, so it works from both tenant and Super Admin contexts); `BranchController::update()/toggle()` call `authorizeScope()` (403 on forged cross-company branch id). `branch_limit` is a **required** field on both company-create forms (SuperAdmin `companies/store` + self-serve `companies/store`); `TenantDefaultsSeeder` reconciles central `branch_count` from the seeded Head Office branch at provisioning. Views: `branches/index.blade.php` (Alpine form, fetch usage, upgrade modal on `branch_limit_reached`, super-admin override checkbox), `superadmin/companies/show.blade.php` (usage badge + inline limit edit w/ Unlimited checkbox â†’ empty input = NULL), `superadmin/companies/create.blade.php`, `companies/index.blade.php`. Audit action `company.branch_limit_updated`. Tests: `tests/Feature/BranchLimitTest.php` (17), SuperAdminPanelTest now 35 (added creation-requires-limit, negative rejection, endpoint set/clear/zero/negative/non-super, show renders usage).

27. **Branch Request â†’ Quotation â†’ Offline Payment â†’ Fulfillment (Aug 6, 2026)** â€” company managers request extra branch capacity; Super Admins approve (issue a priced quotation) or reject; managers record offline payments (bank transfer / cheque / cash) that are NEVER auto-confirmed; only `system_admin`/`accountant`/`billing` staff can confirm, and confirmation is the ONLY path that raises `companies.branch_limit` (additively by quoted quantity).
- **Tables (tenant)** `database/migrations/tenant/2026_08_06_100001..100003`: `branch_requests`, `billing_quotations` (the `quotations` table name was already taken by sales), `payments` (NOTE: model `BranchPayment` MUST set `protected $table = 'payments'` or it pluralizes to `branch_payments` and every insert throws `no such table`). `*_by_user_id` columns are plain unsigned (no FK â€” tenant users are stub rows). Models: `BranchRequest` (statuses `pending_review|quoted|awaiting_payment|approved|rejected|cancelled|expired`, `quotation()` HasOne, `payments()` HasManyThrough), `BillingQuotation` (`pricing_breakdown` JSON array cast), `BranchPayment` (3 modes, `pending|confirmed|rejected`).
- **Flow** (`app/Services/BranchRequests/BranchRequestService.php`): `submit` (one open request/company guard via `OPEN_STATUSES`, error code `CODE_OPEN_REQUEST_EXISTS`); `approveAndQuote` (quote only from pending_review/quoted; total = server-calculated `unit_price Ã— quantity` frozen in `pricing_breakdown` + DB columns; `billing_quotation` numbering via `NumberingSequenceService` `BQ-` prefix â€” added to `documentTypeLabels()` + `seedDefaults()`, raising the seed count 21â†’22); `reject` (pending_review only, reason stored in `admin_notes`); `recordPayment` (requires payable pending quotation; cash requires notes + accountant/billing/system-admin actor; partial amounts flagged in controller flash); `confirmPayment` (serialized on `Company::lockForUpdate()` on the CENTRAL row, `increment('branch_limit', quantity)` additively, requestâ†’approved + quotationâ†’paid; double-confirm blocked `CODE_ALREADY_FULFILLED`; expired quotes never resurrected). `BranchRequestException` carries a stable `errorCode()`.
- **Pricing is swappable**: `BranchPricingService` interface â†’ `FlatBranchPricingService` bound in `AppServiceProvider::register`; config `config/branch_requests.php` (`unit_price_per_branch` 5000, `tax_rate` 0, `validity_days` 14, `bank_reference_prefix` BRQ-).
- **Notifications** (queued mailables, no in-app channel): `BranchRequestSubmittedMail` (to `is_super_admin` users + `billing` role assignees + legacy pivot billing users via `BranchRequestNotifier`), `BranchQuotationIssuedMail` (requester), `BranchRequestRejectedMail` (requester), `BranchPaymentConfirmedMail` (requester â€” dispatched from `BranchPaymentController::confirm()`, not the service). **PHP gotcha**: heredoc `<<<HTML` interpolation does NOT support ternaries with string literals or function calls inside `{$...}` (ParseError "unexpected token ?") and Blade `@if()` in a heredoc renders as literal text â€” precompute every expression into a variable before the heredoc. `Company::users()`/`Company::modules()` were LATENT-BROKEN (missing `use Illuminate\Database\Eloquent\Relations\BelongsToMany;` â†’ return-type TypeError `App\Models\BelongsToMany`) â€” fixed the import in `app/Models/Company.php`; the notifier calls `$request->company->users()` so this was the trigger.
- **Expiry** `app/Console/Commands/ExpireBranchQuotations.php` (`branch-quotations:expire {--company=}`, registered `->withSchedule()` daily in `bootstrap/app.php`): loops ALL active companies (NOT just provisioned â€” legacy shared-DB companies resolve to the default connection unbound, mirroring `BranchLimitService`); binds the tenant connection per provisioned company, `clear()` in `finally`.
- **Controllers**: tenant `BranchRequestController` (index/store/show/cancel; `authorizeScope` 403 on forged cross-company id; `canConfirmPayment`), `BranchPaymentController` (store/confirm/reject with `authorizeScope` + `authorizePayment` â€” the payment must belong to the request's quotation), `SuperAdmin\BranchRequestsController` (queue index across companies + show/approve/reject; `TenantConnectionResolver` bind for provisioned, `clear()` in `finally`). Tenant routes gated `role_or_permission` (store/cancel `system_admin|company_admin`, payments store `+accountant|billing`, confirm/reject `system_admin|accountant|billing`); new **`billing`** company role in `config/permissions.php` (quote/confirm + read-only customers/invoices/quotations/branches/audit/system-health); `viewer`/`auditor` got `branch-requests.view`; new `branch-requests` module actions `view|create|approve|reject|quote|confirm|cancel`.
- **Audit**: central `SuperAdminAuditLog` actions `branch_request.approved`, `branch_request.rejected`, `branch_payment.confirmed` (approved + confirmed both logged on fulfilment).
- **Views**: tenant `branch-requests/index` + `show` (quotation KPI cards, bank-reference callout, payment form, payments table with inline confirm/reject), superadmin `branch-requests/index` (cross-company queue) + `show` (approve/reject forms), `_status.blade.php` partial (`.status-pill`). Nav links in `topbar-two-row` Settings menu + `superadmin/_nav`; Branch Limit Reached modal links to `branch-requests.index`.
- **Tests**: `tests/Feature/BranchRequestTest.php` (16, `Mail::fake()` + seeded `RolePermissionSeeder`; use `assertQueued` NOT `assertSent` for ShouldQueue mailables; logout before asserting a "guest" redirect after `actingAs`). `AdminModuleTest` seed-count assertions bumped 21â†’22. Full suite: **696 passed / 5 skipped / 0 failed (2514 assertions)**.

28. **Tenant migrations on existing DBs (Aug 6, 2026)** â€” new tenant migrations added AFTER a company was provisioned do not exist in its tenant DB, producing `Base table or view not found` at runtime (e.g. `branch_requests`). Fix: `php artisan tenant:migrate [--company=<id>]` (`app/Console/Commands/MigrateTenantDatabases.php`) â€” loops active + provisioned companies with a `db_name`, binds each via `TenantConnectionResolver`, runs `migrate --database=tenant --realpath --path=database/migrations/tenant --force`, restores the default connection in a `finally` (`migrate` switches it), `clear()`s the binding. Verified: ACME (`acct_acme_149593cc`) + BETA (`acct_beta_0b50add8`) both got `branch_requests`/`billing_quotations`/`payments` without touching the central DB.

29. **Auth redesign + code-based password reset (Aug 6, 2026)** â€” the auth area (login/forgot/verify pages) now uses a shared split-panel `AuthLayout`, and the email-link password reset was replaced with a 6-digit verification-code flow.
- **Shared layout**: `LoginLayout` â†’ `AuthLayout` (`app/View/Components/AuthLayout.php`, `resources/views/layouts/auth.blade.php`, old `layouts/login.blade.php` deleted). Props: `back-href` (shows "â† Back to sign in" left, else a spacer), `title` (document title), `wide` (adds `.auth-login-card--wide` â†’ `max-width:400px` for the OTP card; others 380px). Header row `justify-content: space-between`; help link `mailto:support@camelotbooks.com`. Hero panel (dark `#0E1526`, gold CB monogram, centered) is shared by construction. Login/forgot/verify all use `<x-auth-layout>`.
- **Code-based reset flow**: forgot-password now issues a 6-digit code emailed to the account instead of a reset link; the user lands on `/verify-code`, enters the code, and is forwarded to the SAME `reset-password/{token}` form (token minted server-side via `Password::broker()->createToken()`).
- **Central table** `verification_codes` (migration `2026_08_06_200001`): user_id FK â†’ users (cascade), `code_hash` = SHA-256 of the code (plaintext never stored), `expires_at`, `used_at`, timestamps. Model `App\Models\VerificationCode` deliberately NOT TenantScoped (central users); `pending()` scope, `isValidFor()` constant-time `hash_equals`, `is_expired` accessor.
- **Service** `app/Services/Auth/PasswordResetCodeService.php`: `issue()` (deletes prior pending codes, `random_int(0, 999999)` padded to 6 â†’ returns plaintext + `expires_at` for the mailer), `latestPending()`, `verify()` (marks used + wipes others on success), `cooldownEndsAt()` (resend gate = last code `created_at` + cooldown), `maskEmail()` (`jane@x.com` â†’ `j***@x.com`). Config `config/verification_codes.php` (`code_length` 6, `ttl_seconds` 600, `resend_cooldown_seconds` 30).
- **Controller** `app/Http/Controllers/Auth/VerifyCodeController.php`: `show()` reads `session('password_reset_email')` (redirects to forgot when absent / no pending code); `verify()` validates `digits:6`, mints the broker token, returns JSON `{redirect}` or 302; `resend()` gates on cooldown (429 `{retry_after}`) then issues + mails a new code returning `{expires_at, resend_after}`; `cancel()` forgets the session email (fired by the "Back to sign in" click via `fetch keepalive`). Routes (guest group, throttled): `GET verify-code`, `POST verify-code` (`throttle:10,1`), `POST resend-code` (`throttle:3,1`), `POST verify-code/cancel` (`throttle:5,1`).
- **Enumeration safety kept**: `PasswordResetLinkController::store` responds identically whether or not the email exists; the session email is set ONLY when the account exists and never appears in a URL. New app `lang/en/passwords.php` overrides `passwords.sent` to "â€¦verification codeâ€¦". Mail is `App\Mail\VerificationCodeMail` (ShouldQueue, `Mail::to($user)->queue()`, inline-HTML brand template with the code in a dashed box; must include `expires_at`/`masked_email`). The Laravel `ResetPassword` email-link notification is no longer sent anywhere.
- **View `auth/verify-code.blade.php`**: eyebrow "Identity verification", serif "Enter verification code", masked-email subtext, live countdown from server-rendered `expires_at` (`aria-hidden` on the per-second value; expiry announced ONCE via `role="status"` when it hits zero â†’ verify disabled + "Your code has expired" prompt), 6 square OTP boxes (`aspect-ratio:1`, `inputmode=numeric`, `maxlength=1`, per-box `aria-label="Digit N of 6"`, `autocomplete="one-time-code"`), full-width gold-deep "Verify and continue" button enabled only when all 6 filled, inline `auth-login-resend` button ("Resend it") that resets the countdown + cooldowns client-side. JS `resources/js/verify-code.js` (`Alpine.data('verifyCode')`): auto-advance on input, backspace/delete/arrow navigation, paste distributes digits from the focused box, submit via fetch (wrong code â†’ server 422 â†’ clears boxes + refocuses box 1, generic "That code isn't valid."), resend via fetch (429 `retry_after` re-arms the cooldown). Forgot page: subtext/button now say "verification code", confirmation box gained a full-width "Enter verification code" action (`auth-login-recovery-action`). CSS: `.auth-login-otp-*`, `.auth-login-expiry*`, `.auth-login-resend`, `.auth-login-card--wide`, `.auth-login-recovery-action`; `@media (max-width:420px)` tightens OTP gap â†’ 6px + digit font 1.125rem. Form panel stays flat white (no pattern/lines).
- **Gotchas**: (1) `assertSessionHas`/`assertSessionMissing` live on the TestResponse, NOT `$this` (Laravel 9+ removed them from TestCase) â€” chain them on the response. (2) The reset token is in the URL PATH (`/reset-password/{token}?email=â€¦`) â€” parse via `basename(parse_url($u, PHP_URL_PATH))`, not the query string. (3) The resend endpoint must queue its own `VerificationCodeMail` (easy to forget â€” the first draft returned `expires_at` without mailing). (4) `route('password.reset', [...], false)` returns a relative URL via the `route()` HELPER only (never `Redirector::route()` â€” see Phase 3).
- **Tests**: `tests/Feature/Auth/PasswordResetTest.php` rewritten (13 tests / 64 assertions): render, code requested (session email set + mail queued), enumeration neutrality (classic + JSON), masked-email/expiry/OTP markup, no-session/no-pending redirects, generic invalid-code 422, expired-code rejection (even when the code is correct), cooldown 429, resend invalidates old code + verifies with new, cancel clears the session email, and the full reset E2E (forgot â†’ capture code from `Mail::queued()` â†’ verify â†’ extract token â†’ reset form â†’ login, asserts `Hash::check`). Auth folder (22) + CompanyAccessFlow + SecurityHardening all green. Live smoke-tested via `artisan serve`: POST forgot (known email) â†’ 200 `{"status":"sent"}` + session â†’ `/verify-code` renders masked `a***@test.com`/OTP row/countdown â†’ verify with the real code from the mail log â†’ 200 `{redirect:"/reset-password/{token}?email=â€¦"}` â†’ reset page 200. (reset-password form itself still renders on the older `x-guest-layout` â€” conversion to `AuthLayout` is a follow-up).

30. **"Set a new password" page + enforced password policy (Aug 6, 2026)** â€” the reset-password page now renders on `AuthLayout` (the phase-29 follow-up), and the password policy is now ENFORCED server-side on reset/change/registration with a live requirements checklist shared between the reset page and the in-app profile change-password card.
- **Policy service** `app/Services/Auth/PasswordPolicy.php`: builds `Illuminate\Validation\Rules\Password` from `SecuritySettingsController::getPasswordPolicy($companyId)` â€” `min()` + `mixedCase()` (when BOTH upper+lower required) or `rules()` with regexes for one-sided requirements, `numbers()`, `symbols()`. `Password::uppercase()/lowercase()` do NOT exist in this Laravel version (verified `vendor/laravel/framework/src/Illuminate/Validation/Rules/Password.php`: only `min/max/uncompromised/mixedCase/letters/numbers/symbols/rules`). API: `ruleFor(?int $companyId = null)` (validation rule) + `checklist(?int $companyId = null)` (`['key','label']` items matching exactly what `ruleFor` enforces). Global defaults when no company: min 8, upper, lower, number, no symbol.
- **Wired**: `NewPasswordController::create` passes `checklist()` to the reset view; `store` validates `ruleFor()`; `PasswordController::update` uses `ruleFor($request->user()->getActiveCompanyId())`; `RegisteredUserController::store` uses `ruleFor()`; `ProfileController::edit` passes `checklist($request->user()->getActiveCompanyId())`.
- **Invalid/expired token**: `NewPasswordController::store` now redirects to `route('password.request')` with `withErrors(['email' => __($status)])` instead of `back()->withInput()` â€” no dead-end reset form; `forgot-password.blade.php` gained the `$errors->get('email')` display block.
- **Shared partials** (plain `@include`, NOT `x-...` â€” `x-auth.partials.*` fails since they live in `views/auth/partials/`): `auth/partials/new-password-fields.blade.php` (Alpine `newPassword({ policy: {!! json_encode($policy, JSON_HEX_TAG|JSON_HEX_APOS|JSON_HEX_AMP|JSON_HEX_QUOT|JSON_UNESCAPED_UNICODE) !!}, prefix: 'â€¦' })` â€” @json would escape the en dash to `\u2013` and break tests; boxed `.password-input-boxed` with show/hide toggle, `autocomplete="new-password"`, password/confirmation errors, checklist `<ul id="{prefix}password-checklist" class="auth-login-password-checklist"><template x-for>` with `.is-met` indicators; props `policy`/`prefix`/`errorBag` ('' reset, 'updatePassword' profile)/`autofocus`) and `auth/partials/password-toggle.blade.php` (eye/eye-slash button bound to an `x-var`; also used for the current-password field).
- **JS** `resources/js/new-password.js` (`Alpine.data('newPassword', ({policy, prefix}) => â€¦)`): per-key `met()`, `allMet`, `match`, `handleSubmit` blocks submit with localized errors when unmet/mismatch; registered in `app.js`.
- **Reset view** `resources/views/auth/reset-password.blade.php` rewritten on `<x-auth-layout title="Set a new password">` (no back-href): eyebrow "Security", serif "Set a new password", hidden `token`/`email`, fields partial, submit "Update password" `:disabled="submitting || !allMet || !match"`, footnote "Contact your administratorâ€¦". **Profile** `profile/partials/update-password-form.blade.php` rewritten on the same partial: current-password field with toggle + bag errors, fields partial (`prefix='update_password'`, bag `updatePassword`), submit `:disabled="submitting || !allMet"`; requires `$policy` (passed by `ProfileController::edit`).
- **CSS** in `resources/css/app.css` (shared, NOT `.auth-login`-scoped so the profile card works; `var()` fallbacks for non-auth scope): `--green:#067647` token; `.password-input-wrap` (relative); `.password-input-boxed` (flat white, 1.5px border, `padding:11px 42px 11px 13px`, gold focus â€” overrides bottom-border `.input`); `.password-input-toggle` (absolute right, 26px, 18px svg, `:focus-visible` gold); `.auth-login-password-checklist` (card `gap:9px`); `.auth-login-checklist-item/indicator/label` (18px circle, `.is-met` â†’ green bg + white check svg â€” never color-only); `.password-form-submit` (gold-deep mirror of `.auth-login-submit`).
- **Tests**: `PasswordUpdateTest`/`RegistrationTest`/`PasswordResetTest` updated to `'New-password1'`; two new `PasswordResetTest` tests â€” checklist render (strings incl. "At least 8 characters", "One uppercase letter (Aâ€“Z)", "One lowercase letter (aâ€“z)", "One number (0â€“9)", `auth-login-password-checklist`, `newPassword(`; asserts NO "Back to sign in") and invalid-token redirect to `password.request` with `email` error (token hash unchanged). Full suites: Auth + Profile **36 passed (144 assertions)**; SuperAdmin + AdminModule + Tenancy **118 passed (440 assertions)**. Live smoke via `artisan serve`: forgot â†’ code from mail log â†’ verify (needs `Accept: application/json` â€” `wantsJson()` gates the JSON redirect) â†’ reset page renders checklist â†’ weak password rejected 422 â†’ valid reset â†’ login with the new password lands on company picker; `/profile` renders current-password + checklist + toggle. NOTE: dev `accountant@test.com` password is now `Smoke-test2` from the smoke run. Follow-ups: SuperAdmin `UsersController` user creation still uses `Password::defaults()` (min 8, not the company policy); reset-password legacy `x-guest-layout` fully removed.

31. **Super Admin tabbar moved into the sticky topbar (Aug 6, 2026)** â€” `.sa-tabbar` is now a child of the sticky `<header class="topbar">` instead of being the first element of the page slot with a `position:sticky; top:var(--topbar-h,106px)` hardcode. Guarantees the tabs sit FLUSH below `topbar-row-2` (`gapFromRow2:0`) and scroll/stick in lockstep with the topbar as one unit (verified in headless Chrome: identical header/tabbar geometry at rest, after scroll, and back-to-top; header grows 106px â†’ 162px only on `superadmin.*` routes). Mechanism: `layouts/topbar-two-row.blade.php` gains `@if(str_starts_with($routeName,'superadmin.'))` â†’ `@php $saActive = explode('.', substr($routeName, strlen('superadmin.')))[0] ?? 'dashboard'; @endphp` â†’ `@include('superadmin._nav', ['active'=>$saActive])` right before `</header>`. **Why not `@stack`/`@section`**: the topbar include (layouts/app.blade.php:35) renders BEFORE `{{ $slot }}` (:54), so slot-defined pushes/sections are never visible at the topbar â€” route-gating is the only ordering-safe injection. All 19 `@include('superadmin._nav', ...)` calls removed from superadmin views (dead-keys: none â€” `$saActive` derivation maps every `superadmin.{key}.*` route). CSS: `.sa-glass--top` lost `position:sticky/top/z-index/margin` (kept the hairline `box-shadow`); the `@media(min-width:1024px){.sa-glass--top{margin:-32px...}}` and `@media(max-width:720px){.sa-glass--top{top:0}}` rules deleted; `.sa-tabbar` reverted to full-width `padding:0 32px` (mockup shows a full-width glass bar, NOT a max-w-8xl-constrained one â€” do NOT add max-width/margin to it). `--topbar-h:106px` (app.css:19) still used by `.fav-sidebar` sticky top. The `.sa-glass--bottom` sticky action footer stays in the page slot (unchanged). Rebuilt assets (`npm run build`); `SuperAdminPanelTest`+`CurrenciesPanelTest` 45 passed / 163 assertions. Note: tabbar is NOT sticky-positioned itself anymore â€” it inherits stickiness from the parent header (topbar scrolls away if the header ever loses `sticky`).

32. **Review-design migration: shared `review` components + 6 decision pages (Aug 6, 2026)** â€” applied the Branch Request review design (page head + inline status badge, read-only detail card, decision card with footer actions, navy outcome strip for already-decided states) to every approval/review/decision page. Styling/layout only â€” no route, handler, payload, or conditional-rendering changes. `budgets/show` + `bills/show` explicitly out of scope (no rendered approve UI exists).
- **New components** `resources/views/components/review/*.blade.php`: `head` (H1 `text-2xl font-extrabold tracking-[-0.02em] text-gray-900`, `badge` slot, optional back link `h-10 rounded-xl border-shell bg-white/80 shadow-edit hover:-translate-y-px focus-visible:outline-gold-500`), `card` (`rounded-3xl bg-white/[.66] p-[26px] shadow-card backdrop-blur-[14px]`, navy icon tile `from-[#2e4763] to-[#22394f] text-[#e2c069]`, optional `action` slot for header badges), `field` (label `mb-1.5 text-[11px] font-bold uppercase tracking-[0.08em] text-slate-500`, value `text-sm font-semibold text-gray-900`, `mono` prop â†’ `font-mono text-[13px] font-medium`), `badge` (variants `pending` gold / `approved` mint / `rejected` red / `neutral` / `accent` / `navy`; `dot` prop renders 7px dot), `decision` (icon-block header "Review & Decide", `fields` slot grid `mt-[22px] grid grid-cols-1 gap-x-6 gap-y-5 lg:grid-cols-2`, `actions` slot footer `mt-6 flex flex-wrap items-center justify-between gap-3.5 border-t border-line pt-[18px]`), `outcome` (`bg-gradient-to-r from-navy-700 via-navy-800 to-navy-900` + gold icon tile + mono gold chip `#e2c069`; `chip` + `tone` approvedâ†’check / rejectedâ†’X props), `btn` (variants `primary` gold gradient `shadow-new`, `reject` red outline, `ghost`, `edit`; sizes lg/md; default `type="button"`). Shared reason-textarea class: `min-h-[110px] mt-1 block w-full rounded-xl border-shell bg-white/80 focus:border-[rgba(182,145,63,.55)] focus:ring-[3px] focus:ring-[rgba(182,145,63,.15)] focus:outline-none`.
- **Design rules locked in**: single gold approve accent per page (submit-only states put the action INSIDE the decision card via hidden inline form + `form="id"` attribute on `<x-review.btn type="submit" form="...">`); reject = danger outline; required-reason error styling preserved; badge strip moved from legend cards below the toolbar into the page head; legend/quick-actions buttons removed from the left rail (Back replaced by head back-link); non-decision states (draft/submitted/pending) migrate without a decision card.
- **Migrated pages**: `superadmin/branch-requests/show` (reference page; quotation card â†’ `x-review.card` with `action` badge slot, payments table badges mapped to approved/rejected/pending); `accounting/journal-entries/show` (`pending_approval` â†’ decision card w/ inline approve form + reject-reason form, `posted` â†’ APPROVED strip, `rejected` â†’ REJECTED strip w/ reason, `reversed` â†’ REVERSED strip; old `#rejectModal` removed; `$cs` var + print CSS preserved); `accounting/purchase-requisitions/show` (`submitted` â†’ decision card, `approved`/`rejected` strips, Create PO + Back preserved in toolbar); `accounting/quotations/show` (`sent` â†’ decision card, `accepted`/`declined`/`converted` strips, convert-to-invoice kept in toolbar); `accounting/payroll-runs/show` (`calculated` â†’ decision card, `approved` â†’ APPROVED strip w/ approvedByUser/approved_at, `posted`/`partially_paid`/`fully_paid` â†’ POSTED strip, Post-to-GL + payslips/PAYE/pension action row preserved); `branch-requests/show` inline payment confirm/reject restyled to compact outline buttons (green confirm / red reject + red "Confirm Rejection", boxed reason input â€” no second gold accent since Record Payment already uses gold primary).
- **Edit gotcha**: the payroll-runs oldString failed twice because the file's `$statusColors` map had NO `'approved' => 'positive'` entry (the plan draft added it) â€” verify source bytes before large `oldString` blocks.
- **Verification**: all 6 views compile via `blade.compiler->compileString()` + `php -l` on compiled output (no syntax errors); `npm run build` emits all new tokens (`shadow-new`, `border-shell`, `navy-700`, `from-gold-500`, `bg-row`, `text-gold-700`). Full suite: **709 passed / 5 skipped / 0 failed (2599 assertions)**. Commit `5d72984` pushed to origin/main.

33. **Tenant UI standardization onto the superadmin reference design (Aug 7, 2026)** â€” Step 1â€“3 of the redesign: audited the reference, inventoried every page, extracted shared `x-ui.*` primitives from the reference, re-pointed the superadmin components onto them, and retargeted the shared tenant CSS classes so all ~330 tenant views inherit the reference visual language without per-page edits.
- **Docs**: `docs/DESIGN_SYSTEM.md` (page shell, cards, datasheets, badges, buttons, forms, detail pages, tokens, rules) + `docs/PAGE_INVENTORY.md` (Priority A core CRUD ~60 modules/~220 files, B admin ~12, C settings/POS/analytics ~60, D 39 report views; excluded print/auth/error views). `x-slot name="header"` in `layouts/app.blade.php` confirmed dead (0 pages use it) â€” titles render inline.
- **Shared primitives** `resources/views/components/ui/`: `page-head` (H1 `text-2xl font-extrabold tracking-[-0.02em] text-gray-900`, badge/description/action slots), `card` (`rounded-3xl bg-white/[.66] p-6 shadow-card backdrop-blur-[14px]`), `btn` (primary gold gradient `shadow-new` / edit soft-gold / ghost / danger; sizes lg/md), `badge` (active mint+dot / muted / warning / danger / accent core gold / navy; aliases positive/neutral), `th` (align left/right/center), `datasheet` (wrapper + optional `head` slot + `minWidth` default 960px).
- **Superadmin delegation**: `components/superadmin/{page-head,card,btn,badge,th}.blade.php` now thin delegators to `x-ui.*` (all props/slots pass through). Verified by `SuperAdminPanelTest` 35 passed â€” the reference pages now render through the shared primitives.
- **CSS retargets in `resources/css/app.css`** (single source of truth; legacy class names kept so no blade edits were needed):
  - `.list-table`/`.list-table-wrap` â†’ navy gradient thead `#24384fâ†’#182a3eâ†’#132234`, `text-navy-200 #cdd7e2`, inset white/gold hairlines, `rounded-[12px] border-shell bg-row`, `px-5 py-[18px]` cells, hover row white, link hover `#96742c`.
  - `.card` â†’ glass `rounded-3xl` + `shadow-card` + `backdrop-filter:blur(14px)`; `.kpi-label` uppercase 11px bold #64748b, `.kpi-value mt-1`.
  - `.datasheet` thead â†’ navy gradient; `.datasheet-wrap` â†’ `border-shell rounded-12 bg-row`.
  - `.pill-base`/`.status-pill` â†’ `px-3 py-1.5 text-xs font-bold rounded-full shadow-badge border`; positive = `mint-100â†’mint-200` + `green-700` text, neutral/default = `gray-50â†’gray-100` + gray-500, negative = `red-50â†’red-100` + red-700. Legacy `.badge*` aliases kept.
  - `.btn-primary` â†’ gold gradient `#b6913fâ†’#96742c` + `shadow-new` + inset highlight; `.btn-ghost` â†’ `border-gray-300 bg-white` hover gray; `.btn-danger` â†’ `border-red-300 bg-white hover:bg-red-50`; `.btn-commit` kept gold outline.
  - `.input`, `textarea.input`, `.input-label` â†’ boxed 40px `rounded-[12px]` `#e3e6eb` border, gold focus ring `0 0 0 3px rgba(182,145,63,.15)`; labels uppercase 11px bold #64748b. `.scoped-search-field` â†’ same 40px boxed + gold focus ring.
  - `.detail-lbl`/`.detail-val` â†’ reference field language (label `text-[11px] font-bold uppercase tracking-[0.08em] text-slate-500`, value `text-sm font-semibold text-gray-900`; bottom borders removed). `.record-toolbar` â†’ glass `rounded-2xl bg-white/[.66] border-shell` + blur; `.tr-save` gold gradient; `.tr-item`/`.tr-more` slate `#374151` navy-tint hover; `.tr-archive` gray border.
  - `.list-header h1` â†’ Inter extrabold non-italic `text-2xl tracking-[-0.02em] #111827`; `.list-header-create` â†’ gold gradient `rounded-[12px]` + `shadow-new` + hover translateY/brightness; `.list-filter-input/-select` â†’ `#d1d5db` underline gold focus; `.list-filter-clear` hover gold-soft. `.list-sidebar-*` â†’ glass card `rounded-[20px] border-shell bg-white/[.66]`, label #64748b, icon squares navy-tint, hover navy 6%.
- **Verification**: `npm run build` clean; compiled CSS contains all retargeted gradients/shadows/tokens; `ScopedSearchRenderSmokeTest` passes (all converted routes render); targeted suites green: SuperAdminPanel+Currencies+BranchRequest 62 passed, SalesModule+ItemCategory+Cheque+Expense 35 passed. Full `php artisan test` exceeds the 10-min shell timeout (run in slices with `--filter`).
- **Step 4 (structural)**: shared tenant components now emit the reference primitives directly. `x-list-header` (used by 256 pages) delegates to `<x-ui.page-head>` â€” title renders the reference `text-2xl font-extrabold` H1 and the create action becomes the gold-gradient `<x-ui.btn>`; added optional `description` prop + default-slot passthrough for arbitrary actions (report-center search form, bank-reconciliation import/back buttons, companies modal-trigger button). Direct `.list-header` usages in `companies/index`, `panel/index`, `report-center/index`, `accounting/bank-reconciliation/index` migrated to `x-list-header`. Dead duplicate `components/list/{header,quick-links,icon-button,avatar-initials}.blade.php` deleted (live components are the root-level hyphenated `x-list-*`). Verified: `ScopedSearchRenderSmokeTest` passes; slices InvoiceService+FixedAsset+PettyCash+VendorCentre+Budget+CostCenter+GlobalSearch 68 passed, Assembly+LandedCost+StockCount+UOM+ItemCategory+Cheque+Expense 64 passed, AdminModule+BranchLimit+Favourites+PosSale+PosSettlement+PosTerminal 114 passed; `Blade::render` spot-check confirms the delegated output (page-head H1 + gold button).
- **Off-palette purge (Step 4 follow-up)**: audited all 125 blade files for legacy indigo/blue/purple classes (the only off-palette family â€” cyan/teal/violet/fuchsia/sky had zero hits) and bulk-swept 777 replacements to the reference tokens via PowerShell ordered `.Replace()` pairs. Mapping: form focus rings `focus:(ring|border)-(indigo|blue|purple)-500` â†’ `focus:(ring|border)-gold-500` (both orders handled); link/action `text-indigo-600`/`text-blue-600` â†’ `text-gold-700` with `hover:text-*-900` â†’ `hover:text-gold-800`; solid buttons `bg-(indigo|blue|purple)-600` â†’ `bg-gold-600` (+ `hover:bg-gold-700`/`active:bg-gold-800`); neutral info badges `bg-blue-100 text-blue-800` â†’ `bg-gray-100 text-gray-600`; accent "Current"-style `bg-indigo-100 text-indigo-800` â†’ `bg-gold-soft text-gold-800`; positive "Converted" pill `bg-purple-100 text-purple-800` â†’ `bg-mint-100 text-green-700`; info callouts `bg-*-50 border border-*-200` â†’ `bg-gold-soft border border-gold-line` (text â†’ `text-gold-800`); decorative icon tiles `bg-indigo-100`/`bg-blue-100` â†’ `bg-gold-soft`; nav active underline `border-indigo-400` â†’ `border-gold-500`; nav dropdown current-company `bg-indigo-50 text-indigo-700` â†’ `bg-gold-soft text-gold-800`; KPI numbers `text-indigo-700` â†’ `text-gray-900`; bare `text-indigo-700/800/900` â†’ gray/gold per context. **Deliberate keeps**: POS checkout payment-method selector buttons keep their functional color coding (Cash=emerald, Split=amber â€” both already on-palette; the off-palette Card/Mobile blue/purple fell under the bulk sweep and landed as gold-soft/gold-700, `text-blue-700`â†’`text-gray-900`, `text-purple-700`â†’`text-gold-700`), while the checkout's generic form controls (input focus rings, modal Proceed buttons, split checkboxes, balance figures `text-indigo-600`â†’`text-gold-700`) all went gold. Consolidated balance-sheet section headers kept semantic sign colors (Assets gold, Liabilities red, Equity gold after fixing a stray `text-green-700` from the Converted-pill mapping); consolidated income-statement keeps green Income / orange Expenses / red-green Net Income. Fixed-assets show keeps red (disposal) + yellow (impairment) danger/warning actions, transfer/revaluation â†’ gold. Verified: zero remaining `(text|bg|border|ring|focus:*)` indigo/blue/purple/cyan/teal/violet/fuchsia/sky classes in `resources/views`; `npm run build` clean; `ScopedSearchRenderSmokeTest` + feature slices (70 + 101 tests) pass; the 3 context-sensitive files (POS checkout, 2 consolidated reports) compile via `blade.compiler` + `php -l`. NOTE: the sweep's `$exclude` array for POS checkout/consolidated reports used relative paths without the `resources\views\` prefix, so the exclusion was a no-op and those files were swept too â€” harmless, as verified above.
- **Not in scope (decision â€” Aug 7, 2026)**: the structural migration of Priority A pages to `x-ui.datasheet`/`x-ui.card` was DELIBERATELY DECLINED. The CSS retarget already renders every page (all 126 datasheet + 117 card files) in the reference language with zero per-page edits, and the `x-ui.datasheet` component is NOT a faithful replacement (it renders a bare `w-full border-collapse` table WITHOUT the `datasheet` class, so a migration would lose the navy-gradient thead `.datasheet thead` provides). `x-ui.*` primitives remain available for new pages; legacy pages keep their CSS-covered markup. Standardization considered complete.

34. **Centralised feedback system (Aug 7, 2026)** â€” replaced all legacy `confirm()/alert()/prompt()` call-sites, flash banners, and inline alert divs with a unified pixel-matched feedback layer (reference mockup saved at `docs/feedback-system.mockup.html`).
- **JS API** `resources/js/feedback.js` (registered in `app.js`): `window.feedback = { toast(type, title, message?, opts), openConfirm(config), openPrompt(config), alert(msg), confirm(msg, opts), prompt(msg, opts) }`; legacy-compat globals `fbConfirmSubmit(event, msg, opts)`, `fbConfirmButton(event, msg, opts)`, `fbPromptForm(event, msg, opts)`, `fbConfirmOnly(...)`; `window.atlasToast(msg, type)` kept as alias for `favourites.js`. Toast viewport `#fb-toast-viewport` (fixed right-5 top-5 z-120 max-w-380px, max 4 stacked, auto-dismiss 5s/errors 8s, pause-on-hover progress bar via rAF, `aria-live="polite"`/`role="alert"`). Confirm/prompt overlay `#fb-confirm-root` (fixed inset-0 z-130 blur backdrop, focus-trapped, initial focus Cancel, Esc/backdrop cancel, type-to-confirm gating, navy-head variant, promise-based). `fbConfirmSubmit`/`fbConfirmButton` wrap form/button confirms so `event.preventDefault()` + async handling replace the sync native dialogs. Flash emitter reads `#feedback-flashes[data-flashes]` on DOMContentLoaded (JSON `{type: msg}`).
- **Blade components** `resources/views/components/feedback/`: `alert.blade.php` (variant info|success|warning|error, inline SVG icon, `role="alert"` for warning/error, root merges `$attributes->class(['fb-alert', ...])` so call-sites pass spacing like `class="mb-4"` â€” `.fb-alert` CSS has NO default margin), `system-banner.blade.php` (navy gradient, Alpine dismiss, gold action link), `flashes.blade.php` (renders `<div id="feedback-flashes" data-flashes="{{ json }}" hidden>` ONLY when a flash exists; emits `success`/`error`/`warning`/`info` â€” **`status` deliberately EXCLUDED** because Laravel auth/profile set it to status-KEYS like `'profile-updated'`, not user-facing messages; those pages render their own inline status text). `x-feedback.confirm-modal` was implemented JS-only (no Blade file â€” `openConfirm`/`openPrompt` create DOM at runtime).
- **Layout wiring** `layouts/app.blade.php`: legacy `<div id="toast-container">` + inline `atlasToast` builder removed (kept one-line forwarder); added `<x-feedback.flashes />` + `<div id="feedback-confirm-root"></div>`.
- **Call-site migration (70 total)**: 48 form/button confirms â†’ `fbConfirmSubmit`/`fbConfirmButton` (regex: `onsubmit="return confirm(` â†’ `fbConfirmSubmit(event, `, `onclick="return confirm(` â†’ `fbConfirmButton(event, `); special cases â†’ `window.feedback.openConfirm({...})` (budgets/show lock, recurring-journals/show cancel, chart-of-accounts deactivate), `fbPromptForm(event, ...)` (sales-receipts/show, quotations/show), `openPrompt({...}).then(...)` (expenses/show void reason w/ confirmLabel), async `await openConfirm` + `window.feedback.alert` failure branches (bank-reconciliation/show matchLine/matchTransaction), `window.feedback.alert` + promise-wrapped confirms (journal-entries/create, recurring-journals create/edit). Verified: **zero bare `confirm(`/`alert(`/`prompt(` call-sites** remain in app code (rg lookbehind check clean).
- **Flash-banner migration (131 files / 311 blocks)**: `strip_flash_banners.ps1` regex-stripped all `@if(session('success'|'error'|'warning'|'info'))` + legacy `session('x')` div blocks; flashes now surface as toasts via the layout emitter. Remaining `@if(session(` hits are company-context checks only.
- **Inline-alert migration (27 blocks)**: converted `bg-red-100/bg-red-50/bg-yellow-50/bg-amber-50` error/warning/info boxes to `<x-feedback.alert variant="error|warning|info" class="mb-4|mt-4|mt-6">` across assemblies (create/create-bom/unbuild incl. the orange "Unbuild reverses a Build" info callout), item-categories, stock-transfers/adjustments/counts, landed-costs, paye-tables, branch-requests index/show, pos/returns/create, system-settings/index, balance-sheet + cash-flow "out of balance" warnings, cheques/show voided notice, fiscal-years/show year-end warning, analytics/cash-flow-trend projection disclaimer (removed its dangling `</div>`s). **Deliberate keeps**: POS checkout dynamic JS/Alpine classes (lines 9/21/27/305/575/1127/1149 â€” functional state-driven, not banners), `pos/cashier/login` (standalone HTML page, no app CSS/layout â€” can't use the component), `admin/system-health/index` recent-errors list rows (diagnostics list items, not alert banners).
- **Gotchas**: (1) PowerShell double-quoted here-strings interpolate `$error` (an automatic variable) â€” use `@'...'@` single-quoted here-strings for Blade blocks containing `$error`. (2) `.fb-alert` has NO default margin; call-sites pass `class="mb-4"` (component root merges attributes) to match the legacy `mb-4`. (3) `@props` default `class` is NOT merged unless the root element renders `$attributes->class([...])`. (4) `session('status')` must never be auto-toasted â€” it's a status key, not a message.
- **Verification**: `php artisan view:cache` clean (all ~330 views compile); `ScopedSearchRenderSmokeTest` passes (renders every converted route as company_admin); `npm run build` emits `.fb-*` tokens. Remaining legacy-colored boxes are the 3 intentional keeps above.

35. **Create/Edit Bill redesign (Aug 7, 2026)** â€” both screens rebuilt on a shared `resources/views/accounting/bills/_form.blade.php` partial driven by props (`isEdit`, `bill`, `formAction`, `formMethod`, `cancelRoute`, `title`, `cs`). Design per `docs/create-bill.mockup.html`: sticky page header (serif title + status badge on edit, gold "Save Bill"/"Save & New" + ghost "Submit for Approval" actions), navy icon-chip section leaders, fluid line-items table with sticky navy-gradient header, and a live summary card (`bill-amt`/`bill-ci`/`bill-ci-num`/`bill-drop` CSS) with a navy-gradient grand-total bar. Lines use the invoices/purchase-orders JS pattern (`item-selected` event, hidden `tax_rate`/`discount`, `Add Line`/delete/duplicate, product row-selection state). Vendor/account picks are `x-scoped-search-field` (server mode); cost-center is a native select (NOT server-searchable â€” `scopedSearchFieldHtml` requires `search-url`; if ever converted, use `SearchCatalog` `cost-center` entity).
- **Controller additions** (`app/Http/Controllers/Accounting/BillController.php`): `create()`/`edit()` now pass `branches` (Branch where company_id, is_active, orderBy name), `currencies` (`Currency::query()->active()->ordered()->get()` â€” central, NOT TenantScoped), `defaultExpenseAccountId` (`DefaultAccountMapping::getAccountId($companyId, 'default_expense') ?? $expenseAccounts->first()?->id`); `store()`/`update()` validate `branch_id` (nullable, `exists:branches,id`) + `exchange_rate` (nullable numeric min:0) and handle `action` via `handlePostSaveAction()` (the shared `bill-ci` checkmark button posts `action=submit_for_approval`); `update()` calls `$this->billService->update(...)` then `$bill->fresh()` before the action handler (the stale instance would still be `draft`).
- **BillService** (`app/Services/Accounting/BillService.php`): new `submitForApproval(Bill $bill, int $userId): Bill` â€” DB::transaction, `logBillAction('submitted_for_approval')`, flips `draft` â†’ `pending_approval` (`STATUS_PENDING_APPROVAL`).
- **SearchCatalog** product entity (`app/Services/Search/SearchCatalog.php`): added `expense_account_id` to the `select([...])` and the returned array so line-item autofill can set the expense account.
- **Form-field persistence wiring (Aug 7, 2026 follow-up)** â€” the persistence-gap audit is now CLOSED for the mockup fields. Migration `database/migrations/tenant/2026_11_09_000014_add_bill_form_fields_to_bills_table.php` adds `po_number` (string 60), `grn_reference` (string 60), `supplier_notes` (text), `payment_instructions` (text), `freight_charges`/`insurance_charges`/`customs_charges`/`other_charges` (decimal 15,2 default 0). `Bill` model: fields fillable, charges cast `decimal:2`, `totalCharges(): float`, plus new `subtotal`/`tax_total`/`total` accessors (sum of lines + charges; the show page used these names but they DIDN'T EXIST â†’ amounts rendered 0.00). `BillService`: `create()/update()` persist all 8; `updateBillAmount()` = lines sum + `totalCharges()`; `post()` appends one `appendChargeLines()` JE pair per charge (DR first-line `expense_account_id` â†’ fallback `DefaultAccountMapping::getAccount($companyId,'default_expense')` â†’ throw; CR AP, memo `"Bill {n} - {label}"`) in BOTH the standalone and PO-backed branches, before the FX service so charges are exchange-rate scaled; `approve()` LATENT BUG FIXED â€” standalone approve never added `$totalDebit += $line->tax_amount`, so approved bills with tax posted an unbalanced JE. `BillController`: `store()/update()` validate all 8 (nullable; charges numeric min:0; notes max:5000). Show page renders PO/GRN refs, per-charge balance rows between Tax and Total, and supplier notes/payment instructions. `bills.submit` ROUTE WAS MISSING â€” the show page's Submit-for-Approval form posted to an undefined route (500 on every draft-bill show page); added `BillController::submit()` (permission `bills.edit`, `sod:bill`) + route. `x-detail-field` LATENT BUG: `{{ $slot ?? $value }}` never rendered `:value` (Blade `$slot` is always set, even empty) â†’ all 19 pages using `:value` showed blank values; fixed to `{{ $slot->isNotEmpty() ? $slot : $value }}`. Tests: `BillServiceTest` +4 (persistence, update, standalone-post JE w/ charges, approve w/ charges+tax balanced), `BillServicePoBackedTest` +1 (charges balanced in PO-backed post), new `BillControllerTest` (3: store persists, negative charge rejected, show renders). BillServiceTest 14 + PoBackedTest 7 + BillControllerTest 3 all green; `view:cache` clean; `ScopedSearchRenderSmokeTest` passes.
- **Bill attachments end-to-end (Aug 7, 2026 follow-up #2)** â€” the dropzone is now functional (was "shown for reference"). `_form.blade.php`: `bill-form` tag gained `enctype="multipart/form-data"`; attachments section renders existing files (`$billAttachments = $bill?->attachments`) with per-file remove buttons (`billRemoveExistingFile` â†’ hidden `delete_documents[]` inputs), a click-to-browse + drag/drop dropzone (`billWireDropzone`, `.bill-drop--drag` class), and a new-file list with remove buttons (`billNewFiles` array synced into the `#bill-files` input via `DataTransfer` so the browser submits the exact selection). `BillController`: `store()/update()` validate `files` (`array max:10`, each `file mimes:pdf,jpg,jpeg,png,gif,webp,xls,xlsx,doc,docx,txt,csv max:10240`) + `delete_documents` (`array of integer`); private `handleAttachments()` scopes deletes to the bill's own attachments (`attachmentable_type`=Bill::class + id, cross-company forged ids are no-ops) then stores new files on `Storage::disk('public')` at `bill-attachments/{companyId}/{billId}/{random}.{ext}` creating `Attachment` rows (`uploaded_by` = auth id). `Bill` model: `attachments(): MorphMany` (reuses the pre-existing morph `attachments` table). `show()`/`edit()` eager-load `attachments`; show page toolbar button badge uses `$bill->attachments->count()` and a new Attachments card lists download links via `Storage::disk('public')->url()`. New `format_bytes()` helper in `app/helpers.php` (B/KB/MB/GB). CSS: `.bill-drop--drag` (gold ring + copy cursor) + `.bill-file-list` styles. Tests: `BillControllerTest` +3 (store uploads 2 files â†’ rows + disk files + uploaded_by, update deletes flagged attachment + disk file, oversized file rejected `files.0`) â€” 6 passed / 31 assertions; BillServiceTest 14 + BillServicePoBackedTest 7 green; `view:cache` clean; `ScopedSearchRenderSmokeTest` (covers `bills.create`) passes; `npm run build` emits the new tokens.
- **Views**: `create.blade.php`/`edit.blade.php` are thin `<x-app-layout>` + `$cs` + `@include` shells. Verified: `view:cache` clean, compiled `_form` passes `php -l`, `ScopedSearchRenderSmokeTest` passes, `BillServiceTest` (14) + `BillServicePoBackedTest` (7) + `BillControllerTest` (3) green and `npm run build` (bill-ci*/bill-drop tokens emitted).

36. **Remaining mockups implemented: list-page redesign + create-company polish (Aug 7, 2026)** â€” `docs/list-page.mockup.html` ("Customers â€” Redesigned") and `docs/bill-view-edit.mockup.html` (content is actually the "Create Company â€” Redesigned" SA form; the bill view/edit redesign was already delivered in phase 35). Styling/layout only â€” no route/controller/query changes.
- **CSS (`resources/css/app.css`)**: `.list-filter-bar` â†’ toolbar card (`padding:16px 20px`, `radius:20px`, white `.66` + `shadow-card` + backdrop blur); `.list-filter-search` max-width 320â†’420px; `.list-filter-select` â†’ boxed 40px `rounded-12` w/ custom chevron data-URI + gold focus ring; new `.list-filter-count` pill (margin-left auto, `rounded-full`, navy text); `.list-table-wrap` radius 24px + blur/shadow; `.list-table` min-width 880px, th/td padding 13px 18px, row hover `rgba(36,56,79,.035)`; new `.list-pagination*` footer (info text + 30px `rounded-9` bordered nav buttons, `.is-current` navyâ†’gold gradient with gold inset top hairline, `.is-disabled`); `.list-sidebar-link.is-active` navy gradient + 4px gold left bar `::before` + white-tinted icon square (#e2c069 icon); `.list-sidebar-icon-square` border + transition; new `.sa-link` (12px/600 gold link).
- **Components**: `x-list-filter-bar` gained `countText` prop â†’ renders `<span class="list-filter-count">{{ $countText }}</span>` inside the form (no extra wrapper/div); `x-list-quick-links` now computes `$currentFullUrl = rtrim(request()->fullUrl(), '/')` and marks the matching link `is-active` + `aria-current="page"` (both desktop rail and mobile dropdown â€” the mobile dropdown and desktop rail use separate markup, so both needed the same class logic); NEW `x-list-pagination` (`components/list-pagination.blade.php`, props `paginator` + `label`): "Showing Xâ€“Y of Z {label}" info line, â€¹ â€º prev/next, Â±2 page window, first/last with ellipses, current page as `.is-current` span, `appends(request()->query())` inside `hasPages()`.
- **Pages (7)**: customers/vendors/products/employees/invoices/bills/sales-receipts `index.blade.php` each gained a header `description` (passed to `x-list-header`), `countText="... total ..."` on `x-list-filter-bar`, and both desktop + mobile `->links()` blocks swapped â†’ `<x-list-pagination :paginator="..." label="..." />`. Customers additionally got a "Since {{ $customer->created_at?->format('M Y') }}" secondary line under the name cell. Sales-receipts create action label is "Create Sales Receipt" (not "Create Receipt").
- **Create-company polish** (`superadmin/companies/create.blade.php`): DB-preview chip now falls back to `acct_â€¢â€¢â€¢â€¢_pending` before the Alpine fetch resolves; "Branch Limit override" text â†’ gold `.sa-link` to the companies index.
- **New regression test** `tests/Feature/Accounting/ListPageRenderTest.php` (3 tests): customers index renders the mockup markup (description, count chip, "Since" line, `aria-current="page"` sidebar active state), pagination footer renders the info line + `.is-current` button + Next aria-label, and all 7 list routes return 200. NOTE: assertSee HTML-escapes by default â€” attribute assertions need `assertSee(..., false)`; required fixture fields for these list pages: Product `sales_price`+`income_account_id`+`expense_account_id`, Account `sub_type`, Employee `hire_date`, Invoice/Bill/SalesReceipt `created_by`.
- **Verification**: `php artisan view:cache` clean; `npm run build` emits the new tokens (list-pagination-btn, list-filter-count, etc.); `ScopedSearchRenderSmokeTest`, `SuperAdminPanelTest`+`ProductSearchTest` (43) and `ListPageRenderTest` (3) all green.

37. **Create/Edit Quotation redesign (Aug 7, 2026)** â€” both screens rebuilt on a shared `resources/views/accounting/quotations/_form.blade.php` partial driven by props (`isEdit`, `quotation`, `title`, `subtitle`, `submitLabel`) per `docs/create-quotation.mockup.html`; `create.blade.php`/`edit.blade.php` are thin `<x-app-layout>` shells. Routes/validation/search/persistence unchanged.
- **Mockup layout**: sticky page head (`position:sticky; top:var(--topbar-h)` backdrop-blur header with Cancel / Save Draft / Save & New (create only) / Save / gold **Submit for Approval** buttons posting `name="action"`); 2-column shell `lg:grid-cols-[1fr_340px]`; navy icon-chip section leaders (customer info / quotation info / line items / notes / attachments); line-items table with sticky navy-gradient thead (`table-fixed`: Item Code, Item Name scoped-search, Description, Qty, Unit Price, Disc %, Amount, row actions) using the bill/invoices JS pattern (`scopedSearchFieldHtml` product pickers, `item-selected` autofill of SKU/description/sales price/tax rate/income account, hidden `tax_rate`/`discount`/`income_account_id`, Add Line / duplicate (â§‰) / delete, never leaves the table empty, `quotUpdateTotals` recomputes on submit too); right rail sticky **Live Document Preview** (`.quot-prev`/`.quot-paper`/`.quot-p-head`/`.quot-p-grid`/`.quot-p-lines`/`.quot-p-totals`/`.quot-p-gt`/`.quot-p-foot` CSS; ids `p-cust`/`p-contact`/`p-date`/`p-valid`/`p-lines`/`v-sub`/`v-disc`/`v-tax`/`v-gt`/`p-foot`; discount/tax rows shown only when non-zero; Grand Total prefix with `$cs`). Customer picker uses `on-select="quotCustomerSelected"` â†’ fills read-only Contact (`display_name`)/Email/Phone/Payment Terms inputs + preview (`quotSync`); cost centre is `x-scoped-search-field` (entity `cost-center`); Branch native select; Date (`quotation_date`)/Valid Until (`valid_until`, validated `after:quotation_date`)/Reference; `Quotation â„–` read-only auto-assigned; Department & Project are **display-only** (no columns â€” audit gap, NOT migrated). Currency is a native select iterating central `Currency::query()->active()->ordered()`, defaulting MWK when present.
- **Field audit vs mockup (A6, gaps â€” no migrations)**: Department, Project, Internal Notes are display-only (the single `memo` column is printed on the PDF â†’ maps to Customer Notes). Everything else persists to existing columns.
- **Controller** (`app/Http/Controllers/Accounting/QuotationController.php`): `create()`/`edit()` pass `currencies`, `defaultIncomeAccountId` (first active revenue account); `edit()` loads `lines` + `attachments`; `show()` eager-loads `attachments`; `store()`/`update()` validate `currency` (string max:10) + `files` (array max:10, mimes pdf/jpg/jpeg/png/gif/webp/xls/xlsx/doc/docx/txt/csv, max:10240) + `delete_documents` (array of integer), and handle `action` buttons via `handlePostSaveAction()` (`submit_for_approval` â†’ `QuotationService::send()` â†’ status `sent`) and `save_and_new` (â†’ redirect back to create with success); private `handleAttachments()` stores on `Storage::disk('public')` at `quotation-attachments/{companyId}/{quotationId}/{random}.{ext}` (deletes scoped to the quotation's own attachments â€” forged cross-company ids are no-ops).
- **Latent bugs fixed**: (1) `QuotationService::update()` dropped `currency` (never persisted on edit) â€” added to the update array. (2) `SearchCatalog` customer map lacked `email`/`phone`/`display_name`/`payment_terms` â†’ added so the customer picker can autofill the contact fields (additive, no consumer breakage). `Quotation` model gained `attachments(): MorphMany` (reuses pre-existing morph `attachments` table).
- **Show page**: new Attachments card (public-disk download links, `format_bytes()` size) rendered when `attachments` is non-empty.
- **JS gotchas**: line-row pickers are built with `scopedSearchFieldHtml({name, entity, searchUrl, value, label, placeholder})` and are auto-initialized by Alpine v3's built-in MutationObserver (same proven invoices/bills pattern â€” do NOT add a manual `Alpine.initTree`); the `item-selected` listener lives on `#quot-lines-body` and matches rows via `e.target.closest('tr.quot-row')` + `[name*="[product_id]"]`; `quotRowData` reads the picker label from `.scoped-search-field input`.
- **Tests**: new `tests/Feature/Accounting/QuotationFormTest.php` (12 tests / 62 assertions) â€” create/edit render mockup markup, store persists currency/reference/memo/lines with exact flat-discount + tax math (amount 290.00 / tax 47.85 / total 337.85 for 2Ã—150 âˆ’ 10 at 16.5%), submit-for-approval â†’ sent, save-and-new redirect, missing-income-account validation, attachment upload/delete/oversize, update persists currency + recalculates, non-draft update redirects, show renders attachments card. NOTE: service-level tests must include `company_id` in the payload (HTTP store overrides it from the session); direct `QuotationService::create()` needs a seeded active `numbering_sequences` row for `quotation` (getNextNumber throws otherwise). Verified: `view:cache` clean, `npm run build` emits `.quot-*` + `bill-ci-num` tokens, `QuotationFormTest` 12, `SalesModuleTest` quotation lifecycle 4, `BranchRequestTest` 3, `BillControllerTest` 6, `ScopedSearchRenderSmokeTest`, `GlobalSearchTest` 15 + `InvoiceServiceTest` 11 all green.

### Quotation/Bill form layout fixes (Aug 7, 2026 follow-up) â€” three CSS/stacking bugs reported on the create-quotation page, applied to bills too:
- **Sticky page head** â€” the head (`flex items-start justify-between gap-4 flex-wrap pb-4 mb-6 border-b border-line`) had inline `z-index: 40`, HIGHER than the topbar's `.topbar{z-index:30}`, so it painted over the row-2 nav links and its `top: var(--topbar-h)` (106px) overlapped the topbar's 1px bottom border. New shared `.form-page-head` class (`position:sticky; top:var(--topbar-h,106px); z-index:20; background:rgba(245,247,252,.9); backdrop-filter:blur(12px)` â€” z-20 is BELOW topbar z-30 so the nav row always wins). Applied to quotations `_form` (inline style removed) and bills `_form` (head wasn't sticky at all â€” now is).
- **Dropdown hidden behind line-items card** â€” TWO compounding causes: (1) the table wrapper `mt-4 border border-shell rounded-[14px] overflow-hidden bg-[#fbfcfe]` clipped the absolutely-positioned `.scoped-search-dropdown`; (2) `.card` has `backdrop-filter:blur(14px)` â†’ it is a stacking context, so even un-clipped the dropdown (inside the card) painted BELOW the next sibling card in DOM order. Fixed by: wrapper `overflow-hidden` â†’ `overflow-visible round-thead-clip` (new `.round-thead-clip` CSS rounds the thead's first/last `th` corners to 13px so the navy gradient keeps rounded corners without the wrapper clip), and the line-items `<section class="card ...">` gained `relative z-30` so the whole card â€” carrying the dropdown â€” paints above subsequent cards. Also raised `.scoped-search-dropdown` z-index 30 â†’ 60. VERIFIED via headless Chrome `elementFromPoint` repro (dropdown hit-testable above the next card; head sticks at top 106 with z 20 < topbar z 30).
- **Dashed dropzone broken/left-aligned** â€” `.bill-drop` is used on a `<label>` (inline by default) but the CSS had no `display`/`width`, so the dashed box collapsed to content width and sat at the left. Added `display:block; width:100%` to `.bill-drop` (fixes both bills + quotations dropzones globally).
- **Legacy line-item forms (invoices/expenses/PRs/budgets/etc.) NOT migrated**: their table wrapper is `overflow-x-auto` (load-bearing horizontal scroll on wide tables â€” `overflow-x:auto` forces `overflow-y:auto`, so last-row dropdowns still clip there) and they use the older non-sticky head. Left as-is deliberately; the fix targets the redesigned `_form` pages.
- Verified: `npm run build` clean (`.form-page-head`, `.round-thead-clip`, `z-index:60`, `display:block;width:100%` present in `app-Cc34Ey38.css`), `view:cache` clean, `QuotationFormTest`+`BillControllerTest` 18 passed (93 assertions), `ScopedSearchRenderSmokeTest` passes.

38. **Auth suite redesign â€” executive teal split-panel (Aug 8, 2026)** â€” all four guest auth pages rebuilt on a shared `AuthLayout` per `docs/auth-suite.mockup.html`. Styling/layout/UX only; guards, middleware, CSRF, rate limits, expiry, and the code-based reset flow (phases 29â€“30) are untouched.
- **Layout** (`app/View/Components/AuthLayout.php`, `resources/views/layouts/auth.blade.php`): prop `wide` â†’ `centered` (bool; adds `.auth-form-card--center`); `backHref`/`title` unchanged. `body class="auth-login"`, `.auth-split` grid `0.9fr 1.1fr` (rounded 24, `calc(100vh - pad*2)`, min-height 620), left `.auth-brand` (48px CB logo tile `#f4fbfbâ†’#d9edee` glyph `#0e3a3e`, serif H1 "Financial operations, unified.", lede, `.auth-glass` stat card â€” Month-end close 3.2 days/âˆ’38% faster, Auto-reconciled 96.4%, Audit readiness 100% with mint chips â€” Â© 2026), right `.auth-form-col` (`.auth-help` "Need help? Contact support", `.auth-form-card` max-width 480) with conditional `$backHref` â†’ `.auth-backlink` "Back to sign in" (+arrow SVG) and `$footnote` named slot â†’ `.auth-admin`. Inter weight 800 added to fonts link.
- **Pages**: `login` â€” footnote "Don't have access? Contact your administrator."; password field now a plain input in `.password-input-wrap` with `x-data="{ showLoginPassword:false }"` + `auth.partials.password-toggle` (no longer uses `x-text-input`'s built-in toggle); "Protected by 256-bit encryption" shield footnote. `forgot-password` â€” halo mail tile, "Reset access"/"Forgot your password?"/"Send reset link"/"Sendingâ€¦", footnote "Links expire after 30 minutes for your security."; on success JS navigates via `data.redirect`. `verify-code` â€” `centered`, halo shield, "Verification"/"Enter verification code", masked-email subtext, 6 `.auth-login-otp-box` (50Ã—56 r10 20px 800), "Verify & continue"/"Verifyingâ€¦" (`:disabled="submitting || !complete || expired"`), live resend row ("Didn't receive it? â€¦ in 0:42", `<b x-text="resendLabel">` tabular-nums, `.auth-login-resend-timer--expired`), footnote "Codes expire after 5 minutes.". `reset-password` â€” gained `back-href`, halo lock, "Secure reset"/"Set a new password"/"Set new password", footnote "You'll be signed out of all other devices after reset."; keeps hidden token/email + `auth.partials.new-password-fields` include.
- **CSS** (`resources/css/app.css`, AUTH SUITE block ~3223â€“3879, old `.auth-login-*` block removed): tokens `--deep-1 #17565D`, `--sec #128F8E`, `--mint #7FD1C0`, `--ink #0B2A2D`, `--border #DCEAEA`, `--muted #5F7476`, `--faint #8AA5A7`, `--link #0C6B6A`, `--focus #94A3B8`, `--auth-error #DC2626`; boxed 44px r8 inputs (`#dceaea` border), teal focus ring `rgba(18,143,142,.16)`; CTA deep-teal gradient r12 h44 `var(--cta-btn-shadow)` text `#EAFFFF`; halo tiles 52px r16; shared (out-of-scope, var() fallbacks) `.password-input-wrap/-boxed/-toggle`, `.auth-login-password-checklist*`, `.password-form-submit`. Responsive: â‰¤960px single column + hide `.auth-glass`; max-height 820px hide glass + H1 30px; â‰¤420px OTP 46Ã—52/1.125rem. Old classes fully removed (grep-verified): `auth-login-hero/panel/card/trust/expiry/recovery/back-link/header`.
- **JS**: `verify-code.js` â€” added `resendLabel` + `resendRemainingLabel()` (M:SS), `maybeAutoSubmit()` (submit on 6th digit / after paste), cooldown sets + 1s ticker refreshes label. `forgot-password.js` â€” follows `data.redirect` on ok; removed `sent` swap state; 422 shows `data.errors.email[0]` inline.
- **Â§5 enumeration trade-off (allowed backend touch)**: `PasswordResetLinkController::store()` now distinguishes unknown email â€” JSON â†’ `ValidationException::withMessages(['email' => [__("We can't find an account with that email address.")]])` (422 inline); normal â†’ `back()->withErrors([...])`. Known user unchanged (session + `PasswordResetCodeService::issue()` + queued mail + redirect/JSON `{redirect}`). Route keeps `throttle:5,1`. Docblock records the intentional trade-off (enumeration via inline error, throttled).
- **Â§6 OTP auto-submit**: frontend-only â€” reuses the existing `password.verify-code.submit` endpoint (`verify()` already returned JSON `{redirect}` for `wantsJson`); no route/controller change.
- **Tests**: `PasswordResetTest` â€” forgot copy asserts, known-email `assertRedirect(route('password.verify-code'))`, unknown-email split into inline-error + JSON-422 tests, verify-page markup (masked email, "Digit N of 6", OTP, resend), reset-page checklist + back link. `AuthenticationTest` â€” dropped removed `auth-login-hero/panel`, asserts `auth-brand`/`auth-form-col` instead. Gotcha: apostrophes render HTML-escaped (`Didn&#039;t`, `You&#039;ll`) â€” assert with default `$escaped=true` (omit the `false` arg), not raw.
- **Verification**: `view:cache` clean; `npm run build` emits all new tokens; Auth folder + `CompanyAccessFlowTest` + `SecurityHardeningTest` green (48 passed / 230 assertions). Full E2E in headless Chrome via `php artisan serve` (mailer=log): forgot â†’ code QP-decoded from laravel.log (anchor `letter-spacing:12px[^>]*>(\d{6})<`) â†’ OTP auto-submit â†’ reset â†’ login â†’ profile. Geometry probe: split at x=650 (628/768), card centered in column, 410Ã—44 CTA, toggle at field right edge, no horizontal scroll. Screenshots in `C:\Users\eseyama\AppData\Local\Temp\opencode\auth-shots\` (8 PNGs; note: this model can't view images, geometry probe used instead). NOTE: E2E changed the dev `accountant@test.com` password to `New-pass1`.

39. **Dialog System â€” Executive Teal (Aug 8, 2026)** â€” replaced the legacy `.fb-*` modal/toast layer and `window.feedback` API with a new CB dialog system per `docs/dialog-system.mockup.html`. Frontend-only; server flows, promises, forms, and CSRF unchanged. Full detail in `docs/dialog-system-report.md`.
- **API** `resources/js/feedback.js` (rewritten): `window.CB.{confirm,dialog,prompt,modal.open/close,toast,busy,busyStop}`. `confirm({type:'action'|'danger',â€¦})` -> `Promise<boolean>`; `dialog({type:'success'|'warning'|'info',â€¦})`; `prompt({â€¦})` -> `Promise<string|null>`; `toast(type,title,msg?,opts?)` (success|error|warning|info|system; error persists, others auto-dismiss ok/inf 4sÂ·wrn 6s, pause-on-hover, action button, max 4). Legacy bridges all preserved and delegating to CB: `window.feedback.{toast,openConfirm,openPrompt,alert,confirm,prompt,initFlashes}`, `fbConfirmSubmit/Button/PromptForm/ConfirmOnly(event,msg,opts)`, `atlasToast`. `opts.type` selects the accent (danger -> red OK, action -> gold OK).
- **Styling** (app.css `.cb-*` block replacing old `.fb-*` dialog/toast CSS): teal scrim `#11454B->#0C3539->#0A2E32` + radial glows + blur(6px); card `min(430px,100vw-32px)` r20 scale .96->1 180ms (`.wide` 520px), halo icon tiles, fact strip (mono chip + context), summary rows, type-to-confirm gate, focus trap + Esc (ignored while processing), `role="alertdialog"` + `aria-labelledby`; toasts r16 white .92 blur14 with 4px accent bar + 30px icon tile, viewport `right:20; top:calc(var(--nav-h)+12)` (full-bleed 12px gutters <=520px); busy overlay with 36px spinner; `prefers-reduced-motion` disables animations. New `:root` tokens `--red-2`, `--warn-2`, `--shadow-dlg`. `.fb-alert`/`.fb-banner` re-tokenized in place (no markup changes).
- **z-index map**: topbar `.topbar` z 30 -> **60** (was <= toast 95); `.form-page-head` sticky z 20 -> **40**; scrim 80; dialogs 85; toasts 95. Toasts pin below the topbar (never hidden behind it or the sticky heads).
- **Call-site migration (67 replacements / 43 files)**: every `fbConfirmSubmit`/`fbConfirmButton`/`openConfirm`/`openPrompt`/`feedback.alert` call got an explicit `{ type: 'danger'|'action' }` (destructive voids/cancels/deletes/reverses/deactivations = danger; post/approve/lock/archive = action) with identical messages; `openPrompt` (expenses withdrawal reason) -> `CB.prompt`; 12 JS `alert()` -> `CB.toast('error',â€¦)`. `window.feedback.toast` retained by the layout flash forwarder + favourites.js Undo. Unmigrated by design (unchanged from feedback phase): POS checkout dynamic classes, `pos/cashier/login` standalone page, `admin/system-health` diagnostics list.
- **Bug fixed during E2E**: `mountDialog()` guarded on ANY `.cb-dialog`, but the busy overlay renders a `.cb-dialog--processing` node that `busyStop()` removes after a 180ms animation â€” opening a dialog right after `busyStop()` silently failed. Guard now purges `.cb-scrim--leaving` wrappers first and ignores `.cb-dialog--processing`.
- **Verification**: `view:clear` + `npm run build` (no stale `.fb-*` dialog/toast classes in compiled CSS; `.topbar` z 60 / `.form-page-head` z 40 confirmed) + `view:cache` clean. Tests: Auth 21, ScopedSearchRenderSmokeTest 1, TodoTask+SuperAdminPanel 49, Budget/Cheque/Expense/PeriodLocking/Reconciliation 33, BranchRequest+TodoTask 30 â€” green. Headless Chrome (1440/1280/1024/768/375): danger confirm w/ type-to-confirm + Enter, Esc-cancel w/ initial focus on cancel, action confirm focus trap, info dialog Esc-close, prompt value + cancel, busy + busyStop, processing confirm->busy hand-off, inline `fbConfirmButton(event,msg,{type:'danger'})` legacy proof, toast geometry (right 20, top 118, w 360, z 95) + topbar z 60, no JS errors. The `/favourites` 401 poll and accountant dev-user 403s on `/todo`/`/accounting/periods` are pre-existing permission gaps (unrelated).

40. **Favourites rail matched to row-2 header (Aug 8, 2026)** â€” the LEFT pinnable favourites rail (`.fav-sidebar`, `resources/views/components/favourites/sidebar.blade.php`) background retuned from dark navy gradient to the row-2 nav header colour `#DCE6E7` with a `rgba(255,255,255,.55)` right hairline, and all light-on-dark text re-tuned to the row-2 nav language: `.fav-item` `#BFD9DA` â†’ `#0B2A2D` (hover bg `#11454B` â†’ `rgba(11,42,45,.08)`), `.fav-label`/`.fav-unpin-btn`/`.fav-collapse-btn` `rgba(226,244,244,*)` â†’ `#5F7476` (hover `#0B2A2D`), `.fav-divider` `rgba(255,255,255,.1)` â†’ `rgba(11,42,45,.10)`, `.fav-item.current` gold â†’ teal gradient `linear-gradient(90deg,#149897,#128F8E)` + `#FFFFFF` (mirrors `.topbar-nav-link.active`). `.fav-sidebar-toggle .favourite-toggle` (the "add to favourites" star at the rail bottom) was invisible on the light rail (still dark-theme `rgba(226,244,244,*)`) â€” restyled to the row-2 language: `#5F7476`/`rgba(11,42,45,.10)` border, hover `rgba(11,42,45,.08)`/`#0B2A2D`, active white-on-teal-gradient (the `.fav-float-toggle` overrides are untouched). No markup/JS changes; `favourites.js` store untouched. NOTE: this was originally mis-applied to the RIGHT quick-links rail (`.list-sidebar-*`, used by list/form/detail pages) â€” that change was fully reverted (verified `git diff` shows zero `.list-sidebar-*` changes; compiled CSS back to glass `rgba(255,255,255,.66)` + navy-gradient active). Verification: `npm run build` clean; headless Chrome (admin â†’ company A â†’ `/accounting/customers`, rail pinned via `Alpine.store('favourites').pinned=true`): `.fav-sidebar` bg `rgb(220,230,231)` == `.topbar-row-2`, labels `#5F7476`, items `#0B2A2D`, pinned width 64px; star toggle visible at rail bottom (28Ã—28, `#5F7476`/`rgba(11,42,45,.10)` border) and toggles to active white-on-teal; `.list-sidebar-desktop` on same page reverted to `rgba(255,255,255,.66)`/`#DCEAEA` with navy-gradient `.is-active`.

41. **Customer + Invoice pages redesign (Aug 8, 2026)** â€” self-contained restyle of the 8 pages in `accounting/customers/{index,create,edit,show}` and `accounting/invoices/{index,create,edit,show}` per the inline mockup spec (list-page, create-form, show/detail, copy-from-quote). Styling/markup only â€” all routes, controllers, validation, posting, PDF, and search behaviour preserved. Built on the shared `x-list-header`/`x-list-filter-bar`/`x-list-pagination`/`x-record-toolbar`/`x-detail-field` components + new page-local CSS in `resources/css/app.css` (`.x-port` KPI stats, `.x-wset-custlist`, `.x-tb` toolbar, `.x-wset-create` line-item table, `.x-wset-view`, `.x-totals` rail, `.cq-*` copy-from-quote modal block). New shared partials: `customers/_form.blade.php`, `invoices/_form.blade.php` (12-field customer card + line-items table driven by the invoices/purchase-orders JS pattern; `scopedSearchFieldHtml` product/account/cost-centre pickers, `item-selected` autofill, hidden `tax_rate`/`discount`, `Add Line`/duplicate/delete), and `components/copy-quote-picker.blade.php` (Copy-from-Quote modal; `data-copy-quote-url` = `route('accounting.invoices.copy-quote')` ending `?quotation=id`, matches backend `quotation` required|integer). Routes: `GET invoices/copy-from-quote` added BEFORE `invoices/{invoice}` (web.php:444). Controller: `InvoiceController::create` accepts `customer_id` prefill (skipped when `copy_quote` present); `_form.blade.php` customer picker defaults to `old('customer_id', $copyQuote['customer_id'] ?? $preselectCustomer['id'] ?? $invoice?->customer_id ?? '')`; customers.show's New Invoice button links `invoices/create?customer_id={id}`. Tests: `ScopedSearchRenderSmokeTest` extended to `customers.index`/`invoices.{create,edit,show}` + copy-quote routes; `ListPageRenderTest` (3), `QuotationFormTest` (12), `InvoiceServiceTest` (11) green; `php artisan view:cache` clean; `npm run build` emits `app-CMYrbNQh.js`/`app-CgugeS4C.css`. Headless Chrome verified (1440px, `probe@camelot.test` company_admin on tenant 1): customers.index shows the stats row + styled table + terms filter, customers.show/edit render toolbar/portfolio/recent-table + prefilled edit form, invoices.create renders the 10-column line-items table + `#inv-total` + Copy-from-Quote with `window.CopyQuote` loaded.
- **PRE-EXISTING BUG found during headless verification (NOT introduced by this redesign)**: implicit route model binding on tenant routes resolves on the CENTRAL `mysql` connection, not the `tenant` connection. `Illuminate\Routing\Middleware\SubstituteBindings` lives in the `web` group, which runs BEFORE the route-level `tenant.bind` middleware, so `Customer::findOrFail(6)`/`Invoice::findOrFail(4)` issue `select * from customers where id = ?` on `camelot_books` (the legacy shared DB, which still contains the pre-Phase-2 rows). Records that exist in BOTH the legacy central DB and the tenant DB render fine; records created ONLY in the tenant (everything created after Phase 2 migration) 404 on `{entity}/show`/`{entity}/edit`. Proof: SQL log shows `[mysql] select * from customers where id = "5"` and `id = "6"` for the binding of both IDs while the controller's own queries run `[tenant]`; the route-binding pipeline reproduced in isolation (`Router::match` + `substituteImplicitBindings` with the tenant resolved) binds BOTH correctly â€” the divergence only appears when the full web middleware stack runs. Masked in the test suite by `TENANT_ROUTING_OVERRIDE=sqlite` (all tenant models share one DB with the central schema, so binding and controllers agree). Affects ALL tenant entities, not just customers/invoices. Fix direction (NOT applied): force `tenant.bind` to run before `SubstituteBindings` (e.g. add both to `Kernel::$middlewarePriority`, or make the binding query honour `TenantScoped::getConnectionName()`).

42. **Quotations suite redesign: list/create/edit/show + PDF template (Aug 8, 2026)** â€” self-contained restyle of all 5 quotation screens (`accounting/quotations/{index,create,edit,show,print}`) per the inline Â§0â€“Â§13 spec. Styling/markup only; no routes, permissions, validation, search, send/accept/decline/convert/void, or PDF-pipeline changes. Built on the shared `x-list-header`/`x-scoped-search-field`/`x-review.*` components + a new `.q-*` block in `resources/css/app.css` (QUOTATIONS SUITE, ~5750â€“6192).
- **Tooling fix (critical)**: `php -S 127.0.0.1:8010 -t public public/index.php` does NOT serve static files â€” when a router script is given, EVERY request (even existing `.css/.js/.json/.txt` under the docroot) runs through `public/index.php`, so unauthenticated asset requests 302 to `/dashboard` and the browser gets an HTML page for its stylesheet â†’ `document.styleSheets[].cssRules.length === 0`, topbar computed `position: static`, ENTIRE app unstyled. Headless probes that only assert text/class presence cannot catch this (classes exist in markup regardless of CSS). Fix: created the standard Laravel router `server.php` at the repo root (returns `false` for existing files so the built-in server serves them statically; otherwise requires `public/index.php`) and now run `php -S 127.0.0.1:8010 -t public server.php`. Verify with: `curl -s -o NUL -w "%{http_code} %{size_download}" http://127.0.0.1:8010/build/assets/app-DQ34BYXt.css` â†’ `200` with the full byte count (a 302 + ~7KB gzip body was the broken signature).
- **Index** (`index.blade.php`): `x-list-header title="Quotations"` + description + Create Quotation; `.q-stats` strip of 7 `.q-stat` cards (Total head + Draft/Sent/Accepted/Declined/Converted/Void) fed by NEW `$stats`/`$statsTotal` in `QuotationController@index` (`groupBy('status')` `selectRaw('status, count(*) as count, sum(total) as total')`); `.q-filters` card (scoped-search + status select + Filter/Clear); `.q-card q-card--list` with `.q-table` (quotation #, customer, date, valid-until, total, status badge, view/edit/send/void icon actions) in `overflow-x-auto`; `.q-pag` pager (`is-current`/`is-disabled`). Old table/status markup and `pagination` partial dropped (inline pager now).
- **Form** (`_form.blade.php`, shared create/edit): `.q-head q-head--sticky` (sticky under `--nav-h`, z-40) with title + Cancel / Save Draft / Save & New (create only) / Save / gold **Submit for Approval** buttons posting `name="action"`; `.q-page q-page--form` 2-col grid `minmax(0,1fr) 21.25rem`; navy icon-chip `.q-card-sec` section leaders (Customer Info / Quotation Info / Line Items / Notes / Attachments); `.q-input` 2.75rem boxed controls; line-items `.q-table` (`#quot-lines-table`, sticky deep thead) using the invoices/bills JS pattern (`scopedSearchFieldHtml` product pickers, `item-selected` autofill, hidden `tax_rate`/`discount`/`income_account_id`, `#quot-add-line`, duplicate/delete); right rail `.quot-prev q-page--rail` live preview (`#p-lines`/`#v-gt` etc.); `#quot-dropzone`/`#quot-files`/`#quot-new-files`/`#quot-existing-files` attachments. **EVERY `quot-*`/`bill-*` id, name, data-attribute and JS hook preserved** (`quotAddLine/quotRemoveLine/quotDuplicateRow/quotUpdateTotals/quotRowData/quotRemoveExistingFile/quotRemoveNewFile/quotCustomerSelected/quotSync` untouched). `valid_until` field preserved.
- **Show** (`show.blade.php`): `.q-head q-head--sticky` + `q-badge-title` (title + status `.q-badge`) + `.q-toolbar` actions (Edit / Email / Mark as Sent / Convert to Invoice / Void `fbPromptForm` / Print / Back); `.q-view` shell (`minmax(0,1fr) 21.25rem`); detail `.q-field` grid; Review & Decide card (status `sent`) via `x-review.btn` forms; `x-review.outcome` strips for accepted/declined/converted; line-items `.q-card q-card--list` + `.q-totals-box` (Subtotal/Tax/Total); attachments card with public-disk links; right `.q-view--rail` `.q-rail-card` (Print/PDF, Edit, Back, Quotation Status Report link). Existing send/accept/decline/convert/void POST forms + `quotations.convert` permission + `@can` guards preserved.
- **Print/PDF** (`print.blade.php`): REPLACED legacy Arial inline-CSS page with a self-contained Â§10-style A4 stationery template (`@page { size: A4 portrait; margin: 0 }`, header band `#F0F5F5`, CB logo block, "QUOTATION" + number, Prepared For box + meta grid, ledger table, Subtotal/Tax/Grand Total, notes + terms, two signature blocks, teal footer, `print-color-adjust:exact`, `onload="window.print()"`). Removed the `\App\Helpers\MoneyHelper::toWords` call (helper does not exist). This is served by `QuotationController@print` (`quotations.print` route), NOT the PdfController pipeline â€” that pipeline (`PdfPresenter::quotation()`, pdf.document) is untouched.
- **CSS tokens**: `:root` += `--acc:#128F8E; --acc-2:#149897; --headbg:#11454B; --mint:#7FD1C0; --rcol:300px; --shadow-dropdown; --shadow-paper`. New `.q-*` classes (~630 lines): shells/grids/rail, sticky head, 4 button variants, seg pill, 5 status badges + dots, mono chips, section cards/tiles/rules, 4-col field grid, 2.75rem inputs + chevron + dashed textarea, deep-thead table (10.5px uppercase, `.q-amt`, `.q-ibtn` + `--del`), totals box, rail cards, pipeline stats, pager, filters; responsive collapse at 1180px (single column + rail static) and 720px (grids/1-col).
- **Tests**: `ScopedSearchRenderSmokeTest` now covers `quotations.index/create/edit/show/print` â€” **edit uses a DRAFT fixture** (`STATUS_DRAFT`) because `QuotationController@edit` redirects non-drafts (302 breaks a 200 assertion); the sent fixture (`STATUS_SENT`) covers show. Regression suites green: QuotationFormTest 12 + ListPageRenderTest 3 + ScopedSearchRenderSmokeTest 1 (16 / 88), SalesModuleTest + InvoiceServiceTest + GlobalSearchTest + BillControllerTest 39 / 153. `view:cache` clean; `npm run build` emits all `.q-*` tokens (`app-DQ34BYXt.css` 252.78 kB / `app-CMYrbNQh.js` 303.69 kB).
- **Headless verification (1440/1280/1024/768/375)**: `index` â€” title/stats/filters/table/badges/create all present, page content zero h-scroll; `create` â€” `.q-page--form` computed `display:grid; grid-template-columns:980px 340px`, `.q-head--sticky` `sticky/106px/z40`, 5 section cards, scoped customer field, 1 empty line row, live-preview ids, all 4 buttons, dropzone. Only page-level h-scroll is the PRE-EXISTING topbar row-2 nav (`BUTTON.topbar-overflow-btn` right=1439; see #41). Live `show` untestable due to the #41 route-binding bug (ACME tenant has 0 quotations); covered by the smoke test. NOTE: the CSS-styling probe failure mode described in the Tooling fix above means all earlier "verified" headless probes (customers/invoices phase, etc.) likely asserted text-only â€” re-check computed styles before trusting a styled render.

43. **Quotations suite â€” form finalization, draft-delete, export, reports + bug fixes (Aug 8, 2026)** â€” follow-up to #42 closing the remaining scope. Phase report: `docs/quotations-suite-report.md`.
- **`_form.blade.php` finalized** (rewritten this phase): sticky `.q-head--sticky` with `.q-chip` mono nÂº + `.q-badge`; create actions = Cancel / Save & New (`save_and_new`) / seg[Save Draft (`save_draft`), Save (`save`), **Submit for Approval** (`submit_for_approval`, teal cta)]; edit = Cancel / **Delete** (danger-o) / seg[Save Changes (`save`), **Save & Email** (`save_and_email`, cta)]. Sub: edit = "{Customer} Â· quoted {date} Â· valid until {date}", create = "Quote products & services with a live summary." `.q-shell` grid `minmax(0,1fr) 18.75rem` rail; sections (a) Customer (scoped picker + `#quot-contact/email/phone/terms` readonly), (b) Quotation Info (â„– "Auto-assigned on save" readonly, branch, cost-centre scoped, Department/Prepared By/Project display-only), (c) Line Items (`#quot-lines-table` + `#quot-add-line`, `quotLineRow()` via runtime `scopedSearchFieldHtml({...})`, hidden `tax_rate`/`discount`/`income_account_id`, duplicate/delete, never-empty), (d) Notes (Customer Notes `memo` + Internal display-only), (e) Attachments (`#quot-dropzone` drag/drop `.bill-drop--drag`, `#quot-files` `files[]`, `#quot-existing-files` + `delete_documents[]`, `#quot-new-files`). Rail group = **Summary** (create) / **Breakdown** (edit); `.q-srow-lite` `#p-cust/#p-contact/#p-date/#p-valid` + Subtotal `#v-sub` + hidden Discount `#r-disc/#v-disc` + hidden Tax `#r-tax/#v-tax` + `.q-strip` Grand Total `#v-gt` + preview `#p-lines`/`#p-foot`; Quick Nav (edit: Print/PDF, Email POST `quotations.email` only when customer has address, Attach File, Back; create: Quotations List, New Customer, Day Book). Form `id="quotation-form"` `.q-form` `enctype="multipart/form-data"` `data-customer-name`; head buttons use `form="quotation-form"`.
- **Draft deletion (new feature)**: `GET quotations/export` (CSV of the filtered list, same `baseQuery`, `streamDownload`), `DELETE quotations/{quotation}` â†’ `QuotationController::destroy` (`requirePermission('quotations.edit')`, cross-company 403, draft-only redirect + error on non-draft, deletes attachment disk files + rows) â†’ `QuotationService::destroy` (DB transaction, deletes lines then quotation). Route middleware `['permission:quotations.edit', 'sod:quotation']` â€” **SOD blocks the record creator** (403), so the form's Delete button AND `#quotation-delete-form` are gated to `created_by !== auth()->id()` (previously shown unconditionally = guaranteed 403 for creators).
- **Index upgraded**: `.q-fbox-grid` (7 clickable status fboxes fed by `$stats`/`$statsTotal` from one grouped `selectRaw('status, COUNT(*), SUM(total)')`), sort select, `#quot-list-form` hidden status/search/sort, `appends()` pager; rail Views (All/Open=status=open draft+sent/Accepted/Converted) + Reports links.
- **Sort/export via `orderByFor()`** â€” but see the BUG below: associative-array spread crashes.
- **Two new sales reports** wired into `ReportRegistry` (`quotation_register`, `sales_pipeline`) + routes `accounting.reports.quotation-register`/`-sales-pipeline`: `QuotationRegisterService`/`Controller` + `quotation-register.blade.php` (every quotation in range, status pills, grand total) and `SalesPipelineService`/`Controller` + `sales-pipeline.blade.php` (per-status funnel count/value/%, win-rate = accepted+converted Ã· decided, open-quote aging 0â€“7/8â€“30/31â€“60/61+ days with relative bars).
- **BUG FIXED â€” `orderBy(...$this->orderByFor($sort))` named-parameter crash**: spreading the associative sort array invokes `orderBy(quotation_date: 'desc', id: 'desc')` but `orderBy`'s first param is `$column` â†’ "Unknown named parameter $quotation_date" on EVERY `quotations.index` load (default sort) and `export`. Caught by `ScopedSearchRenderSmokeTest`. Fixed with a `foreach ($this->orderByFor($sort) as $column => $direction) { $q->orderBy($column, $direction); }` loop at both call sites (`QuotationController@index` + `@export`).
- **`QuotationController@store/update`** both handle `action=save_and_email` via `redirectAfterEmail()` (distinguishes no-customer-email warning from successful send); `handlePostSaveAction()` handles `submit_for_approval` â†’ `QuotationService::send()`.
- **Tests**: `QuotationFormTest` now **14 tests / 69 assertions** â€” the two render tests were updated because the old "Live Document Preview" card title is gone per Â§8.2 (create asserts `Summary`, edit asserts `Breakdown`); NEW `destroy` tests create via a SECOND user (creator passes SOD) â€” draft deleted + redirect to index, non-draft â†’ redirect to show + `error` flash + row kept. `ScopedSearchRenderSmokeTest` covers `quotations.index/create/edit/show/print` (edit uses a **draft** fixture, show uses sent). All green: QuotationFormTest 14, ScopedSearchRenderSmokeTest, SalesModuleTest 7, ListPageRenderTest 3. `view:cache` clean.
- **`server.php`** (from #42) confirmed as the required dev-server router for static assets (`php -S 127.0.0.1:8010 -t public server.php`); `php -S ... public/index.php` unstyled the whole app.
- **NOT done (deferred)**: Â§12 full headless matrix re-check (blocked by #41 route-binding bug + dev-server static file nuance); the mockup's "Department/Project/Internal Notes" remain display-only (audit gap â€” no columns).
- **STALE-CSS FIX (Aug 8, 2026 follow-up)**: the compiled bundle `app-DQ34BYXt.css` was stale â€” it lacked ALL phase-43 `q-*` classes (`q-fbox-grid`, `q-shell--main`, `q-srow-lite`, `q-strip`, `q-rail-sec`, `q-vitem`, `q-dropzone`, `q-pipe-row`, `q-istat`, â€¦) even though `resources/css/app.css` had them, so the quotations pages rendered unstyled live. Rebuilt via `& "C:\Program Files\nodejs\npm.cmd" run build` (bare `npm run build` is blocked by PowerShell ExecutionPolicy) â†’ new bundle `app-DiGIa-GM.css` (265.11 kB / 2561 rules); JS bundle `app-CMYrbNQh.js` unchanged (already current). Also added the 2 remaining classes the views reference but source CSS lacked: `.q-rail-memo` (rail memo paragraph, near `.q-srow-lite`) and `.q-row-link` (index quotation-# link: teal, `text-decoration:none`, hover underline, near `.q-row-name`). Verified live via CDP probe: index fboxes/sort/rail all styled, create `.q-shell` grid `1020px 300px` + sticky head + 5 section cards + dropzone dashed, both new reports render; only remaining `no h-scroll` flag is the pre-existing topbar overflow (see #41/#42) and `/favourites` 401. `view:cache` clean. **Lesson**: after any `app.css` phase, grep the compiled bundle for the new class names before declaring the phase verified â€” a stale `public/build` silently unstyles pages.
44. **Quotations suite redesign — DESIGN 2 (Aug 8, 2026)** â€” ported all 4 quotation screens (`accounting/quotations/{index,create,edit,show}`) plus the two report views (`reports/quotation-register`, `reports/sales-pipeline`) onto `docs/QUOTATIONS-all-pages DESIGN 2.html` (teal executive language). Styling/markup only; routes, permissions, validation, search, send/accept/decline/convert/void, PDF pipeline and the print template (already ported in #42) untouched.
- **Namespaced `.q2-*` CSS**: the mockupâ€™s bare `.btn/.input/.card/.badge/.toolbar` would have clobbered 79 global app.css rules (591â€“982), so EVERY class got a `.q2-` prefix (`.q2-btn`, `.q2-input`, `.q2-card`, `.q2-badge--*`, `.q2-toolbar`, â€¦). New `:root` tokens match the mockup palette (`--deep-1 #17565D, --deep-2 #0C3539, --deep-3 #0A2E32, --sec #128F8E, --sec-2 #149897, --mist-a/b, --line #E2ECEC, --border #DCEAEA, --card rgba(255,255,255,.75), --muted #5F7476, --faint #8AA5A7, --red-2 #B91C1C, --cta-btn-shadow, --teal-btn-shadow`). Block appended at end of `app.css` after `.q-row-total` (ÃŸ~6705â€“6966). Sections: primitives (head/title/sub/btn cta|sec|ghost|soft|danger + sm/xs/icon/block, `.q2-seg` segmented control, `.q2-badge--draft/sent/accepted/declined/converted/void` + `.q2-dot`, `.q2-ibtn`/`--del`, `.q2-field/.q2-label/.q2-input/.q2-select` + `--sm/--invalid`, `.q2-hint/.q2-error`, `.q2-card/--list`, `.q2-shell/--form`, `.q2-main/.q2-rail/.q2-railcard/.q2-rail-group/.q2-vitem(.is-active)`, `.q2-rule`), Â§1 list (`.q2-toolbar/.q2-search`, `.q2-fbox-grid` 7-col + `.q2-fbox` hover-lift/`is-active` gold? no â€” teal top-bar, `.q2-fbox-ic--ink/steel/teal/mint/red/gray`, `.q2-tbl` mist-gradient thead + `.q2-tbl-wrap` overflow-x-auto, `.q2-empty`, `.q2-pag` pager), Â§2/3 form (`.q2-sec/.q2-sec-head/.q2-sec-ic` navy+gold tile, `.q2-g4/g3/g2`, `.q2-drop` dropzone + `.q2-li*` file rows, `.q2-railsum/.q2-srow(.tot/.gt)/.q2-tbtns/.q2-rail-memo`), Â§4 detail (`.q2-prof/.q2-pbar/.q2-pid/.q2-pic/.q2-plabel/.q2-pname/.q2-pmeta/.q2-cchip/.q2-pacts`, `.q2-tabs/.q2-tab(.is-active)/.q2-tdiv`, `.q2-statgrid/.q2-stat` + `--ic--teal/mint/steel/red/ink` + `.q2-stat-lbl/val/var/note`), plus `.q2 .scoped-search-field` overrides (customer + line-item pickers sized to `.q2-input`), and NEW report block (`.q2-filters`, `.q2-sec-rule`, `.q2-pipe-row/.q2-pipe-ic--*/label/bar/count/val`, `.q2-tbl tfoot td` + `.q2-lbl`). Responsive: 1180px (fboxes 4-col, shells single-col, rail static) / 720px (fboxes 2-col, grids 1-col, pbar column). `.q2-input[readonly]/:disabled` get mist bg (readonly contact fields).
- **Index** (`index.blade.php`): `.q2-head` (title + Export ghost + teal CTA Create) â†’ `.q2-toolbar` (scoped-search input `name="search"` posting to hidden `#quot-list-form`, sort select) â†’ 7 clickable `.q2-fbox` cards (Total/Draft/Sent/Accepted/Declined/Converted/Void, counts from `$stats`/`$statsTotal`, `is-active` top-bar on current status) â†’ `.q2-shell` with `.q2-tbl` list card (quotation â„– link, customer, date, valid-until, total, `.q2-badge` status, view/edit/send/void `.q2-ibtn` actions, send+void confirm via `fbConfirmButton`) + `.q2-pag` pager + right `.q2-rail` (Views group: All / Open (Draft+Sent) `status=open` / Accepted / Converted; Reports group: Quotation Register + Sales Pipeline links). Inline `debounceQuotSearch()` 350ms.
- **Form** (`_form.blade.php` shared create/edit): `.q2-head--sticky` (sticky top `--topbar-h` z-40) with `.q2-badge` status on edit + action seg; create = Export? no â€” Cancel / Save & New / seg[Save Draft, Save, **Submit for Approval** (teal cta)]; edit = Cancel / **Delete** (danger-o, only when `created_by !== auth()->id()` â€” SOD) / seg[Save Changes, **Save & Email** (cta)]. `.q2-shell--form` 2-col (`minmax(0,1fr) 18.75rem`); 5 `.q2-sec` cards (Customer with scoped-search + `#quot-contact/email/phone/terms` readonly; Quotation Info with number readonly + `quotation_date`/`valid_until`/`reference`/`currency` select/`branch_id` + cost-centre scoped-search + dept/prepared-by/project display-only; Line Items `#quot-lines-table`; Notes `#memo` + internal readonly; Attachments `#quot-dropzone`/`#quot-files`/`#quot-new-files`/`#quot-existing-files`); rail = **Summary** (create) / **Breakdown** (edit) with `#p-cust/p-contact/p-date/p-valid/v-sub/r-disc/v-disc/r-tax/v-tax/v-gt/p-lines/p-foot` + Quick Nav (edit: print/email/attach/back; create: quotations list/new customer/day book). ALL JS hooks preserved verbatim (`quotLineRow` via runtime `scopedSearchFieldHtml`, `quotAddLine/RemoveLine/DuplicateRow/RowData/UpdateTotals/CustomerSelected/Sync`, `item-selected` autofill, `quotAddFiles/RemoveNewFile/RemoveExistingFile`, `quotWireDropzone`, `#quotation-delete-form` + `fbConfirmSubmit`).
- **Show** (`show.blade.php`): `.q2-head--sticky` + `.q2-badge` + per-status actions (draft: Edit/Email to Customer/Mark as Sent; sent: Convert to Invoice; Void via `fbPromptForm`; Print/Back); `.q2-prof` profile header (navy `.q2-pic`, `.q2-plabel/pname/pmeta` chips customer/date/valid/currency, `.q2-pacts`); `.q2-tabs` Overview/Line Items/Attachments (`data-target` + `#tab-*` panels); `.q2-statgrid` 4 tiles (Subtotal/Tax/Grand Total/Validity days-remaining); `sent` â†’ `x-review.btn` Decline+Accept forms (`#quotation-decline-form`/`#quotation-accept-form`), accepted/declined/converted â†’ `x-review.outcome`; details `.q2-g4` grid; line-items table + attachments tab.
- **Reports migrated** (were still `.q-*`): `quotation-register.blade.php` â†’ `.q2-card.q2-filters` date range + Apply/Reset/Open Quotations, `.q2-tbl` with `.q2-badge--*` statuses + `.q2-tbl tfoot` grand total; `sales-pipeline.blade.php` â†’ `.q2-statgrid` 5 `.q2-stat` tiles (Open Value/Win Rate/Open Quotes/Accepted Value/Total Value) + `.q2-sec` cards with `.q2-pipe-row`/`.q2-pipe-bar` funnel + aging. Both keep `<x-list-header>` + route names `accounting.reports.quotation-register`/`accounting.reports.sales-pipeline`.
- **Verification**: `view:cache` clean; `npm run build` â†’ `app-DeKNcUfK.css` 285.35 kB (grep confirms `.q2-fbox`, `.q2-pipe-row`, `.q2-filters`, `.q2-sec-rule`, `.q2-stat-ic--ink`, `.q2-tbl tfoot td` present). Tests: QuotationFormTest 14 + ListPageRenderTest 3 + ScopedSearchRenderSmokeTest + SalesModuleTest 7 + BranchRequestTest + GlobalSearchTest (16 + 8 + 31) all green. CDP probe (1440px, `probe@camelot.test`): index/create/register/pipeline all show ZERO elements wider than the viewport (computed `grid/1020px 300px`, sticky `sticky/106px/40`, 7 fboxes, 5 sec cards, live-preview ids present, 10 pipe rows with bars, tfoot total); remaining small-viewport h-scroll is ONLY the pre-existing topbar NAV overflow (#41/#42). Probe script: `cb-probe\q2-probe.js`.

45. **Sales Receipts suite redesign — DESIGN 2 (Aug 8, 2026)** â€” ported all 6 sales-receipt screens (`accounting/sales-receipts/{index,create,edit,show,post-page,print}`) plus both report views (`reports/daily-summary`, `reports/sales-receipts-cashbook`) onto the same teal executive `.q2-*` language as #44. Styling/markup only; routes, permissions, validation, posting, PDF and print (self-contained thermal template) untouched. `print.blade.php` deliberately left un-restyled (print views keep own print CSS).
- **Index** (`index.blade.php`): `.q2-head` (title + Export ghost + teal CTA Create) â†’ `.q2-toolbar` (scoped-search input `name="search"` posting to hidden `#sr-list-form`, sort select) â†’ `q2-fbox-grid--4` (4-col variant: Total/Draft/Posted/Voided) â†’ `.q2-shell` `.q2-tbl` list card (receipt â„– link, date, customer, method, total, `.q2-badge--draft/posted/voided`, view/edit/post/void `.q2-ibtn` actions) + `.q2-pag` pager + right `.q2-rail` (Views: All/Draft/Posted/Voided; Reports: Daily Summary + Cashbook). Actions respect status: edit draft only (`@can sales-receipts.edit`), post draft via GET link to `post-page` (route is GET, NOT a POST form) under `sales-receipts.post`, void posted via `fbConfirmButton` under `sales-receipts.void`. Inline `debounceSrSearch()` 350ms.
- **Form** (`_form.blade.php`, already existed; create/edit are thin `<x-app-layout>` wrappers): create passes `selectedCustomerId` from `request()->query('customer_id','')` + `defaultIncomeAccountId` fallback `$incomeAccounts->first()?->id ?? ''`; edit passes `submitLabel=__('Save Changes')`. Both wrappers pass the full var set the form uses (`$branches,$customers,$defaultIncomeAccountId,$incomeAccounts,$isEdit,$mobileProviders,$paymentMethods,$products,$salesReceipt,$selectedCustomerId,$submitLabel,$subtitle,$title`).
- **Show** (`show.blade.php`): `.q2-head--sticky` (Edit/Post Receipt for draft, Email/Void for posted, Print, Back) â†’ `.q2-prof` (navy `.q2-pic`, customer label/name, meta chips date/status/ref) â†’ `.q2-tabs` Overview/Payments/Line Items (`.q2-tab` + `.q2-tab-panel`, `data-target`/`id`, inline JS toggles `style.display`; panels default to block so all render) â†’ `.q2-statgrid` 4 tiles (Total/Received/Unallocated/Tax) â†’ Overview `.q2-g4` detail grid + voided note, Payments `.q2-tbl` + railsum, Line Items `.q2-tbl` â†’ `.q2-railcard` quick nav (Print/Edit/Post/Email/All Receipts).
- **Post page** (`post-page.blade.php`, GET) — new: `.q2-head--sticky` (Cancel â†’ show; teal **Post Receipt** submit â†’ `sales-receipts.post` POST) â†’ Posting Summary `.q2-ro-grid` (Receipt â„–, Customer, Status `.q2-badge--draft`, Date, Method from payments, Total) â†’ Journal Entry Preview `.q2-tbl` (uses `$jeLines`/`$accounts` from controller; Balanced chip `.q2-chip-t` + `.q2-note-info` warning when unbalanced) + `.q2-jfoot` totals row â†’ rail Receipt Summary `.q2-railsum` + back links.
- **Reports** (both new): `daily-summary.blade.php` + `sales-receipts-cashbook.blade.php` â†’ `x-list-header` + `.q2-card.q2-filters` GET form (date range) + `.q2-statgrid` summary tiles + `.q2-card--list` `.q2-tbl` with `tfoot` `.q2-lbl` totals row + `.q2-empty` fallback; `q2-filters-actions` Apply/Reset links to `accounting.reports.sales-receipts.daily-summary`/`cashbook`.
- **CSS** (`resources/css/app.css`): added `.q2-tab-panel { margin-top: 1.25rem }` (was missing â€” quotations show used the class but it only existed as JS-toggled `display`; both suites now get panel spacing). Used new/verified classes: `q2-fbox-grid--4`, `q2-chip-t`, `q2-ro-grid/q2-ro/q2-ro-lbl/q2-ro-val`, `q2-jfoot`, `q2-tbl tfoot td.q2-lbl`, `q2-pag`, `q2-note-info`. CSS-class cross-check script (`rg` of every `q2-*` token used in the 5 new views against `app.css`) found exactly ONE gap (`.q2-tab-panel`) â€” now fixed.
- **Tests**: `ScopedSearchRenderSmokeTest` extended with receipts fixtures (draft `RCT-0001` tax-free 100/100/0 payment line + posted `RCT-0002`; PosPaymentMethod Cash w/ clearing+settlement bank account) + routes `sales-receipts.index|create|edit|show|post-page|print` + both report routes. NOTE: fixture MUST be tax-free â€” post-page GET throws `InvalidArgumentException: Journal entry is unbalanced by X` when the draft has tax. `ListPageRenderTest` `$routes` map now has nameâ†’marker; `sales-receipts.index` asserts `q2-tbl` (was `list-table`). Verification: `view:cache` clean; `npm run build` â†’ `app-CW1qqD1E.css` 290.69 kB (`.q2-tab-panel` present); ScopedSearchRenderSmokeTest 1 + ListPageRenderTest 3 + SalesModuleTest 7 all green.

46. **Sales Receipts suite redesign — DESIGN 3 (`.sr-suite`)** â€” all 6 screens (`accounting/sales-receipts/{index,create,edit,show,post-page,print}`) re-ported onto the NEW `docs/sales-receipt-suite_mockup.html` as a self-contained `.sr-suite` block in `resources/css/app.css` (lines ~7097â€“7507) replacing the phase-45 `.q2-*` markup. Styling/markup only; routes, permissions, validation, posting, PDF, and print untouched.
- **Index**: `.sr-suite .fbox` 4 status tiles w/ icons, search/sort posting to hidden `#sr-list-form`, `.li-wrap` list card, empty state, rail Views/Reports groups, New/Export buttons, `.li-wrap { overflow: auto }` (internal scroll, no card blowout).
- **Create/Edit** (`_form.blade.php`): sticky `.sticky-head` (top `var(--topbar-h)` z-40), `#receipt-form` `enctype="multipart/form-data"`, scoped-search customer `input[name="customer_id"]`, payments table `#sr-payments-body` + `#sr-add-payment`, line-items `.li-wrap` table `#sr-lines-table` + `#sr-add-line` + `.li-totals` box, right rail `.railsum` live preview. Cards keep `overflow: visible` so scoped-search dropdowns are never clipped. ALL legacy ids (`sr-add-line`, `sr-add-payment`, `receipt-form`, `receipt-delete-form`, `sr-payments-body`, `sr-lines-body`, `sr-list-form`) and field names preserved.
- **Reports**: `daily-summary`/`cashbook` remain legacy `.q2-*` pages (NOT `.sr-suite`); `show`/`post-page`/`print` unchanged.
- **Responsive bug fixed (this phase)**: at small viewports the create-form card's min-content was 602px (375px overflow). Root cause: the `.li-wrap` scroll container contributes its line table's 552px min-content to the grid column because the breakpoint used bare `1fr`. Fix: `minmax(0, 1fr)` everywhere responsive grids collapse to 1 col â€” `.sr-suite .shell` (1100px) + `.g4/.sgrid/.ro-grid` (1100/720px), AND the same latent bugs in the q2 suite: `.q2-shell` (1180px), `.q2-g3/.q2-g2/.q2-g4` (720px), `.q2-paygrid/.q2-ro-grid` (720px). Verified headless @375px: shell 316px, `.li-wrap` internal scroll (client 268px / content 552px), no suite element forces page width; the ONLY remaining page-level h-scroll is the pre-existing topbar row-2 nav overflow (#41/#42, out of scope). Same latent bare-`1fr` pattern was left intact elsewhere (`.`); grep `grid-template-columns: 1fr` before adding new scroll-container grids.
- **Verification**: `npm run build` â†’ `app-DNakOdO3.css` (309.70 kB); compiled CSS contains all `.sr-suite` tokens; `view:cache` clean; tests green (11 passed / 56 assertions: ScopedSearchRenderSmokeTest + ListPageRenderTest + SalesModuleTest). `ListPageRenderTest` marker for `sales-receipts.index` updated `q2-tbl` â†’ `li-wrap` (phase-45 marker obsolete). Headless probe scripts: `C:\Users\eseyama\AppData\Local\Temp\opencode\cb-probe\sr-probe.js` (full suite + multi-viewport) / `sr-375.js` (focused 375px check with watchdog â€” the full probe's CDP WebSocket can die mid-run, leaving `send()` promises hanging; the focused script rejects pending on `ws.onclose`).

47. **Customers suite pixel pass vs `docs/icons 4.png` (Aug 9, 2026)** â€” icon-size normalization + mockup token parity + 6-column list + Export CSV on `accounting/customers/{index,create,edit,show}`. Styling/markup only; routes/permissions/validation/search/posting untouched.
- **Icon-size token pass in `resources/css/app.css` (cs-suite block)**: page-head `.btn svg` 15px (+ `.toolbar .btn svg` 14px), `.sec-ic svg` 15px (+ `.rail-sec .sec-ic svg` 14px), `.sbox.ic .t svg` 16px, `.vitem .ic svg` + `.ibtn svg` 14px (`.ibtn.view svg` 15px), `.search svg` 14px + `.search .input` padding-left 36px.
- **Mockup tokens**: `.tchip` (10px/700, `padding:3px 9px`, `line-height:1; align-items:center` â€” WITHOUT the line-height fix the chip inherits a global table line-height and stretches to 28px tall; live probe measured 18px after fix), `.b-inact` (info `#46708C` tones), `.numr.red` (`var(--red-2,#B91C1C)`), `.sbox .v.red` red-2, `.cust .ava` 34px `rgba(17,69,75,.08)` bg + `1px rgba(17,69,75,.15)` border + `.st` 10px status dot (green `#22c55e` active, `#94a3b8` `.st.inact`, 2px `#fafdfd` ring, `right/bottom:-1px`).
- **Table**: 7 columns (Name/Email/Phone/Terms/Balance/Status/Actions) matching the mockup; `thead th` + `tbody td` `white-space:nowrap`, `td.em` (email/phone) ellipsis, `table-layout:fixed` width split 22/22/15/9/12/11/9%. "Since {{ $customer->created_at?->format('M Y') }}" secondary line kept. Terms cell renders `.tchip`; balances use `format_number()` + `.numr` (`.numr.red` when positive due); status via `.b-act`/`.b-inact` badges; row actions = view/`.ibtn.view`, edit, and the POST `accounting.customers.toggle` trash form with `fbConfirmSubmit` (`.ibtn.del`).
- **Index layout**: `.cs-suite .sgrid` + `.controls` margin-top 16px; Portfolio wrapped in `.card .card-sec` with `.sec-head` (`.sec-ic` users icon + `.rule`); stat tiles Customers/Active/Outstanding; `.controls` form = search magnifier + status/terms selects + Filter/Clear `.btn.ghost` + `.chip-t` count; **numbered `.pg` pagination PRESERVED** (mockup Prev/Next style NOT adopted â€” tests assert literal `Showing 1â€“15 of 20 customers` + `.pagi`). Right rail `.railsum` card with Views (All/Active/Inactive `.vitem.on`) + Reports (AR Aging / Sales by Customer) `.rail-sec`s.
- **Export CSV (new)**: `CustomerController::index()` now computes stats only; private `baseQuery(Request)` (search/status/terms) shared with new `export()`; export uses `$this->requirePermission($request, 'customers.view')` (helper lives in base `app/Http/Controllers/Controller.php`, NOT the controller), `streamDownload` `text/csv`, header `Name,Display Name,Email,Phone,Payment Terms,Payment Terms Days,Balance,Status,Billing Address,Shipping Address`, balance `number_format(...,2)`, filename `customers-Y-m-d-His.csv`. Route `web.php:316` `Route::get('customers/export',...)` inserted BEFORE `customers/{customer}` (verified via a tinker-free boot script — `route:list` crashes on pre-existing `PdfController` ReflectionException). Live-verified by CDP `Browser.setDownloadBehavior` download capture (`customers-2026-08-09-183749.csv`, header+rows correct).
- **Audit**: create/edit/_form/show have no inline 30px icons; profile avatars use the cs-suite `.ava` (13px initials) â€” mockup-consistent, no changes.
- **Headless verification (cust-probe.js, 1440px)**: export/create 15px svg, sec-ic 15px, sbox tile 40Ã—40 + 16px icon, vitem/ibtn 14px, ava 34px + green 10px dot, tchip 18px @10px, search padding-left 36px, `.shell` grid `956px 300px`, zero elements wider than viewport (hscroll = pre-existing topbar nav #41/#42 only).
- **Tests**: `ListPageRenderTest` markers updated (customers.index `'1 customers'`, `Since M Y`, `aria-current="page"`, `li-wrap`); 25 passed / 128 assertions across ListPageRenderTest + ScopedSearchRenderSmokeTest + QuotationFormTest + SalesModuleTest. `git diff` scoped to CustomerController + customers/* views + web.php + app.css + ListPageRenderTest + the 3 docs files (mockup + `icons 3.png`/`icons 4.png`).

48. **Settings module redesign — `.ss-suite` (Aug 9, 2026)** — rebuilt the Company Preferences / System Settings module (index shell + 9 tab partials, Settings Audit Log, Features) and the shared Admin Backups page onto a new scoped `.ss-suite` design language. Styling/markup only — routes, controllers, validation, permissions, update handlers, and the central FeatureManagement read path are untouched (the Reports module was explicitly out of scope; `git diff -- resources/views/reports` is empty).
- **Layout pattern**: every settings/backups page renders a SINGLE `.sticky-head` toolbar (`position:sticky; top:var(--topbar-h); z-index:40`, glass bg) — left group `@include('system-settings._tabnav', ['active' => '...'])` (`.glabel` "Navigate" + `.tbtns` of `.btn` links; active tab gets `.btn.cta`), right group `.glabel` "Actions" + submit/CTA buttons. Forms submit via `form="..."` attributes on the sticky-head buttons (button outside the form). The old left sidebar layout is gone.
- **Deleted 6 dead files**: `components/settings/sidebar.blade.php`, `system-settings/{_approval-tab,_audit-log-tab,_data-hub-tab,_import-export-tab}.blade.php`, plus the legacy duplicate `system-settings/_notifications-tab.blade.php` (unreferenced — only found during the verification sweep; naming didn't match the `_tab-*` scheme).
- **Components re-skinned** (legacy `settings-field/toggle/callout/table` classes removed): `field` → `.field/.label/.req/.input/.hint/.err`; `toggle` → `.toggle-row/.toggle-ui` + hidden `0` + checkbox `.toggle-input` + `.toggle-track/.toggle-thumb` + `.toggle-text`; `callout` → `.note-info` (+`.note-warn`), slot rendered raw (no "Note:"/"Warning:" prefix); `table` → `.li-wrap` wrapper + `.empty` empty cell (`emptyMessage` prop preserved).
- **New `_tabnav.blade.php`**: 12-tab route array (company, regional, currency, accounts, accounting, approval, notifications, data-hub, import-export, features, backups, audit-log), labels matching the old sidebar; `$active` param; active = `.btn.cta`.
- **`index.blade.php` shell**: `.ss-suite` wrapper + error alert + dispatch to 8 partials (`company` fallback; regional/currency/accounts/accounting/approval/notifications/data-hub/import-export); `features`, `backups`, `audit-log` are separate full pages.
- **`audit-log.blade.php`**: sticky-head nav + "Export CSV" ghost → `admin.audit-log.export-csv`; filter card (group select, user scoped-search-field, from/to dates, Filter/Clear); `.li-wrap.li-wrap-auto` diff table (Date/Time, User, Settings Group badge, Changes with `.diff-old/.diff-new/.diff-arrow`, IP Address); `$logs->links()`.
- **`features.blade.php`**: now a full page (legacy version had NO `x-app-layout`/`x-list-header`), nav active "features", chip-t "Controlled by your system administrator", callout, features table with `.b-act`/`.b-gray` badges.
- **9 `_tab-*.blade.php` partials**, each with its own sticky-head + save button via `form="..."`: company (profile `.g3` + logo card-sec with preview + "Remove current logo" checkbox, `enctype="multipart/form-data"`); regional (full timezone/date/time/number/format selects); currency; accounts (`.map-row` grid + scoped-search-field + `.mapping-ok/.mapping-warn/.mapping-muted` callouts); accounting (3 toggles + rounding tolerance + posting-closed-period callout); approval (master toggle + global threshold + per-document rows with checkbox/disabled JS in `@push('scripts')`); notifications (email content fields + templates table + "Manage Templates" → `admin.notifications.index`); data-hub (export e-rows with real CSV/PDF links + "Open Report Center" ghost → `accounting.report-center.index` + 4 static import cards); import-export (download/import forms + callouts).
- **`admin/backups/index.blade.php`**: tabnav active backups; "Trigger Backup" `.btn.cta` via hidden `#trigger-form` + `fbConfirmButton`; database-backups table with `.b-act/.b-gray/.b-red` status + `.exit-code`; snapshots card (create form + Restore/Delete with `fbConfirmSubmit`).
- **CSS**: `.ss-suite` block appended at the END of `resources/css/app.css` (after the `.suite` tail media queries); the old `.settings-*` block was DELETED (`rg settings-` in app.css is clean). Single-column no-rail layout, sticky-head/toolbar/glabel/tbtns/tdiv, btn ghost/sec/cta/danger-o/sm (34px), card/card-sec/sec-head/sec-ic/rule, g3/g2/sp2, field/label/req/input/select chevron/textarea, chip-t, badges (b-act/b-inact/b-teal/b-mint/b-red/b-gray + `.bdot`), toggle-*, li-wrap + li-wrap-auto tables (mono/em/numr/empty), note-info(+note-warn), map-row/map-key/mapping-*, diff-old/new/arrow/log-key, e-row/import-grid/import-card/ic, mono-chip, exit-code; media fallbacks at 1100px (g3 → 2-col) and 720px (grids → 1-col, sticky-head align-start). The bare `.suite` block is unrelated Phase-50 WIP — do not touch.
- **BLADE FOOTGUN (fixed, caught by smoke test)**: short-form `@php($x = ...)` followed LATER by a block-form `@endphp` is silently mangled — Blade's `storePhpBlocks` regex `/(?<!@)@php(.*?)@endphp/s` matches from the FIRST `@php` (the short form) all the way to the first `@endphp`, so the inner block-form `@php` becomes literal text and its variables are UNDEFINED at runtime (`_tabnav` → "Undefined variable $tabs"; the mangled compiled file had `<?php($active = $active ?? '')` with no `; ?>` and `$tabs = [...]` emitted as raw text). Fix: use block-form `@php ... @endphp` exclusively — NEVER mix a short-form `@php(...)` before a block. Short-form alone compiles fine (`compilePhp` → `<?php $x; ?>`). RULE: `rg -n "@php\("` a view before shipping it.
- **Verification**: `view:cache` clean; `npm run build` → `app-B9-DLo-x.css` (384.12 kB, all `.ss-suite` tokens present in the bundle); zero `href="#"`/`href=""`/`javascript:void(0)` and zero legacy `settings-*` classes across the settings/backups views; `AdminModuleTest|ListPageRenderTest|FavouritesTest` **48 passed / 279 assertions**. `ScopedSearchRenderSmokeTest` extended with all 12 settings/backups GET routes (`system-settings.index` ×9 tabs, `system-settings.features`, `system-settings.audit-log`, `admin.backups.index`) → all render 200 as company_admin. Routes: system-settings group `web.php:856-869`, backups `831-835`, admin audit-log CSV `818`, report-center `708`. `$eventLabels` confirmed passed by `SettingsController@index` (`EmailTemplate::eventLabels()` exists).

49. **Report Center redesign — Variant A (Aug 11, 2026)** — rebuilt the Report Center content area (route `accounting.report-center.index`, view `resources/views/report-center/index.blade.php`) into a sticky-sidebar directory: search + category nav + per-user favourites shelf + dense report rows that link EXACT report routes. UI + persistence only; the header rails/sumbar/export handlers/routes are untouched.
- **Persistence**: new tenant migration `2026_08_11_000001_add_report_favourites_to_user_preferences_table.php` adds `report_favourites` JSON column to `user_preferences` (after `sidebar_pinned`); `UserPreference` model: added to `$fillable` + `'report_favourites' => 'array'` cast. `user_preferences.user_id` is the primary key (non-incrementing), so `firstOrCreate(['user_id' => $user->id])` works per-user. Favourites live HERE (NOT `rail_prefs`, NOT `user_favourites`/FavouritesService — the header favourites dropdown is a separate system, not bridged). Migration applied to all 3 live tenant DBs via `php artisan tenant:migrate`.
- **`ReportCenterController`** (`app/Http/Controllers/Accounting/ReportCenterController.php`) rewritten: `index` builds a `sidebar` label/count map from `ReportRegistry::getAccessibleGrouped($user, $companyId)`; for each report computes `url` (only when `Route::has($route)` AND the named route's parameter count === 0 — parameterized routes stay null/blank) and `is_favorite`; maps to `groups` (`key/label/reports` each with `key/name/description/url/is_favorite`), slim `favorites` list (`key/name/url`), `total`; passes `initialSearch`/`initialCategory` (request query). New `toggleFavorite` (POST, JSON): 404 on unknown registry key, `UserPreference::firstOrCreate`, toggles the key in `report_favourites`, returns `{favorited: bool}`. Private `favoriteKeysFor()` + `reportUrl()` helpers. Favourites no longer accessible (feature flag OFF / permission lost) are pruned server-side from the stored array on read.
- **View** `report-center/index.blade.php` rewritten as Variant A (`.va-*` suite): `x-data="reportCenter({...})"` bootstrapped with `{{ Js::from(...) }}` (groups/favorites/toggleUrl/initialSearch/initialCategory inside the JSON payloads — NOTE the payloads render as `JSON.parse('...')` so raw substring assertions must expect `\u0022`-escaped JSON, and `favorites` is unique among payloads because its slim entries put `\u0022name\u0022...\u0022url\u0022` adjacent with no `description`). Layout: `.va-head` (title + serif? no — Inter extrabold + count) → `.va-shell` grid `minmax(0,1fr) 18.75rem` rail → `.va-side` sticky (search field w/ clear, `.va-nav` category list with live counts, `.va-shelf` favourites shelf `x-show="favs.length"` + `x-cloak`) → `.va-main` (`.va-toolbar` count + sort select, `.va-rows` bordered rows, `.va-empty` state). Each row is an `<a :href="r.url">` with name + description; star `<button>` (`@click.prevent.stop`, `:aria-pressed`, filled when favourite). Deleted dead `report-center/_report-card.blade.php`.
- **JS** `resources/js/report-center.js` — Alpine `reportCenter` data (registered via `document.addEventListener('alpine:init', ...)`, imported in `app.js`): `q/cat/sort/groups/favs/toggleUrl`, computed `cats/totalCount/visibleGroups/visibleCount`, `isFav/toggleFav` (optimistic UI + POST to `toggleUrl` with `.replace(':key', r.key)`, rollback on failure, reuses `resources/js/http.js` `fetchJson`). `/` shortcut focuses the search input unless typing in input/textarea/select/contenteditable; `init/destroy` add/remove the listener. Client-side search/category/sort mutate an immutable server payload.
- **CSS** appended `.va-*` block to `resources/css/app.css`: search, sticky `.va-side`, nav, shelf, toolbar, bordered `.va-rows`, star (gold `--star: #F59E0B`, new token added to `:root`), empty state; responsive at 1100/900/720px; `prefers-reduced-motion` respected. Also added `html, body { overflow-x: clip; }` just after the `[x-cloak]` rule (horizontal-scroll guard).
- **Tests** `tests/Feature/Accounting/ReportCenterTest.php` (7 / 29 assertions): index renders variant-A markup (va-suite + reportCenter payload + All Reports), toggle add persists + persist-removes + invalid-key 404, shelf renders for favourited report (`va-shelf` + `favorites: JSON.parse(` + adjacent-name/url escaped match), inaccessible feature report dropped from shelf, groups payload contains `\u0022trial_balance\u0022` + `\u0022is_favorite\u0022`. Assertions target the serialized payload, NOT template markup — the `x-for` `va-fav`/`va-row` templates always render regardless of state. Setup mirrors ScopedSearchRenderSmokeTest (company_admin + RolePermissionSeeder + session company + features enabled + income Account).
- **Verification**: `php artisan view:cache` clean; `php artisan tenant:migrate` OK on all 3 tenant DBs; `npm run build` → `app-DwEHtGC7.css` (391.32 kB — all `.va-*` tokens + `--star: #F59E0B` confirmed in bundle via rg) + `app-DF0kJIDl.js`; `ReportCenterTest` 7 passed, `ScopedSearchRenderSmokeTest` passes (161s).

50. **Report Center follow-ups (Aug 12, 2026)** - sorting by name A-Z/Z-A, clear-filters empty state, full-registry regression test, and a PRE-EXISTING Alpine `x-for` bug fix on the Variant A page.
- **Sort select rewritten** in `report-center/index.blade.php`: the old `alpha`/`cat` options are gone; now `value="az"` "Name A-Z" and `value="za"` "Name Z-A" only, default `az`. JS `report-center.js`: `sort: 'az'` default; `visibleGroups` now ALWAYS sorts each group's reports by `name` (`dir = -1` for `za`, `1` otherwise, `localeCompare`), rebuilding the group objects with explicit `key/label/icon/reports` fields. Category-based sorting removed entirely.
- **Empty state action**: `.va-empty` (shown when `visibleGroups.length === 0`) gained a "Clear search & filters" `<button class="va-empty-clear" @click="clearFilters">`; `report-center.js` added `clearFilters()` -> resets `q=''`, `cat=''`, `sort='az'` (restores A-Z default too). New `.va-empty-clear` CSS (bordered pill, teal hover) in app.css.
- **BUG FIXED - Alpine `x-for` on non-`<template>` elements**: the Variant A scaffold put `x-for="r in g.reports"` directly on `<a class="va-row">`, `x-for="f in favs"` on `<a class="va-fav">`, and `x-for="f in r.formats"` on `<span class="va-fmt">`. Alpine 3 only iterates `x-for` on `<template>` tags, so the report rows and favourites shelf NEVER rendered in the browser: the DOM showed one literal row per group whose `.va-t` text was "r" (the `:key` var name) and 12 `.va-row` total (one per category) instead of 77 reports. All three loops now wrap their content in `<template x-for="..." :key="...">`. VERIFY any Alpine `x-for` is on a `<template>` element - a bare-element `x-for` is a silent no-op that tests can't catch (the Bladed `x-for` template strings always exist in the DOM regardless of iteration).
- **Full-registry regression test** `test_index_renders_full_registry` in `tests/Feature/Accounting/ReportCenterTest.php`: asserts exactly 77 `\u0022description\u0022` occurrences (proves every registry report is in the payload), all 12 category labels present, previously-hidden report keys (`financial_ratios`, `budget_variance`, `true_total_cost`, `pos_x_report`, `purchase_register`, `cheque_register`, `payslip_report`, `asset_disposal_report`) now present, and zero `\u0022url\u0022:null` rows (all 77 have real links).
- **Js::from DOUBLE-ENCODING gotcha (test footgun)**: `{{ Js::from($array) }}` on an array renders `JSON.parse('...')` where the inner JSON is ENCODED AGAIN as a JSON string. A label containing `/` renders as `Sales \\\/ Accounts Receivable` (three chars: `\` `\` `\/`) in the HTML, and `&` as `\u0026`. To assert a label substring, double-encode it in PHP: `$flags = JSON_HEX_TAG | JSON_HEX_APOS | JSON_HEX_AMP | JSON_HEX_QUOT; $expected = substr(json_encode(json_encode($label, $flags), $flags), 1, -1);` then `assertStringContainsString('\\u0022label\\u0022:' . $expected, $content)`. The single-encode form `json_encode($label, $flags)` does NOT match.
- **Verification**: `npm run build` (vite v7.3.6) -> `app-CZIEIeOz.js` / `app-5W-DQXK7.css`; `php artisan view:cache` clean; `ReportCenterTest` 8 passed / 63 assertions; `ScopedSearchRenderSmokeTest` passes (157s). Headless Chrome (1440px, probe@camelot.test): 77 rows render, head total 77, sidebar 13 nav items with live counts (All 77 | Financial Statements 11 | Sales 12 | Purchasing 8), toolbar "77 of 77 reports" live-updates to "2 of 77" on search "trial", sort A-Z (Balance Sheet first) and Z-A (Trial Balance Comparison first) both work, empty state with Clear search & filters restores 77 rows + A-Z + All Reports active, favourites star toggles the shelf (renders "Balance Sheet" link to /accounting/balance-sheet, persists across reload). Probe script pattern: `C:\Users\eseyama\AppData\Local\Temp\opencode\cb-probe\rc-*.js` (deleted after use).

51. **All 77 Report Center routes render 200 (Aug 12, 2026)** — closed the render gap: every report in `ReportRegistry` (`app/Services/Reporting/ReportRegistry.php`) now returns HTTP 200 through the full web middleware stack, verified by `tests/Feature/Accounting/ReportRenderSmokeTest.php` (renders all 77 registry routes as company_admin with features enabled; `assertCount(77, $routes)`).
- **Mangled-view defect (pre-existing at HEAD)**: several report views had the signature `$rows = $service->run(...) . '); @endphp' . [entire body duplicated]`. The `); @endphp` was literal text (leaked to the page and broke the row loop), and Blade's short `@php($rows = ...)` form doesn't take a closing `@endphp`. Fix = truncate the file at its FIRST `</x-app-layout>`. Applied to 18 views total (customer-credit-balance rewritten + 17 truncations). RULE: when a view has a short-form `@php($x = ...)` near the top and an `@endphp` + duplicated body later, it's mangled — check `rg -n "</x-app-layout>" <file>` for >1 hit.
- **Service arrays are the contract; views must not read `$rows`** — rewired views to the exact keys each reporting service returns: deposits-in-transit (`$lines`, per-row `debit`), asset-revaluation (`$revaluations`: `previous_fair_value`/`new_fair_value`/`surplus_amount`/`journal_entry_id`), asset-impairment (`$events`: `is_reversal`), stock-count-variance (`$lines`: `product_name`/`sku`/`expected`/`counted`/`variance_qty`/`unit_cost`/`variance_cost`), sales-by-customer (`$customers`: `customer_name`/`invoices_total`/`invoices_count`/`receipts_total`/`pos_total`/`grand_total`; header totals computed in `@php`), quotation-status (`$quotations` + `$summary`; `$statusTotals` derived from `$summary`), purchases-by-vendor (`$vendors`: `vendor_name`), purchases-by-item (`$items`: `product_name`/`sku`), unbilled-receipts (`$lines`: `quantity`/`total_cost`), trial-balance-comparison (`$lines` with `$row['account']->code/name`; footer aliases `$total_debit_1/2`, `$total_credit_1/2`, `$variance_debit/credit`), employee-cost-by-branch (`$by_branch`/`$by_cost_center`: `branch_name`/`cost_center_name`), tax-depreciation-schedule (`$schedule`: key `cost` NOT `acquisition_cost`), bank-balances (`$accounts` array: `account_code`/`account_name`/`type`/`balance`), sales-by-item (`$items`: `product_name`/`sku`/`qty`/`total`/`count`), item-profitability (`$items`: `product_name`/`sku`/`revenue`/`qty_sold`/`cogs`/`profit`/`margin_pct`). PendingApprovalsAgingService already matched its view (`pending` rows: `days_pending`/`vendor_or_employee`/`date`) — no change.
- **`PosSaleLine` relation is `sale()`** (belongsTo `PosSale`, FK `pos_sale_id`) — NOT `posSale()`. Two reporting services called the wrong name (`SalesByItemService.php:32`, `ItemProfitabilityService.php:23`); fixed to `whereHas('sale', …)`.
- **`TaxDepreciationScheduleService`** now also emits `'status' => $asset->status` (the schedule view shows it).
- **Feature gates confirmed**: `accounting.budgets.index` (registry `budget_variance`) sits behind `feature:budgets` (`routes/web.php:667`); POS X/Z routes (`pos.reports.x-report`/`z-report`) behind `feature:pos` (`web.php:937-938`). Smoke test now enables `'budgets'` in its feature loop (was missing — route 404'd until added).
- **`@return` is NOT a Blade directive** — it renders as literal text and does NOT short-circuit the view (POS X/Z views used it inside `@if(!$data)` and then dereferenced `$data` → "Trying to access array offset on value of type null"). ALSO: `@php return; @endphp` inside the `x-app-layout` slot LEAKS AN OUTPUT BUFFER (PHPUnit "did not close its own output buffers" risky) — `return` exits the compiled slot template before the component's slot-capture `ob_end_clean()` runs. CORRECT pattern: wrap the whole body in `@if($data) … @else <empty card> @endif` (empty card first, then `@else`), no early return. POS `PosReportController` without a `session_id` queries open→latest till session; in tests there are none, so `data` is null and the empty branch renders.
- **Verification**: `php artisan view:cache` clean; `ReportRenderSmokeTest` PASSES (all 77 routes 200, 2 assertions, ~127s, no risky). Buffer-leak detection loop (temp test comparing `ob_get_level()` before/after each route) confirmed the ONLY leakers were x-report/z-report. Full-suite runtime exceeds the 10-min shell timeout — run slices with `--filter`.

52. **Cash Position redesign (Aug 12, 2026)** — rebuilt the banking Cash Position page (`accounting.cash-position.index`) as a financial-position/cash-flow dashboard per the inline spec (no mockup file exists). Server math solved entirely in the controller; presentation/navigation only — reconciliation, transfers, deposits, cheque handling, exports, ledger math untouched. Page carries ZERO rails markup (the global rails exclusion hook was never located — open question for later).
- **`app/Http/Controllers/Accounting/CashPositionController.php`** fully rewritten. Static option helpers: `periodOptions()` (today/week/month/quarter/year/custom), `periodRange()` (custom range defaults to month start/end; default period = month), `transactionTypeOptions()` (~40 source_module labels incl. `petty_cash_establish/expense/replenish`, `depreciation_run`, `fixed_asset_*`, `realized/unrealized_fx_*`), `transactionTypeLabel()` (unknown → "Other"), `statusOptions()` (draft/pending_approval/posted/reversed), `reconciledOptions()`. `resolveFilters()` validates period against keys, coerces account_id/branch_id/cost_center_id to ints, allows `status`/`source_module`/`q`/`currency` free strings. `index()`: cash+bank account query (`cashBankAccounts`: `is_active` + `is_bank_account OR is_petty_cash`, optional account_id/currency/q filters) → `movementPerAccount` → hero totals (opening/inflow/outflow/closing/net/transactions) → top-5 `bars` (by inflow+outflow desc, `maxBar` denom guard 0.01) → `movementCategories` (group by source_module, filtered to in/out > 0) → `recentTransactions` (BankTransaction JOIN accounts + journal_entries, last 10, per-txn inflow/outflow split by sign, `source_label`) → branches/costCenters (active) + currencies (central, `active()->ordered()`) → `reconciliationUrl` (first active bank account's `bank-reconciliation.index`, else bank-accounts.index). `exportCsv`/`exportPdf`/`print` via shared `renderPrint($request, $direct)` (direct → self-contained print view; PDF → `accounting.print-export` shell).
- **Movement math**: `lineSums()` JOINs `journal_entry_lines` to `journal_entries` filtering `company_id` + `account_id IN` + statuses (posted+reversed by default, single status when filter set) + date window + optional branch/cost_center/source_module/q, grouped by account_id → `SUM(debit)/SUM(credit)/COUNT(*)`. `movementPerAccount()` computes `prePeriod` (dateTo = date_from − 1 day) vs `period`; opening = pre-period sum + `opening_balance` (sign flipped by `isDebitNormal()`), inflow/outflow = period debit/credit per normal balance, closing = opening + inflow − outflow; per-row `reconciled` only for bank accounts via `BankService::getReconciledBalance()`. Two grouped queries total (no per-account N+1).
- **Routes** (`routes/web.php:640-643`, inside the `feature:banking` group at :596): `cash-position`, `cash-position/export/csv`, `cash-position/export/pdf`, `cash-position/print`.
- **Views**: `index.blade.php` as a `.cp-` suite: `.cp-head` (title + More `<details>` dropdown with Export CSV/Print/PDF carrying `$exportParams = request()->except('page')` + gold Make deposit CTA → `deposits.create`), `.cp-hero` flow (Opening → Inflow → Outflow → Net Change → Closing, teal gradient step cell; meta line period label + date range + txn count), 3 holdings `.cp-chips` (accounts in use / bank accounts / cash & petty), `.cp-pills` nav (Dashboard/Banking/Bank accounts/Cash position active/Bank reconciliation → `reconciliationUrl`/Make deposits/Cheque management/General Ledger/Reports), filter form (q + period + from/to + Apply/Clear/Advanced; advanced Alpine `x-data="{ adv }"` toggling source_module/status/branch/cost_center/reconciled selects), `.cp-shell` with movement table (bank link → `bank-accounts.register`, cash link → `petty-cash.index`, currency chip links back to index filtered by that currency, bank/cash badges, tfoot totals) + recent-transactions table (description → `journal-entries.show` when journal_entry_id present, source chip, reconciled/unreconciled badge, signed in/out) + `.cp-rail` (Movement Bars top-5 with relative fill widths, Movement Categories). Footer notes amounts exclude symbols. `print.blade.php`: self-contained A4 template (`.page`, gradient `.head` with CB logo + company name, Summary 4-box grid, per-account table + categories table, footer), `onload="window.print()"`.
- **CSS**: `.cp-*` block appended at the very end of `resources/css/app.css` (~8519-8935) after the Report Center `.va-*` block; uses existing teal tokens (`--sec`, `--deep-1/2/3`, `--mint`, `--line`, `--muted`), non-sticky design, responsive at 1020/720px + `prefers-reduced-motion`.
- **Tests** `tests/Feature/Accounting/CashPositionTest.php` (8 / 53 assertions, fixture pattern from ScopedSearchRenderSmokeTest): index full render (hero labels, chips, pills, movement/recent/categories sections, drill links incl. `bank-reconciliation.index`), movement math (opening_balance 1000 + pre-period JE 100 → opening 1,100.00; in-period JE 250 → inflow 250.00 / closing 1,350.00), filter persistence (form value + export links carry `period=quarter`/`q=Main`), account filtering by q, recent-txn badge/type/link, empty state, CSV stream (`streamedContent()`), print + export-pdf render. Gotchas: `assertSee('value="quarter"')` needs the `false` unescaped arg (Blade `e()` escapes the needle to `&quot;`); `assertHeader('Content-Type', 'text/csv')` fails because Symfony appends `; charset=utf-8` — assert the full string. Laravel 12 `TestResponse::streamedContent()` (TestResponse.php:1892) captures the StreamedResponse body via an ob callback — safe for CSV asserts.
- **Verification**: `view:cache` clean; `npm run build` → `app-CDWXpYC9.css` (394.22 kB; `.cp-hero`/`.cp-suite`/`.cp-pills`/`.cp-badge--bank` confirmed in the bundle — the PREVIOUS bundle was stale per the phase-43 lesson); CashPositionTest 8 passed; ScopedSearchRenderSmokeTest 1 passed (147s). NOTE: the rails system was never located (grep `layouts|components`, `resources/js`, `UserPreference` empty; the phase-7 note references `rail_prefs`) — this page was built with zero rails markup as the spec required, but the app-wide rails exclusion hook remains an open question.

53. **Cash Position redesign — DESIGN 2 (`.cp2` suite, Aug 12, 2026)** — FULL REPLACEMENT of the phase-52 `.cp-*` implementation on the same `accounting.cash-position.{index,export-csv,export-pdf,print}` routes (no route/controller-math changes beyond what phase 52 already shipped). Presentation/markup only — the data model (period options, `lineSums`, `movementPerAccount` receipts/payments/transfers split, chips, bars, recent, CSV/print via `renderPrint`) is untouched. **The phase-52 `.cp-*` suite is obsolete and removed; the page now renders the teal executive Target Design (§3 exact CSS, §4 exact markup).**
- **CSS**: phase-52 `.cp-*` block DELETED (app.css truncated at line 8519); new `.cp2` block appended (lines 8520–8940, source at `C:\Users\eseyama\AppData\Local\Temp\opencode\cp2-block.css`). ALL rules scoped under `.cp2` (verified NO bare global `.btn/.pill/.card/.wrap/...` classes exist in app.css, so scoping is required — an unscoped port would clobber 79+ global rules). New `:root` tokens: `--amber: #D97706` (aliases existing `--warn`) + `--shadow-cta` (aliases `--cta-btn-shadow`); `--red-2` already existed. Suite: `.wrap` max-width 1440px, `.page-head/.sub/.cluster` + `.btn/.btn-ghost/.btn-cta/.more/.more-menu` dropdown, `.pills` 10-up pill nav with `.on` aria-current, `.filterbar` (flex-wrap card: `.seg` segmented control + `select.f`/`input.f` 38px boxed + Apply/Clear + `.advlink` toggling `.advpanel` 4-col), `.hero-panel` (teal gradient, `.net` pill `+ $x.xx this period`, `.flow` grid `1fr auto 1fr auto 1fr auto 1.3fr` = Opening→+ Receipts→− Payments→Closing with 3 `.farrow` svgs, colored drill links `#7fe0c3`/`#ffb4b4`; ≤1000px collapses to 2-col + arrows hidden + Closing full-width), `.chips` ×3 with amber `warn` variant, `.card/.card-h/.n`, mist-gradient thead `linear-gradient(180deg,#F4F8F8,#E8F0F0)`, `.numr.neg`, `.acct-link/.drill`, `.mov` 2-col in/out bars (in = teal gradient, out = amber→red gradient, `.vl` 76px, max-denominator guard), responsive at 1100/1000/900/720/700 + reduced-motion. `.seg` got `flex-wrap:wrap` + smaller button padding ≤720px (probe measured `.seg` 378px > 365px viewport at 375px — fixed).
- **View** `index.blade.php` rewritten to exact §4 structure: `.cp2.wrap` → `.page-head` (h1 + sub `{period} · {from} – {to} · How much cash we hold, where, and what moved.` + cluster Reconcile/Transfer/New Cash Transaction/More dropdown) → 10 `.pill` nav (Cash Position active / Cash Receipts / Cash Payments / Bank Accounts / Cash Accounts / Transfers / Bank Reconciliation / Cash Forecast / Cash Flow Statement / General Ledger) → GET filter form (`#cp2-form`, `x-data="{ period, adv }"` seg + branch/account/currency selects + custom from/to + Apply/Clear + Advanced panel with q/source_module/status/cost_center/reconciled) → `.hero-panel` (net pill + 4-step flow + 3 chips) → Cash Position by Account card (`tbl-wrap` table Account/Opening/Receipts/Payments/Transfers In/Transfers Out/Closing + TOTAL tfoot, each account links `general-ledger.account?id&date_from&date_to`, "View ledger →") → Cash Movement card (Inflows/Outflows bars with relative fills) → Recent Cash Transactions card (Date/Reference mono/Description→JE show/Account→ledger/Debit/Credit/View, "Last N", "View all →" → general-ledger). Empty states: "No cash or bank accounts match the current filters.", "No receipts/payments in this period.", "No bank transactions in this period.". `$periodLabel` was computed but UNRENDERED (test gap) → now in the sub line. Hidden `period` input got a static `value="{{ $f['period'] }}"` so no-JS/server-side assertion works alongside Alpine `:value`.
- **Print** `print.blade.php` rewritten to the new row schema: 5 summary boxes (Period Opening / Receipts / Payments / Net Change / **Closing Balance** `.alt` teal) + table Account/Currency/Opening/Receipts/Payments/Transfers In/Transfers Out/Closing + `tr.total` tfoot, self-contained A4 with gradient header (`$company->name`), `onload="window.print()"`.
- **Rails exclusion — open question RESOLVED**: the hook IS `FavouritesService::metaForRoute($routeName)` in `layouts/app.blade.php:44` — when it returns null AND no `metaForRecord` match, `x-favourites.sidebar` renders nothing and `fav-float-toggle` is skipped. Removing `'accounting.cash-position.index' => ['cash-position', 'Cash Position', 'bank']` from `app/Services/FavouritesService.php` PAGES is the per-page opt-out; the page now carries ZERO rails/star markup. (`FavouritesTest` 13 passed — the PAGES-driven render test doesn't depend on cash-position.)
- **Tests** `tests/Feature/Accounting/CashPositionTest.php` rewritten to the new markup (8 / 81 assertions): full page (cp2, sub, cluster CTAs, 10 pills, hero flow steps, chips, `Cash Position by Account`/`Transfers In`/`View ledger`, movement Inflows/Outflows, recent + DEP-0001, reconciliation URL, `assertDontSee` empty-state), movement math (1,100.00/250.00/1,350.00 + `This Month`), filter persistence (`value="quarter"` on hidden period now matches via static `value`; export links carry `q=Main`/`period=quarter`), account restriction (q=Savings → `1 accounts` — `assertDontSee('Main Bank')` FAILS because the unfiltered account select dropdown legitimately lists it; assert the row count instead), recent txn debit/credit/link (WD-0001 mono, 150.00, JE show), empty state (3 empty rows), CSV (`TOTAL`/`Transfers In`/`Closing`, `text/csv; charset=utf-8` full string), print + export-pdf (`Period Opening`/`Closing Balance`).
- **Verification**: `php artisan view:cache` clean; `npm run build` → `app-CsyyKq70.css` (137 `.cp2` occurrences, ZERO `.cp-suite/.cp-wrap/.cp-hero/.cp-btn` in bundle); CashPositionTest 8 passed (81), FavouritesTest 13 passed (178). Headless Chrome (cp2-probe.js, 1440/1280/1024/768/375): h1/sub/10 pills/6 seg buttons/4 flow steps + 3 arrows/3 chips/3 account rows + TOTAL/4 bar rows/7 recent columns all render; `flow` grid 7-col at ≥1024px, 2-col + arrows hidden + Closing full-width at ≤1000px, advpanel 4→2→1-col; **page-level h-scroll false at every viewport** (wide items = pre-existing topbar row-2 nav overflow #41/#42 and the internally-scrolling `.tbl-wrap` table only); hero net/chips show live K data (`K4,000.00 Bank Balance`). Probe: `cb-probe/cp2-probe.js` → `cp2-probe-result.txt`.

54. **Cash Position follow-ups (Aug 12, 2026)** — three post-redesign fixes from the phase-53 review (commit `11564a7`):
- **Net pill moved into chips row**: the `+ K0.00 this period` pill (`.net`) was removed from the `.hero-panel` top (deleted the `.hero-top` wrapper + its dead CSS `.cp2 .hero-top`) and added as the FOURTH chip in the `.chips` row, after the Unreconciled `.chip.warn`. Live probe: `.net` parentElement = `.chips`, row renders `chip|K4,000.00 Bank Balance / chip|K0.00 Cash on Hand / chip warn|K0.00 Unreconciled / net|+ K0.00 this period`.
- **Advanced-filter Alpine scope fix**: `.advpanel` was a SIBLING of `.filterbar`, but the `x-data="{ period, adv }"` lived on `.filterbar` — `x-show="adv"` on the panel was out of scope so the Advanced toggle never opened. Hoisted `x-data="{ period: '...', adv: ... }"` onto the wrapping `<form id="cp2-form">`; `.filterbar` now carries NO x-data. Verified in headless Chrome: `form x-data` present, `filterbar x-data: NONE`, `.advlink` click flips `.advpanel` `display: none → grid`.
- **Period-aware ledger drill (server math)**: `GeneralLedgerController::account()` AND `exportPdf()` previously opened the account statement at the raw all-time `opening_balance`, so Cash Position's `general-ledger/{id}?date_from&date_to` drill showed a wrong opening when transactions predated the selected period. Both now compute a PRE-PERIOD opening when `date_from` is present: `JournalEntryLine::where(account_id)->whereHas(journalEntry, posted + date < date_from)` (honoring `branch_id`/`cost_center_id` filters), `opening = opening_balance + (isDebitNormal() ? preDebit - preCredit : preCredit - preDebit)` — the same directional math `CashPositionController` uses, so the statement's Opening/Closing match the page's Opening/Closing columns. Live probe: `/accounting/general-ledger/1?date_from=2026-08-01&date_to=2026-08-31` renders with the date fields pre-filled and a correct statement.
- **Tests** `tests/Feature/Accounting/CashPositionTest.php` 8→10: chips test now also asserts `View unreconciled` + `class="net` (unescaped); NEW `test_advanced_panel_lives_inside_filter_form_scope` (asserts `id="cp2-form" x-data="{ period:` in HTML, `.advpanel` + `x-show="adv"` are inside the `<form>` block via `strpos`/`substr` windowing, and `class="filterbar" x-data=` is ABSENT); NEW `test_ledger_drill_opens_period_aware_account_statement` (bank opening_balance 1000 + pre-period posted entry 100 (last month) + in-period 250 → statement asserts `1,100.00` opening / `250.00` debit / `1,350.00` closing, AND the Cash Position page's drill links carry `date_from=`/`date_to=`).
- **Verification**: `php -l` clean on the controller; `php artisan view:cache` clean; `npm run build` → `app-sockH9nh.css` (408.04 kB; `.cp2 .net` present, `hero-top` absent in bundle); suites green — CashPositionTest 10 passed, FavouritesTest + ListPageRenderTest + CostCenterTest 24 passed (224), ScopedSearchRenderSmokeTest 1 passed (144.76s). Headless probes: `cb-probe/cp2-fix-probe.js` (net pill in chips, form x-data hoist, filterbar x-data none, advpanel toggle none→grid, drill href carries `date_from&date_to`) + `cb-probe/cp2-ledger-probe.js` (statement renders with prefilled date fields). NOTE: the second fix-probe run FATALs on `Cannot navigate to invalid URL` — that is a probe bug (it prepended `BASE` to an already-absolute href); all assertions ran before it.