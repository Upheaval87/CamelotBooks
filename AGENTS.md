# AGENTS.md

## Commands
- Build: `npm run build` (compile Vite assets)

## State
### Completed redesign phases:
1. **CSS tokens** — `ink`/`line`/`panel`/`gold`/`brick`/`forest` color scales, serif/mono fonts, `max-w-8xl`
2. **App CSS** — component-layer: cards, buttons (4 variants), inputs, datasheets, modals, status pills, KPI cards, page titles
3. **Button component** — unified `x-button`, all old button components delegate to it
4. **Header overhaul** — all pages (~180) stripped old `<h2 class="font-semibold...">` wrapper from `<x-slot name="header">`; titles now plain text (rendered below topbar)
5. **Datasheets** — all tables (~178) converted to `datasheet` class
6. **Form layouts** — all forms (~160) use `card p-6`, `form-section-label`, `input mt-1` for selects
7. **Content widths** — all pages now use consistent `max-w-8xl mx-auto sm:px-6 lg:px-8` (was a mix of max-w-7xl/5xl/4xl/3xl/2xl/full)
8. **Show pages** — all 24 show pages converted: `card p-6`, `detail-grid`/`x-detail-field`, `kpi-card`/`kpi-label`/`kpi-value`, `x-record-toolbar`, `status-pill`, `datasheet`, `x-button`, entity names appended where appropriate (Customer Detail - Name, Employee Detail: Name)
9. **Invoice-form input styling** — shared `.input` CSS changed to bottom-border-only (no box/bg/radius), `.input-label` → 11px, `.btn-primary` → gold accent, `.btn-ghost` → permanent border, `.btn-remove` added
10. **Record page redesign** — all 35 show pages converted to new `x-record-toolbar` (#F3ECDC bg, grouped actions), `detail-grid` (3-column), `balance-grid`/`balance-total-row`, `x-detail-field` component
11. **Two-row topbar navigation** — replaced sidebar + single-row topnav with horizontal two-row bar. Row 1 (60px `bg-ink`): gold "L" brand mark + "CamelotBooks" + divider + company/branch (session-derived) + user avatar/name/role + logout. Row 2 (46px `#212B37`): Dashboard, Payments, Invoices, Customers, Journal, Reports + "···" overflow dropdown with all other section links (Inventory, Banking, Chart of Accounts, General Ledger, Trial Balance, Budgets, Fixed Assets, Payroll, Analytics, BI, POS, Settings, Users). Old sidebar CSS removed. Dark mode sidebar CSS removed.
12. **System Settings redesign** — left sidebar nav (3 groups, gold accent), bottom-border-only inputs, pill toggles, callouts, numbered eyebrow headers, status pills, inline audit-log diffs. Components: `resources/views/components/settings/` (sidebar, field, toggle, callout, table). CSS: `.settings-*` styles in `app.css`. 9 tab partials (`_tab-*.blade.php`) plus backups and audit-log pages rewritten.
13. **List page redesign** — applied customer-list design to all 7 entity list pages (Customers, Vendors, Products, Employees, Invoices, Bills, Sales Receipts). Shared components: `resources/views/components/list/` (quick-links, icon-button, avatar-initials, filter-bar, header). CSS: `.list-*` styles in `app.css` (sidebar layout, warm-header table, avatar initials, icon buttons with tooltips, mobile card rows below 720px). Pattern: italic serif title + Create button header, sticky 240px quick-links sidebar, underline-style filter bar, warm-header table with avatar initials for person entities, right-aligned bold numbers, status pills, icon-only action buttons with hover tooltips, mobile card rows.
14. **Currency symbol moved to column headers** — all 8 core financial report views (~28 files): replaced `format_money()` → `format_number()` in every amount cell; added `({{ $cs }})` suffix to column headers where missing (Inventory Valuation, AR/AP Aging); stripped redundant `{{ $cs }}` prefix from subtotals/totals in print views (Income Statement, Balance Sheet, Cash Flow). Symbol now appears ONLY in column headers, never on individual amounts.
15. **Two-mode search system** — Mode 1 scoped inline search (`<x-scoped-search-field>` + `scopedSearchFieldHtml()` in `resources/js/scoped-search-field.js`, server mode queries `accounting.search.{entity}`, client mode for static lists, barcode/SKU exact-match auto-select, emits `item-selected`) retrofitted into every picker: 11 product line-item pickers (invoices, purchase-orders, purchase-requisitions, quotations create/edit, GRN, credit-notes, sales-receipts), customer pickers on 7 forms, POS checkout product search. Mode 2 global search modal (`<x-global-search-modal>` in `layouts/app.blade.php`, Alpine data in `resources/js/global-search-modal.js`, groups results from `accounting.search.global`, keyboard nav + Ctrl+K, `open-global-search` window event). Topbar Row 1 search button added. All 7 list/index pages now show a boxed scoped-search-style search (`.list-filter-search` → `.scoped-search-field`) with a "search across all records" button that opens the global search modal (shared `components/list-filter-bar.blade.php`; dead duplicate `components/list/filter-bar.blade.php` deleted). Modal opened from a scoped field is scoped to that entity (gold chip to expand to all) and SELECTS into the field via `global-search-selected` (field-ref targeted, full row passed through so line-item autofill works); opened from topbar/Ctrl+K it navigates to the record. POS listens and calls `selectProduct`. Legacy `searchable-select`/`advanced-search-modal` components deleted. Later expansion batch: entity pickers → `x-scoped-search-field` with `old()`/request precedence for the label via `->firstWhere('id', (int) ...)`: vendor pickers (bills/expenses/fixed-assets create+edit, GRN create, landed-costs create, vendor-payments create w/ `onVendorSelected`→`loadBills()` reading `input[name="vendor_id"]`, vendor-credits create, PO create+edit), bank-account pickers (settlements/deposits/cheques create, expenses create+edit, customer-payments create, vendor-payments create, petty-cash show ×2, payroll-runs show ×3), asset pickers (asset-usage index, asset-transfers/revaluations/impairments/disposals create), report pickers (customer-statement customer, vendor-statement vendor, payslip-report employee + payroll-run, stock-movement product+branch), index-filter pickers (accounts/credit-notes/quotations/vendor-credits index search boxes, expenses index vendor, general-ledger index account, cheques index+register bank, audit-log index user, bank-reconciliation index bank w/ query-selector JS). Hidden input keeps `name` so GET filter forms submit normally; `required` preserved where the original select was required. Final sweep batch (Aug 3, 2026): account pickers on products create/edit (`income_account_id`/`expense_account_id`), item-categories create/edit (3 default accounts), accounts create/edit `parent_id` (removed `filterParentAccounts()`/`parentSelect` JS), cheques create `debit_account_id`, bank-accounts transfer (`from_account_id`/`to_account_id` → bank-account) + manual-transaction (`debit_account_id`/`credit_account_id`), petty-cash show `debit_account_id`, asset-transfers create (`from_cost_center_id`/`to_cost_center_id`), asset-categories create/edit (all 7 account pickers), POS payment-methods (clearing + settlement — settlement is an `accounts` FK validated `exists:accounts`, so `entity="account"` NOT bank-account; label from `$accounts`), landed-costs create components section rewritten from Alpine to Blade server rows using `scopedSearchFieldHtml({name: 'components[idx][payee_account_id]', entity: 'account', ...})` with `componentIndex = {{ count($oldComponents) }}` and `ACCOUNT_SEARCH_URL`. Dynamic line rows on invoices/create: `income_account_id`/`cost_center_id` via `scopedSearchFieldHtml` + `ACCOUNT_SEARCH_URL`/`COST_CENTER_SEARCH_URL`, product-selected autofills account via `scopedSearchFieldSet()`. Remaining filters: budget-vs-actual-trend `fiscal_year_id` (fiscal-year), audit-log `user_id` (user), assemblies history `assembly_product_id` (product), POS checkout customer (server picker + `window.posCustomerSelected`/`clearPosCustomer` dispatching `pos-customer-selected` → Alpine sets `customerId`), POS returns create `pos_sale_id` (client-mode with `window.posReturnSaleSelected` → `pos-return-sale-selected` event → `loadSale()`), system-settings `_tab-accounts.blade.php` default-account-mapping selects (looped `availableKeys()`, each `name="{{ $key }}"` → `entity="account"`). Client-mode convention for small reference lists: `mode="client"`, `:items="$collection->map(...['id','label','subtitle'])`, `:value`, `:label`, `placeholder`. Component `entity` prop now defaults to `''` so client-mode calls without entity don't error (caught by smoke test — GRN create crashed on `Undefined variable $entity`). Left as native selects (not in SearchCatalog): assemblies create/unbuild `bom_id`, POS settlements `payment_method_id`, till-sessions/cashier-login `terminal_id`, admin permissions `role_id`, users edit `role`, sales-receipts Alpine payment method/provider selects, chart-of-accounts type, accounts index type, audit-log group, report `type`/`status`/`compare_mode`/`dimension`/`payment_terms`/`gender`/`frequency`/`depreciation_method_*`/`residual_value_type_*` enums. List-page search magnifier: `x-list-filter-bar` now accepts an `entity` prop; pages with a catalog entity (customers/vendors/products/employees) dispatch `open-global-search` with `entity` so the modal opens pre-scoped to that list (gold chip to expand to all), and `selectResult` in `global-search-modal.js` navigates when opened without a field ref (list/topbar) vs fills the picker field when a `_fieldRef` exists. SearchCatalog extended (Aug 3, 2026) with document entities `invoice`, `bill`, `sales-receipt`, `quotation`, `credit-note`, `vendor-credit` (each searches number + reference + related customer/vendor name, links to its show page). List pages now all entity-scoped: customers, vendors, products, employees, invoices, bills, sales-receipts (`x-list-filter-bar entity="..."`) plus accounts, credit-notes, quotations, vendor-credits (inline `.scoped-search-open` buttons dispatch with entity). Topbar/Ctrl+K remain unscoped global.

### Completed performance audit (Jul 30, 2026):
- **N+1 fixes (7 locations)**: CreditNoteController (added `'invoice'`), VendorCreditController (added `'bill'`), ItemCategoryController (added accounts), TrialBalanceController (replaced per-account SUM loop with single grouped query), BalanceSheetService (replaced per-account computeBalanceAsOf with single grouped query), InventoryValuationController (single cost-layer query for all products), VendorCentreService (getVendorSummary: 5 queries/vendor → 4 grouped; getVendorStats: 8 queries → 4 grouped)
- **Indexes migration**: `2026_07_30_200423_add_performance_indexes.php` — added indexes on purchase_orders, purchase_requisitions, goods_received_notes, landed_cost_vouchers, inventory_adjustments, inventory_transfers, inventory_cost_layers, stock_counts, budgets, cheques, journal_entry_lines
- **Unbounded queries**: Added `->limit(100)` to CustomerPaymentController::create() and VendorPaymentController::create() open invoice/bill queries
- **Async mail queueing**: Added `implements ShouldQueue` to all 4 mailables (SalesReceiptMail, QuotationMail, PayslipMail, ExecutiveDigestMail); changed `->send()` → `->queue()` in controllers/commands
- **Other findings**: EncryptedPayslipService generates PDF synchronously with mPDF (blocking); QUEUE_CONNECTION=sync (needs redis/beanstalkd in production); SESSION_DRIVER=database, CACHE_STORE=database (both can switch to file/redis); jobs table migration already exists; `optimize-autoloader: true` in composer.json

### Render-verification bug fixes (Aug 3, 2026)
Found via `ScopedSearchRenderSmokeTest` (renders every converted route as company_admin with features enabled); all were pre-existing in HEAD:
- `AssetTransferController` — `Branch::...->active()` → `where('is_active', true)` (Branch has no `active()` scope; Asset/Account do).
- `CustomerStatementController` + `VendorStatementController` — service called with wrong arg order (`generate($companyId, $dateFrom, $dateTo, $customerId)` vs signature `(companyId, customerId, dateFrom, dateTo)`), no empty state when no entity selected, and never passed `$customer`/`$vendor` to the view.
- `PayslipReportController` — missing `$runsList`/`$employeesList` view vars (added); view `$total_net_pay` undefined (now computed inline from `$items`); payroll-run select converted to `<x-scoped-search-field entity="payroll-run">`.
- `stock-movement.blade.php` — file was mangled (duplicated body + broken `@php` missing `'$`); service returns arrays (`product_name`, `sku`, `quantity`, `running_qty`) not objects, and `reconciliation` is a per-product list (`running_qty`/`on_hand`/`variance`), not `opening`/`net_movement`/`closing`.
- `bank-reconciliation/index.blade.php` — stray `</x-slot>` after an inline-closed header slot → moved toolbar div into the `<x-slot name="header">` wrapper.
- `payroll-runs/show.blade.php` — `$color` undefined (inline status map added); dead routes `payroll-runs.print-payslips` + `payroll-runs.print` → `payroll-runs.payslips`; pay-employee modal posted to `{employeeId}` route with the payroll-run-item id → button now passes `$item->employee_id`, Alpine state holds `employeeId`, form `:action` builds `/accounting/payroll-runs/{run}/pay-employee/{employeeId}`.
- `petty-cash/show.blade.php` — removed dead `petty-cash.print` quick-action link (no such route/view exists).
- `invoices/show.blade.php` + `bills/show.blade.php` — `@if` directive embedded inside a component `:groups="[...]"` attribute expression (Blade leaves it verbatim → invalid PHP at render, missed by `view:cache` since it doesn't lint). Fixed by precomputing `$invoiceInsightLinks`/`$billInsightLinks` in a `@php` block. Rule: NEVER put Blade directives inside component attribute expressions (`:prop="[ @if ... ]"`); build the array in `@php` first. Verify with: compile via `view()`, then `php -l` on the compiled output.

### Personal task list ("My Tasks", Aug 4, 2026)
- **`todo_tasks` table** — migration `2026_08_04_000001`. Personal + company-scoped: every query filters by **both** `company_id` AND `user_id` (`forCompany()`/`forUser()` scopes); `authorizeAccess()` in the controller 404s otherwise. No delegation/approval flow — a task is always owned by the creating user.
- **Deadlines**: store the end-of-period date (Sun 23:59:59 for week, last day for month, Dec 31 for year) + original `deadline_granularity` (`day|week|month|year`). `TodoTask::deadlineFor()` mirrors the JS chips in `resources/js/todo.js` (`todoDeadline` Alpine data, hidden inputs `<name>_granularity`/`<name>_date`).
- **Buckets** — `TodoTask::bucketKey()` shared by the controller's index grouping AND the dashboard counts so they always agree: `overdue`, `today`, `this_month`, `this_year`, `no_deadline`. View: `resources/views/todo/index.blade.php` (+ `_deadline-chips`, `_task-row`, `_task-row-completed`), CSS `.todo-*` in `app.css`, Alpine in `resources/js/todo.js` (registered in `app.js`).
- **Task detail modal** — a SINGLE shared `<x-modal name="task-detail">` rendered at page level in `index.blade.php`, OUTSIDE the `.todo-list` card. `.todo-list { overflow: hidden }` clips `fixed` descendants, so never nest modals inside it. Rows dispatch `open-task-detail` (window event with task data) → `todoDetailModal()` Alpine data (`todo.js`) populates state and opens the modal. Modal form posts via `:action="updateUrl"`, delete via `:action="deleteUrl"`. Deadline chips re-initialize per task via `:key="taskId"`. `@item-selected` on the modal body updates the link via `onLinkSelected`. **IMPORTANT — the containing-block bug**: `layouts/app.blade.php` wraps every page in `<div class="animate-fade-in-up">`. Its animation used `both` fill-mode and the `fadeInUp`/`scaleIn` keyframes end with `transform: translateY(0)`/`scale(1)` — even a "no-op" transform is a non-none `transform`, so the wrapper stays the containing block for ALL `fixed` descendants, breaking backdrop coverage (`fixed inset-0`) and centering (`fixed left-1/2 top-1/2`) of every modal app-wide. Fixed in `app.css`: `.animate-fade-in`/`.animate-fade-in-up`/`.animate-scale-in` fill mode changed `both` → `backwards` so no transform is retained after the animation ends. Do NOT re-add `both`/`forwards` to an animation whose 100% keyframe sets a transform, and NEVER rely on `x-teleport` to dodge this (teleported content with a nested `x-data`/component scope is fragile — `global-search-modal` uses it because its markup is self-contained, but a scoped component inside a teleport template breaks Alpine scope resolution). **No nested forms**: the delete `<form id="task-delete-form">` is a sibling AFTER the update form; the Delete button uses `type="submit" form="task-delete-form"` (a button outside a form can submit it by `id`). Browsers drop nested `<form>` tags, so a nested delete form's hidden `_method=DELETE` becomes part of the outer form and the submit deletes the task instead of updating it. **Link-resolution fix (Aug 4, 2026)**: `todoResolveLinkFromEvent()` reads the entity key as `item.groupKey || item.entity || item.type`. Global-search rows (selected via the magnifier) carry `type` (the catalog key) but NOT `entity`/`groupKey` — `selectResult(row)` passes the raw row from `group.results`, not the `flatRows` version. Before the fix, `entityKey` fell through to `''`, `linkableType` was empty, and `TodoTaskController::normalizeLink()` wiped the link on save (task kept its label visually until reload, then "No record linked"). Verified live in headless Chrome: inline dropdown + global-search magnifier both populate `linkable_type` and persist after save+reopen; modal Delete works.
- **Record links are snapshots**: `linkable_type`/`linkable_id`/`link_label`/`link_url` all stored; `LINKABLE_CLASS_MAP` maps the SearchCatalog entity key → model class (validation restricts `linkable_type` via `Rule::in(array_values(...))`). Deleting the linked record leaves a muted "no longer available" chip, no error. Wildcard pickers use `<x-scoped-search-field entity="" allow-global-search search-url="route('accounting.search.any')">` — accepts selections of ANY entity (new `GET accounting/search/any` endpoint, `GlobalSearchController::any()` merges `entity` into each row); `global-search-selected` handler + `window.TODO_LINKABLE_CLASS_MAP` (set in `index.blade.php`).
- **Routes**: `routes/web.php` `todo.*` group under `role_or_permission:system_admin|company_admin|accountant|approver|viewer|bookkeeper|cashier|auditor` (index/store/update/complete/reopen/destroy). NOTE: `TodoTaskController` must be imported at the top of `routes/web.php` (it's a `use App\Http\Controllers\...` import, not fully-qualified inline) — forgetting it yields "Target class [TodoTaskController] does not exist".
- **Dashboard widget**: `DashboardController::index` computes `$todoOverdue`/`$todoToday` (personal, `->get(['id','title','deadline_date','deadline_granularity'])`), dashboard.blade.php shows a "My Tasks" card; topbar overflow menu has a "My Tasks" link.
- **CSS tokens**: `app.css` now defines a `:root` block with `--ink`, `--ink-soft`, `--ink-muted`, `--gold`, `--gold-soft`, `--gold-line`, `--brick`, `--forest`, `--forest-soft`, `--font-serif`. These were previously USED by `.report-*`/`.todo-*` but never defined; the block fixes both.
- **Tests**: `tests/Feature/Accounting/TodoTaskTest.php` — personal isolation (same-company + cross-company 404s), CRUD + complete/reopen, bucket grouping, `deadlineFor()`/`bucketKey()` unit checks, link-snapshot survival after linked-record deletion, dashboard counts, validation (bad `linkable_type`, bad granularity/priority), auth redirect.

16. **Quick-actions sidebar redesign** — form/detail quick-actions now render the SAME `list-sidebar` visual language as list pages (icon squares, warm hover, dividers, mobile trigger). `components/form/quick-actions.blade.php` + `components/detail-quick-actions.blade.php` both delegate to a new shared partial `components/quick-actions/sidebar.blade.php` (prop `asideClass` picks `form-page-sidebar`/`detail-page-sidebar` wrapper). The partial keeps the iconMap key system (union of form + detail icon sets, incl. `statement`/`adjust`/`transfer`/`payslip`/`settings`/`attach`/`document`/`banknotes`/etc.) and adds optional `subtitle` per link. Dead `.form-sidebar-*`/`.detail-sidebar-*` CSS (plus dark variants) removed from `app.css`; wrapper classes `.form-page-sidebar`/`.detail-page-sidebar` (sticky 240px) retained. ScopedSearchRenderSmokeTest extended with `customers.create`/`customers.edit`/`customers.show`.

17. **Page-container padding reduced** — all 51 blade pages using `<div class="py-12">` page wrappers (plus report views with `max-w-7xl mx-auto py-12 px-4`) switched to `py-6` (24px top/bottom). Verified zero `py-12` remaining in `resources/views`. Pure Tailwind-class change, no asset rebuild needed. Performed via inline PowerShell bulk replace (script files blocked by execution policy; use `Get-ChildItem` + `[System.IO.File]::WriteAllText`, not `rg` piped relative paths).

### Key patterns:
- Page titles: `<x-slot name="header">{{ __('Title') }}</x-slot>` — renders as `<h1>` below the topbar (italic serif, `text-ink`)
- Show pages with entity: `{{ __('Customer Detail') }} - {{ $customer->name }}`
- Show pages with ref#: `{{ __('Invoice') }} #{{ $invoice->invoice_number }}`
- Record toolbar: `<x-record-toolbar>` with `.tr-group`/`.tr-group-label`/`.tr-item`/`.tr-save`/`.tr-archive`/`.tr-more` classes
- Detail grid: `<div class="detail-grid">` with `<x-detail-field label="...">value</x-detail-field>`
- Balance panel: `<div class="balance-grid">` + `<div class="balance-total-row">`
- Form inputs: `.input` class (bottom-border only), `<x-input-label>` (11px uppercase), `<x-text-input>`
- List pages: `<x-list-header>`, `<x-list-filter-bar>` (underline-style inputs/selects in a form), `<x-list-quick-links>` (sidebar with groups/links/icons), `<x-list-avatar-initials>` (initials in circle), `list-table-wrap` + `list-table` (warm-header `#EDE8DA`), `icon-btn` (28px icon buttons with tooltip on hover), `list-mobile-cards` (card view below 720px)
- Settings pages: `<x-settings.sidebar>` (sticky 220px nav with divider groups), `<x-settings.field>` (bottom-border label+value), `<x-settings.toggle>` (pill switch), `<x-settings.callout>` (info/warning block), `<x-settings.table>` (compact with mapping statuses)

18. **Inter-only font system (Aug 5, 2026)** — body font (Inter) applied to ALL on-screen text. `fontFamily.serif`/`fontFamily.mono` in `tailwind.config.js` now resolve to Inter (`sans` stack); `--font-serif` CSS token repointed to Inter. All `font-serif`/`font-mono` utility classes in 22 screen blade files bulk-replaced → `font-sans` (report `td` cells, textareas, `<code>`/kbd spans). Direct `font-family` declarations in `app.css` (`.settings-exit-code`, `.global-search-title`, `.global-search-kbd`, `.global-search-footer kbd`, `.avatar-initials`, `.list-numeric`, `.report-head .report-title`, `.report-line .code`, `.report-cell-code`) and `Chart.defaults.font.family` in `analytics.js` switched to `'Inter', system-ui, sans-serif`. Webfont link in `app.blade.php` trimmed to `inter:400,500,600,700` only. Print/PDF views untouched (POS thermal receipt, quotation/invoice/sales-receipt prints, `print-export.blade.php`, payroll paye/pension/payslip schedules, POS cashier login) — they keep Courier New/Arial/Figtree/Georgia per print styling. Unrouted `welcome.blade.php` (Laravel starter with embedded Tailwind v4 CSS) left as-is. Verified: `font-mono`/`font-sans`/`font-serif` utilities all emit `font-family:Inter,...`; `ScopedSearchRenderSmokeTest` passes.

19. **Multi-Tenancy Phase 1 — DB-per-tenant provisioning (Aug 5, 2026)** —
- `app/Services/Tenancy/CompanyProvisioningService.php`: `provision(Company, ?string $migrationPath)` → generate unique DB name (`acct_{slug}_xxxxxxxx`, checks information_schema + `companies.db_name`), `CREATE DATABASE` via the `provisioning` connection (config has `database => null`), register a runtime connection `tenant_{id}` (array-merged from `connections.mysql` + optional per-company creds), run `migrate --database tenant_{id} --path ...` (passes `--realpath` when the path is absolute), seed tenant defaults, seed central `modules`/`company_modules`, then mark `provisioning_status = active`, persist `db_name`/`provisioned_at`, return `$company->fresh()`. Compensating rollback on ANY `Throwable`: restore the central default connection FIRST, drop the created DB, mark `failed` + `last_provisioning_error`, rethrow. Guard: provisioning an already-active company throws.
  - **WARNING**: `migrate --database X` switches the app's default connection (`Migrator::setConnection()` → `DatabaseManager::setDefaultConnection()`). The service captures `DB::getDefaultConnection()` at entry and restores it in both the success and catch paths — otherwise the failed-status `$company->save()` would hit the (already-dropped) tenant DB and mask the original error.
- `app/Services/Tenancy/TenantDefaultsSeeder.php`: `seed(Company $company, string $connection, string $centralConnection)` — mirrors central `branches` (or seeds a Head Office row), writes tenant `companies`/`branches`, fiscal year, 12 accounting periods, full parent-linked chart of accounts, supplemental accounts, default account mappings, approval settings + thresholds, numbering sequences. Restores the default connection to `$centralConnection` in a `finally`.
- **Boot fix (pre-existing breakage)**: `bootstrap/app.php` called `->withMigrationPaths([...])`, a method that does NOT exist on Laravel 12's `ApplicationBuilder` — the app could not boot AT ALL (every `php artisan`/phpunit invocation crashed). Removed the call; the tenant migration dir is now registered via `AppServiceProvider::boot()` → `loadMigrationsFrom(database_path('migrations/tenant'))`. This is the supported way to add migration paths (default `database/migrations` is auto-registered; don't re-add it or the same files glob twice).
- **Central/tenant merge conflict**: central (`0001_01_01_000000` users, `2026_01_01_000010` companies, `2026_01_01_000020` branches) and the tenant bootstrap used to BOTH `create` those tables, so migrating the merged set failed with "table users already exists". Renamed `database/migrations/tenant/0000_01_01_000000_create_tenant_bootstrap_tables.php` → `2026_01_01_000025_create_tenant_bootstrap_tables.php`. In the merged central DB it now sorts after the central create migrations, so its `hasTable` guards make it a no-op (central full-schema tables already exist); in a fresh tenant-only DB it still runs first (before `accounts` @000050) to provide the minimal `companies`/`branches`/`users` the tenant schema and `TenantDefaultsSeeder` need. Existing tests depend on tenant tables (e.g. Spatie `permissions`) being in the central DB — do NOT remove `loadMigrationsFrom`.
- **Tests**: `tests/Feature/Tenancy/CompanyProvisioningTest.php` — happy path (creates a REAL MySQL tenant DB via fixtures `tests/Fixtures/TenancyMigrations/valid`, asserts probe/accounts/numbering_sequences tables, Head Office branch, chart of accounts, fiscal year/periods/approvals/modules, default connection restored to `sqlite`), rollback (fixture `tests/Fixtures/TenancyMigrations/broken` has a migration that `throw new RuntimeException('...must fail...')` → DB dropped (no new `acct_%` DBs) + company `failed` + error recorded + connection restored), already-active guard, `generateDatabaseName()` slugging. Skips via `markTestSkipped` if the `provisioning` connection is unreachable. Tenant DBs dropped in `tearDown`. Full suite after these changes: 582 tests / 1953 assertions OK (5 skipped).

20. **Multi-Tenancy Phase 2 — live-data migration to tenant DBs (Aug 5, 2026)** — copy an existing company's rows OUT of the shared DB into a fresh tenant DB. Command: `php artisan company:provision-and-migrate --company=<id> [--verify-only] [--yes]`.
- `app/Services/Tenancy/CompanyDataMigrator.php` — manifest-driven raw-SQL `INSERT…SELECT` copy that **preserves original primary keys** (so intra-company FKs survive). Copy order: `ALL_ROWS_TABLES` (Spatie `permissions`/`roles`/`role_has_permissions` — company-scoped via `company_id IS NULL`/explicit join) → `DIRECT_TABLES` (85 company-scoped tables, `WHERE company_id=?`) → `CHAIN_TABLES` (child/line tables with EXISTS-vs-parent predicates, `{source}` placeholder → backticked source DB) → `MEMBERSHIP_TABLES` (`user_favourites`/`user_preferences`, via `company_user.user_id`) → stub users. `assertSchemaParity()` throws `\RuntimeException('Schema drift')` listing every manifest table whose columns differ between source and tenant before copying. `sourceSelect()`/`sourceCount()` are the single source of truth for scoped row queries and are reused by the verifier so counts always agree. Entire copy wrapped in `SET FOREIGN_KEY_CHECKS=0`/`=1` (restored in a `finally`).
- **Stub users**: the tenant `users` table is `id`+timestamps only. The migrator seeds a stub row per user referenced by any copied row (via per-table `USER_FK_COLUMNS`, plus `model_has_roles.model_id`, `company_user.user_id`, `user_favourites`/`user_preferences`), `INSERT IGNORE` so no FKs ever dangle. No passwords/emails carried over (auth is a later phase).
- **`seedDefaults=false` on `CompanyProvisioningService::provision(Company, ?string $migrationPath = null, bool $seedDefaults = true)`**: `TenantDefaultsSeeder` seeds fiscal year/periods/chart/mappings/approvals **unconditionally** — running it before a real-data copy duplicates them. Migration mode passes `false`, which skips the seeder and restores the central default connection manually (the seeder's `finally` normally does it). New/user-created companies keep the seeder path. `registerConnection()` is now public (verify-only needs it without provisioning).
- `app/Services/Verification/CompanyMigrationVerifier.php` — `verify(Company, tenantConnection, sourceConnection)` returns `['passed' => bool, 'checks' => [...]]`. Check categories: per-manifest-table row counts (source via `sourceCount`, live shared DB — **shared DB is never modified at copy time** so counts remain comparable), `SUM_SPOT_CHECKS` aggregates (`accounts.opening_balance`, `invoices.amount`/`amount_paid`, `bills.amount`, `journal_entry_lines.debit`/`credit`; `journal_entries` deliberately excluded — it has no `amount` column), `stub_rows` (companies=1, users≥1), `trial_balance` (posted+reversed `journal_entry_lines` SUM(debit)=SUM(credit)), and 11 FK-orphan `LEFT JOIN` checks.
- `app/Console/Commands/CompanyProvisionAndMigrate.php` — flow: resolve company → **preflight** (ping `provisioning`; if `companies.provisioning_status` missing, run `migrate --force` for the 5 `2026_12_01_*` central migrations; `pruneStaleMigrationRecords()` deletes recorded migration rows whose file no longer exists on disk) → plan table (per-table row counts) → `--yes`/confirm → `provision($company, null, false)` → `migrate()` → "Rows copied" table → verification table → SUCCESS only if all checks pass. Any failure → `rollback()` (drop tenant DB via `provisioning`, company → `failed`, `db_name` null, `last_provisioning_error` truncated). `--verify-only` registers the runtime connection WITHOUT provisioning and **does not drop on failure** (tenant left for inspection).
- **Live-DB gotchas hit during the ACME run**: (1) the live `migrations` table had stale rows from renamed files (`2026_07_27_*` → `2026_11_09_*` quotations/sales_receipts/bank_transfer, and `2026_07_24_084804` cash_tendered), and the renamed `2026_11_09_000002`/`000003` were pending while their tables/columns already existed — fixed by deleting the 4 stale rows and `INSERT`-ing the 2 renamed names as applied (batch 31). (2) `--verify-only` previously crashed on the array-valued aggregate cells passed to the Symfony table renderer — coerced with `json_encode`. Both companies now `active`: ACME → `acct_acme_149593cc`, BETA → `acct_beta_0b50add8`.
- **Not in scope of this phase (later)**: runtime tenant connection routing (`db_name` switching), BI mart rebuild per tenant (`bi:refresh-data-mart` — `dim_*`/`fact_*`/`bi_refresh_log` are `REBUILT_TABLES`, never copied), auth/permission binding to tenant `users`, go-live/cutover flip, retention ≥90 days before dropping a source tenant DB.
- **Tests**: `tests/Feature/Tenancy/CompanyDataMigrationTest.php` — spins a real MySQL scratch DB (`migrator_source_xxxxxxxx`) migrated from the real tenant migrations, seeds representative rows (companies/users/branches/accounts/periods/customers/products/one posted JE pair/invoice+lines/Spatie grants/company_user/user_favourites+preferences), provisions with `seedDefaults=false`, runs `migrate()` + `verify()`, asserts ID preservation, row counts, stub users `[1,2]`, no duplicated defaults, central connection restored to `sqlite`, and that `assertSchemaParity()` catches a dropped column. Both tests pass (24 assertions); combined with Phase 1: 6 tenancy tests green.

21. **Multi-Tenancy Phase 3 — runtime tenant routing + two-step auth (Aug 5, 2026)** — the default connection NEVER flips to tenant; a session-scoped `tenant` connection is bound per request.
- **`app/Models/Concerns/TenantScoped.php`**: `getConnectionName()` → `TenantConnectionResolver::connectionName()` — returns `null` when unbound (models use default/central or test sqlite) and `'tenant'` when bound (or `tenancy.routing.connection_override` in tests). Applied via PowerShell patch to **116 model files** plus `App\Models\Permission`/`Role` (Spatie classes extended + trait; `config/permission.php` now imports `App\Models\Permission`/`Role`). Central models NOT trait-scoped: `User`, `Company`, `Module`, `CompanyModule`, `UserCompanyAssignment`, `CompanyAccessLog`. Before this, no model in the repo had a per-model connection override.
- **`app/Services/Tenancy/TenantConnectionResolver.php`** (container **singleton** via `AppServiceProvider::register()` — per-request bound state must survive container resolutions): `resolve(Company)` idempotently registers a runtime `tenant` connection (`Config::set("database.connections.tenant", ...)` merged from the base connection + company `db_host/db_port/db_username/db_password` (decrypted) + `db_name`, then `DB::purge('tenant')`), keyed by `(company_id, configHash)` so middleware runs every request without reconnecting. Throws `\RuntimeException` for non-provisioned/inactive/blank-`db_name` companies. `clear()` unbinds. `connectionOverride()` reads `config('tenancy.routing.connection_override')`.
- **`config/tenancy.php`**: `routing.base_connection` (`TENANT_ROUTING_BASE_CONNECTION`, default `mysql`), `routing.connection_override` (`TENANT_ROUTING_OVERRIDE`, `null` in prod), `audit.enabled` (`TENANT_ACCESS_AUDIT`, default true).
- **Test override**: `phpunit.xml` sets `TENANT_ROUTING_OVERRIDE=sqlite` — in tests the resolver does NOT register a real connection; bound tenant models resolve to the shared sqlite `:memory:` DB (RefreshDatabase merged schema), `Config('database.connections.tenant')` stays null. `TenantRoutingMySqlTest` sets it to `null` to exercise REAL MySQL routing (E2E: provision `tests/Fixtures/TenancyMigrations/valid`, bind, `Account::create` code `1400` — `1000` already seeded by `TenantDefaultsSeeder`'s 42-account chart — and asserts the row lands in the real tenant DB while the central `Company` model still reads sqlite).
- **`app/Http/Middleware/BindTenantConnection.php`** (alias `tenant.bind` in `bootstrap/app.php`; route group order is `tenant.bind` → `company.context` → `company.active`): company id = `session('current_company_id')` with legacy fallback to `$user->current_company_id` (persisted into the session); rejects (forget session + redirect `companies.index` w/ error) when the user lacks `hasAccessToCompany()` or the company is missing/inactive. **Legacy mode**: only PROVISIONED companies get the tenant connection bound — unprovisioned companies (pre-Phase-2 data still in the shared DB) pass through unbound. This is what keeps the ~600 pre-existing feature tests green (their fixtures create companies with `provisioning_status=pending`). `setPermissionsTeamId($company->id)` runs on every bound/legacy request.
- **Two-step auth** (`AuthenticatedSessionController::store()`): super admin → `panel.dashboard` (never bound, no access log); else `resolveCompanyForLogin()` → exactly 1 active `user_company_assignments` row (or 1 legacy `company_user` row) → `CompanyAccessService::enter(..., ACTION_LOGIN)` (bind-if-provisioned + session + `setPermissionsTeamId` + `company_access_logs` row if `audit.enabled`) → dashboard; multiple/none → `companies.index` picker. **Laravel 12 gotcha**: `redirect()->route(..., absolute: false)` throws `Unknown named parameter $absolute` (the `Redirector::route()` signature has no `$absolute` param — only the global `route()` helper does) — use `redirect(route('name', absolute: false))`.
- **Company entry** (`CompanyController::select()`, now `POST /companies/{id}/select`): 404 on forged/mismatched/nonexistent company ids; inactive → redirect w/ error; `CompanyAccessService::enter()` logs `support` for super admins (explicit logged support access) vs `select` for normal users. `index()` lists `activeCompanyAssignments()->with('company')` (falls back to legacy `company_user` pivot when empty). `store()` now attempts `CompanyProvisioningService::provision()`; on success auto-enters, on failure leaves `pending` ("Provisioning will complete shortly").
- **Assignment authority** (`app/Models/User.php`): `companyAssignments()`/`activeCompanyAssignments()` (central `user_company_assignments`, `is_active`), `hasAccessToCompany(int)` (active assignment OR legacy `company_user` row — back-compat; assignments are authoritative going forward), `accessibleCompanies()` (assignments with company, else legacy relation). `CompanyContext::userCompanies` shares `accessibleCompanies()`; **the `current_company_id` column fallback in `CompanyContext::handle()` is REQUIRED** — removing it broke 138 tests (`RbacTest`-style tests `actingAs()` + set the user column in `setUp`, never the session). The column is server-set-only so it stays trusted, and `BindTenantConnection` re-verifies on every request.
- **Access audit**: central `app/Models/CompanyAccessLog.php` (no `updated_at`, `UPDATED_AT = null`), migration `2026_12_01_000050_create_company_access_logs_table.php`, actions `login`/`select`/`support`. Also `app/Models/UserCompanyAssignment.php` (fillable user_id/company_id/role/branch_ids/is_active, casts branch_ids array).
- **Panel placeholder (Phase 4)**: `GET /panel` (`panel.dashboard`, OUTSIDE the tenant group) → `app/Http/Controllers/PanelController.php` + `resources/views/panel/index.blade.php` listing `$user->accessibleCompanies()` with POST switch forms. Super admins land here at login and are never bound until they explicitly select a company (`support` log).
- **Tests**: `TenantConnectionResolverTest` (10: null unbound, config building, bind/idempotent/clear, throws, `resolveForCompanyId`, override routing), `CompanyAccessFlowTest` (13: single-assignment auto-select, multi/no-assignment → picker, super-admin → panel + support log, forged/mismatched ids 404, switch-without-re-login A/B, **legacy mode for unprovisioned companies** on select + middleware, deactivated-assignment + middleware rejection, bound models resolve to override connection), `TenantRoutingMySqlTest` (2 E2E). 31 tenancy tests / 164 assertions green (incl. Phase 1/2). Pre-existing test adjustments: `AuthenticationTest` no-assignment login now asserts `companies.index`; the 2 POS isolation tests attach the acting user to the "other" company so the middleware passes and the controller's `abort_unless(resource->company_id === session company, 403)` still fires. Full suite: **604 passed / 5 skipped / 0 failed (2086 assertions)**.
- **Not in scope (later)**: super-admin Panel UI (Phase 4), auth/permission binding to tenant `users`, tenant routing for queue/jobs, BI mart rebuild per tenant.

22. **Multi-Tenancy Phase 4 — Super Admin panel + central feature management + central audit (Aug 5, 2026)** — `/superadmin` group OUTSIDE the tenant group, guarded by the `superadmin` middleware on the central `is_super_admin` flag (+ `is_active`); the default connection is never flipped and no tenant connection is bound while browsing it.
- **Feature management is now central (ask #1)**: `app/Services/FeatureManagement.php` rewritten to read/write central `company_modules` (`Module::where('code', ...)` lookup) instead of tenant `system_settings` `features` rows — `isEnabled()` = `CompanyModule?->is_active === true` (missing row = OFF), `enable()/disable($companyId, $feature, ?int $actorId = null)` upsert with `activated_at`/`activated_by`. Old tenant `system_settings` feature rows are inert. Tenant System Settings Features page (`resources/views/system-settings/features.blade.php`) is now READ-ONLY (status pills + "controlled by your system administrator" callout; Action column + form removed); `SettingsController::featuresToggle()` deleted; `system-settings.features.toggle` route removed. Core modules (accounting/sales/reports/system/users, `modules.is_core`) are always-on/locked; the 10 feature modules default off and toggle via the panel. Existing tests that enabled features via `SystemSetting::setValue('features', ...)` were switched to `FeatureManagement::enable/disable` (RbacTest ×2, GlobalSearchTest ×2).
- **Central audit log (ask #4)**: `super_admin_audit_logs` (migration `2026_12_02_000010`) + `app/Models/SuperAdminAuditLog.php` — **deliberately NOT TenantScoped** so it always writes to the central DB even while a tenant is bound; genuinely separate from each tenant's `audit_logs` (which IS TenantScoped). `UPDATED_AT = null`, `log($actorId, $action, $companyId, $targetType, $targetId, $before, $after, $description)`; action constants `company.created/suspended/reactivated/provision_failed`, `module.enabled/disabled`, `user.created/updated/deactivated/reactivated/password_reset`, `assignment.created/updated/deleted`.
- **Users**: migration `2026_12_02_000020` adds `users.is_active` (boolean default true); `User` `$fillable`+cast; `UserFactory` sets `'is_active' => true` (without it a factory-created model's `is_active` is NULL → falsy → the new middleware 403s every superadmin test). Login blocks deactivated accounts (pre-auth email lookup → back with errors) and super admins now redirect to `superadmin.dashboard` (was `panel.dashboard`; `CompanyAccessFlowTest` assertion updated). `Admin\UserController` untouched.
- **Middleware**: `app/Http/Middleware/SuperAdmin.php` (abort 403 unless central `is_super_admin` AND `is_active`), alias `superadmin` in `bootstrap/app.php`.
- **Controllers** (`app/Http/Controllers/SuperAdmin/`): `DashboardController` (KPIs + recent central audit), `CompaniesController` (index with `withCount` active_modules/assignments; create+`store` calls the REAL `CompanyProvisioningService::provision()` inside try/catch — success → show, failure → `company.provision_failed` audit + error flash; suspend/reactivate; show with provisioning-status pill/error banner/assigned users/active modules/recent activity; `modules` + `toggleModule` with core locked + audit; `dbPreview` GET returning `previewDatabaseName()`; `branches` GET returning `TenantBranchReader` result), `UsersController` (index/create/store/show/edit/update/deactivate/reactivate/resetPassword — self-deactivate blocked, reset clears `failed_login_attempts`/`locked_until` + sets `password_changed_at`), `AssignmentsController` (index paginated; create with `RoleCatalog::companyRoles()`; store takes `assignments[]` rows each `{company_id, role, branch_ids[]}`, upserts `UserCompanyAssignment` + syncs Spatie roles; edit loads branches from the tenant DB; update; destroy clears roles; private `syncCompanyRole()` swaps team via `getPermissionsTeamId()`/`setPermissionsTeamId()` + `syncRoles`), `AuditLogController` (central log, filterable by company/action).
- **Services**: `app/Services/SuperAdmin/RoleCatalog.php` — `companyRoles()` = config/permissions.php roles filtered to company scope (excludes `system_admin`), `isValidRole()`; validation uses `Rule::in(array_keys(...))`. `app/Services/SuperAdmin/TenantBranchReader.php` — `branchesFor(Company)` binds the tenant connection via `TenantConnectionResolver`, reads that company's `branches`, and **`clear()`s in a `finally`** so no tenant binding leaks into the superadmin request; returns [] for unprovisioned/inactive companies. `CompanyProvisioningService::previewDatabaseName()` (public) returns `acct_{slug}_{xxxxxxxx}` (x-padded) without a DB round-trip.
- **Views** (`resources/views/superadmin/`): shared `_nav.blade.php` horizontal tab bar (Overview/Companies/Users/Assignments/Audit Log) using `x-nav-link`; pages use `x-app-layout` + `.list-header`/`list-table`/`card`/`x-status-badge`/`x-detail-field` conventions. `companies/create` has an Alpine `x-data` block debouncing a fetch to `superadmin.db-preview` and showing the live tenant DB name before submit. `assignments/create` uses Alpine `x-data` rows (`template x-for`) with per-row company/role selects + branch checkboxes fetched from `superadmin.companies.branches` (`route(...['company'=>'__ID__']).replace(...)`), "+ Add Another Company Assignment", `:name="'assignments[' + index + '][...]'"` — checkbox `:value="branch.id"` (shared `value=""` + x-model breaks the id link). Topbar overflow menu gains a "Super Admin" link when `$user?->is_super_admin`.
- **Login test nuance**: in feature tests, after `actingAs($super)` a subsequent `$this->post('/login', ...)` is a no-op redirect because the user is already authenticated — call `Auth::logout()` first. Middleware `is_active` check must read a loaded attribute (`UserFactory` sets it).
- **Tests**: `tests/Feature/SuperAdmin/SuperAdminPanelTest.php` (23 tests: non-super 403 / guest redirect / all sections render / companies create success-via-mock + failure-fallback audit / suspend-reactivate / db-preview name / user CRUD + audit / deactivated-login block / self-deactivate blocked / password-reset unlock / assignment create+update+remove role-sync / role validation / catalog excludes system_admin / FeatureManagement central reads / module toggle+audit / core locked / modules screen / audit filterable / edit pages render / deactivated superadmin 403). `CompanyProvisioningService` mocked in tests (`$this->mock(...)->shouldReceive('provision')`) so no real MySQL is touched. Full suite after Phase 4: **626 passed / 5 skipped / 0 failed (2172 assertions)**.
- **Not in scope (later)**: auth/permission binding to tenant `users`, tenant routing for queue/jobs, BI mart rebuild per tenant, tenant-side feature setting rows cleanup.

23. **Multi-Tenancy Phase 5 — per-tenant BI data mart (Aug 5, 2026)** — the BI mart moved from the central shared DB into each tenant DB. `bi:refresh-data-mart` now loops provisioned companies, binds each tenant connection, and rebuilds dims/facts on the bound mart connection; `bi:send-digest` does the same for executive digest schedules.
- **`app/Services/BI/Concerns/MartConnection.php`** (new trait): `mart(): Connection` / `martTable(string $table): Builder` resolve via `DB::connection(TenantConnectionResolver::connectionName())`. Passing `null` to `DB::connection()` falls back to the default, so a single expression works for bound MySQL tenants, unbound contexts, and the sqlite test suite. Applied to all 5 fact builders, `DimSyncService`, `DimDateFiscalMapper`, `BiRefreshService`, and the 5 BI consumers (`CustomerLifetimeValueService`, `TrueTotalCostService`, `EmployeeProductivityService`, `BranchProfitabilityService`, `ExecutiveDigestService`). Consumer behavior is unchanged in web requests (no tenant bound → default connection).
- **Fact builders are now company-scoped**: every builder takes `build(?int $companyId = null)` and each source leg adds `->when($companyId, fn ($q) => $q->where('<table>.company_id', $companyId))`. In production each tenant DB holds one company (filter is a no-op); it makes the shared-DB sqlite test override actually isolate tenants. Back-compat: no-arg `->build()` (BiPosCoverageTest) still works.
- **`DimDateSeeder::run(?string $connection = null)`** now chunk-inserts the calendar in 500-row batches — the single ~2,190-row insert exceeded SQLite's 999-variable prepared-statement limit (surfaced by the new command test; MySQL would also have hit packet limits).
- **`app/Console/Commands/RefreshDataMart.php`** rewritten: `{--company=}` optional filter; selects `is_active` + `provisioning_status = active` companies; per company — `resolve()` binding, log `running` → truncate+seed `dim_date` → `DimSyncService->syncAll($company)` (note `dim_company` is central `Company` data, `syncCompanies($company)` filters to the current company, all other dims read/write the mart connection) → `DimDateFiscalMapper::mapFiscalYears()` → truncate+rebuild each fact via `build($company->id)` → log `completed` with `json_encode`d `rows_refreshed`; any throw marks log `failed` and rethrows (outer `handle()` catches, counts, returns `FAILURE`). Binding cleared in `finally`. `bi_refresh_log` is written on the mart connection (has `company_id`), and `rows_refreshed` is a JSON column so raw inserts need `json_encode` (Laravel's cast is not applied to query-builder inserts).
- **`app/Console/Commands/SendExecutiveDigest.php`**: `bi:send-digest {--dry-run}` — same per-company bind loop; for each company queries due `BiDigestSchedule` rows on the mart connection, `ExecutiveDigestService->collectForSchedule($schedule)`, queues `ExecutiveDigestMail` to `$schedule->recipients`, stamps `last_sent_at`; counts sent/failed and returns `FAILURE` if any failed. The `TenantConnectionResolver` singleton (`AppServiceProvider::register()`) is required so per-company bound state survives command/service container resolutions, and `clear()` in `finally` prevents leakage.
- **Tests**: `tests/Feature/BI/RefreshDataMartTenantTest.php` (4 tests) — full single-company rebuild (facts keyed by `company_key`, dims populated, `dim_date` seeded, completed log with JSON `rows_refreshed`, resolver unbound after run), company scoping (refreshing A leaves B untouched), unprovisioned-company skip → non-zero exit, refresh-all loop (both companies get completed logs). NOTE: with the sqlite routing override all tenants share one DB, so a later company's truncate wipes an earlier one's facts in the refresh-all test — asserted via logs, not rows. Full suite after Phase 5: **631 passed / 5 skipped / 0 failed (2204 assertions)**.
- **Not in scope (later)**: auth/permission binding to tenant `users`, tenant routing for queue/jobs, BI mart E2E on real MySQL (covered in sqlite-override + `TenantRoutingMySqlTest` separately), per-tenant digest scheduling beyond `last_sent_at`.

24. **Multi-Tenancy Phase 6 — Security & Hardening (Aug 6, 2026)** — forge-proof company context, `db_password` at-rest encryption hygiene, rate-limited + logged login/company-switch, per-tenant backups via a shared service, tenant audit-log verification, and explicit super-admin support sessions (actor/target/duration).
- **Shared backup service** (`app/Services/Admin/DatabaseBackupService.php`): one dump primitive parameterized by a connection config. `backupConfigFor(Company)` → `TenantConnectionResolver::connectionConfig()` (decrypted per-company MySQL config) when provisioned+`db_name`, else base `mysql` (legacy shared-DB mode). `backup(Company, ?userId, triggeredBy)` self-manages the tenant binding (saves `boundCompanyId()`, resolves when needed, restores prior binding/`clear()` in `finally`) so it works from both the HTTP controller (already bound) and the scheduler command (unbound); writes the `BackupLog` on the bound (tenant) connection with `success`/`failed` + `error_message`. Filename `backup_{Y-m-d_His}_company{id}.sql`.
- **WINDOWS GOTCHA (fixed)**: dumps used to be raw `exec()` strings with `escapeshellarg()`'d values — on Windows PHP runs through cmd.exe which passes single quotes through verbatim, so mysqldump got `--host='127.0.0.1'` → exit code 2, empty output. Both `dumpToFile()`/`restoreFromFile()` now use **Symfony `Process` with an array command** (proper per-arg quoting, no shell): dump captures SQL on stdout and `file_put_contents`'s it (no `>` redirection), restore streams the SQL file in as Process input (no `<` redirection). `errorLines()` extracts stderr. Binaries resolve via `config('database.backup.binary'/'restore_binary')` (env `DB_MYSQLDUMP`/`DB_MYSQL`) — tests resolve XAMPP/Program Files candidates at runtime.
- **`BackupController::trigger()`** refactored to `app(DatabaseBackupService::class)->backup($company, $user->id, 'manual')`, same flash behavior. **`app/Console/Commands/BackupDatabases.php`** (`backup:databases {--company=}`): loops `is_active` + `provisioning_status = active` companies, per-tenant BackupLog, per-company try/catch, FAILURE exit with summary (Windows Task Scheduler can invoke it per tenant, or pass `--company`).
- **`db_password` at rest**: cast was already `encrypted`; added `protected $hidden = ['db_password']` so the plaintext never leaves via `toArray()`/`json`/`Response` (decryption still works through the cast for `connectionConfig()`). Tests assert the stored raw value differs from plaintext and the show view never renders it.
- **Login logging** (`login_attempt_logs` migration `2026_12_03_000010` + central `app/Models/LoginAttemptLog.php`, immutable, `UPDATED_AT = null`, `record()` helper): `LoginRequest::authenticate()` logs `invalid_credentials` (user_id null), `success` (user_id set, `user_agent`), and `rate_limited` inside `ensureIsNotRateLimited()`. `AuthenticatedSessionController` also logs the deactivated-account rejection (`failure_reason = 'deactivated'`). Login was already rate-limited at 5/min (`LoginRequest` throttle), company-switch was logged but not throttled → **`routes/web.php`**: `companies.store` → `throttle:5,1`, `companies.select` → `throttle:20,1` (test sends 21 selects → 429).
- **Forge-proofing already existed** (`CompanyController::select()` 404s on forged ids + `BindTenantConnection` re-verifies `hasAccessToCompany()` every request server-side); new tests lock the behavior in (forged session company → middleware redirect + session clear; select without assignment → 404).
- **Support sessions** (`company_support_sessions` migration `2026_12_03_000020` + central `app/Models/CompanySupportSession.php`): `started_at`/`ended_at`/`ended_reason` (`logout`/`context_changed`/`super_admin_revoked`), `duration` = `started_at->diffForHumans($end, true)`, **both `UPDATED_AT = null` AND `CREATED_AT = null`** (table has no `created_at` column — `UPDATED_AT = null` alone still makes Eloquent insert `created_at` and fail). `CompanyAccessService::enter()` closes all open support sessions for the user first, then when `action === ACTION_SUPPORT` creates one; `closeOpenSupportSessions(User, string $reason): int`. `AuthenticatedSessionController::destroy()` closes them on logout. SuperAdmin `CompaniesController::show()` loads latest 10 + "Enter Company (Support)" POST form (visible when `is_active && db_name`) + Support Access Sessions table (admin/started/ended/duration/reason, "Ongoing" badge). Binding: a support session is active only while the super admin is actually bound to that tenant.
- **Tests**: `tests/Feature/Tenancy/SecurityHardeningTest.php` (10) — db_password at-rest encryption + not in `toArray()`/json; superadmin show never renders plaintext; forged-session middleware rejection; select-without-assignment 404; failed/success/deactivated login logging; switch rate-limit 429; support-session entry→switch (`context_changed`)→logout (`logout`) with duration; **backup→restore round trip on REAL MySQL** (skipped when `provisioning` unreachable): scratch source tenant migrated via tenant migrations (asserts `audit_logs` table exists → tenant audit logs land in the tenant DB), `backup()` via service, BackupLog row count in tenant DB, restore into a fresh DB, `CompanyMigrationVerifier::verify()` reconciles (asserts `passed`), resolver unbound afterward. Real-MySQL tests set `Config::set('tenancy.routing.connection_override', null)` and use the `provisioning` connection. Full suite after Phase 6: **646 passed / 5 skipped / 0 failed (2245 assertions)**.
- **Not in scope (later)**: auth/permission binding to tenant `users`, tenant routing for queue/jobs, BI mart rebuild per tenant, backup retention/rotation/pruning beyond the manual runbook.

25. **Currency catalog (Aug 6, 2026)** — central `currencies` table (migration `2026_12_03_000030` seeds 20 defaults incl. MWK as first, UTF-8 verified; `CurrencySeeder` is idempotent and wired into `DatabaseSeeder`). `App\Models\Currency` is deliberately NOT TenantScoped (global reference data); `label()` → `"CODE - Name"`, `active()`/`ordered()` scopes. Super Admin panel gains **Currencies** CRUD (`SuperAdmin\CurrenciesController`, nav tab, index/create/edit views, toggle, `currency.created/updated/toggled` central audit actions). All three hardcoded base-currency selects now iterate the catalog: `superadmin/companies/create` (defaults to MWK now), `companies/index` create modal, `system-settings/_tab-currency`. Empty-catalog fallback option added to each select. **Gotchas**: (1) a `@forelse($currencies as $currency)` loop in `_tab-currency.blade.php` SHADOWS the outer `$currency` SystemSetting array used later in the same view — name the loop var `$curOption`; (2) unique-index test fixtures must use codes NOT in the migration seed set (MWK/ZWL/XAF collide). Tests: `tests/Feature/SuperAdmin/CurrenciesPanelTest.php` (10). Suites green: SuperAdminPanel 23, AdminModule/CompanyAccessFlow/Auth/SecurityHardening 59.

26. **Company branch-limit enforcement (Aug 6, 2026)** — per-company `branch_limit`/`branch_count` on the CENTRAL `companies` row (migration `2026_08_06_000010`; `branch_limit` nullable unsigned = unlimited, `0` = blocked; `branch_count` unsigned cached count of ACTIVE branches). Creation is enforced in `app/Services/Accounting/BranchLimitService.php` for every actor incl. Company Managers (they keep the ability, the system enforces the count); `superadmin.companies.branch-limit` PATCH endpoint (Super Admin can set/clear the limit directly — exception path; normal limit raises go through a future billing flow). `NULL`/pre-existing companies are never blocked; migration does NOT backfill limits. Concurrency: `createBranch()` runs in a DB transaction with `Company::lockForUpdate()` on the CENTRAL row (works while branch rows live in tenant/shared DBs because every creator takes the same lock); live active-branch count is authoritative and the cached `branch_count` reconciles on every enforcement/usage read, so stale values self-heal. **Override is server-side only**: client may send `override=true`, but it only takes effect when `User::isSuperAdmin()` is verified; `assertCanCreate($company, $override, $actor)`. Blocked → `BranchLimitExceededException` (`error_code 'branch_limit_reached'`, payload `{success, error_code, message, branch_limit, branch_count}`) → JSON 422 or redirect w/ session errors. Per-creation **tenant audit** (`branch_audit_log`, singular table name — model MUST set `protected $table = 'branch_audit_log'`): branch_id, company_id, created_by_user_id (no FK — tenant users hold stub rows), created_by_role (`super_admin`/`company_manager`), was_override, branch_limit_at_creation, branch_count_at_creation, created_at (UPDATED_AT = null). **Gotchas**: (1) `branch_count_at_creation` must snapshot the count BEFORE `$locked->increment()` — `increment()` mutates the in-memory model, so reading it after yields post-create count; (2) a BLOCKED create rolls back the reconcile save, so the persisted cached count stays stale until the next successful create or `usage()` read (assert via `usage()`, not `fresh()->branch_count`); (3) tenant table name vs Eloquent plural — keep them consistent. Routes: branch writes (`branches.store/update/toggle`) now gated `role_or_permission:system_admin|company_admin` (were UNGUARDED before); `GET /branches/usage` → `{branch_limit, branch_count}` (binds tenant connection only when needed, clears after, so it works from both tenant and Super Admin contexts); `BranchController::update()/toggle()` call `authorizeScope()` (403 on forged cross-company branch id). `branch_limit` is a **required** field on both company-create forms (SuperAdmin `companies/store` + self-serve `companies/store`); `TenantDefaultsSeeder` reconciles central `branch_count` from the seeded Head Office branch at provisioning. Views: `branches/index.blade.php` (Alpine form, fetch usage, upgrade modal on `branch_limit_reached`, super-admin override checkbox), `superadmin/companies/show.blade.php` (usage badge + inline limit edit w/ Unlimited checkbox → empty input = NULL), `superadmin/companies/create.blade.php`, `companies/index.blade.php`. Audit action `company.branch_limit_updated`. Tests: `tests/Feature/BranchLimitTest.php` (17), SuperAdminPanelTest now 35 (added creation-requires-limit, negative rejection, endpoint set/clear/zero/negative/non-super, show renders usage).

27. **Branch Request → Quotation → Offline Payment → Fulfillment (Aug 6, 2026)** — company managers request extra branch capacity; Super Admins approve (issue a priced quotation) or reject; managers record offline payments (bank transfer / cheque / cash) that are NEVER auto-confirmed; only `system_admin`/`accountant`/`billing` staff can confirm, and confirmation is the ONLY path that raises `companies.branch_limit` (additively by quoted quantity).
- **Tables (tenant)** `database/migrations/tenant/2026_08_06_100001..100003`: `branch_requests`, `billing_quotations` (the `quotations` table name was already taken by sales), `payments` (NOTE: model `BranchPayment` MUST set `protected $table = 'payments'` or it pluralizes to `branch_payments` and every insert throws `no such table`). `*_by_user_id` columns are plain unsigned (no FK — tenant users are stub rows). Models: `BranchRequest` (statuses `pending_review|quoted|awaiting_payment|approved|rejected|cancelled|expired`, `quotation()` HasOne, `payments()` HasManyThrough), `BillingQuotation` (`pricing_breakdown` JSON array cast), `BranchPayment` (3 modes, `pending|confirmed|rejected`).
- **Flow** (`app/Services/BranchRequests/BranchRequestService.php`): `submit` (one open request/company guard via `OPEN_STATUSES`, error code `CODE_OPEN_REQUEST_EXISTS`); `approveAndQuote` (quote only from pending_review/quoted; total = server-calculated `unit_price × quantity` frozen in `pricing_breakdown` + DB columns; `billing_quotation` numbering via `NumberingSequenceService` `BQ-` prefix — added to `documentTypeLabels()` + `seedDefaults()`, raising the seed count 21→22); `reject` (pending_review only, reason stored in `admin_notes`); `recordPayment` (requires payable pending quotation; cash requires notes + accountant/billing/system-admin actor; partial amounts flagged in controller flash); `confirmPayment` (serialized on `Company::lockForUpdate()` on the CENTRAL row, `increment('branch_limit', quantity)` additively, request→approved + quotation→paid; double-confirm blocked `CODE_ALREADY_FULFILLED`; expired quotes never resurrected). `BranchRequestException` carries a stable `errorCode()`.
- **Pricing is swappable**: `BranchPricingService` interface → `FlatBranchPricingService` bound in `AppServiceProvider::register`; config `config/branch_requests.php` (`unit_price_per_branch` 5000, `tax_rate` 0, `validity_days` 14, `bank_reference_prefix` BRQ-).
- **Notifications** (queued mailables, no in-app channel): `BranchRequestSubmittedMail` (to `is_super_admin` users + `billing` role assignees + legacy pivot billing users via `BranchRequestNotifier`), `BranchQuotationIssuedMail` (requester), `BranchRequestRejectedMail` (requester), `BranchPaymentConfirmedMail` (requester — dispatched from `BranchPaymentController::confirm()`, not the service). **PHP gotcha**: heredoc `<<<HTML` interpolation does NOT support ternaries with string literals or function calls inside `{$...}` (ParseError "unexpected token ?") and Blade `@if()` in a heredoc renders as literal text — precompute every expression into a variable before the heredoc. `Company::users()`/`Company::modules()` were LATENT-BROKEN (missing `use Illuminate\Database\Eloquent\Relations\BelongsToMany;` → return-type TypeError `App\Models\BelongsToMany`) — fixed the import in `app/Models/Company.php`; the notifier calls `$request->company->users()` so this was the trigger.
- **Expiry** `app/Console/Commands/ExpireBranchQuotations.php` (`branch-quotations:expire {--company=}`, registered `->withSchedule()` daily in `bootstrap/app.php`): loops ALL active companies (NOT just provisioned — legacy shared-DB companies resolve to the default connection unbound, mirroring `BranchLimitService`); binds the tenant connection per provisioned company, `clear()` in `finally`.
- **Controllers**: tenant `BranchRequestController` (index/store/show/cancel; `authorizeScope` 403 on forged cross-company id; `canConfirmPayment`), `BranchPaymentController` (store/confirm/reject with `authorizeScope` + `authorizePayment` — the payment must belong to the request's quotation), `SuperAdmin\BranchRequestsController` (queue index across companies + show/approve/reject; `TenantConnectionResolver` bind for provisioned, `clear()` in `finally`). Tenant routes gated `role_or_permission` (store/cancel `system_admin|company_admin`, payments store `+accountant|billing`, confirm/reject `system_admin|accountant|billing`); new **`billing`** company role in `config/permissions.php` (quote/confirm + read-only customers/invoices/quotations/branches/audit/system-health); `viewer`/`auditor` got `branch-requests.view`; new `branch-requests` module actions `view|create|approve|reject|quote|confirm|cancel`.
- **Audit**: central `SuperAdminAuditLog` actions `branch_request.approved`, `branch_request.rejected`, `branch_payment.confirmed` (approved + confirmed both logged on fulfilment).
- **Views**: tenant `branch-requests/index` + `show` (quotation KPI cards, bank-reference callout, payment form, payments table with inline confirm/reject), superadmin `branch-requests/index` (cross-company queue) + `show` (approve/reject forms), `_status.blade.php` partial (`.status-pill`). Nav links in `topbar-two-row` Settings menu + `superadmin/_nav`; Branch Limit Reached modal links to `branch-requests.index`.
- **Tests**: `tests/Feature/BranchRequestTest.php` (16, `Mail::fake()` + seeded `RolePermissionSeeder`; use `assertQueued` NOT `assertSent` for ShouldQueue mailables; logout before asserting a "guest" redirect after `actingAs`). `AdminModuleTest` seed-count assertions bumped 21→22. Full suite: **696 passed / 5 skipped / 0 failed (2514 assertions)**.

28. **Tenant migrations on existing DBs (Aug 6, 2026)** — new tenant migrations added AFTER a company was provisioned do not exist in its tenant DB, producing `Base table or view not found` at runtime (e.g. `branch_requests`). Fix: `php artisan tenant:migrate [--company=<id>]` (`app/Console/Commands/MigrateTenantDatabases.php`) — loops active + provisioned companies with a `db_name`, binds each via `TenantConnectionResolver`, runs `migrate --database=tenant --realpath --path=database/migrations/tenant --force`, restores the default connection in a `finally` (`migrate` switches it), `clear()`s the binding. Verified: ACME (`acct_acme_149593cc`) + BETA (`acct_beta_0b50add8`) both got `branch_requests`/`billing_quotations`/`payments` without touching the central DB.

29. **Auth redesign + code-based password reset (Aug 6, 2026)** — the auth area (login/forgot/verify pages) now uses a shared split-panel `AuthLayout`, and the email-link password reset was replaced with a 6-digit verification-code flow.
- **Shared layout**: `LoginLayout` → `AuthLayout` (`app/View/Components/AuthLayout.php`, `resources/views/layouts/auth.blade.php`, old `layouts/login.blade.php` deleted). Props: `back-href` (shows "← Back to sign in" left, else a spacer), `title` (document title), `wide` (adds `.auth-login-card--wide` → `max-width:400px` for the OTP card; others 380px). Header row `justify-content: space-between`; help link `mailto:support@camelotbooks.com`. Hero panel (dark `#0E1526`, gold CB monogram, centered) is shared by construction. Login/forgot/verify all use `<x-auth-layout>`.
- **Code-based reset flow**: forgot-password now issues a 6-digit code emailed to the account instead of a reset link; the user lands on `/verify-code`, enters the code, and is forwarded to the SAME `reset-password/{token}` form (token minted server-side via `Password::broker()->createToken()`).
- **Central table** `verification_codes` (migration `2026_08_06_200001`): user_id FK → users (cascade), `code_hash` = SHA-256 of the code (plaintext never stored), `expires_at`, `used_at`, timestamps. Model `App\Models\VerificationCode` deliberately NOT TenantScoped (central users); `pending()` scope, `isValidFor()` constant-time `hash_equals`, `is_expired` accessor.
- **Service** `app/Services/Auth/PasswordResetCodeService.php`: `issue()` (deletes prior pending codes, `random_int(0, 999999)` padded to 6 → returns plaintext + `expires_at` for the mailer), `latestPending()`, `verify()` (marks used + wipes others on success), `cooldownEndsAt()` (resend gate = last code `created_at` + cooldown), `maskEmail()` (`jane@x.com` → `j***@x.com`). Config `config/verification_codes.php` (`code_length` 6, `ttl_seconds` 600, `resend_cooldown_seconds` 30).
- **Controller** `app/Http/Controllers/Auth/VerifyCodeController.php`: `show()` reads `session('password_reset_email')` (redirects to forgot when absent / no pending code); `verify()` validates `digits:6`, mints the broker token, returns JSON `{redirect}` or 302; `resend()` gates on cooldown (429 `{retry_after}`) then issues + mails a new code returning `{expires_at, resend_after}`; `cancel()` forgets the session email (fired by the "Back to sign in" click via `fetch keepalive`). Routes (guest group, throttled): `GET verify-code`, `POST verify-code` (`throttle:10,1`), `POST resend-code` (`throttle:3,1`), `POST verify-code/cancel` (`throttle:5,1`).
- **Enumeration safety kept**: `PasswordResetLinkController::store` responds identically whether or not the email exists; the session email is set ONLY when the account exists and never appears in a URL. New app `lang/en/passwords.php` overrides `passwords.sent` to "…verification code…". Mail is `App\Mail\VerificationCodeMail` (ShouldQueue, `Mail::to($user)->queue()`, inline-HTML brand template with the code in a dashed box; must include `expires_at`/`masked_email`). The Laravel `ResetPassword` email-link notification is no longer sent anywhere.
- **View `auth/verify-code.blade.php`**: eyebrow "Identity verification", serif "Enter verification code", masked-email subtext, live countdown from server-rendered `expires_at` (`aria-hidden` on the per-second value; expiry announced ONCE via `role="status"` when it hits zero → verify disabled + "Your code has expired" prompt), 6 square OTP boxes (`aspect-ratio:1`, `inputmode=numeric`, `maxlength=1`, per-box `aria-label="Digit N of 6"`, `autocomplete="one-time-code"`), full-width gold-deep "Verify and continue" button enabled only when all 6 filled, inline `auth-login-resend` button ("Resend it") that resets the countdown + cooldowns client-side. JS `resources/js/verify-code.js` (`Alpine.data('verifyCode')`): auto-advance on input, backspace/delete/arrow navigation, paste distributes digits from the focused box, submit via fetch (wrong code → server 422 → clears boxes + refocuses box 1, generic "That code isn't valid."), resend via fetch (429 `retry_after` re-arms the cooldown). Forgot page: subtext/button now say "verification code", confirmation box gained a full-width "Enter verification code" action (`auth-login-recovery-action`). CSS: `.auth-login-otp-*`, `.auth-login-expiry*`, `.auth-login-resend`, `.auth-login-card--wide`, `.auth-login-recovery-action`; `@media (max-width:420px)` tightens OTP gap → 6px + digit font 1.125rem. Form panel stays flat white (no pattern/lines).
- **Gotchas**: (1) `assertSessionHas`/`assertSessionMissing` live on the TestResponse, NOT `$this` (Laravel 9+ removed them from TestCase) — chain them on the response. (2) The reset token is in the URL PATH (`/reset-password/{token}?email=…`) — parse via `basename(parse_url($u, PHP_URL_PATH))`, not the query string. (3) The resend endpoint must queue its own `VerificationCodeMail` (easy to forget — the first draft returned `expires_at` without mailing). (4) `route('password.reset', [...], false)` returns a relative URL via the `route()` HELPER only (never `Redirector::route()` — see Phase 3).
- **Tests**: `tests/Feature/Auth/PasswordResetTest.php` rewritten (13 tests / 64 assertions): render, code requested (session email set + mail queued), enumeration neutrality (classic + JSON), masked-email/expiry/OTP markup, no-session/no-pending redirects, generic invalid-code 422, expired-code rejection (even when the code is correct), cooldown 429, resend invalidates old code + verifies with new, cancel clears the session email, and the full reset E2E (forgot → capture code from `Mail::queued()` → verify → extract token → reset form → login, asserts `Hash::check`). Auth folder (22) + CompanyAccessFlow + SecurityHardening all green. Live smoke-tested via `artisan serve`: POST forgot (known email) → 200 `{"status":"sent"}` + session → `/verify-code` renders masked `a***@test.com`/OTP row/countdown → verify with the real code from the mail log → 200 `{redirect:"/reset-password/{token}?email=…"}` → reset page 200. (reset-password form itself still renders on the older `x-guest-layout` — conversion to `AuthLayout` is a follow-up).

30. **"Set a new password" page + enforced password policy (Aug 6, 2026)** — the reset-password page now renders on `AuthLayout` (the phase-29 follow-up), and the password policy is now ENFORCED server-side on reset/change/registration with a live requirements checklist shared between the reset page and the in-app profile change-password card.
- **Policy service** `app/Services/Auth/PasswordPolicy.php`: builds `Illuminate\Validation\Rules\Password` from `SecuritySettingsController::getPasswordPolicy($companyId)` — `min()` + `mixedCase()` (when BOTH upper+lower required) or `rules()` with regexes for one-sided requirements, `numbers()`, `symbols()`. `Password::uppercase()/lowercase()` do NOT exist in this Laravel version (verified `vendor/laravel/framework/src/Illuminate/Validation/Rules/Password.php`: only `min/max/uncompromised/mixedCase/letters/numbers/symbols/rules`). API: `ruleFor(?int $companyId = null)` (validation rule) + `checklist(?int $companyId = null)` (`['key','label']` items matching exactly what `ruleFor` enforces). Global defaults when no company: min 8, upper, lower, number, no symbol.
- **Wired**: `NewPasswordController::create` passes `checklist()` to the reset view; `store` validates `ruleFor()`; `PasswordController::update` uses `ruleFor($request->user()->getActiveCompanyId())`; `RegisteredUserController::store` uses `ruleFor()`; `ProfileController::edit` passes `checklist($request->user()->getActiveCompanyId())`.
- **Invalid/expired token**: `NewPasswordController::store` now redirects to `route('password.request')` with `withErrors(['email' => __($status)])` instead of `back()->withInput()` — no dead-end reset form; `forgot-password.blade.php` gained the `$errors->get('email')` display block.
- **Shared partials** (plain `@include`, NOT `x-...` — `x-auth.partials.*` fails since they live in `views/auth/partials/`): `auth/partials/new-password-fields.blade.php` (Alpine `newPassword({ policy: {!! json_encode($policy, JSON_HEX_TAG|JSON_HEX_APOS|JSON_HEX_AMP|JSON_HEX_QUOT|JSON_UNESCAPED_UNICODE) !!}, prefix: '…' })` — @json would escape the en dash to `\u2013` and break tests; boxed `.password-input-boxed` with show/hide toggle, `autocomplete="new-password"`, password/confirmation errors, checklist `<ul id="{prefix}password-checklist" class="auth-login-password-checklist"><template x-for>` with `.is-met` indicators; props `policy`/`prefix`/`errorBag` ('' reset, 'updatePassword' profile)/`autofocus`) and `auth/partials/password-toggle.blade.php` (eye/eye-slash button bound to an `x-var`; also used for the current-password field).
- **JS** `resources/js/new-password.js` (`Alpine.data('newPassword', ({policy, prefix}) => …)`): per-key `met()`, `allMet`, `match`, `handleSubmit` blocks submit with localized errors when unmet/mismatch; registered in `app.js`.
- **Reset view** `resources/views/auth/reset-password.blade.php` rewritten on `<x-auth-layout title="Set a new password">` (no back-href): eyebrow "Security", serif "Set a new password", hidden `token`/`email`, fields partial, submit "Update password" `:disabled="submitting || !allMet || !match"`, footnote "Contact your administrator…". **Profile** `profile/partials/update-password-form.blade.php` rewritten on the same partial: current-password field with toggle + bag errors, fields partial (`prefix='update_password'`, bag `updatePassword`), submit `:disabled="submitting || !allMet"`; requires `$policy` (passed by `ProfileController::edit`).
- **CSS** in `resources/css/app.css` (shared, NOT `.auth-login`-scoped so the profile card works; `var()` fallbacks for non-auth scope): `--green:#067647` token; `.password-input-wrap` (relative); `.password-input-boxed` (flat white, 1.5px border, `padding:11px 42px 11px 13px`, gold focus — overrides bottom-border `.input`); `.password-input-toggle` (absolute right, 26px, 18px svg, `:focus-visible` gold); `.auth-login-password-checklist` (card `gap:9px`); `.auth-login-checklist-item/indicator/label` (18px circle, `.is-met` → green bg + white check svg — never color-only); `.password-form-submit` (gold-deep mirror of `.auth-login-submit`).
- **Tests**: `PasswordUpdateTest`/`RegistrationTest`/`PasswordResetTest` updated to `'New-password1'`; two new `PasswordResetTest` tests — checklist render (strings incl. "At least 8 characters", "One uppercase letter (A–Z)", "One lowercase letter (a–z)", "One number (0–9)", `auth-login-password-checklist`, `newPassword(`; asserts NO "Back to sign in") and invalid-token redirect to `password.request` with `email` error (token hash unchanged). Full suites: Auth + Profile **36 passed (144 assertions)**; SuperAdmin + AdminModule + Tenancy **118 passed (440 assertions)**. Live smoke via `artisan serve`: forgot → code from mail log → verify (needs `Accept: application/json` — `wantsJson()` gates the JSON redirect) → reset page renders checklist → weak password rejected 422 → valid reset → login with the new password lands on company picker; `/profile` renders current-password + checklist + toggle. NOTE: dev `accountant@test.com` password is now `Smoke-test2` from the smoke run. Follow-ups: SuperAdmin `UsersController` user creation still uses `Password::defaults()` (min 8, not the company policy); reset-password legacy `x-guest-layout` fully removed.

31. **Super Admin tabbar moved into the sticky topbar (Aug 6, 2026)** — `.sa-tabbar` is now a child of the sticky `<header class="topbar">` instead of being the first element of the page slot with a `position:sticky; top:var(--topbar-h,106px)` hardcode. Guarantees the tabs sit FLUSH below `topbar-row-2` (`gapFromRow2:0`) and scroll/stick in lockstep with the topbar as one unit (verified in headless Chrome: identical header/tabbar geometry at rest, after scroll, and back-to-top; header grows 106px → 162px only on `superadmin.*` routes). Mechanism: `layouts/topbar-two-row.blade.php` gains `@if(str_starts_with($routeName,'superadmin.'))` → `@php $saActive = explode('.', substr($routeName, strlen('superadmin.')))[0] ?? 'dashboard'; @endphp` → `@include('superadmin._nav', ['active'=>$saActive])` right before `</header>`. **Why not `@stack`/`@section`**: the topbar include (layouts/app.blade.php:35) renders BEFORE `{{ $slot }}` (:54), so slot-defined pushes/sections are never visible at the topbar — route-gating is the only ordering-safe injection. All 19 `@include('superadmin._nav', ...)` calls removed from superadmin views (dead-keys: none — `$saActive` derivation maps every `superadmin.{key}.*` route). CSS: `.sa-glass--top` lost `position:sticky/top/z-index/margin` (kept the hairline `box-shadow`); the `@media(min-width:1024px){.sa-glass--top{margin:-32px...}}` and `@media(max-width:720px){.sa-glass--top{top:0}}` rules deleted; `.sa-tabbar` reverted to full-width `padding:0 32px` (mockup shows a full-width glass bar, NOT a max-w-8xl-constrained one — do NOT add max-width/margin to it). `--topbar-h:106px` (app.css:19) still used by `.fav-sidebar` sticky top. The `.sa-glass--bottom` sticky action footer stays in the page slot (unchanged). Rebuilt assets (`npm run build`); `SuperAdminPanelTest`+`CurrenciesPanelTest` 45 passed / 163 assertions. Note: tabbar is NOT sticky-positioned itself anymore — it inherits stickiness from the parent header (topbar scrolls away if the header ever loses `sticky`).

32. **Review-design migration: shared `review` components + 6 decision pages (Aug 6, 2026)** — applied the Branch Request review design (page head + inline status badge, read-only detail card, decision card with footer actions, navy outcome strip for already-decided states) to every approval/review/decision page. Styling/layout only — no route, handler, payload, or conditional-rendering changes. `budgets/show` + `bills/show` explicitly out of scope (no rendered approve UI exists).
- **New components** `resources/views/components/review/*.blade.php`: `head` (H1 `text-2xl font-extrabold tracking-[-0.02em] text-gray-900`, `badge` slot, optional back link `h-10 rounded-xl border-shell bg-white/80 shadow-edit hover:-translate-y-px focus-visible:outline-gold-500`), `card` (`rounded-3xl bg-white/[.66] p-[26px] shadow-card backdrop-blur-[14px]`, navy icon tile `from-[#2e4763] to-[#22394f] text-[#e2c069]`, optional `action` slot for header badges), `field` (label `mb-1.5 text-[11px] font-bold uppercase tracking-[0.08em] text-slate-500`, value `text-sm font-semibold text-gray-900`, `mono` prop → `font-mono text-[13px] font-medium`), `badge` (variants `pending` gold / `approved` mint / `rejected` red / `neutral` / `accent` / `navy`; `dot` prop renders 7px dot), `decision` (icon-block header "Review & Decide", `fields` slot grid `mt-[22px] grid grid-cols-1 gap-x-6 gap-y-5 lg:grid-cols-2`, `actions` slot footer `mt-6 flex flex-wrap items-center justify-between gap-3.5 border-t border-line pt-[18px]`), `outcome` (`bg-gradient-to-r from-navy-700 via-navy-800 to-navy-900` + gold icon tile + mono gold chip `#e2c069`; `chip` + `tone` approved→check / rejected→X props), `btn` (variants `primary` gold gradient `shadow-new`, `reject` red outline, `ghost`, `edit`; sizes lg/md; default `type="button"`). Shared reason-textarea class: `min-h-[110px] mt-1 block w-full rounded-xl border-shell bg-white/80 focus:border-[rgba(182,145,63,.55)] focus:ring-[3px] focus:ring-[rgba(182,145,63,.15)] focus:outline-none`.
- **Design rules locked in**: single gold approve accent per page (submit-only states put the action INSIDE the decision card via hidden inline form + `form="id"` attribute on `<x-review.btn type="submit" form="...">`); reject = danger outline; required-reason error styling preserved; badge strip moved from legend cards below the toolbar into the page head; legend/quick-actions buttons removed from the left rail (Back replaced by head back-link); non-decision states (draft/submitted/pending) migrate without a decision card.
- **Migrated pages**: `superadmin/branch-requests/show` (reference page; quotation card → `x-review.card` with `action` badge slot, payments table badges mapped to approved/rejected/pending); `accounting/journal-entries/show` (`pending_approval` → decision card w/ inline approve form + reject-reason form, `posted` → APPROVED strip, `rejected` → REJECTED strip w/ reason, `reversed` → REVERSED strip; old `#rejectModal` removed; `$cs` var + print CSS preserved); `accounting/purchase-requisitions/show` (`submitted` → decision card, `approved`/`rejected` strips, Create PO + Back preserved in toolbar); `accounting/quotations/show` (`sent` → decision card, `accepted`/`declined`/`converted` strips, convert-to-invoice kept in toolbar); `accounting/payroll-runs/show` (`calculated` → decision card, `approved` → APPROVED strip w/ approvedByUser/approved_at, `posted`/`partially_paid`/`fully_paid` → POSTED strip, Post-to-GL + payslips/PAYE/pension action row preserved); `branch-requests/show` inline payment confirm/reject restyled to compact outline buttons (green confirm / red reject + red "Confirm Rejection", boxed reason input — no second gold accent since Record Payment already uses gold primary).
- **Edit gotcha**: the payroll-runs oldString failed twice because the file's `$statusColors` map had NO `'approved' => 'positive'` entry (the plan draft added it) — verify source bytes before large `oldString` blocks.
- **Verification**: all 6 views compile via `blade.compiler->compileString()` + `php -l` on compiled output (no syntax errors); `npm run build` emits all new tokens (`shadow-new`, `border-shell`, `navy-700`, `from-gold-500`, `bg-row`, `text-gold-700`). Full suite: **709 passed / 5 skipped / 0 failed (2599 assertions)**. Commit `5d72984` pushed to origin/main.

33. **Tenant UI standardization onto the superadmin reference design (Aug 7, 2026)** — Step 1–3 of the redesign: audited the reference, inventoried every page, extracted shared `x-ui.*` primitives from the reference, re-pointed the superadmin components onto them, and retargeted the shared tenant CSS classes so all ~330 tenant views inherit the reference visual language without per-page edits.
- **Docs**: `docs/DESIGN_SYSTEM.md` (page shell, cards, datasheets, badges, buttons, forms, detail pages, tokens, rules) + `docs/PAGE_INVENTORY.md` (Priority A core CRUD ~60 modules/~220 files, B admin ~12, C settings/POS/analytics ~60, D 39 report views; excluded print/auth/error views). `x-slot name="header"` in `layouts/app.blade.php` confirmed dead (0 pages use it) — titles render inline.
- **Shared primitives** `resources/views/components/ui/`: `page-head` (H1 `text-2xl font-extrabold tracking-[-0.02em] text-gray-900`, badge/description/action slots), `card` (`rounded-3xl bg-white/[.66] p-6 shadow-card backdrop-blur-[14px]`), `btn` (primary gold gradient `shadow-new` / edit soft-gold / ghost / danger; sizes lg/md), `badge` (active mint+dot / muted / warning / danger / accent core gold / navy; aliases positive/neutral), `th` (align left/right/center), `datasheet` (wrapper + optional `head` slot + `minWidth` default 960px).
- **Superadmin delegation**: `components/superadmin/{page-head,card,btn,badge,th}.blade.php` now thin delegators to `x-ui.*` (all props/slots pass through). Verified by `SuperAdminPanelTest` 35 passed — the reference pages now render through the shared primitives.
- **CSS retargets in `resources/css/app.css`** (single source of truth; legacy class names kept so no blade edits were needed):
  - `.list-table`/`.list-table-wrap` → navy gradient thead `#24384f→#182a3e→#132234`, `text-navy-200 #cdd7e2`, inset white/gold hairlines, `rounded-[12px] border-shell bg-row`, `px-5 py-[18px]` cells, hover row white, link hover `#96742c`.
  - `.card` → glass `rounded-3xl` + `shadow-card` + `backdrop-filter:blur(14px)`; `.kpi-label` uppercase 11px bold #64748b, `.kpi-value mt-1`.
  - `.datasheet` thead → navy gradient; `.datasheet-wrap` → `border-shell rounded-12 bg-row`.
  - `.pill-base`/`.status-pill` → `px-3 py-1.5 text-xs font-bold rounded-full shadow-badge border`; positive = `mint-100→mint-200` + `green-700` text, neutral/default = `gray-50→gray-100` + gray-500, negative = `red-50→red-100` + red-700. Legacy `.badge*` aliases kept.
  - `.btn-primary` → gold gradient `#b6913f→#96742c` + `shadow-new` + inset highlight; `.btn-ghost` → `border-gray-300 bg-white` hover gray; `.btn-danger` → `border-red-300 bg-white hover:bg-red-50`; `.btn-commit` kept gold outline.
  - `.input`, `textarea.input`, `.input-label` → boxed 40px `rounded-[12px]` `#e3e6eb` border, gold focus ring `0 0 0 3px rgba(182,145,63,.15)`; labels uppercase 11px bold #64748b. `.scoped-search-field` → same 40px boxed + gold focus ring.
  - `.detail-lbl`/`.detail-val` → reference field language (label `text-[11px] font-bold uppercase tracking-[0.08em] text-slate-500`, value `text-sm font-semibold text-gray-900`; bottom borders removed). `.record-toolbar` → glass `rounded-2xl bg-white/[.66] border-shell` + blur; `.tr-save` gold gradient; `.tr-item`/`.tr-more` slate `#374151` navy-tint hover; `.tr-archive` gray border.
  - `.list-header h1` → Inter extrabold non-italic `text-2xl tracking-[-0.02em] #111827`; `.list-header-create` → gold gradient `rounded-[12px]` + `shadow-new` + hover translateY/brightness; `.list-filter-input/-select` → `#d1d5db` underline gold focus; `.list-filter-clear` hover gold-soft. `.list-sidebar-*` → glass card `rounded-[20px] border-shell bg-white/[.66]`, label #64748b, icon squares navy-tint, hover navy 6%.
- **Verification**: `npm run build` clean; compiled CSS contains all retargeted gradients/shadows/tokens; `ScopedSearchRenderSmokeTest` passes (all converted routes render); targeted suites green: SuperAdminPanel+Currencies+BranchRequest 62 passed, SalesModule+ItemCategory+Cheque+Expense 35 passed. Full `php artisan test` exceeds the 10-min shell timeout (run in slices with `--filter`).
- **Step 4 (structural)**: shared tenant components now emit the reference primitives directly. `x-list-header` (used by 256 pages) delegates to `<x-ui.page-head>` — title renders the reference `text-2xl font-extrabold` H1 and the create action becomes the gold-gradient `<x-ui.btn>`; added optional `description` prop + default-slot passthrough for arbitrary actions (report-center search form, bank-reconciliation import/back buttons, companies modal-trigger button). Direct `.list-header` usages in `companies/index`, `panel/index`, `report-center/index`, `accounting/bank-reconciliation/index` migrated to `x-list-header`. Dead duplicate `components/list/{header,quick-links,icon-button,avatar-initials}.blade.php` deleted (live components are the root-level hyphenated `x-list-*`). Verified: `ScopedSearchRenderSmokeTest` passes; slices InvoiceService+FixedAsset+PettyCash+VendorCentre+Budget+CostCenter+GlobalSearch 68 passed, Assembly+LandedCost+StockCount+UOM+ItemCategory+Cheque+Expense 64 passed, AdminModule+BranchLimit+Favourites+PosSale+PosSettlement+PosTerminal 114 passed; `Blade::render` spot-check confirms the delegated output (page-head H1 + gold button).
- **Off-palette purge (Step 4 follow-up)**: audited all 125 blade files for legacy indigo/blue/purple classes (the only off-palette family — cyan/teal/violet/fuchsia/sky had zero hits) and bulk-swept 777 replacements to the reference tokens via PowerShell ordered `.Replace()` pairs. Mapping: form focus rings `focus:(ring|border)-(indigo|blue|purple)-500` → `focus:(ring|border)-gold-500` (both orders handled); link/action `text-indigo-600`/`text-blue-600` → `text-gold-700` with `hover:text-*-900` → `hover:text-gold-800`; solid buttons `bg-(indigo|blue|purple)-600` → `bg-gold-600` (+ `hover:bg-gold-700`/`active:bg-gold-800`); neutral info badges `bg-blue-100 text-blue-800` → `bg-gray-100 text-gray-600`; accent "Current"-style `bg-indigo-100 text-indigo-800` → `bg-gold-soft text-gold-800`; positive "Converted" pill `bg-purple-100 text-purple-800` → `bg-mint-100 text-green-700`; info callouts `bg-*-50 border border-*-200` → `bg-gold-soft border border-gold-line` (text → `text-gold-800`); decorative icon tiles `bg-indigo-100`/`bg-blue-100` → `bg-gold-soft`; nav active underline `border-indigo-400` → `border-gold-500`; nav dropdown current-company `bg-indigo-50 text-indigo-700` → `bg-gold-soft text-gold-800`; KPI numbers `text-indigo-700` → `text-gray-900`; bare `text-indigo-700/800/900` → gray/gold per context. **Deliberate keeps**: POS checkout payment-method selector buttons keep their functional color coding (Cash=emerald, Split=amber — both already on-palette; the off-palette Card/Mobile blue/purple fell under the bulk sweep and landed as gold-soft/gold-700, `text-blue-700`→`text-gray-900`, `text-purple-700`→`text-gold-700`), while the checkout's generic form controls (input focus rings, modal Proceed buttons, split checkboxes, balance figures `text-indigo-600`→`text-gold-700`) all went gold. Consolidated balance-sheet section headers kept semantic sign colors (Assets gold, Liabilities red, Equity gold after fixing a stray `text-green-700` from the Converted-pill mapping); consolidated income-statement keeps green Income / orange Expenses / red-green Net Income. Fixed-assets show keeps red (disposal) + yellow (impairment) danger/warning actions, transfer/revaluation → gold. Verified: zero remaining `(text|bg|border|ring|focus:*)` indigo/blue/purple/cyan/teal/violet/fuchsia/sky classes in `resources/views`; `npm run build` clean; `ScopedSearchRenderSmokeTest` + feature slices (70 + 101 tests) pass; the 3 context-sensitive files (POS checkout, 2 consolidated reports) compile via `blade.compiler` + `php -l`. NOTE: the sweep's `$exclude` array for POS checkout/consolidated reports used relative paths without the `resources\views\` prefix, so the exclusion was a no-op and those files were swept too — harmless, as verified above.
- **Not in scope (decision — Aug 7, 2026)**: the structural migration of Priority A pages to `x-ui.datasheet`/`x-ui.card` was DELIBERATELY DECLINED. The CSS retarget already renders every page (all 126 datasheet + 117 card files) in the reference language with zero per-page edits, and the `x-ui.datasheet` component is NOT a faithful replacement (it renders a bare `w-full border-collapse` table WITHOUT the `datasheet` class, so a migration would lose the navy-gradient thead `.datasheet thead` provides). `x-ui.*` primitives remain available for new pages; legacy pages keep their CSS-covered markup. Standardization considered complete.

34. **Centralised feedback system (Aug 7, 2026)** — replaced all legacy `confirm()/alert()/prompt()` call-sites, flash banners, and inline alert divs with a unified pixel-matched feedback layer (reference mockup saved at `docs/feedback-system.mockup.html`).
- **JS API** `resources/js/feedback.js` (registered in `app.js`): `window.feedback = { toast(type, title, message?, opts), openConfirm(config), openPrompt(config), alert(msg), confirm(msg, opts), prompt(msg, opts) }`; legacy-compat globals `fbConfirmSubmit(event, msg, opts)`, `fbConfirmButton(event, msg, opts)`, `fbPromptForm(event, msg, opts)`, `fbConfirmOnly(...)`; `window.atlasToast(msg, type)` kept as alias for `favourites.js`. Toast viewport `#fb-toast-viewport` (fixed right-5 top-5 z-120 max-w-380px, max 4 stacked, auto-dismiss 5s/errors 8s, pause-on-hover progress bar via rAF, `aria-live="polite"`/`role="alert"`). Confirm/prompt overlay `#fb-confirm-root` (fixed inset-0 z-130 blur backdrop, focus-trapped, initial focus Cancel, Esc/backdrop cancel, type-to-confirm gating, navy-head variant, promise-based). `fbConfirmSubmit`/`fbConfirmButton` wrap form/button confirms so `event.preventDefault()` + async handling replace the sync native dialogs. Flash emitter reads `#feedback-flashes[data-flashes]` on DOMContentLoaded (JSON `{type: msg}`).
- **Blade components** `resources/views/components/feedback/`: `alert.blade.php` (variant info|success|warning|error, inline SVG icon, `role="alert"` for warning/error, root merges `$attributes->class(['fb-alert', ...])` so call-sites pass spacing like `class="mb-4"` — `.fb-alert` CSS has NO default margin), `system-banner.blade.php` (navy gradient, Alpine dismiss, gold action link), `flashes.blade.php` (renders `<div id="feedback-flashes" data-flashes="{{ json }}" hidden>` ONLY when a flash exists; emits `success`/`error`/`warning`/`info` — **`status` deliberately EXCLUDED** because Laravel auth/profile set it to status-KEYS like `'profile-updated'`, not user-facing messages; those pages render their own inline status text). `x-feedback.confirm-modal` was implemented JS-only (no Blade file — `openConfirm`/`openPrompt` create DOM at runtime).
- **Layout wiring** `layouts/app.blade.php`: legacy `<div id="toast-container">` + inline `atlasToast` builder removed (kept one-line forwarder); added `<x-feedback.flashes />` + `<div id="feedback-confirm-root"></div>`.
- **Call-site migration (70 total)**: 48 form/button confirms → `fbConfirmSubmit`/`fbConfirmButton` (regex: `onsubmit="return confirm(` → `fbConfirmSubmit(event, `, `onclick="return confirm(` → `fbConfirmButton(event, `); special cases → `window.feedback.openConfirm({...})` (budgets/show lock, recurring-journals/show cancel, chart-of-accounts deactivate), `fbPromptForm(event, ...)` (sales-receipts/show, quotations/show), `openPrompt({...}).then(...)` (expenses/show void reason w/ confirmLabel), async `await openConfirm` + `window.feedback.alert` failure branches (bank-reconciliation/show matchLine/matchTransaction), `window.feedback.alert` + promise-wrapped confirms (journal-entries/create, recurring-journals create/edit). Verified: **zero bare `confirm(`/`alert(`/`prompt(` call-sites** remain in app code (rg lookbehind check clean).
- **Flash-banner migration (131 files / 311 blocks)**: `strip_flash_banners.ps1` regex-stripped all `@if(session('success'|'error'|'warning'|'info'))` + legacy `session('x')` div blocks; flashes now surface as toasts via the layout emitter. Remaining `@if(session(` hits are company-context checks only.
- **Inline-alert migration (27 blocks)**: converted `bg-red-100/bg-red-50/bg-yellow-50/bg-amber-50` error/warning/info boxes to `<x-feedback.alert variant="error|warning|info" class="mb-4|mt-4|mt-6">` across assemblies (create/create-bom/unbuild incl. the orange "Unbuild reverses a Build" info callout), item-categories, stock-transfers/adjustments/counts, landed-costs, paye-tables, branch-requests index/show, pos/returns/create, system-settings/index, balance-sheet + cash-flow "out of balance" warnings, cheques/show voided notice, fiscal-years/show year-end warning, analytics/cash-flow-trend projection disclaimer (removed its dangling `</div>`s). **Deliberate keeps**: POS checkout dynamic JS/Alpine classes (lines 9/21/27/305/575/1127/1149 — functional state-driven, not banners), `pos/cashier/login` (standalone HTML page, no app CSS/layout — can't use the component), `admin/system-health/index` recent-errors list rows (diagnostics list items, not alert banners).
- **Gotchas**: (1) PowerShell double-quoted here-strings interpolate `$error` (an automatic variable) — use `@'...'@` single-quoted here-strings for Blade blocks containing `$error`. (2) `.fb-alert` has NO default margin; call-sites pass `class="mb-4"` (component root merges attributes) to match the legacy `mb-4`. (3) `@props` default `class` is NOT merged unless the root element renders `$attributes->class([...])`. (4) `session('status')` must never be auto-toasted — it's a status key, not a message.
- **Verification**: `php artisan view:cache` clean (all ~330 views compile); `ScopedSearchRenderSmokeTest` passes (renders every converted route as company_admin); `npm run build` emits `.fb-*` tokens. Remaining legacy-colored boxes are the 3 intentional keeps above.

35. **Create/Edit Bill redesign (Aug 7, 2026)** — both screens rebuilt on a shared `resources/views/accounting/bills/_form.blade.php` partial driven by props (`isEdit`, `bill`, `formAction`, `formMethod`, `cancelRoute`, `title`, `cs`). Design per `docs/create-bill.mockup.html`: sticky page header (serif title + status badge on edit, gold "Save Bill"/"Save & New" + ghost "Submit for Approval" actions), navy icon-chip section leaders, fluid line-items table with sticky navy-gradient header, and a live summary card (`bill-amt`/`bill-ci`/`bill-ci-num`/`bill-drop` CSS) with a navy-gradient grand-total bar. Lines use the invoices/purchase-orders JS pattern (`item-selected` event, hidden `tax_rate`/`discount`, `Add Line`/delete/duplicate, product row-selection state). Vendor/account picks are `x-scoped-search-field` (server mode); cost-center is a native select (NOT server-searchable — `scopedSearchFieldHtml` requires `search-url`; if ever converted, use `SearchCatalog` `cost-center` entity).
- **Controller additions** (`app/Http/Controllers/Accounting/BillController.php`): `create()`/`edit()` now pass `branches` (Branch where company_id, is_active, orderBy name), `currencies` (`Currency::query()->active()->ordered()->get()` — central, NOT TenantScoped), `defaultExpenseAccountId` (`DefaultAccountMapping::getAccountId($companyId, 'default_expense') ?? $expenseAccounts->first()?->id`); `store()`/`update()` validate `branch_id` (nullable, `exists:branches,id`) + `exchange_rate` (nullable numeric min:0) and handle `action` via `handlePostSaveAction()` (the shared `bill-ci` checkmark button posts `action=submit_for_approval`); `update()` calls `$this->billService->update(...)` then `$bill->fresh()` before the action handler (the stale instance would still be `draft`).
- **BillService** (`app/Services/Accounting/BillService.php`): new `submitForApproval(Bill $bill, int $userId): Bill` — DB::transaction, `logBillAction('submitted_for_approval')`, flips `draft` → `pending_approval` (`STATUS_PENDING_APPROVAL`).
- **SearchCatalog** product entity (`app/Services/Search/SearchCatalog.php`): added `expense_account_id` to the `select([...])` and the returned array so line-item autofill can set the expense account.
- **Form-field persistence wiring (Aug 7, 2026 follow-up)** — the persistence-gap audit is now CLOSED for the mockup fields. Migration `database/migrations/tenant/2026_11_09_000014_add_bill_form_fields_to_bills_table.php` adds `po_number` (string 60), `grn_reference` (string 60), `supplier_notes` (text), `payment_instructions` (text), `freight_charges`/`insurance_charges`/`customs_charges`/`other_charges` (decimal 15,2 default 0). `Bill` model: fields fillable, charges cast `decimal:2`, `totalCharges(): float`, plus new `subtotal`/`tax_total`/`total` accessors (sum of lines + charges; the show page used these names but they DIDN'T EXIST → amounts rendered 0.00). `BillService`: `create()/update()` persist all 8; `updateBillAmount()` = lines sum + `totalCharges()`; `post()` appends one `appendChargeLines()` JE pair per charge (DR first-line `expense_account_id` → fallback `DefaultAccountMapping::getAccount($companyId,'default_expense')` → throw; CR AP, memo `"Bill {n} - {label}"`) in BOTH the standalone and PO-backed branches, before the FX service so charges are exchange-rate scaled; `approve()` LATENT BUG FIXED — standalone approve never added `$totalDebit += $line->tax_amount`, so approved bills with tax posted an unbalanced JE. `BillController`: `store()/update()` validate all 8 (nullable; charges numeric min:0; notes max:5000). Show page renders PO/GRN refs, per-charge balance rows between Tax and Total, and supplier notes/payment instructions. `bills.submit` ROUTE WAS MISSING — the show page's Submit-for-Approval form posted to an undefined route (500 on every draft-bill show page); added `BillController::submit()` (permission `bills.edit`, `sod:bill`) + route. `x-detail-field` LATENT BUG: `{{ $slot ?? $value }}` never rendered `:value` (Blade `$slot` is always set, even empty) → all 19 pages using `:value` showed blank values; fixed to `{{ $slot->isNotEmpty() ? $slot : $value }}`. Tests: `BillServiceTest` +4 (persistence, update, standalone-post JE w/ charges, approve w/ charges+tax balanced), `BillServicePoBackedTest` +1 (charges balanced in PO-backed post), new `BillControllerTest` (3: store persists, negative charge rejected, show renders). BillServiceTest 14 + PoBackedTest 7 + BillControllerTest 3 all green; `view:cache` clean; `ScopedSearchRenderSmokeTest` passes.
- **Bill attachments end-to-end (Aug 7, 2026 follow-up #2)** — the dropzone is now functional (was "shown for reference"). `_form.blade.php`: `bill-form` tag gained `enctype="multipart/form-data"`; attachments section renders existing files (`$billAttachments = $bill?->attachments`) with per-file remove buttons (`billRemoveExistingFile` → hidden `delete_documents[]` inputs), a click-to-browse + drag/drop dropzone (`billWireDropzone`, `.bill-drop--drag` class), and a new-file list with remove buttons (`billNewFiles` array synced into the `#bill-files` input via `DataTransfer` so the browser submits the exact selection). `BillController`: `store()/update()` validate `files` (`array max:10`, each `file mimes:pdf,jpg,jpeg,png,gif,webp,xls,xlsx,doc,docx,txt,csv max:10240`) + `delete_documents` (`array of integer`); private `handleAttachments()` scopes deletes to the bill's own attachments (`attachmentable_type`=Bill::class + id, cross-company forged ids are no-ops) then stores new files on `Storage::disk('public')` at `bill-attachments/{companyId}/{billId}/{random}.{ext}` creating `Attachment` rows (`uploaded_by` = auth id). `Bill` model: `attachments(): MorphMany` (reuses the pre-existing morph `attachments` table). `show()`/`edit()` eager-load `attachments`; show page toolbar button badge uses `$bill->attachments->count()` and a new Attachments card lists download links via `Storage::disk('public')->url()`. New `format_bytes()` helper in `app/helpers.php` (B/KB/MB/GB). CSS: `.bill-drop--drag` (gold ring + copy cursor) + `.bill-file-list` styles. Tests: `BillControllerTest` +3 (store uploads 2 files → rows + disk files + uploaded_by, update deletes flagged attachment + disk file, oversized file rejected `files.0`) — 6 passed / 31 assertions; BillServiceTest 14 + BillServicePoBackedTest 7 green; `view:cache` clean; `ScopedSearchRenderSmokeTest` (covers `bills.create`) passes; `npm run build` emits the new tokens.
- **Views**: `create.blade.php`/`edit.blade.php` are thin `<x-app-layout>` + `$cs` + `@include` shells. Verified: `view:cache` clean, compiled `_form` passes `php -l`, `ScopedSearchRenderSmokeTest` passes, `BillServiceTest` (14) + `BillServicePoBackedTest` (7) + `BillControllerTest` (3) green and `npm run build` (bill-ci*/bill-drop tokens emitted).

36. **Remaining mockups implemented: list-page redesign + create-company polish (Aug 7, 2026)** — `docs/list-page.mockup.html` ("Customers — Redesigned") and `docs/bill-view-edit.mockup.html` (content is actually the "Create Company — Redesigned" SA form; the bill view/edit redesign was already delivered in phase 35). Styling/layout only — no route/controller/query changes.
- **CSS (`resources/css/app.css`)**: `.list-filter-bar` → toolbar card (`padding:16px 20px`, `radius:20px`, white `.66` + `shadow-card` + backdrop blur); `.list-filter-search` max-width 320→420px; `.list-filter-select` → boxed 40px `rounded-12` w/ custom chevron data-URI + gold focus ring; new `.list-filter-count` pill (margin-left auto, `rounded-full`, navy text); `.list-table-wrap` radius 24px + blur/shadow; `.list-table` min-width 880px, th/td padding 13px 18px, row hover `rgba(36,56,79,.035)`; new `.list-pagination*` footer (info text + 30px `rounded-9` bordered nav buttons, `.is-current` navy→gold gradient with gold inset top hairline, `.is-disabled`); `.list-sidebar-link.is-active` navy gradient + 4px gold left bar `::before` + white-tinted icon square (#e2c069 icon); `.list-sidebar-icon-square` border + transition; new `.sa-link` (12px/600 gold link).
- **Components**: `x-list-filter-bar` gained `countText` prop → renders `<span class="list-filter-count">{{ $countText }}</span>` inside the form (no extra wrapper/div); `x-list-quick-links` now computes `$currentFullUrl = rtrim(request()->fullUrl(), '/')` and marks the matching link `is-active` + `aria-current="page"` (both desktop rail and mobile dropdown — the mobile dropdown and desktop rail use separate markup, so both needed the same class logic); NEW `x-list-pagination` (`components/list-pagination.blade.php`, props `paginator` + `label`): "Showing X–Y of Z {label}" info line, ‹ › prev/next, ±2 page window, first/last with ellipses, current page as `.is-current` span, `appends(request()->query())` inside `hasPages()`.
- **Pages (7)**: customers/vendors/products/employees/invoices/bills/sales-receipts `index.blade.php` each gained a header `description` (passed to `x-list-header`), `countText="... total ..."` on `x-list-filter-bar`, and both desktop + mobile `->links()` blocks swapped → `<x-list-pagination :paginator="..." label="..." />`. Customers additionally got a "Since {{ $customer->created_at?->format('M Y') }}" secondary line under the name cell. Sales-receipts create action label is "Create Sales Receipt" (not "Create Receipt").
- **Create-company polish** (`superadmin/companies/create.blade.php`): DB-preview chip now falls back to `acct_••••_pending` before the Alpine fetch resolves; "Branch Limit override" text → gold `.sa-link` to the companies index.
- **New regression test** `tests/Feature/Accounting/ListPageRenderTest.php` (3 tests): customers index renders the mockup markup (description, count chip, "Since" line, `aria-current="page"` sidebar active state), pagination footer renders the info line + `.is-current` button + Next aria-label, and all 7 list routes return 200. NOTE: assertSee HTML-escapes by default — attribute assertions need `assertSee(..., false)`; required fixture fields for these list pages: Product `sales_price`+`income_account_id`+`expense_account_id`, Account `sub_type`, Employee `hire_date`, Invoice/Bill/SalesReceipt `created_by`.
- **Verification**: `php artisan view:cache` clean; `npm run build` emits the new tokens (list-pagination-btn, list-filter-count, etc.); `ScopedSearchRenderSmokeTest`, `SuperAdminPanelTest`+`ProductSearchTest` (43) and `ListPageRenderTest` (3) all green.
