@props(['number' => '', 'title' => ''])
<div class="section-head">
    <span class="section-head-num">{{ $number }}</span>
    <span class="section-head-title">{{ $title }}</span>
    <span class="section-head-rule"></span>
</div>
